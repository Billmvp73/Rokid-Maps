---
phase: quick-260703-05e
plan: 01
subsystem: planning-docs
tags: [design-review, osrm, silent-via, waypoints, roadmap, strava, navigation]

# Dependency graph
requires:
  - phase: quick-260702-wvg
    provides: iteration-3 design-doc fixes (2 blockers + 7 warnings) that this convergence-round-4 pass builds on
provides:
  - ROADMAP Phase 4 GPX strategy specifies waypoints=0;{last} silent-via routing (single leg), a defensive non-final zero-distance arrive-step filter, and a URL-length note (~200 coords ≈ 4KB GET)
  - ARCHITECTURE.md carries the identical silent-via amendment in Flow 2's stanza, its Architecture-decision paragraph, and Pattern 4's What (exactly 3 occurrences)
  - STATE.md decision row backing the silent-via amendment with the ~199-spurious-arrivals rationale (verified against live OSRM + OsrmClient/MainActivity/HudView code paths)
  - FEATURES.md no longer claims imported-route navigation "works immediately"; matrix cost is MEDIUM; table-stakes HUD metric list drops moving time (summary-only in v1)
  - SUMMARY.md acknowledges two new library declarations (security-crypto, browser) instead of "no new libraries"
  - ARCHITECTURE.md sport_state JSON sample carries a v:1 version field
  - PITFALLS.md recommends a thread-based timeout (no coroutines), drops the pause/resume test claim, and states the ~999-point integer-stride bound
  - CLAUDE.md dependency table marks the 4 OkHttp/Gson rows as explicitly declared (12-message-type count verified already correct)
affects: [phase-1-activity-recording, phase-2-glasses-sport-hud, phase-4-route-import-navigation]

# Tech tracking
tech-stack:
  added: []
  patterns: []

key-files:
  created: []
  modified:
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/research/ARCHITECTURE.md
    - .planning/research/FEATURES.md
    - .planning/research/PITFALLS.md
    - .planning/research/SUMMARY.md
    - CLAUDE.md

key-decisions:
  - "Via-point routing must pass waypoints=0;{last} (silent via points, single leg) — without it every intermediate via splits a leg and emits arrive/depart step pairs (~199 spurious mid-route 'Arrived!' banners/TTS on a 200-point route); defensive filter drops non-final zero-distance arrive steps (recorded as new STATE.md decision row)"

patterns-established: []

requirements-completed: []  # docs-only pass; plan frontmatter requirements is empty

# Metrics
duration: 7min
completed: 2026-07-03
---

# Quick Task 260703-05e: Iteration-4 Design-Doc Fixes from Re-Review #3 Summary

**Applied the convergence-round-4 fixes: 2 blockers (B1 OSRM silent-waypoint leg merging — waypoints=0;{last} now specified verbatim in 2/3/1 places across ROADMAP/ARCHITECTURE/STATE; B2 FEATURES false imported-route-reuse claims) and 7 warnings (HUD metric list, dependency-declaration claim, sport_state v:1 sample, no-coroutines timeout, test scope, stride bound, CLAUDE.md dependency rows) across 6 planning docs + CLAUDE.md.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-07-03T07:17:08Z
- **Completed:** 2026-07-03T07:24:58Z
- **Tasks:** 2/2 completed
- **Files modified:** 7 (docs only; git diff vs base confirms zero changes under phone/, glasses/, shared/)

## Accomplishments

- **B1 closed (substantive):** The Phase 4 GPX strategy now specifies `waypoints=0;{last}` silent-via routing so OSRM returns a single leg. Without it, ~200 plain via-waypoints split the response into ~200 legs, and the existing code paths (OsrmClient leg-step concatenation + arrive mapping, MainActivity per-step TTS, HudView "Arrived!" banner) would produce ~199 spurious mid-route arrival announcements. Amendment landed verbatim in ROADMAP line 64 (strategy sentence + scope item (a) + URL-length note), ARCHITECTURE Flow 2 stanza (with new defensive-filter line), the Architecture-decision paragraph, Pattern 4's What, and a new STATE.md decision row. Occurrence counts: exactly 2x ROADMAP, 3x ARCHITECTURE, 1x STATE, 0x everywhere else in the target docs.
- **B2 closed:** FEATURES MVP bullet no longer claims imported-route navigation "works immediately once GPX is converted" — it now routes through the Phase 4 waypoint-accepting NavigationManager path + OSRM via-point routing; prioritization-matrix cost corrected LOW → MEDIUM (aligned with the table-stakes row and ROADMAP's MEDIUM-HIGH).
- **W1:** Table-stakes sport-metrics Notes cell drops moving time from the HUD metric list (tracked, phone-summary-only in v1 per STATE decision) — FEATURES.md now carries the "summary-only in v1 per STATE decision" phrase exactly twice (this row + the pre-existing auto-pause row).
- **W2:** SUMMARY.md stack bullet corrected from "No new libraries needed" to "Only two new library declarations (security-crypto, browser)".
- **W3:** ARCHITECTURE sport_state JSON sample now opens with `"v": 1` (protocol version, v1 scope: sport_state only), comment column aligned.
- **W4:** PITFALLS Pitfall 6 test scope corrected to state transitions (IDLE→TRACKING→FINISHED) + metric computation — v1 has no PAUSED state.
- **W5:** PITFALLS Pitfall 2 step 6 replaced the coroutines-only `withTimeoutOrNull` with a thread-based timeout (Handler timeout or ExecutorService future.get(30, SECONDS)) — explicit no-coroutines codebase constraint.
- **W6:** PITFALLS Pitfall 3 stride claim corrected: integer stride targets ~500 points but can emit up to ~999 for routes just under the threshold.
- **W7:** (i) VERIFY-only confirmed — CLAUDE.md has zero "11 message types" and exactly one "12 message types"; (ii) the 4 OkHttp/converter-gson/logging-interceptor/gson dependency rows now read "explicitly declared in phone/build.gradle.kts; previously transitive via CXR SDK" (Retrofit row and Language Features bullets untouched, per spec).
- **STATE.md Last activity** bumped to Iteration-4 / re-review #3.

## Task Commits

Each task was committed atomically:

1. **Task 1: Apply B1 silent-via OSRM amendment to ROADMAP, ARCHITECTURE, and STATE** - `b2b7af4` (docs) — STATE.md decision row applied to working tree but left uncommitted per orchestrator constraint
2. **Task 2: Apply B2 + W1-W7 corrections and bump STATE.md last activity** - `9d56da6` (docs) — STATE.md last-activity bump likewise left uncommitted

**Plan metadata:** committed by orchestrator (STATE.md + this SUMMARY left uncommitted by design)

## Files Created/Modified

- `.planning/ROADMAP.md` - Phase 4 strategy line: silent-via amendment (2 occurrences), silent-via scope item (a) with arrive-step filter, URL-length note (committed `b2b7af4`)
- `.planning/research/ARCHITECTURE.md` - Flow 2 stanza + defensive-filter line, Architecture-decision paragraph, Pattern 4 What (committed `b2b7af4`); sport_state JSON `"v": 1` field (committed `9d56da6`)
- `.planning/STATE.md` - New waypoints=0;{last} decision row + Iteration-4 last-activity line (UNCOMMITTED — orchestrator commits)
- `.planning/research/FEATURES.md` - B2a MVP bullet rewrite, B2b matrix LOW→MEDIUM, W1 HUD metric list (committed `9d56da6`)
- `.planning/research/SUMMARY.md` - W2 two-new-library-declarations bullet (committed `9d56da6`)
- `.planning/research/PITFALLS.md` - W4 test scope, W5 thread-based timeout, W6 stride bound (committed `9d56da6`)
- `CLAUDE.md` - W7(ii) four dependency-table Purpose cells (committed `9d56da6`)

## Decisions Made

None beyond the plan — all decisions were FINAL (convergence round 4); replacement text applied verbatim as specified.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Task 1 gate's ROADMAP count check used line-counting where the plan's intent is occurrence-counting**
- **Found during:** Task 1 (B1 silent-via amendment), first gate run
- **Issue:** The gate asserts `grep -Fc 'waypoints=0;{last}' .planning/ROADMAP.md` equals 2, but `grep -c` counts matching *lines*, not occurrences. Both ROADMAP occurrences land on the single physical line 64 (the whole Phase 4 strategy paragraph is one line), so the check returns 1 and the gate fails even though the plan's own count-discipline paragraph ("appears exactly 2x in ROADMAP.md") is satisfied.
- **Fix:** Corrected the counting method in the gate command only — `grep -oF 'waypoints=0;{last}' <file> | wc -l` (occurrence count) for the three count assertions; every other check ran unchanged. The deliverable text was NOT altered — replacement text is verbatim-final per the plan and was applied exactly as specified. Verified line-vs-occurrence: ROADMAP 1 line / 2 occurrences; ARCHITECTURE 3/3; STATE 1/1.
- **Files modified:** None (gate command fix only)
- **Verification:** Corrected gate prints B1-GATES-PASS; occurrence counts are exactly 2/3/1; string absent from FEATURES/PITFALLS/SUMMARY/CLAUDE.md
- **Committed in:** n/a (no file change; documented here)

---

**Total deviations:** 1 auto-fixed (1 bug — in the verification command, not the deliverable)
**Impact on plan:** None on content. All 14 EDITs applied verbatim; the W7(i) VERIFY-only item confirmed via the Task 2 gate, which passed as written on the first run.

## Issues Encountered

- Worktree merge-base differed from the dispatched base commit at startup; reset to `9ec34d7` per the worktree_branch_check instructions before any work.

## Known Stubs

None — docs-only change, no code or data wiring involved.

## Threat Flags

None — docs-only change; no new network endpoints, auth paths, file access patterns, or trust-boundary schema changes. Threat register mitigation T-q05e-01 (exact-count grep gates) applied, with the ROADMAP count assertion corrected to occurrence-counting to match the plan's stated intent.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 4 planners inherit an unambiguous OSRM contract: `waypoints=0;{last}` silent-via routing (single leg), a defensive non-final zero-distance arrive-step filter, and a URL-length budget — specified identically in ROADMAP and ARCHITECTURE, backed by a STATE.md decision row.
- Phase 1 planners see the corrected test scope (IDLE→TRACKING→FINISHED, no pause/resume), the no-coroutines timeout guidance, and the v:1 sport_state sample.
- CLAUDE.md's dependency table now matches build reality (4 explicit declarations), so future agents won't re-add or re-classify those dependencies.

## Self-Check: PASSED

- All 7 target files + this SUMMARY exist on disk
- Commits `b2b7af4` and `9d56da6` exist on `worktree-agent-a0cb7ec547d8c560c`
- B1 gate passes with occurrence-corrected counting (2/3/1 occurrences of `waypoints=0;{last}` in ROADMAP/ARCHITECTURE/STATE; 0 in FEATURES/PITFALLS/SUMMARY/CLAUDE.md)
- ITER4 gate passes exactly as written in the plan
- `git status` shows only STATE.md (modified) and this SUMMARY (untracked) uncommitted — by design per orchestrator constraint
- Zero changes under phone/, glasses/, shared/ vs base `9ec34d7`

---
*Phase: quick-260703-05e*
*Completed: 2026-07-03*
