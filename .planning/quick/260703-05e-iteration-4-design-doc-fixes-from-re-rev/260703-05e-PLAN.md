---
phase: quick-260703-05e
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES.md
  - .planning/research/PITFALLS.md
  - .planning/research/SUMMARY.md
  - CLAUDE.md
autonomous: true
requirements: []
must_haves:
  truths:
    - "ROADMAP Phase 4 GPX strategy specifies waypoints=0;{last} silent-via routing (single leg), a defensive non-final zero-distance arrive-step filter, and a URL-length note"
    - "ARCHITECTURE.md carries the same silent-via amendment in Flow 2's stanza, its Architecture-decision paragraph, and Pattern 4's What"
    - "STATE.md Decisions records the waypoints=0;{last} decision with the ~199-spurious-arrivals rationale, and Last activity reads Iteration-4"
    - "FEATURES.md no longer claims imported-route navigation 'works immediately'; matrix cost is MEDIUM; table-stakes row no longer lists moving time as a HUD metric"
    - "SUMMARY.md acknowledges two new library declarations (security-crypto, browser) instead of 'no new libraries'"
    - "ARCHITECTURE.md sport_state JSON sample carries a v:1 version field"
    - "PITFALLS.md recommends a thread-based timeout (no coroutines), drops the pause/resume test claim, and states the ~999-point stride bound"
    - "CLAUDE.md dependency table marks the 4 OkHttp/Gson rows as explicitly declared (12-message-type count already correct)"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Phase 4 strategy with silent-via amendment"
      contains: "waypoints=0;{last}"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "Flow 2 + Pattern 4 silent-via amendment; versioned sport_state sample"
      contains: "waypoints=0;{last}"
    - path: ".planning/STATE.md"
      provides: "New decision row + Iteration-4 last-activity line"
      contains: "waypoints=0;{last}"
    - path: ".planning/research/FEATURES.md"
      provides: "Corrected reuse claims and HUD metric list"
      contains: "waypoint-accepting NavigationManager path"
    - path: ".planning/research/SUMMARY.md"
      provides: "Corrected dependency-declaration claim"
      contains: "Only two new library declarations"
    - path: ".planning/research/PITFALLS.md"
      provides: "No-coroutines timeout guidance; corrected stride bound; corrected test scope"
      contains: "future.get(30, SECONDS)"
    - path: "CLAUDE.md"
      provides: "Dependency table reflecting explicit declarations"
      contains: "previously transitive via CXR SDK"
  key_links:
    - from: ".planning/ROADMAP.md"
      to: ".planning/research/ARCHITECTURE.md"
      via: "identical silent-via phrase in Phase 4 strategy and Flow 2/Pattern 4"
      pattern: "waypoints=0;\\{last\\}"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "decision row backing the Phase 4 strategy amendment"
      pattern: "waypoints=0;\\{last\\}"
---

<objective>
Apply iteration-4 design-doc fixes from re-review #3: 2 blockers (B1 OSRM silent-waypoint leg merging, B2 FEATURES false-reuse claims) + 7 warnings (W1-W7) + STATE.md last-activity bump. Docs-only — `.planning/*.md` and `CLAUDE.md`. NO source changes.

All decisions are FINAL (convergence round 4); replacement text below is exact and must be applied verbatim. All anchors were verified against current file content during planning; every item is a live EDIT except W7(i), which is VERIFY-only (CLAUDE.md line 240 already reads "12 message types"; zero "11 message" occurrences remain).

Purpose: The B1 fix is substantive — verified live against OSRM, sending ~200 plain via-waypoints splits the response into ~200 legs, and existing code paths (OsrmClient.kt:68-88 leg-step concatenation, :96 arrive-mapping; MainActivity.kt:842 TTS per step; HudView.kt:234-243 "Arrived!" banner) would produce ~199 spurious mid-route arrival announcements. The design docs must specify `waypoints=0;{last}` before Phase 4 is planned.

Output: 6 corrected planning docs + CLAUDE.md, committed as one docs fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
@CLAUDE.md
</context>

<coverage_audit>
| Review item | Task | Status |
|-------------|------|--------|
| B1a ROADMAP Phase 4 strategy amendment | 1 | EDIT |
| B1b ARCHITECTURE Flow 2 stanza + decision paragraph | 1 | EDIT |
| B1c ARCHITECTURE Pattern 4 What | 1 | EDIT |
| B1d STATE.md decision row | 1 | EDIT |
| B2a FEATURES MVP bullet (~line 113) | 2 | EDIT |
| B2b FEATURES matrix row (~line 149) | 2 | EDIT |
| W1 FEATURES table-stakes row (~line 20) | 2 | EDIT |
| W2 SUMMARY line 16 | 2 | EDIT |
| W3 ARCHITECTURE sport_state JSON (~lines 507-516) | 2 | EDIT |
| W4 PITFALLS Pitfall 6 prevention #4 (~line 212) | 2 | EDIT |
| W5 PITFALLS Pitfall 2 step 6 (~line 70) | 2 | EDIT |
| W6 PITFALLS Pitfall 3 (~line 95) | 2 | EDIT |
| W7(i) CLAUDE.md "11 message types" | 2 | VERIFY-only (already fixed; gate confirms 0 stale / 1 correct) |
| W7(ii) CLAUDE.md dependency table rows | 2 | EDIT (4 rows, lines 62-65; Retrofit row 61 unchanged) |
| STATE.md Last activity bump | 2 | EDIT |
</coverage_audit>

<tasks>

<task type="auto">
  <name>Task 1: Apply B1 silent-via OSRM amendment to ROADMAP, ARCHITECTURE, and STATE</name>
  <files>.planning/ROADMAP.md, .planning/research/ARCHITECTURE.md, .planning/STATE.md</files>
  <action>
Use the Edit tool with exact old/new strings. Four sub-edits, all part of one amendment:

**(a) ROADMAP.md line 64** — three edits within the "GPX navigation strategy (decided 2026-07-02)" line:

1. Replace "then call OSRM /route with ALL downsampled points as via-waypoints (steps=true) to get real turn-by-turn steps" with: "then call OSRM /route with ALL downsampled points as via-waypoints (steps=true) with `waypoints=0;{last}` so intermediate points are silent via points (single leg — without this, every via point splits a leg and emits spurious arrive/depart steps) to get real turn-by-turn steps"

2. Replace scope item "(a) OsrmClient multi-waypoint support (currently builds a 2-point URL only)," with: "(a) OsrmClient multi-waypoint support with silent-via handling (`waypoints=0;{last}`) and a defensive filter dropping non-final zero-distance arrive steps (currently builds a 2-point URL only),"

3. Append to the end of the strategy line (after "(d) follow-route fallback mode."): " URL-length note: ~200 coordinates ≈ 4KB GET URL — acceptable; if OSRM rejects the request, reduce the via-point count."

**(b) ARCHITECTURE.md Flow 2** (lines ~219-221 inside the fenced flow diagram, and the "Architecture decision:" paragraph at line ~230):

1. In the flow stanza, replace the line "OSRM /route with ALL downsampled points as via-waypoints (steps=true)" with: "OSRM /route with ALL downsampled points as via-waypoints (steps=true, waypoints=0;{last} so intermediate points are silent via points — single leg)". Then insert a new line "    (defensive filter: drop any non-final zero-distance arrive step)" immediately after the existing line "    -> road-snapped route geometry + real turn-by-turn steps" and before the "(fallback: ...)" line, matching the stanza's 4-space indentation.

2. In the "**Architecture decision:**" paragraph, replace "calls OSRM /route with all downsampled points as via-waypoints (steps=true). This returns" with: "calls OSRM /route with all downsampled points as via-waypoints (steps=true) and `waypoints=0;{last}` so only the first and last coordinates are leg-boundary waypoints — intermediate points silently shape the route (single leg; without this, every via point splits a leg and emits a spurious arrive/depart step pair). This returns". Then replace "all work for Strava routes. It requires extending OsrmClient" with: "all work for Strava routes. A defensive filter drops any non-final zero-distance arrive step. URL-length note: ~200 coordinates ≈ 4KB GET URL — acceptable; if OSRM rejects the request, reduce the via-point count. It requires extending OsrmClient"

**(c) ARCHITECTURE.md Pattern 4** (line ~409, the "**What:**" sentence): replace "as via-waypoints with steps=true — producing a road-snapped route and real turn-by-turn steps." with: "as via-waypoints with steps=true and `waypoints=0;{last}` (silent via points — single leg; without this, every via point splits a leg and emits spurious arrive/depart steps) — producing a road-snapped route and real turn-by-turn steps; a defensive filter drops any non-final zero-distance arrive step."

**(d) STATE.md Decisions table** — append this exact row after the last existing row ("NavigationManager data-race fix owned by Phase 4 ...", line ~52): "| Via-point routing must pass waypoints=0;{last} (silent via points, single leg) | Without it every intermediate via splits a leg and emits arrive/depart step pairs — ~199 spurious mid-route 'Arrived!' banners/TTS on a 200-point route (verified against live OSRM and the OsrmClient/MainActivity/HudView code paths); defensive filter drops non-final zero-distance arrive steps | 2026-07-03 |"

Count discipline: after this task, the literal string `waypoints=0;{last}` appears exactly 2x in ROADMAP.md, exactly 3x in ARCHITECTURE.md (Flow 2 stanza, decision paragraph, Pattern 4 What), and exactly 1x in STATE.md. Do NOT add it anywhere else — FEATURES.md line 18 and PITFALLS.md are intentionally NOT amended for B1 (spec scope is final).
  </action>
  <verify>
    <automated>test "$(grep -Fc 'waypoints=0;{last}' .planning/ROADMAP.md)" -eq 2 && test "$(grep -Fc 'waypoints=0;{last}' .planning/research/ARCHITECTURE.md)" -eq 3 && test "$(grep -Fc 'waypoints=0;{last}' .planning/STATE.md)" -eq 1 && grep -Fq '4KB GET URL' .planning/ROADMAP.md && grep -Fq '4KB GET URL' .planning/research/ARCHITECTURE.md && grep -Fq 'non-final zero-distance arrive step' .planning/ROADMAP.md && grep -Fq 'non-final zero-distance arrive step' .planning/research/ARCHITECTURE.md && grep -Fq 'non-final zero-distance arrive steps' .planning/STATE.md && echo B1-GATES-PASS</automated>
  </verify>
  <done>ROADMAP.md carries the silent-via strategy amendment (2 occurrences), scope item (a) names silent-via handling + arrive-step filter, and the URL-length note closes the strategy paragraph. ARCHITECTURE.md carries the amendment in exactly 3 places. STATE.md has the new decision row. Gate prints B1-GATES-PASS.</done>
</task>

<task type="auto">
  <name>Task 2: Apply B2 + W1-W7 corrections and bump STATE.md last activity</name>
  <files>.planning/research/FEATURES.md, .planning/research/SUMMARY.md, .planning/research/ARCHITECTURE.md, .planning/research/PITFALLS.md, CLAUDE.md, .planning/STATE.md</files>
  <action>
Use the Edit tool with exact old/new strings for each item:

**B2a — FEATURES.md line ~113 (MVP "Launch With (v1)" bullet):** replace "**Navigate imported route on glasses** — reuse existing navigation pipeline. This works immediately once GPX is converted." with: "**Navigate imported route on glasses** — via the Phase 4 waypoint-accepting NavigationManager path + OSRM via-point routing (existing pipeline is destination-only; see ROADMAP Phase 4 scope)."

**B2b — FEATURES.md line ~149 (Feature Prioritization Matrix):** replace the row "| Navigate imported route on glasses | HIGH | LOW | P1 |" with "| Navigate imported route on glasses | HIGH | MEDIUM | P1 |" (aligns with the same doc's table-stakes row and ROADMAP's MEDIUM-HIGH).

**W1 — FEATURES.md line ~20 (table-stakes sport-metrics row, Notes cell):** replace "Speed already displayed; add pace (min/km, min/mi), moving time, distance." with: "Speed already displayed; add pace (min/km, min/mi), distance (moving time is tracked and shown in the phone summary — summary-only in v1 per STATE decision)." The HUD metric list is now elapsed time, speed/pace, distance only. Note: the auto-pause row (~line 47) already carries this parenthetical from a prior pass — after this edit FEATURES.md contains the phrase "summary-only in v1 per STATE decision" exactly twice.

**W2 — SUMMARY.md line ~16 (Stack section, first bullet):** replace "- **No new libraries needed beyond what's already on the classpath.** OkHttp 4.12.0, logging-interceptor 4.12.0, and Gson 2.10.1 are already explicitly declared in phone/build.gradle.kts — verify versions only; no build changes needed." with: "- **Only two new library declarations (security-crypto, browser); OkHttp/Gson/logging-interceptor are already declared in phone/build.gradle.kts.** OkHttp 4.12.0, logging-interceptor 4.12.0, and Gson 2.10.1 need version verification only; the only build changes are adding androidx security-crypto and browser (Chrome Custom Tabs)."

**W3 — ARCHITECTURE.md sport_state JSON sample (~lines 506-516):** inside the fenced JSON block, insert a new line immediately after the opening brace "{" and before the line containing `"t": "sport_state",`: `  "v": 1,             // protocol version (v1 scope: sport_state only)`. Align the trailing comment with the existing comment column.

**W4 — PITFALLS.md line ~212 (Pitfall 6, prevention #4):** replace "covering state transitions, pause/resume, and metric computation" with "covering state transitions (IDLE→TRACKING→FINISHED) and metric computation" (v1 has no PAUSED state).

**W5 — PITFALLS.md line ~70 (Pitfall 2, step 6):** replace "6. Wrap all location requests in `withTimeoutOrNull(30_000L)` with a priority fallback chain (HIGH_ACCURACY -> BALANCED_POWER -> LOW_POWER -> lastLocation)." with: "6. Bound location-request waits with a thread-based timeout (Handler timeout or ExecutorService future.get(30, SECONDS)) and a priority fallback chain (HIGH_ACCURACY → BALANCED_POWER → LOW_POWER → lastLocation) — no coroutines (codebase constraint)." (withTimeoutOrNull is a coroutines API; the codebase is explicitly no-coroutines.)

**W6 — PITFALLS.md line ~95 (Pitfall 3, Why it happens):** replace "strides route geometry down to ≤500 points" with "strides route geometry toward ~500 points (integer stride — up to ~999 points for routes just under the threshold)".

**W7(i) — CLAUDE.md message-type count:** VERIFY-only. Planning-time grep confirmed zero occurrences of "11 message" (case-insensitive) and exactly one "12 message types" (line 240). Do not edit; the gate re-confirms.

**W7(ii) — CLAUDE.md Key Dependencies table (lines 62-65):** update exactly these four Purpose cells, keeping the table format intact. Leave the Retrofit row (line 61) and the Language Features bullets (lines 89-91) unchanged:
- converter-gson row: replace "Gson converter (transitive)" with "Gson converter (explicitly declared in phone/build.gradle.kts; previously transitive via CXR SDK)"
- okhttp row: replace "OkHttp (transitive, not used directly - app uses `HttpURLConnection`)" with "OkHttp (explicitly declared in phone/build.gradle.kts; previously transitive via CXR SDK — legacy app code uses `HttpURLConnection`; the Strava client will use OkHttp)"
- logging-interceptor row: replace "Logging interceptor (transitive)" with "Logging interceptor (explicitly declared in phone/build.gradle.kts; previously transitive via CXR SDK)"
- gson row: replace "Gson (transitive, not used directly - app uses `org.json`)" with "Gson (explicitly declared in phone/build.gradle.kts; previously transitive via CXR SDK — legacy app code uses `org.json`; the Strava client will use Gson)"

**STATE.md Last activity (line ~26):** replace "**Last activity:** 2026-07-03 -- Iteration-3 design-doc fixes applied (2 blockers + 7 warnings from re-review #2)" with "**Last activity:** 2026-07-03 -- Iteration-4 design-doc fixes applied (2 blockers + 7 warnings from re-review #3)"
  </action>
  <verify>
    <automated>test "$(grep -Fc 'This works immediately once GPX is converted' .planning/research/FEATURES.md)" -eq 0 && test "$(grep -Fc '| Navigate imported route on glasses | HIGH | MEDIUM | P1 |' .planning/research/FEATURES.md)" -eq 1 && test "$(grep -Fc 'min/mi), moving time' .planning/research/FEATURES.md)" -eq 0 && test "$(grep -Fc 'summary-only in v1 per STATE decision' .planning/research/FEATURES.md)" -eq 2 && test "$(grep -Fc 'No new libraries needed' .planning/research/SUMMARY.md)" -eq 0 && test "$(grep -Fc 'Only two new library declarations' .planning/research/SUMMARY.md)" -eq 1 && test "$(grep -Fc '"v": 1' .planning/research/ARCHITECTURE.md)" -eq 1 && test "$(grep -Fc 'withTimeoutOrNull' .planning/research/PITFALLS.md)" -eq 0 && test "$(grep -Fc 'future.get(30, SECONDS)' .planning/research/PITFALLS.md)" -eq 1 && test "$(grep -Fc 'pause/resume, and metric computation' .planning/research/PITFALLS.md)" -eq 0 && test "$(grep -Fc 'down to ≤500 points' .planning/research/PITFALLS.md)" -eq 0 && test "$(grep -Fc 'up to ~999 points' .planning/research/PITFALLS.md)" -eq 1 && test "$(grep -c '11 message types' CLAUDE.md)" -eq 0 && test "$(grep -c '12 message types' CLAUDE.md)" -eq 1 && test "$(grep -Fc '(transitive)' CLAUDE.md)" -eq 0 && test "$(grep -Fc '(transitive,' CLAUDE.md)" -eq 0 && test "$(grep -c 'previously transitive via CXR SDK' CLAUDE.md)" -eq 4 && grep -Fq 'Iteration-4 design-doc fixes applied (2 blockers + 7 warnings from re-review #3)' .planning/STATE.md && echo ITER4-GATES-PASS</automated>
  </verify>
  <done>All B2/W1-W7 stale phrases are gone from their target files, replacement phrases appear at the exact expected counts, CLAUDE.md's four dependency rows carry the explicit-declaration wording (Retrofit row untouched), and STATE.md Last activity reads Iteration-4. Gate prints ITER4-GATES-PASS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Docs-only change (.planning/*.md + CLAUDE.md); no code, config, or dependency changes; no untrusted input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q05e-01 | Tampering | Design docs guiding Phase 4 | mitigate | Exact-count grep gates per target file verify the amendment landed verbatim and nowhere else |
| T-q05e-SC | Tampering | Package installs | accept | N/A — no npm/pip/cargo/gradle installs in this plan (docs only) |
</threat_model>

<verification>
Both task gates pass (B1-GATES-PASS, ITER4-GATES-PASS). Additionally:
- `git diff --stat` shows changes ONLY in the 7 declared files (no source files under phone/, glasses/, shared/).
- Gates are scoped to exact target files; never grep .planning/ recursively (this plan file quotes the stale phrases and would self-match).
</verification>

<success_criteria>
- All 15 coverage-audit rows applied (14 EDITs + 1 VERIFY-only), verbatim per the final spec — no paraphrasing, no scope expansion (FEATURES line 18, PITFALLS Pitfall 3 fallback text, CLAUDE.md Retrofit row, and CLAUDE.md lines 89-91 remain untouched).
- `waypoints=0;{last}` appears exactly 2/3/1 times in ROADMAP/ARCHITECTURE/STATE respectively and nowhere else in the repo docs.
- Both automated gates pass in a single run each.
</success_criteria>

<output>
Create `.planning/quick/260703-05e-iteration-4-design-doc-fixes-from-re-rev/260703-05e-SUMMARY.md` when done. Commit all 7 files + the summary as one commit: `docs: iteration-4 design-doc fixes from re-review #3 (B1 OSRM silent-via, B2 reuse claims, W1-W7)`
</output>
