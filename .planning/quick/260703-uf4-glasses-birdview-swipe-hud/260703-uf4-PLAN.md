---
phase: quick-260703-uf4
plan: 260703-uf4
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolConstants.kt
  - shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt
  - shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt
  - shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt
  - phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt
  - phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt
  - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt
  - phone/src/test/java/com/rokid/hud/phone/NavigationRouteTest.kt
  - glasses/src/main/java/com/rokid/hud/glasses/HudState.kt
  - glasses/src/main/java/com/rokid/hud/glasses/BluetoothClient.kt
  - glasses/src/main/java/com/rokid/hud/glasses/HudActivity.kt
  - glasses/src/main/java/com/rokid/hud/glasses/HudView.kt
  - glasses/src/main/java/com/rokid/hud/glasses/RouteBounds.kt
  - glasses/src/test/java/com/rokid/hud/glasses/HudStateTest.kt
  - glasses/src/test/java/com/rokid/hud/glasses/RouteBoundsTest.kt
autonomous: true
requirements: [UF4-D1, UF4-D2, UF4-D3, UF4-D4]

must_haves:
  truths:
    - "D1: A WHOLE_ROUTE layout mode exists that draws the entire route fit-to-bounds (start/end/current markers) in green-monochrome, without per-turn zoom."
    - "D2: The touchpad tap cycle advances FULL_SCREEN -> SMALL_CORNER -> SPORT -> WHOLE_ROUTE -> FULL_SCREEN (4-way, SMALL_CORNER kept); MINI_BOTTOM/MINI_SPLIT stay phone-driven."
    - "D3: A reverse-cycle method exists on HudState (previous page) and both onFling (HudView) and dpad/nav keys (HudActivity.dispatchKeyEvent) drive prev/next; single-tap still advances forward; double-tap still quits."
    - "D4: The route BT message carries an optional boolean full flag (default false, backward-compatible); phone sends full=true on the FIRST broadcast of each navigation start and false on reroute rebroadcasts; glasses store a full-flagged route into a SEPARATE wholeRoute (birdview source) while the live route keeps updating from every route message."
    - "D4 no-clobber (unit-proven): applying a full=true route sets wholeRoute; a subsequent full=false route with DIFFERENT waypoints updates the live route but leaves wholeRoute UNCHANGED (reroutes never overwrite the birdview source)."
    - "Message type count stays at 13 (full is a flag on the existing route message, not a new type)."
    - "testDebugUnitTest + assembleDebug both exit 0 after the change (existing ~208 tests still green + new tests)."
  artifacts:
    - path: "shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolConstants.kt"
      provides: "FIELD_ROUTE_FULL constant"
      contains: "FIELD_ROUTE_FULL"
    - path: "shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt"
      provides: "RouteMessage.full field (default false)"
      contains: "full"
    - path: "shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt"
      provides: "encode/decode of the route full flag"
      contains: "FIELD_ROUTE_FULL"
    - path: "glasses/src/main/java/com/rokid/hud/glasses/HudState.kt"
      provides: "WHOLE_ROUTE enum value, wholeRoute field, 4-way toggleLayout, reverse toggleLayoutBack"
      contains: "WHOLE_ROUTE"
    - path: "glasses/src/main/java/com/rokid/hud/glasses/RouteBounds.kt"
      provides: "Pure fit-to-bounds projection helper for the birdview"
      contains: "object RouteBounds"
    - path: "glasses/src/main/java/com/rokid/hud/glasses/HudView.kt"
      provides: "drawWholeRoute birdview + onFling horizontal swipe handling"
      contains: "drawWholeRoute"
    - path: "glasses/src/main/java/com/rokid/hud/glasses/HudActivity.kt"
      provides: "dpad/nav swipe key handling in dispatchKeyEvent"
      contains: "KEYCODE_DPAD_LEFT"
    - path: "shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt"
      provides: "route full flag round-trip + legacy-default tests"
    - path: "glasses/src/test/java/com/rokid/hud/glasses/HudStateTest.kt"
      provides: "4-page forward + reverse cycle tests + D4 no-clobber wholeRoute test"
    - path: "glasses/src/test/java/com/rokid/hud/glasses/RouteBoundsTest.kt"
      provides: "fit-to-bounds helper tests"
  key_links:
    - from: "phone NavigationManager (start paths)"
      to: "sendRoute(full=true)"
      via: "onRouteCalculated full param"
      pattern: "full"
    - from: "glasses BluetoothClient.processMessage Route branch"
      to: "HudState.wholeRoute"
      via: "full-flag gate"
      pattern: "wholeRoute"
    - from: "glasses HudView.drawWholeRoute"
      to: "HudState.wholeRoute + RouteBounds.fit"
      via: "birdview projection"
      pattern: "RouteBounds"
    - from: "glasses gestures (onFling + dispatchKeyEvent)"
      to: "HudState.toggleLayout / toggleLayoutBack"
      via: "onSwipeForward / onSwipeBack callbacks -> BluetoothClient"
      pattern: "toggleLayoutBack"
---

<objective>
Add a bird's-eye "whole route" HUD page to the Rokid glasses and formalize a swipeable 4-page touchpad cycle, plus a backward-compatible protocol addition so the birdview always shows the ORIGINAL imported route (never the rerouted live route). v1.x enhancement on the verified v1.

Purpose: Cyclists/runners can glance at the full route shape at any time, independent of the turn-by-turn live route (which reroutes overwrite when off-route). Four LOCKED decisions drive this (treated as CONTEXT — do not revisit):
- D1: new WHOLE_ROUTE layout (fit-to-bounds birdview, no per-turn zoom).
- D2: 4-way tap cycle FULL_SCREEN -> SMALL_CORNER -> SPORT -> WHOLE_ROUTE -> FULL_SCREEN (SMALL_CORNER kept; MINI_* stay phone-driven).
- D3: left/right swipe switches pages (both onFling AND dpad/nav key forms — hardware unknown); single-tap forward fallback; double-tap quits unchanged; add a reverse-cycle method.
- D4: optional boolean full flag on the existing route message; phone sets full=true on the first broadcast of each nav start and false on reroutes; glasses store full-flagged routes into a separate wholeRoute for the birdview; live route keeps updating; message-type count stays 13.

Output: shared protocol full flag (constant + model + codec), phone wiring (nav-start = full, reroute = not-full), glasses state (WHOLE_ROUTE + wholeRoute + 4-way + reverse), glasses rendering (drawWholeRoute + swipe gestures), and JVM tests for the cycle, the flag round-trip, the D4 no-clobber invariant, and the bounds-fit helper.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolConstants.kt
@shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt
@shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt
@shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt
@phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt
@phone/src/test/java/com/rokid/hud/phone/NavigationRouteTest.kt
@glasses/src/main/java/com/rokid/hud/glasses/HudState.kt
@glasses/src/main/java/com/rokid/hud/glasses/HudView.kt
@glasses/src/main/java/com/rokid/hud/glasses/HudActivity.kt
@glasses/src/main/java/com/rokid/hud/glasses/BluetoothClient.kt
@glasses/src/test/java/com/rokid/hud/glasses/HudStateTest.kt

# Build+test gate for EVERY task (exit 0 required; NO gradlew script — invoke the wrapper main class directly):
#   export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && \
#   java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain testDebugUnitTest assembleDebug -q
# Do NOT plan any device deploy — the orchestrator deploys + screencap-verifies on real hardware.

# Constraints (CLAUDE.md): Kotlin only; NO coroutines (Thread/Handler/Executors only); org.json for BT protocol
# (NOT Gson); green-monochrome rendering only (reuse the hudBrightGreen/hudGreen/hudDimGreen/hudDarkGreen/
# hudFaintGreen palette + tilePaint green ColorMatrix already in HudView); match existing HudView drawing +
# naming; findViewById (no ViewBinding); UPPER_SNAKE_CASE constants in companion objects; TAG logging.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Shared route `full` flag end-to-end + phone nav-start/reroute wiring</name>
  <files>shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolConstants.kt, shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt, shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt, shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt, phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt, phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt, phone/src/main/java/com/rokid/hud/phone/MainActivity.kt, phone/src/test/java/com/rokid/hud/phone/NavigationRouteTest.kt</files>
  <behavior>
    - encodeRoute(RouteMessage(full=true)) then decode -> ParsedMessage.Route with msg.full == true.
    - encodeRoute(RouteMessage(full=false)) then decode -> msg.full == false.
    - decode of a legacy route JSON WITHOUT the "full" key -> msg.full == false (backward compatibility; mirrors the existing missingFieldsDecodeWithDefaults sport_state test).
    - The route JSON still carries exactly one new key ("full") beyond the existing t/waypoints/distance/duration; message-type set is unchanged (still 13 types in ProtocolConstants.MessageType).
    - The 5-arg NavigationCallback.onRouteCalculated compiles for ALL THREE overriders (HudStreamingService, MainActivity.navCallback, FakeNavigationCallback in NavigationRouteTest); the existing NavigationRouteTest suite still passes.
  </behavior>
  <action>
    Implements D4 (protocol half + phone flag source). Keep the message-type count at 13 — this is a FLAG on the existing "route" message, NOT a new type.

    SHARED:
    - ProtocolConstants.kt: add `const val FIELD_ROUTE_FULL = "full"` alongside the other FIELD_* constants. Do NOT add anything to MessageType.
    - Messages.kt: add `val full: Boolean = false` as the LAST field of `RouteMessage` (default false so every existing constructor call site and test still compiles).
    - ProtocolCodec.kt encodeRoute: after the existing distance/duration puts, add `put(ProtocolConstants.FIELD_ROUTE_FULL, msg.full)`.
    - ProtocolCodec.kt decode ROUTE branch: read `full = json.optBoolean(ProtocolConstants.FIELD_ROUTE_FULL, false)` and pass it into the `RouteMessage(...)` constructor. optBoolean-with-default is the established lenient pattern (see the SETTINGS/SPORT_STATE branches) — a missing key or an old-glasses build both degrade to false.

    PHONE (flag source = the phone, per D4): the single sendRoute call lives in the NavigationCallback.onRouteCalculated, which fires on BOTH first-broadcast and reroutes, so the flag MUST be threaded from the call sites that know which is which. Add a `full: Boolean` parameter to `NavigationCallback.onRouteCalculated` (the cleanest discriminator; the callback is the only edge that reaches sendRoute):
    - NavigationManager.kt: change the interface method to `onRouteCalculated(waypoints, totalDistance, totalDuration, steps, full)`. NavigationManager is the interface OWNER/caller (it holds `callback: NavigationCallback` and INVOKES onRouteCalculated at ~lines 118/242/312/330) — it does NOT override the method; update those call sites, not an override:
      - `startNavigationWithRoute` (imported-route START) -> call onRouteCalculated(..., full = true).
      - `calculateRoute(...)`: add a `full: Boolean` parameter; forward it to its onRouteCalculated call. `startNavigation` -> `calculateRoute(currentLat, currentLng, destLat, destLng, full = true)`. The destination-only off-route reroute at the off-route branch -> `calculateRoute(lat, lng, destLat, destLng, full = false)`. The empty-waypoints degrade inside rerouteThroughRemainingWaypoints that calls calculateRoute -> `full = false`.
      - `rerouteThroughRemainingWaypoints` success + follow-route fallback onRouteCalculated calls -> `full = false` (both are reroutes).
    - HudStreamingService.kt: update the `onRouteCalculated` override (the `object : NavigationCallback` at ~line 304) signature to include `full`; pass it through: `sendRoute(waypoints, totalDistance, totalDuration, full)` and also forward to the ui callback `uiCallback?.onRouteCalculated(waypoints, totalDistance, totalDuration, steps, full)`. Add a `full: Boolean` parameter to `fun sendRoute(...)` and build `RouteMessage(waypoints, totalDistance, totalDuration, full)`. The `stopNavigation()` clearing call becomes `sendRoute(emptyList(), 0.0, 0.0, full = false)`.
    - MainActivity.kt: update its `onRouteCalculated(...)` override (the `object : NavigationCallback` at ~line 216) signature to include the new `full: Boolean` param (it can ignore the value — UI does not use it) so the interface still compiles.
    - phone/src/test/java/com/rokid/hud/phone/NavigationRouteTest.kt: this file defines a THIRD real implementer — `private class FakeNavigationCallback : NavigationCallback` (line 33) with `override fun onRouteCalculated(waypoints, totalDistance, totalDuration, steps)` at line 40. Because `testDebugUnitTest` compiles the test source set, a stale 4-arg override here fails the build ('onRouteCalculated' overrides nothing + abstract member not implemented) the moment the interface gains `full`. Update this override to the new 5-arg signature `override fun onRouteCalculated(waypoints: List<Waypoint>, totalDistance: Double, totalDuration: Double, steps: List<NavigationStep>, full: Boolean)`. It can IGNORE `full` (exactly like MainActivity ignores it) and keep its existing body `routeCalculatedSteps.add(steps)` — no new assertions are required. Do NOT otherwise change NavigationRouteTest's NAVV-01/02c/03 tests.

    Do NOT reformat unrelated code. Do NOT introduce Gson/coroutines. Keep KDoc accurate where you touch startNavigationWithRoute / sendRoute (note that full=true marks the preserved original route for the glasses birdview).

    TESTS (mirror ProtocolCodecTest style — plain JUnit4, real org.json on the shared test classpath, exact-value assertEquals/assertTrue): add to ProtocolCodecTest.kt:
    - routeFullFlagRoundTripTrue: encode RouteMessage(waypoints=[Waypoint(47.0,-122.0),Waypoint(47.1,-122.1)], 1000.0, 300.0, full=true) -> decode is ParsedMessage.Route -> assertTrue(msg.full) and assert waypoints/distance/duration survive.
    - routeFullFlagRoundTripFalse: same with full=false -> assertFalse(msg.full).
    - legacyRouteWithoutFullDefaultsFalse: decode of a hand-written legacy JSON string with t/waypoints/distance/duration but NO "full" key -> ParsedMessage.Route with msg.full == false (backward-compat guard).
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain testDebugUnitTest assembleDebug -q</automated>
  </verify>
  <done>FIELD_ROUTE_FULL exists; RouteMessage has full (default false); encodeRoute/decode round-trip the flag and legacy JSON defaults to false; phone nav-start paths pass full=true and both reroute paths pass full=false through the (now 5-arg) onRouteCalculated into sendRoute; MessageType still has 13 entries; the interface OWNER NavigationManager updates its call sites and all THREE onRouteCalculated implementers compile against the 5-arg signature — HudStreamingService (object : NavigationCallback), MainActivity.navCallback (object : NavigationCallback), and FakeNavigationCallback (NavigationRouteTest.kt); NavigationRouteTest's existing tests still pass; testDebugUnitTest + assembleDebug exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Glasses HudState — WHOLE_ROUTE mode + wholeRoute field + 4-way & reverse cycle + BluetoothClient store</name>
  <files>glasses/src/main/java/com/rokid/hud/glasses/HudState.kt, glasses/src/main/java/com/rokid/hud/glasses/BluetoothClient.kt, glasses/src/test/java/com/rokid/hud/glasses/HudStateTest.kt</files>
  <behavior>
    - HudState() default layoutMode is FULL_SCREEN.
    - toggleLayout() forward cycle: FULL_SCREEN -> SMALL_CORNER -> SPORT -> WHOLE_ROUTE -> FULL_SCREEN (4-way).
    - toggleLayoutBack() reverse cycle: FULL_SCREEN -> WHOLE_ROUTE -> SPORT -> SMALL_CORNER -> FULL_SCREEN.
    - Round trip: any of the four cycle modes, toggleLayout() then toggleLayoutBack(), returns to the same mode; and toggleLayoutBack() then toggleLayout() returns to the same mode.
    - MINI_BOTTOM and MINI_SPLIT are phone-driven: toggleLayout() maps both to FULL_SCREEN (unchanged); toggleLayoutBack() also maps both to FULL_SCREEN (do not strand a phone mode in the cycle).
    - D4 no-clobber: applying a full=true route sets HudState.wholeRoute; a subsequent full=false route with DIFFERENT waypoints updates the live waypoints but leaves wholeRoute UNCHANGED.
  </behavior>
  <action>
    Implements D2 (4-way cycle), D3 (reverse method), and the glasses half of D4 (store full-flagged route into wholeRoute).

    HudState.kt:
    - Add `WHOLE_ROUTE` to the `MapLayoutMode` enum (place it after SPORT, before the phone-driven MINI_* entries) with a KDoc: "Tap/swipe-cycle only: bird's-eye view of the entire route fit-to-bounds (D1)."
    - Add `val wholeRoute: List<Waypoint> = emptyList()` to the HudState data class (near `waypoints`), KDoc: "The ORIGINAL imported route, set once from a full-flagged route message; rendered only by WHOLE_ROUTE. NOT overwritten by reroutes (D4)."
    - Update `toggleLayout()` to the 4-way forward cycle: FULL_SCREEN -> SMALL_CORNER -> SMALL_CORNER -> SPORT, SPORT -> WHOLE_ROUTE, WHOLE_ROUTE -> FULL_SCREEN; keep MINI_BOTTOM -> FULL_SCREEN and MINI_SPLIT -> FULL_SCREEN. `when` must remain exhaustive over all enum values (no else branch).
    - Add `fun toggleLayoutBack(): HudState = copy(layoutMode = when (layoutMode) { ... })` implementing the reverse: FULL_SCREEN -> WHOLE_ROUTE, WHOLE_ROUTE -> SPORT, SPORT -> SMALL_CORNER, SMALL_CORNER -> FULL_SCREEN, MINI_BOTTOM -> FULL_SCREEN, MINI_SPLIT -> FULL_SCREEN. Exhaustive `when`.

    BluetoothClient.kt:
    - Add `fun toggleLayoutBack() { currentState = currentState.toggleLayoutBack(); onStateUpdate(currentState) }` next to the existing toggleLayout().
    - In processMessage's `is ParsedMessage.Route` branch: keep updating the live route (waypoints/totalDistance/totalDuration) EXACTLY as today, AND when `parsed.msg.full` is true, also set `wholeRoute = parsed.msg.waypoints` in the same copy(). So: `currentState = currentState.copy(waypoints = parsed.msg.waypoints, totalDistance = ..., totalDuration = ..., wholeRoute = if (parsed.msg.full) parsed.msg.waypoints else currentState.wholeRoute)`. Full=false/absent leaves wholeRoute untouched — reroutes never clobber the birdview.

    Do NOT touch the Settings branch's SPORT-preservation logic. Do NOT add coroutines. Keep the green-render palette out of this task (pure state only).

    TESTS (append to HudStateTest.kt, mirroring its style — pure JVM, no android.jar, injected values):
    - fourPageForwardCycle: from HudState(), assert successive toggleLayout() gives SMALL_CORNER, SPORT, WHOLE_ROUTE, FULL_SCREEN (extend/replace the existing tapCycle intent; keep coverage of SMALL_CORNER and SPORT).
    - fourPageReverseCycle: from HudState(), assert successive toggleLayoutBack() gives WHOLE_ROUTE, SPORT, SMALL_CORNER, FULL_SCREEN.
    - forwardThenBackIsIdentity: for each of the four cycle modes, toggleLayout().toggleLayoutBack() == original mode.
    - miniModesReverseToFull: HudState(layoutMode = MINI_BOTTOM).toggleLayoutBack() and MINI_SPLIT.toggleLayoutBack() both == FULL_SCREEN (keep the existing miniModesTapReturnToFull for the forward direction).
    - fullFlaggedRouteSetsWholeRouteAndRerouteDoesNotClobber (D4 no-clobber — the load-bearing D4 invariant, tested at the pure-JVM HudState/copy seam this task already uses; NO Robolectric needed since it only exercises data-class copy()). Given routeA waypoints=[Waypoint(47.0,-122.0),Waypoint(47.1,-122.1)] and routeB waypoints=[Waypoint(48.0,-121.0),Waypoint(48.5,-120.5)] (DIFFERENT from routeA): start from HudState(); apply the full=true store — `s1 = s0.copy(waypoints = routeA, wholeRoute = routeA)` (i.e. the exact copy() the BluetoothClient full=true branch performs) — assert s1.wholeRoute == routeA AND s1.waypoints == routeA. Then apply the full=false store — `s2 = s1.copy(waypoints = routeB, wholeRoute = s1.wholeRoute)` (the full=false branch keeps the prior wholeRoute) — assert s2.waypoints == routeB (live route updated) AND s2.wholeRoute == routeA (birdview source UNCHANGED, != routeB). This mirrors the copy() the BluetoothClient Route branch performs so the assertion tracks the real store logic without a Looper.
    Update the existing tapCycleFullCornerSportFull test to the new 4-way order (SPORT now advances to WHOLE_ROUTE, and WHOLE_ROUTE advances to FULL_SCREEN) so it does not assert the stale 3-way order.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain testDebugUnitTest assembleDebug -q</automated>
  </verify>
  <done>MapLayoutMode has WHOLE_ROUTE; HudState has wholeRoute (default empty); toggleLayout() is the 4-way forward cycle and toggleLayoutBack() the exact reverse (both exhaustive, MINI_* -> FULL_SCREEN in both); BluetoothClient stores a full-flagged route into wholeRoute while still updating the live route and exposes toggleLayoutBack(); new + updated HudStateTest cases pass INCLUDING fullFlaggedRouteSetsWholeRouteAndRerouteDoesNotClobber (full=true sets wholeRoute; a differing full=false route updates the live waypoints but leaves wholeRoute unchanged); testDebugUnitTest + assembleDebug exit 0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Glasses birdview rendering (drawWholeRoute + RouteBounds helper) + swipe gestures (onFling + dispatchKeyEvent)</name>
  <files>glasses/src/main/java/com/rokid/hud/glasses/RouteBounds.kt, glasses/src/main/java/com/rokid/hud/glasses/HudView.kt, glasses/src/main/java/com/rokid/hud/glasses/HudActivity.kt, glasses/src/test/java/com/rokid/hud/glasses/RouteBoundsTest.kt</files>
  <behavior>
    - RouteBounds.fit(points, viewW, viewH, padding) for a single point returns a projector centered in the view (that point maps to view center) and does not divide by zero (degenerate bbox).
    - For two+ points spanning a bbox, both extreme corners map INSIDE [padding, viewW-padding] x [padding, viewH-padding] (fit-to-bounds, aspect-preserving so the route is not stretched).
    - The projector maps larger longitude to larger x, and larger latitude to SMALLER y (screen y grows downward) — i.e. north is up.
    - fit(emptyList, ...) returns null (caller draws the empty-state text instead of crashing).
  </behavior>
  <action>
    Implements D1 (birdview rendering) and the rendering/gesture half of D3. Green-monochrome only — reuse HudView's existing palette (hudBrightGreen/hudGreen/hudDimGreen/hudDarkGreen), routePaint/routeGlowPaint, tilePaint green ColorMatrix, mapBorderPaint, dotPaint, smallTextPaint. NO per-turn zoom — fit the full route bounding box.

    NEW RouteBounds.kt (pure, JVM-testable, no android.* imports — same discipline as SportFormat): `object RouteBounds` with a `fit(points: List<Waypoint>, viewW: Float, viewH: Float, padding: Float): Projector?` returning null for empty input. Use equirectangular projection adequate for a city-scale route: project lng directly and lat scaled by cos(midLatRad) so the aspect is not distorted, compute min/max of projected x/y, pick ONE uniform scale = min((viewW-2*padding)/spanX, (viewH-2*padding)/spanY) with a guard so a zero span (single point / vertical or horizontal line) does not divide by zero (fall back to scale that centers the degenerate axis), and center the bbox in the view. Expose a `Projector` (a small class or fun interface) with `project(lat, lng): PointF-like pair` — to keep it android-free, return a `FloatArray(2)` or a data class `ScreenXY(val x: Float, val y: Float)` rather than android.graphics.PointF. Map larger lng -> larger x and larger lat -> smaller y (invert the y axis so north is up).

    HudView.kt:
    - onDraw when-branch: add `MapLayoutMode.WHOLE_ROUTE -> drawWholeRoute(canvas, w, h)`. Keep the `when` exhaustive.
    - drawModeIndicator: add `MapLayoutMode.WHOLE_ROUTE -> "[ ROUTE ]"` to the label `when` (keep exhaustive).
    - Add `private fun drawWholeRoute(canvas, w, h)`: if `state.wholeRoute.size < 2` draw a centered "No route" hint via smallTextPaint (mirror the "Waiting for GPS..." empty-state) and return. Otherwise call `RouteBounds.fit(state.wholeRoute, w, h, padding=24f)`; if null, draw the hint and return. Optionally draw a black map area border (mapBorderPaint) — it is ACCEPTABLE to render the route line over black (no tiles at this fit-zoom); prioritize showing the full route SHAPE (per D1). Build a Path by projecting every wholeRoute point through the projector; draw routeGlowPaint then routePaint (same two-pass style as drawRouteOnTiles). Draw markers: START = a filled dot (dotPaint) at wholeRoute.first(); END = a hollow ring (a Paint with STROKE, hudBrightGreen) at wholeRoute.last(); CURRENT position = the existing player-arrow triangle (reuse a small filled triangle via arrowPaint) at the projection of (state.latitude, state.longitude) IF state has a real fix (lat/lng not both 0.0) — otherwise omit the current marker. Do NOT rotate to bearing (birdview is north-up, unlike drawLiveMap). Keep all allocations modest (Path + a couple of Paints); this page is not on the 1Hz hot path.
    - GestureDetector: add `override fun onFling(e1, e2, velocityX, velocityY): Boolean` — if `abs(velocityX) > abs(velocityY)` and `abs(velocityX)` exceeds a small threshold (e.g. a `SWIPE_MIN_VELOCITY` companion const ~ 200f), treat it as a horizontal swipe: velocityX > 0 (left-to-right / rightward) -> `onSwipeForward?.invoke()`, velocityX < 0 -> `onSwipeBack?.invoke()`, return true; otherwise return false so taps/other gestures still work. e1 may be nullable depending on API — guard with `?`. Add two new public lambdas `var onSwipeForward: (() -> Unit)? = null` and `var onSwipeBack: (() -> Unit)? = null` next to onLayoutToggle/onDoubleTap. Keep onSingleTapConfirmed (forward fallback) and onDoubleTap (quit) UNCHANGED.

    HudActivity.kt:
    - Wire the new callbacks in onCreate near the existing hudView.onLayoutToggle/onDoubleTap: `hudView.onSwipeForward = { btClient.toggleLayout() }` and `hudView.onSwipeBack = { btClient.toggleLayoutBack() }`.
    - dispatchKeyEvent: ALONGSIDE the existing KEYCODE_ENTER double-tap-to-quit, on ACTION_DOWN handle the likely touchpad swipe key codes (hardware unknown, so cover both families): `KEYCODE_DPAD_RIGHT` and `KEYCODE_SYSTEM_NAVIGATION_RIGHT` -> `btClient.toggleLayout()` (forward) + return true; `KEYCODE_DPAD_LEFT` and `KEYCODE_SYSTEM_NAVIGATION_LEFT` -> `btClient.toggleLayoutBack()` (back) + return true. Log at TAG which key fired (matches the existing KEYCODE_ENTER log line). Leave KEYCODE_ENTER handling and the super.dispatchKeyEvent fall-through intact. Degrade gracefully: if neither swipe form is delivered by the hardware, single-tap-forward (already wired) still cycles.

    Constraints recap: Kotlin, no coroutines, green-only, findViewById already used, UPPER_SNAKE_CASE consts in the companion object, TAG logging. Do NOT add a tile fetch for the birdview zoom (tiles for the whole-route fit are not cached; black background is acceptable per D1).

    TESTS (NEW RouteBoundsTest.kt in glasses/src/test — pure JUnit4, no android deps; RouteBounds is android-free so it runs on plain JVM like SportFormatTest):
    - singlePointCentersWithoutNaN: fit(listOf(Waypoint(37.0,-122.0)), 400f, 400f, 20f) is non-null; project that point -> x within [20,380], y within [20,380], and neither coordinate is NaN/Infinite.
    - twoPointsFitInsideBoundsAndNorthUp: fit for Waypoint(37.0,-122.0) and Waypoint(37.1,-121.9); project both -> both x in [20, 380] and y in [20,380]; the point with the LARGER lat has the SMALLER y (north up); the point with the LARGER lng has the LARGER x.
    - emptyReturnsNull: fit(emptyList(), 400f, 400f, 20f) == null.
    (drawWholeRoute itself needs a Canvas/android — do NOT unit-test the draw; the RouteBounds math is the extracted testable seam, and the orchestrator screencap-verifies the actual birdview on hardware.)
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain testDebugUnitTest assembleDebug -q</automated>
  </verify>
  <done>RouteBounds.fit exists (android-free, null on empty, no divide-by-zero on degenerate bbox, north-up); HudView renders WHOLE_ROUTE via drawWholeRoute (full route line fit-to-bounds + start/end/current markers, green-only, north-up, black background acceptable) and shows "[ ROUTE ]" in the mode indicator; onFling drives onSwipeForward/onSwipeBack (horizontal-dominant, thresholded) with tap/double-tap unchanged; HudActivity wires those callbacks and handles DPAD_LEFT/RIGHT + SYSTEM_NAVIGATION_LEFT/RIGHT in dispatchKeyEvent alongside KEYCODE_ENTER; RouteBoundsTest passes; testDebugUnitTest + assembleDebug exit 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phone -> glasses (BT SPP) | Route messages (now carrying the optional `full` flag) cross the untrusted Bluetooth serial link and are decoded by ProtocolCodec on the glasses. |
| touchpad -> glasses app | Touch/fling MotionEvents and key events (DPAD/SYSTEM_NAVIGATION) are attacker-uncontrolled local hardware input, but malformed/rapid input must not crash the HUD. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-uf4-01 | Tampering | ProtocolCodec.decode route branch | mitigate | `full` read via `optBoolean(..., false)`; a malformed/missing/absent key degrades to false (backward-compatible), never throws — decode already returns ParsedMessage.Unknown on any parse exception. |
| T-uf4-02 | Denial of Service | HudView.drawWholeRoute / RouteBounds.fit | mitigate | Empty or single-point wholeRoute and zero-span bbox are guarded (null / centered fallback, no divide-by-zero); draws are off the 1Hz hot path and allocate only a Path + a few Paints. |
| T-uf4-03 | Denial of Service | HudView.onFling / HudActivity.dispatchKeyEvent | mitigate | Fling handler is horizontal-dominant + velocity-thresholded and returns false otherwise (no gesture starvation); key handler only consumes the specific swipe/enter codes and falls through to super for everything else. Rapid page flips only mutate an immutable HudState copy — no unbounded work. |
| T-uf4-04 | Information Disclosure | route waypoints on BT | accept | Route geometry is already broadcast unencrypted over SPP in v1 (StateMessage/RouteMessage); the `full` flag adds no new PII and does not change the existing threat posture. |
| T-uf4-SC | Tampering | npm/pip/cargo installs | n/a | No package-manager installs in this change — pure Kotlin edits against existing deps (junit + org.json already on the test classpath). No legitimacy gate required. |
</threat_model>

<verification>
- Build+test gate passes for every task: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain testDebugUnitTest assembleDebug -q` exits 0.
- No new BT message type: `ProtocolConstants.MessageType` still declares exactly 13 constants.
- Backward compatibility: a legacy route JSON (no `full` key) decodes with `full == false` (covered by test).
- Whole-route independence: after a reroute (route message with full=false) with DIFFERENT waypoints, `HudState.wholeRoute` is unchanged while the live route updates — enforced by the Task 2 BluetoothClient store logic AND proven at the pure-JVM copy() seam by fullFlaggedRouteSetsWholeRouteAndRerouteDoesNotClobber.
- Existing ~208 tests remain green (no regression) — the onRouteCalculated signature change adds `full` to the interface OWNER's call sites (NavigationManager) and updates ALL THREE implementers: HudStreamingService (object : NavigationCallback), MainActivity.navCallback (object : NavigationCallback), and FakeNavigationCallback (phone/src/test .../NavigationRouteTest.kt). testDebugUnitTest compiles the test source set, so the FakeNavigationCallback override MUST be migrated to 5 args in the same task or the build fails.
</verification>

<success_criteria>
- D1: WHOLE_ROUTE layout draws the entire route fit-to-bounds with start/end/current markers, green-monochrome, no per-turn zoom (screencap-verified on hardware by the orchestrator).
- D2: tap cycle is FULL_SCREEN -> SMALL_CORNER -> SPORT -> WHOLE_ROUTE -> FULL_SCREEN; MINI_* remain phone-driven (unit-tested).
- D3: onFling AND dpad/nav keys drive prev/next; single-tap forward fallback + double-tap quit unchanged; reverse-cycle method exists (unit-tested + hardware-verified which input the touchpad emits).
- D4: route message carries optional `full` (default false, backward-compatible); phone sends full=true on each nav-start first broadcast and false on reroutes; glasses store full-flagged routes into a separate wholeRoute; live route keeps updating; the no-clobber invariant is unit-proven (fullFlaggedRouteSetsWholeRouteAndRerouteDoesNotClobber); message-type count stays 13 (round-trip + legacy-default unit-tested).
- testDebugUnitTest + assembleDebug exit 0 across all three tasks.
</success_criteria>

<output>
Create `.planning/quick/260703-uf4-glasses-birdview-swipe-hud/260703-uf4-SUMMARY.md` when done.
</output>
</content>
</invoke>
