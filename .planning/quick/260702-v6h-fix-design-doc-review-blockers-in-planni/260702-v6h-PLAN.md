---
phase: quick-260702-v6h
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/research/ARCHITECTURE.md
  - .planning/research/FEATURES.md
  - .planning/research/PITFALLS.md
  - .planning/research/SUMMARY.md
autonomous: true
requirements: [NAVV-02, REC-05, REC-06, REC-07, HUD-02, HUD-03, UPL-01, UPL-03]
must_haves:
  truths:
    - "REQUIREMENTS.md defines 25 v1 requirements including REC-06 (JSON persistence + checkpoint) and REC-07 (sport_state protocol message + 1Hz broadcast), both mapped to Phase 1 in traceability"
    - "Every doc describing GPX navigation states the OSRM via-point strategy (Douglas-Peucker downsample, all points as via-waypoints, steps=true) with follow-route fallback; no doc claims waypoint reuse works unchanged or that OSRM is skipped for GPX routes"
    - "No planning doc claims Strava supports PKCE or cites 600 requests/30min; rate limits are 300 reads/15min + 1,000 writes/15min with a verify-at-developers.strava.com note"
    - "ROADMAP Phase 2 success criteria describe the real tap cycle (Full -> Corner -> Sport -> Full; phone-set Mini modes unchanged, tap returns to Full)"
    - "All three research docs carry a phase-numbering banner mapping research phases to final roadmap phases"
    - "ARCHITECTURE.md defers PAUSED to v2 in every state-machine reference (v1: IDLE -> TRACKING -> FINISHED) and notes moving time comes from a speed threshold, not a paused state"
    - "STATE.md Decisions records via-point routing, protocol split, PAUSED-deferral, and moving-time-consumer rows; Pending Todos holds the four review-derived items"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "REC-06/REC-07 definitions; reworded NAVV-02, HUD-02, UPL-01, UPL-03; expanded REC-05; 25-requirement traceability"
      contains: "REC-07"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 1 REC-01..REC-07 with persistence/broadcast/unit-test criteria; Phase 2 tap-cycle criteria; Phase 4 GPX via-point strategy note"
      contains: "via-waypoints"
    - path: ".planning/STATE.md"
      provides: "4 new decision rows, 4 review todos, updated activity/session lines"
      contains: "via-point"
    - path: ".planning/PROJECT.md"
      provides: "rate-limit verification note appended to Strava API constraint"
      contains: "developers.strava.com"
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "phase banner; via-point Flow 2 and Pattern 4; OkHttp+Gson build-order bullet; PAUSED deferred to v2"
      contains: "P3(Glasses)"
    - path: ".planning/research/FEATURES.md"
      provides: "GPX conversion + navigation rows at MEDIUM complexity describing required OsrmClient/NavigationManager extensions"
      contains: "via-waypoints"
    - path: ".planning/research/PITFALLS.md"
      provides: "numbering banner; follow-route harmonization in Pitfall 3; corrected Pitfall 5 attribution naming ActivitySessionManager and OverpassSpeedLimitClient"
      contains: "OverpassSpeedLimitClient"
    - path: ".planning/research/SUMMARY.md"
      provides: "phase-numbering banner"
      contains: "P3(Glasses)"
  key_links:
    - from: ".planning/REQUIREMENTS.md HUD-02"
      to: ".planning/REQUIREMENTS.md REC-07"
      via: "explicit cross-reference (protocol delivered Phase 1, consumed Phase 2)"
      pattern: "REC-07"
    - from: ".planning/REQUIREMENTS.md UPL-03"
      to: ".planning/REQUIREMENTS.md REC-06"
      via: "explicit cross-reference (persistence owned by REC-06)"
      pattern: "REC-06"
    - from: ".planning/ROADMAP.md Phase 1 Requirements line"
      to: ".planning/REQUIREMENTS.md REC-06/REC-07"
      via: "requirements list"
      pattern: "REC-05, REC-06, REC-07"
    - from: ".planning/research/ARCHITECTURE.md Flow 2"
      to: ".planning/ROADMAP.md Phase 4 strategy note"
      via: "matching via-waypoint strategy wording"
      pattern: "via-waypoints"
---

<objective>
Apply 12 code-verified design-review fixes across 8 planning documents so every doc reflects verified code facts (NavigationManager/OsrmClient only support 2-point destination routing; glasses toggleLayout() cycles Full<->Corner only; Strava has no PKCE; no cumulative-distance code exists) and the final 5-phase roadmap structure.

Purpose: These docs drive Phase 1-5 planning. False claims ("waypoint reuse works unchanged", "skip OSRM entirely", 5-mode tap cycle, PKCE) would propagate into phase plans and produce broken implementations. All decisions are FINAL from a completed 2-pass review — do not re-litigate; apply exactly.

Output: Corrected .planning/REQUIREMENTS.md, ROADMAP.md, STATE.md, PROJECT.md, and research/{ARCHITECTURE,FEATURES,PITFALLS,SUMMARY}.md. NO source code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/FEATURES.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md

IMPORTANT — prior fix pass: commit ae4fa84 ("docs: fix critical + warning issues from 4-reviewer audit") already applied part of this review; the working tree is clean. The following review items are ALREADY APPLIED and need verification only, not editing:

- FIX 3 (PKCE): PROJECT.md Strava context + constraint lines and FEATURES.md OAuth row already read "Authorization Code Grant (no PKCE)"; grep confirms zero occurrences of "OAuth 2.0 with PKCE", "+ PKCE", or "600 request" in any of the 8 docs or CLAUDE.md.
- FIX 4 (REQUIREMENTS half): HUD-03 already describes the real tap cycle (Full -> Corner -> Sport -> Full; Mini modes phone-set, tap returns to Full). Only ROADMAP Phase 2 SC alignment remains.
- FIX 5 (PITFALLS half): all seven "Phase to address:" lines and the Pitfall-to-Phase Mapping table already use final roadmap numbering. Only the banners (all three research docs) remain.
- FIX 6 (mostly): ARCHITECTURE.md component table ("OkHttp-based client"), Integration Points ("OkHttp with Authenticator interceptor"), and Internal Boundaries ("OkHttp call") are done. Only the build-order bullet at line ~547 remains.
- FIX 12 (mostly): PITFALLS.md Pitfall 5 "Why it happens" already states haversine is proximity-only and REC-02 distance is net-new. Only the ActivitySessionManager/OverpassSpeedLimitClient naming remains.

Do NOT hunt for stale text that grep shows is already gone. Confirmed still-stale anchors: ARCHITECTURE.md lines ~218, ~224, ~401 (skips-OSRM claims), ~83/~318/~529 (PAUSED/v1.x), ~547 (build-order bullet); FEATURES.md lines ~18-19; PITFALLS.md line ~104.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix core planning docs (REQUIREMENTS, ROADMAP, STATE, PROJECT)</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md</files>
  <action>
Apply the following edits verbatim with the Edit tool (line references approximate; anchor strings exact).

**.planning/REQUIREMENTS.md** (FIX 1, 2, 4-verify, 8, 11):

1. Replace the NAVV-02 body (currently "Route line and map guidance work for Strava-routed navigation. Voice directions announce distance to next waypoint. Note: maneuver arrows from OSRM turn data may not be available for pure GPX routes without OSRM refinement; \"Follow route\" display is the fallback.") with:
   "- [ ] **NAVV-02**: Existing navigation features (route line, maneuver arrows, voice directions via TTS/A2DP) work for Strava-routed navigation via OSRM via-point routing; when OSRM routing is unavailable, navigation degrades gracefully to follow-route mode (route line + distance to next waypoint, no turn instructions)"

2. In REC-05, after "AlarmManager watchdog to detect silent GPS stoppage", append: ", and GPS-staleness detection (warn when no fix received for >30s)" — per FIX 11, REC-05 must carry the full multi-layer battery-defense scope (background location permission, watchdog, staleness detection).

3. Insert two new requirements immediately after the REC-05 line (FIX 2):
   "- [ ] **REC-06**: Session data (track points + computed metrics) persists to local JSON on recording stop, with a periodic checkpoint (every 60s or 500 points, whichever comes first) for crash resilience; persisted sessions survive app restart"
   "- [ ] **REC-07**: Phone defines a `sport_state` Bluetooth protocol message (shared module: message type + codec) and broadcasts it at ~1Hz during recording with elapsed time, moving time, distance, current speed/pace, and session state; elapsed time and distance are monotonic non-decreasing within a session"

4. Replace the HUD-02 body (currently "Sport metrics update in real-time (~1Hz) via a new `sport_state` Bluetooth protocol message from phone") with:
   "- [ ] **HUD-02**: Sport metrics update in real-time (~1Hz) on glasses by consuming the `sport_state` Bluetooth message (message type + phone-side broadcast delivered in Phase 1 via REC-07)"

5. HUD-03: VERIFY only — current text already matches the FIX 4 target. Do not edit.

6. Replace the UPL-01 body with (FIX 8, adds moving time):
   "- [ ] **UPL-01**: After navigation ends, phone displays an activity summary: total time, moving time, distance, average speed/pace, route map"

7. Replace the UPL-03 body (currently "Activity track data is persisted locally as JSON before upload (data survives upload failures, app restarts)") with (FIX 2, avoids duplicating REC-06):
   "- [ ] **UPL-03**: Upload failure never deletes local activity data (persisted per REC-06); failed uploads can be retried"

8. In the Traceability table, insert after the "| REC-05 | Phase 1 | Pending |" row:
   "| REC-06 | Phase 1 | Pending |"
   "| REC-07 | Phase 1 | Pending |"

9. Update Coverage: "- v1 requirements: 23 total" -> "- v1 requirements: 25 total"; "- Mapped to phases: 23" -> "- Mapped to phases: 25".

10. Update footer: "*Last updated: 2026-07-02 after roadmap creation*" -> "*Last updated: 2026-07-02 after design-doc review fixes*".

**.planning/ROADMAP.md** (FIX 1, 2, 4, 11):

11. Phase 1 "**Requirements:** REC-01, REC-02, REC-03, REC-04, REC-05" -> "**Requirements:** REC-01, REC-02, REC-03, REC-04, REC-05, REC-06, REC-07".

12. Phase 1 SC#1: after "moving time, and recording state" append "; broadcast occurs while recording and is verifiable via logs" (covers FIX 2's broadcast criterion without duplicating SC#1).

13. Phase 1 SC#4: replace "Session data (track points + metrics) persists after recording stops and survives app restart" with "Session data (track points + computed metrics) persists as local JSON on recording stop and survives app restart, with a periodic checkpoint (every 60s or 500 points) for crash resilience".

14. Phase 1 SC#7: replace "ActivitySessionManager has unit tests (JUnit 4) covering state machine transitions (IDLE → TRACKING → FINISHED) and metric computation (distance, pace, elapsed time)" with "Unit tests exist and pass (first tests in the repo; JUnit in shared/phone modules) covering ProtocolCodec `sport_state` encode/decode round-trip and ActivitySessionManager state machine transitions (IDLE → TRACKING → FINISHED) + metric computation (distance, pace, elapsed time)" (FIX 11).

15. Phase 2 SC#1: replace "User can cycle the glasses display through layout modes to reach a new SPORT mode" with "User can cycle the glasses layout via tap to reach the new SPORT mode: tap cycle is Full → Corner → Sport → Full" (FIX 4).

16. Phase 2 SC#4: replace "Temple-tap cycling includes SPORT mode in the layout cycle" with "Phone-set Mini modes (Mini Strip, Mini Split) are unchanged: glasses tap from a Mini mode returns to Full (existing behavior preserved)" (FIX 4 — SC#1 now carries the cycle, SC#4 carries Mini-mode preservation).

17. Phase 4: insert a new line directly after the "**Requirements:** RIMP-01, RIMP-02, RIMP-03, RIMP-04, NAVV-01, NAVV-02, NAVV-03" line (FIX 1):
   "**GPX navigation strategy (decided 2026-07-02):** Downsample imported GPX with Douglas-Peucker (epsilon ~10-20m, target ≤200 points), then call OSRM /route with ALL downsampled points as via-waypoints (steps=true) to get real turn-by-turn steps and a road-snapped route. Graceful fallback: if OSRM via-point routing fails or is unavailable, navigate the raw GPX waypoints in follow-route mode (route line + distance-to-next-waypoint; no turn arrows or TTS). Complexity: MEDIUM-HIGH. Scope: (a) OsrmClient multi-waypoint support (currently builds a 2-point URL only), (b) a new waypoint-accepting NavigationManager path (startNavigation() currently accepts only a destination) — fix the known steps/currentStepIndex data race in code touched by this work, (c) Douglas-Peucker downsampling, (d) follow-route fallback mode."

**.planning/STATE.md** (FIX 1, 7, 10, plus the "Also" items):

18. Append four rows to the Decisions table:
   "| GPX routes navigate via OSRM via-point routing: Douglas-Peucker downsample (epsilon ~10-20m, ≤200 points), then OSRM /route with all points as via-waypoints (steps=true); fallback to follow-route mode (route line + distance to next waypoint, no turn instructions/TTS) when OSRM fails | Verified code facts: NavigationManager.startNavigation() accepts only a destination and OsrmClient builds 2-point URLs — the research claim that waypoint reuse works unchanged was false. Via-points restore real turn-by-turn for Strava routes | 2026-07-02 |"
   "| Protocol split: sport_state message type + phone-side ~1Hz broadcast ship in Phase 1 (REC-07); glasses consumption ships in Phase 2 (HUD-02) | Phase 1 is log-verifiable without glasses work; Phase 2 consumes an already-proven message | 2026-07-02 |"
   "| PAUSED session state deferred to v2; v1 machine is IDLE → TRACKING → FINISHED (with reset) | Moving time is computed from a speed threshold (<0.5 m/s = stopped), not from a paused state | 2026-07-02 |"
   "| Moving time consumer in v1 = activity summary only (UPL-01), not the sport HUD | Keeps the SPORT layout simple; sport_state still carries moving time so the summary pipeline has it | 2026-07-02 |"

19. Replace the entire Pending Todos bullet list (currently 4 bullets: Douglas-Peucker epsilon / OEM test devices / Sport HUD layout / client_secret management) with exactly (FIX 10):
   "- Douglas-Peucker epsilon (10m vs 20m) — decide empirically in Phase 4 with a real Strava route"
   "- OEM battery-optimization verification for Phase 1 SC — test on the user's actual connected phone (30-min screen-off recording)"
   "- Sport HUD layout design (metric arrangement on monochrome green) — resolve during Phase 2 discuss/UI-spec"
   "- Verify Strava rate-limit figures at developers.strava.com during Phase 3 research"
   The dropped client_secret bullet is a settled decision already captured under Key Constraints and PROJECT.md — not a todo.

20. Update "**Last activity:** 2026-07-02 -- Roadmap created" -> "**Last activity:** 2026-07-02 -- Design-doc review fixes applied to planning docs (12 blockers)".

21. Session Continuity: "**Last session:** 2026-07-02 -- Roadmap and state files created" -> "**Last session:** 2026-07-02 -- Design-doc review fixes applied"; and "**Stopped at:** All 23 v1 requirements mapped across 5 phases. Awaiting approval." -> "**Stopped at:** All 25 v1 requirements mapped across 5 phases. Awaiting approval." (count consistency after adding REC-06/REC-07).

**.planning/PROJECT.md** (FIX 3-verify, 9):

22. VERIFY only: the Strava API context bullet (OAuth 2.0 Authorization Code Grant, no PKCE, BuildConfig, EncryptedSharedPreferences) and the Constraints "**Strava API**" line already carry the corrected FIX 3 wording. Do not rewrite them.

23. In the Constraints "**Strava API**" line, replace "Subscriber rate limits: 300 read/15min, 1,000 write/15min. Tokens stored in EncryptedSharedPreferences." with "Subscriber rate limits: 300 reads/15min, 1,000 writes/15min (verify current figures at developers.strava.com during Phase 3 research). Tokens stored in EncryptedSharedPreferences." (FIX 9 note; normalizes to plural matching CLAUDE.md).

CLAUDE.md is already consistent (300 reads/15min, 1,000 writes/15min, no PKCE) — do not edit it.
  </action>
  <verify>
    <automated>cd /Users/bilhuang/Documents/rokid-maps && test "$(grep -c 'REC-0[67]' .planning/REQUIREMENTS.md)" -ge 6 && grep -q '25 total' .planning/REQUIREMENTS.md && grep -q 'Mapped to phases: 25' .planning/REQUIREMENTS.md && grep -q 'REC-05, REC-06, REC-07' .planning/ROADMAP.md && grep -q 'via-waypoints' .planning/ROADMAP.md && grep -q 'follow-route mode' .planning/REQUIREMENTS.md && grep -qE 'UPL-01.*moving time' .planning/REQUIREMENTS.md && grep -q 'GPS-staleness detection' .planning/REQUIREMENTS.md && grep -q 'developers.strava.com' .planning/PROJECT.md && grep -q 'developers.strava.com' .planning/STATE.md && grep -q 'deferred to v2' .planning/STATE.md && grep -q 'via-point' .planning/STATE.md && grep -q 'encode/decode round-trip' .planning/ROADMAP.md && ! grep -qE 'OAuth 2.0 with PKCE|OAuth 2.0 \+ PKCE|600 request' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md && echo TASK1-OK</automated>
  </verify>
  <done>REQUIREMENTS.md has 25 mapped requirements with REC-06/REC-07 defined and cross-referenced from HUD-02/UPL-03; NAVV-02, UPL-01, REC-05 carry the review wording; ROADMAP Phase 1 lists REC-01..REC-07 with updated SC#1/#4/#7, Phase 2 SC#1/#4 state the real tap cycle, Phase 4 records the via-point strategy; STATE.md has 4 new decision rows, the 4 review todos, and updated activity/session/count lines; PROJECT.md carries the rate-limit verify note. Verify command prints TASK1-OK.</done>
</task>

<task type="auto">
  <name>Task 2: Fix research docs (ARCHITECTURE, FEATURES, PITFALLS, SUMMARY)</name>
  <files>.planning/research/ARCHITECTURE.md, .planning/research/FEATURES.md, .planning/research/PITFALLS.md, .planning/research/SUMMARY.md</files>
  <action>
Apply the following edits verbatim with the Edit tool.

**.planning/research/ARCHITECTURE.md** (FIX 1, 5, 6, 7):

1. Banner (FIX 5): insert a new paragraph after the "**Confidence:** HIGH" header line (before "## Standard Architecture for This Domain"):
   "> **NOTE:** Phase numbers in this document predate the final roadmap. Mapping: Research P1(Protocol)+P2(Recording)→Roadmap Phase 1; P3(Glasses)→Phase 2; P4(Auth+Import)→Phase 3 (Auth) + Phase 4 (Import+Nav); P5(Upload)→Phase 5."

2. Line ~14 (industry-pattern bullet, FIX 7): change "idle -> tracking -> paused -> finished." to "idle -> tracking -> paused -> finished (this project defers PAUSED to v2 — see Session State Machine below)."

3. Flow 2 diagram tail (FIX 1, lines ~214-218): replace the two flow stanzas
   "Parse GPX -> extract waypoints (lat/lng sequence) -> create route waypoints list" and
   "Pass waypoints to NavigationManager.startNavigation()" / "(NavigationManager uses these as route waypoints, skips OSRM call since waypoints are pre-defined)"
   with (plain flow text, keep the arrow/pipe style of adjacent stanzas):
   "Parse GPX -> extract track points -> Douglas-Peucker downsample (epsilon ~10-20m, target ≤200 points)" then
   "OSRM /route with ALL downsampled points as via-waypoints (steps=true)" with sub-lines "-> road-snapped route geometry + real turn-by-turn steps" and "(fallback: if OSRM via-point routing fails/unavailable, use raw GPX waypoints in follow-route mode)" then
   "Pass route + steps to a new waypoint-accepting NavigationManager path"

4. Flow 2 "**Architecture decision:**" paragraph (FIX 1, line ~224): replace the entire paragraph (from "**Architecture decision:** When navigating a Strava route, the phone uses the GPX waypoints directly..." through "...optionally OSRM the waypoints to get step data).") with:
   "**Architecture decision:** When navigating a Strava route, the phone downsamples the GPX (Douglas-Peucker, epsilon ~10-20m, target ≤200 points) and calls OSRM /route with all downsampled points as via-waypoints (steps=true). This returns a road-snapped route AND real turn-by-turn steps, so the existing route line, maneuver arrows, and TTS voice directions all work for Strava routes. It requires extending OsrmClient (currently builds a 2-point URL only) and adding a waypoint-accepting NavigationManager path (startNavigation() currently accepts only a destination). Graceful fallback: if OSRM via-point routing fails or is unavailable, navigate the raw GPX waypoints in follow-route mode — route line + distance to next waypoint, no turn arrows or TTS."

5. Pattern 4 (FIX 1, line ~401): retitle "### Pattern 4: GPX Waypoints as Direct Route (No OSRM)" to "### Pattern 4: GPX Routes via OSRM Via-Point Routing (Follow-Route Fallback)". Rewrite the body:
   - What: "Downsample GPX track points with Douglas-Peucker, then request OSRM /route using the downsampled points as via-waypoints with steps=true — producing a road-snapped route and real turn-by-turn steps. If OSRM via-point routing fails or is unavailable, fall back to navigating the raw GPX waypoints in follow-route mode."
   - When to use: "For any pre-planned route (e.g., Strava GPX) that is not a simple origin-to-destination OSRM query."
   - Update the gpxToWaypoints code-comment block: comments now describe parsing <trkpt> elements, Douglas-Peucker simplification (epsilon ~10-20m) to ≤200 points, and passing the result to an extended multi-waypoint OsrmClient call (not "every Nth point").
   - Key decision paragraph: keep the density/accuracy tradeoff but reframe — downsampled points become OSRM via-waypoints; the displayed route line uses OSRM's returned geometry (or the full GPX in fallback mode); navigation logic uses OSRM's returned steps (or the downsampled points in fallback mode).

6. Build order Phase 4 bullet (FIX 6, line ~547): replace "- Add OkHttp dependency to phone module (explicit declaration; no Retrofit needed — see STACK.md rationale)" with "- Declare OkHttp + Gson explicitly in the phone module (already transitive via the CXR SDK; no Retrofit — see STACK.md rationale)". VERIFY (already applied, do not edit): component table "OkHttp-based client", Integration Points "OkHttp with Authenticator interceptor", Internal Boundaries "OkHttp call".

7. PAUSED deferral (FIX 7):
   - Diagram line ~83: replace "(pause/resume deferred to v1.x)" with "(pause/resume deferred to v2)  " — note the two trailing spaces, which preserve the ASCII box border alignment (replacement must be the same character width as the original).
   - State machine "**Note:**" paragraph (line ~318): replace "**Note:** Pause/resume is deferred to v1.x per FEATURES.md. The initial v1 state machine is deliberately simple: start → track → stop. Adding PAUSED state later will insert between TRACKING and FINISHED." with "**Note:** Pause/resume (PAUSED state) is deferred to v2. The v1 state machine is deliberately simple: IDLE → TRACKING → FINISHED (with reset). Moving time is computed from a speed threshold (<0.5 m/s = stopped), not from a paused state. Adding PAUSED in v2 will insert between TRACKING and FINISHED."
   - Build order Phase 2 bullet (line ~529): replace "state machine (IDLE/TRACKING/PAUSED/FINISHED)" with "state machine (IDLE/TRACKING/FINISHED; PAUSED deferred to v2)".

**.planning/research/FEATURES.md** (FIX 1; FIX 3 verify):

8. Table-stakes row "**Convert Strava route (GPX) to navigable waypoints**" (line ~18): change complexity cell "LOW" to "MEDIUM" and replace the Notes cell ("Parse GPX `<trkpt>` coordinates, pass as waypoints to existing `OsrmClient.getRoute()`. OSRM already handles waypoint-to-turn-by-turn conversion.") with:
   "Parse GPX `<trkpt>` coordinates, downsample with Douglas-Peucker (epsilon ~10-20m, target ≤200 points), then call OSRM /route with the downsampled points as via-waypoints (steps=true). Requires extending `OsrmClient`, which currently builds a 2-point URL only."

9. Table-stakes row "**Navigate imported route on glasses**" (line ~19): change complexity cell "LOW" to "MEDIUM" and replace the Notes cell ("`NavigationManager.startNavigation()` already accepts waypoints. Strava routes feed directly in. Existing route line, maneuver arrows, and voice directions work unchanged.") with:
   "Requires a new waypoint-accepting `NavigationManager` path — `startNavigation()` currently accepts only a destination. With OSRM via-point routing, the existing route line, maneuver arrows, and voice directions work; if OSRM via-point routing is unavailable, degrade to follow-route mode (route line + distance to next waypoint, no turn instructions)."

10. VERIFY only (FIX 3/9 already applied): the "**Strava OAuth 2.0 login**" row already reads "OAuth 2.0 Authorization Code Grant (Strava does NOT support PKCE...)" with "300 read/15min, 1,000 write/15min". Do not edit.

**.planning/research/PITFALLS.md** (FIX 1, 5, 12):

11. Banner (FIX 5, adapted): insert after the "**Confidence:** HIGH (multiple verified sources for each finding)" header line:
   "> **NOTE:** \"Phase to address\" references in this document use the final roadmap numbering (Phase 1 Recording / Phase 2 Glasses HUD / Phase 3 Auth / Phase 4 Import+Nav / Phase 5 Upload). Original research-phase mapping: Research P1(Protocol)+P2(Recording)→Roadmap Phase 1; P3(Glasses)→Phase 2; P4(Auth+Import)→Phase 3 (Auth) + Phase 4 (Import+Nav); P5(Upload)→Phase 5."
   Why adapted from the review's verbatim banner: the working tree already renumbered every "Phase to address" line to roadmap numbering, so the verbatim text ("Phase numbers in this document predate the final roadmap") would be false here. The mapping content is preserved exactly.

12. VERIFY only (FIX 5): all seven "Phase to address:" lines and the bottom "Pitfall-to-Phase Mapping" table already use roadmap numbering (P1 OAuth->Phase 3+4, battery->Phase 1, GPX->Phase 4, upload->Phase 5, GPS noise->Phase 1, fragility->Phase 1 ongoing, protocol->Phase 1+Phase 2). Correct only if a straggler is found; expected: no edits.

13. Pitfall 3 prevention #5 (FIX 1, line ~104): replace the final sentence "Skip OSRM route calculation entirely for GPX-sourced routes." with "If OSRM via-point routing fails, degrade to follow-route mode." (Leave the rest of item 5 intact — it correctly describes the follow-route display.)

14. Pitfall 5 "Why it happens" opening paragraph (FIX 12, line ~161): replace the final sentence "REC-02 distance computation is net-new code that must be built from scratch." with "REC-02 distance computation is net-new code built from scratch in ActivitySessionManager; the existing haversine utility serves proximity checks only (a duplicate haversine implementation also lives in OverpassSpeedLimitClient)." VERIFY the preceding sentences already state proximity-only usage (they do — no other edits).

**.planning/research/SUMMARY.md** (FIX 5):

15. Banner: insert after the "**Sources:** STACK.md, FEATURES.md, ARCHITECTURE.md, PITFALLS.md, CONCERNS.md" header line (before "## Executive Summary"):
   "> **NOTE:** Phase numbers in this document predate the final roadmap. Mapping: Research P1(Protocol)+P2(Recording)→Roadmap Phase 1; P3(Glasses)→Phase 2; P4(Auth+Import)→Phase 3 (Auth) + Phase 4 (Import+Nav); P5(Upload)→Phase 5."

Scope guard: do not edit any other research-doc content (e.g., FEATURES.md v1.x sections legitimately use "v1.x"; ARCHITECTURE.md line ~533 "Wire NavigationManager start/stop to auto-start/stop recording" and the SessionState protocol box are research-era details outside the 12 fixes — leave them; the banner frames the build-order section as research-era).
  </action>
  <verify>
    <automated>cd /Users/bilhuang/Documents/rokid-maps && test "$(grep -l 'P3(Glasses)' .planning/research/ARCHITECTURE.md .planning/research/PITFALLS.md .planning/research/SUMMARY.md | wc -l)" -eq 3 && ! grep -q 'skips OSRM call' .planning/research/ARCHITECTURE.md && ! grep -q 'No OSRM' .planning/research/ARCHITECTURE.md && ! grep -q 'Skip OSRM route calculation entirely' .planning/research/PITFALLS.md && ! grep -q 'work unchanged' .planning/research/FEATURES.md && ! grep -q 'IDLE/TRACKING/PAUSED/FINISHED' .planning/research/ARCHITECTURE.md && ! grep -q 'deferred to v1.x' .planning/research/ARCHITECTURE.md && grep -q 'via-waypoints' .planning/research/ARCHITECTURE.md && grep -q 'via-waypoints' .planning/research/FEATURES.md && grep -q 'OkHttp + Gson' .planning/research/ARCHITECTURE.md && grep -q 'OverpassSpeedLimitClient' .planning/research/PITFALLS.md && grep -q 'degrade to follow-route mode' .planning/research/PITFALLS.md && echo TASK2-OK</automated>
  </verify>
  <done>All three research docs carry the phase-numbering banner; ARCHITECTURE.md Flow 2, its architecture-decision paragraph, and Pattern 4 describe OSRM via-point routing with follow-route fallback; FEATURES.md GPX rows are MEDIUM complexity naming the OsrmClient/NavigationManager extensions; PITFALLS.md Pitfall 3 #5 and Pitfall 5 attribution are harmonized; every PAUSED reference in ARCHITECTURE.md defers to v2; build order declares OkHttp + Gson. Verify command prints TASK2-OK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Documentation-only change: markdown edits under .planning/. No source code, no untrusted input handling, no network calls, no dependency installs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-q260702-01 | Tampering | .planning/*.md docs | accept | Docs-only edits; reviewed via git diff before commit; no package-manager installs in this plan (supply-chain gate not applicable) |
</threat_model>

<verification>
Run from /Users/bilhuang/Documents/rokid-maps after both tasks:

1. Task 1 gate prints TASK1-OK and Task 2 gate prints TASK2-OK (commands in each task's verify block).
2. Cross-doc consistency sweep (must produce no output):
   grep -rnE 'OAuth 2.0 with PKCE|600 request|work unchanged|skips OSRM call|Skip OSRM route calculation entirely' .planning/PROJECT.md .planning/REQUIREMENTS.md .planning/ROADMAP.md .planning/STATE.md .planning/research/SUMMARY.md .planning/research/FEATURES.md .planning/research/ARCHITECTURE.md .planning/research/PITFALLS.md
   (Scope greps to these 8 files only — never to all of .planning/, because this PLAN quotes the stale phrases and would self-match.)
3. `git status --porcelain` shows changes confined to the 8 listed docs (plus this plan's directory). NO files outside .planning/ modified; CLAUDE.md untouched.
4. Spot-read: ROADMAP Phase 4 strategy note present; STATE.md Decisions table has 10 rows (6 original + 4 new); REQUIREMENTS traceability lists 25 requirements.
</verification>

<success_criteria>
- All 12 review fixes are applied or verified-already-applied per the task actions; none skipped, none re-litigated.
- REQUIREMENTS.md: 25 v1 requirements (REC-06/REC-07 added), traceability and coverage counts consistent, NAVV-02/HUD-02/UPL-01/UPL-03/REC-05 reworded per spec, HUD-03 confirmed correct.
- ROADMAP.md: Phase 1 owns protocol + persistence + unit tests (REC-01..REC-07); Phase 2 SC match real tap-cycle behavior; Phase 4 records the via-point strategy with MEDIUM-HIGH complexity and 4-item scope.
- STATE.md: 4 new decisions, 4 review todos, updated last-activity/session lines, 25-requirement count.
- Research docs: banners present in all three; no skips-OSRM/works-unchanged claims anywhere; PAUSED deferred to v2 throughout ARCHITECTURE.md; OkHttp + Gson build-order bullet; Pitfall 5 names ActivitySessionManager and OverpassSpeedLimitClient.
- Both automated gates (TASK1-OK, TASK2-OK) and the consistency sweep pass; no source code changed.
</success_criteria>

<output>
Create `.planning/quick/260702-v6h-fix-design-doc-review-blockers-in-planni/260702-v6h-SUMMARY.md` when done, listing each of the 12 fixes with its disposition (edited / verified already-applied) and the files touched.
</output>
