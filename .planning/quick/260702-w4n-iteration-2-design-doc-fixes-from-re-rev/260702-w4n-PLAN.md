---
phase: quick-260702-w4n
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/research/ARCHITECTURE.md
  - .planning/research/PITFALLS.md
  - .planning/research/SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - CLAUDE.md
autonomous: true
requirements: [AUTH-01, UPL-01]  # requirement-TEXT corrections only — docs task, no source code changes
must_haves:
  truths:
    - "ARCHITECTURE.md has zero ActivityMetrics/ACTIVITY_METRICS tokens and no claim that navigation auto-starts/stops recording (recording is user-initiated per REC-01)"
    - "REQUIREMENTS.md AUTH-01 cites https://www.strava.com/oauth/mobile/authorize"
    - "PITFALLS.md Pitfall 3 and Pitfall 7 describe verified code behavior: forward-only step index (NavigationManager.kt:72-101) and unknown BT messages already logged (BluetoothClient.kt:258-259)"
    - "Docs describe exactly one new protocol message (sport_state); activity summary is phone-side UI (UPL-01), no glasses message"
    - "ROADMAP.md: Phase 3 delivers the Strava API client, Phase 1 SC#5 scopes versioning to sport_state only, Phase 5 SC#1 triggers on recording stop"
    - "STATE.md has two new decision rows, the OPPO ACCESS_BACKGROUND_LOCATION validation todo, and an iteration-2 Last activity line"
    - "CLAUDE.md says '12 message types:'; PROJECT.md notes STRA-01..09 are superseded by REQUIREMENTS.md IDs"
  artifacts:
    - path: ".planning/research/ARCHITECTURE.md"
      provides: "SportState naming throughout, opt-in recording lifecycle, HUD-03 tap-cycle-only SPORT mode, SessionState message marked NOT in v1"
    - path: ".planning/research/PITFALLS.md"
      provides: "Corrected Pitfall 3/7 code models; Gson-with-typed-data-classes tech-debt guidance (no new serialization library)"
    - path: ".planning/research/SUMMARY.md"
      provides: "Single new message type (sport_state) in Executive Summary and Architecture findings"
    - path: ".planning/REQUIREMENTS.md"
      provides: "AUTH-01 mobile authorize endpoint; UPL-01 recording-stop trigger"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 3 API-client ownership, Phase 1 SC#5 versioning scope, Phase 5 SC#1 trigger wording"
    - path: ".planning/STATE.md"
      provides: "Two new decision rows, OPPO background-location todo, iteration-2 Last activity line"
    - path: ".planning/PROJECT.md"
      provides: "STRA-01..09 superseded-by note"
    - path: "CLAUDE.md"
      provides: "Corrected Bluetooth message-type count (12)"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/research/PITFALLS.md"
      via: "same Strava mobile authorization endpoint in AUTH-01 and Integration Gotchas"
      pattern: "oauth/mobile/authorize"
    - from: ".planning/research/ARCHITECTURE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "REC-07 message naming (sport_state / SportStateMessage)"
      pattern: "sport_state"
    - from: ".planning/STATE.md"
      to: ".planning/ROADMAP.md"
      via: "sport_state-only protocol versioning decision mirrored in Phase 1 SC#5"
      pattern: "version"
---

<objective>
Apply the iteration-2 fresh-eyes re-review fixes to the design docs: 3 blockers (B1-B3), 10 warnings (W1-W10), plus 2 new STATE.md decision rows and a Last-activity update. All decisions are FINAL from a completed code-verified re-review — implement the edits exactly as specified; do not re-litigate.

Purpose: The planning docs still contain claims that contradict locked requirements (auto-start recording vs REC-01 opt-in), a wrong OAuth endpoint that PITFALLS.md itself labels the common mistake, false models of existing code behavior, stale ActivityMetrics naming, and an orphan activity_summary message. These would mislead phase planners and executors.

Output: 8 corrected doc files (.planning/*.md + CLAUDE.md). No source code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
All anchors below were re-verified against the working tree on this planning pass (a concurrent session touched the repo earlier today). Line numbers are current as of planning; if any drift, locate by the quoted anchor string — every edit site has a unique quoted anchor.

Target files (do not read others):
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/research/SUMMARY.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/PROJECT.md
@CLAUDE.md

**CRITICAL grep-scope rule:** NEVER grep .planning/ recursively for stale phrases — this PLAN file itself quotes every stale phrase and would self-match. All verification greps are scoped to the exact target file, as written in each task's verify gate.

**VERIFY-only findings (confirmed during planning, no edit needed):**
- B1d: ARCHITECTURE.md Flow 3 head (line 235) already reads "User explicitly starts recording (manual opt-in, independent of navigation)" with the REC-01 note at line 240. Confirm it stays unchanged.
- W1 token `sport_metrics`: ZERO occurrences in ARCHITECTURE.md (confirmed) — nothing to rename for that token; only the reconciliation note at line 504 needs the update described in Task 1.
- STATE.md Risk Register row R8 already mentions ACCESS_BACKGROUND_LOCATION — leave that row unchanged; W8 adds a separate Pending Todo.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix research/ARCHITECTURE.md — recording lifecycle (B1), SportState renames (W1), SPORT-mode reachability (W2), SessionState-message v1 status (W4)</name>
  <files>.planning/research/ARCHITECTURE.md</files>
  <action>
Apply these edits to .planning/research/ARCHITECTURE.md only. When editing lines inside ASCII diagrams, keep the right box border aligned by adjusting trailing spaces (new text lengths differ from old).

**B1c (line 66, system-overview diagram edge label):** Replace the label "activity auto-starts/ends with navigation" with "recording is user-initiated (opt-in, REC-01)". New label is 3 chars longer — remove 3 trailing spaces before the closing border.

**B1b (Flow 4 head, lines 269 and 272):** Replace line 'User arrives at destination OR taps "Stop Recording"' with 'User taps "Stop Recording" (arrival may surface a stop prompt — auto-stop deferred to v1.x)'. Replace line "NavigationManager.onArrived() OR user action -> ActivitySessionManager.stopSession()" with "User action -> ActivitySessionManager.stopSession()".

**B1a (build-order bullet, line 539):** Replace "- Wire `NavigationManager` start/stop to auto-start/stop recording" with "- Recording start/stop is user-initiated (REC-01 opt-in) — do NOT wire NavigationManager lifecycle to auto-start/stop recording; on arrival, surface a stop prompt (auto-stop deferred to v1.x)".

**B1d (VERIFY only):** Confirm Flow 3 head (line 235) still reads "User explicitly starts recording (manual opt-in, independent of navigation)" and the "(Navigation start does NOT auto-trigger recording — per REC-01)" note at line 240 is intact. Do not edit.

**W1 renames (single-line occurrences):** ActivityMetricsMessage → SportStateMessage on lines 84, 167, 168, 260, 359, 371, 498, 526, 537, 583. ParsedMessage.ActivityMetrics → ParsedMessage.SportState on lines 112, 529, 545. broadcastActivityMetrics → broadcastSportState on line 367. MessageType.ACTIVITY_METRICS → MessageType.SPORT_STATE on line 372. Diagram lines 84 and 112: new names are 5 chars shorter — add 5 trailing spaces to preserve the border.

**W1 + W4 shared-module protocol box (lines 145-155) — tokens SPLIT across lines here; a plain find-replace misses them.** Current box columns contain: Messages.kt column "- ActivityMetrics" / "Message" (147-148) and "- SessionState" / "Message (start/" / "pause/resume/" / "stop)" (149-152); ProtocolConstants column "- MessageType." / "ACTIVITY_METRICS" (152-153); ProtocolCodec column "- encodeActivityMetrics()" (147) and "ParsedMessage." / "ActivityMetrics" (150). Rewrite the box interior so:
  - Messages.kt column lists the SportStateMessage entry (may stay split as "- SportState" / "Message" or fit on one line), and the SessionState Message entry is marked "NOT in v1" — v1 ships only sport_state, which carries session state as a field (compact phrasing inside the box is fine, e.g. "- SessionState" / "Message: NOT in v1" / "(state rides in" / "sport_state)").
  - ProtocolCodec column shows "encodeSportState()" and decode -> "ParsedMessage." / "SportState".
  - ProtocolConstants column shows "MessageType." / "SPORT_STATE" (FIELD_* constants unchanged).
  Re-pad columns as needed; borders must render cleanly.

**W1 reconciliation note (line 504):** Replace "**New message type:** `sport_state` (matching REQUIREMENTS.md/ROADMAP.md naming)" with "**New message type:** `sport_state` (per REC-07)".

**W2 (line 548, build order Phase 3):** Replace "- Add auto-switch to SPORT mode when `sport_state` message with `state=tracking` arrives (or manual toggle from phone settings)" with "- SPORT mode is reachable via the glasses tap cycle only (Full → Corner → Sport → Full) per HUD-03; auto-switch on message arrival is deferred".

Do NOT touch anything else in this file (e.g., the StravaUploader box "after nav ends" at line 56, SessionState.TRACKING enum reference at line 368, and the sport_state JSON sample at 507-516 are all correct as-is).
  </action>
  <verify>
    <automated>! grep -q "ActivityMetrics" .planning/research/ARCHITECTURE.md && ! grep -q "ACTIVITY_METRICS" .planning/research/ARCHITECTURE.md && ! grep -q "auto-starts/ends" .planning/research/ARCHITECTURE.md && ! grep -q "arrives at destination OR taps" .planning/research/ARCHITECTURE.md && ! grep -q "onArrived() OR user action" .planning/research/ARCHITECTURE.md && ! grep -q "start/stop to auto-start/stop" .planning/research/ARCHITECTURE.md && ! grep -q "Add auto-switch" .planning/research/ARCHITECTURE.md && ! grep -q "matching REQUIREMENTS.md/ROADMAP.md naming" .planning/research/ARCHITECTURE.md && grep -q "SportStateMessage" .planning/research/ARCHITECTURE.md && grep -q "ParsedMessage.SportState" .planning/research/ARCHITECTURE.md && grep -q "SPORT_STATE" .planning/research/ARCHITECTURE.md && grep -q "encodeSportState" .planning/research/ARCHITECTURE.md && grep -q "broadcastSportState" .planning/research/ARCHITECTURE.md && grep -q "NOT in v1" .planning/research/ARCHITECTURE.md && grep -q "per HUD-03" .planning/research/ARCHITECTURE.md && grep -q "user-initiated (opt-in, REC-01)" .planning/research/ARCHITECTURE.md && grep -q "auto-stop deferred to v1.x" .planning/research/ARCHITECTURE.md && grep -q "manual opt-in, independent of navigation" .planning/research/ARCHITECTURE.md && echo T1-PASS</automated>
  </verify>
  <done>ARCHITECTURE.md contains zero ActivityMetrics/ACTIVITY_METRICS tokens (including the split-line box instances), no auto-start/auto-stop-of-recording claims (only the negated do-NOT-wire guidance), the HUD-03 tap-cycle-only SPORT mode statement, the SessionState message marked NOT in v1, and the "per REC-07" note. Flow 3 opt-in head unchanged. T1-PASS prints.</done>
</task>

<task type="auto">
  <name>Task 2: Fix research/PITFALLS.md (B3, W3), research/SUMMARY.md (W4), REQUIREMENTS.md (B2, W5)</name>
  <files>.planning/research/PITFALLS.md, .planning/research/SUMMARY.md, .planning/REQUIREMENTS.md</files>
  <action>
**PITFALLS.md — B3a (Pitfall 3 "Why it happens", bullets at lines 97-98):** Replace the two false-model bullets ("**Loops and overlapping segments:** NavigationManager uses closest-point proximity to find the \"next\" waypoint..." and "**Over-density:** Thousands of waypoints means the step advancement in NavigationManager advances on every GPS tick (waypoint spacing < 5m)...") with bullets stating the VERIFIED actual mechanism and reframed risks:
  - Actual mechanism: NavigationManager advances a forward-only currentStepIndex, and only when the user comes within 150m of the NEXT step's turn point (with a one-step lookahead skip) — NavigationManager.kt:72-101. Closest-point matching exists ONLY in off-route detection (nearestRouteDistance, NavigationManager.kt:144-147).
  - Risk (i): dense GPX-derived steps spaced <150m cause rapid multi-step advancement — several steps consumed in quick succession, instructions racing ahead of the rider.
  - Risk (ii): on overlapping/doubled-back segments, the off-route closest-point check can match the wrong pass of the route — suppressing legitimate off-route alarms or masking wrong-direction travel.
  Keep the pitfall's overall message (raw GPX density breaks navigation), keep the intro paragraph (line 95) and the "**Under-density after naive downsampling:**" bullet (line 99) intact.

**PITFALLS.md — B3b (Pitfall 3 prevention #3, line 104):** Replace '3. Implement "position-along-route" tracking: instead of closest-point proximity, maintain the index of the last-consumed waypoint and always advance forward. This prevents butterfly behavior on loops. Only allow index rewind if...' with '3. Preserve and extend the EXISTING forward-only index behavior (NavigationManager already advances a forward-only step index): maintain the index of the last-consumed waypoint and always advance forward. As an extension, only allow index rewind if the user is >100m behind the last consumed point (off-route detected).'

**PITFALLS.md — B3c (Pitfall 7):** In the "Why it happens" paragraph (line 233), replace the final sentence "The existing pattern of `if (message is Unknown) return` at the top of `processMessage()` means unrecognized messages are dropped with no log." with a corrected claim: unknown message types are ALREADY logged (BluetoothClient.kt:258-259 logs a Log.w "Unknown message" warning); the real gaps are (a) no protocol version negotiation, and (b) a known message type with renamed or mis-typed fields decodes to wrong defaults silently. Also adjust the earlier phrase "the glasses silently ignore the message" in the same paragraph to reflect logged-then-ignored (e.g. "the glasses log and ignore the message"). Then replace prevention #2 (line 239) "**Add a log/warning for unrecognized message types in BluetoothClient.processMessage().** Currently unrecognized types are silently dropped. Add a `Log.w()` at minimum." with "**Keep/extend the existing unknown-type warning** — BluetoothClient already logs unknown messages (BluetoothClient.kt:258-259)."

**PITFALLS.md — W3 (Technical Debt table row, line 262, "Using raw `org.json` for Strava API responses"):** Reword the Long-term Cost and When Acceptable cells so the recommendation becomes: use Gson (already transitive via the CXR SDK, per STACK.md's final decision) with typed data classes for Strava API responses; do NOT add a new serialization library (Moshi/kotlinx.serialization). The stale phrase "Add a serialization library" must not survive. The Long-term Cost cell may keep the no-type-safety description of raw org.json.

**SUMMARY.md — W4 (line 11, Executive Summary):** Replace "the shared protocol gets 2 new message types (`sport_state` for live metrics, `activity_summary` for final stats)" with "the shared protocol gets 1 new message type (`sport_state` for live metrics; the activity summary is phone-side UI per UPL-01 — no glasses message)".

**SUMMARY.md — W4 (line 31, "Shared protocol gains:"):** Replace "- **Shared protocol gains:** `sport_state` message (elapsed time, distance, speed/pace, moving time), `activity_summary` message (final stats on completion)." with "- **Shared protocol gains:** `sport_state` message only (elapsed time, distance, speed/pace, moving time, session state); the activity summary is phone-side UI per UPL-01 — no summary message."

**REQUIREMENTS.md — B2 (AUTH-01, line 12):** Change the authorization endpoint from `https://www.strava.com/oauth/authorize` to `https://www.strava.com/oauth/mobile/authorize` (this matches PITFALLS.md's own Integration Gotchas table). Only the endpoint URL changes; rest of AUTH-01 unchanged.

**REQUIREMENTS.md — W5 (UPL-01, line 48):** Change "After navigation ends, phone displays an activity summary" to "After recording stops, phone displays an activity summary". Rest of the line unchanged.
  </action>
  <verify>
    <automated>! grep -q "closest-point proximity to find" .planning/research/PITFALLS.md && ! grep -q "advances on every GPS tick" .planning/research/PITFALLS.md && ! grep -q "instead of closest-point proximity" .planning/research/PITFALLS.md && ! grep -q "dropped with no log" .planning/research/PITFALLS.md && ! grep -q "silently dropped" .planning/research/PITFALLS.md && ! grep -q "silently ignore the message" .planning/research/PITFALLS.md && ! grep -q "Add a serialization library" .planning/research/PITFALLS.md && grep -q "forward-only" .planning/research/PITFALLS.md && grep -q "NavigationManager.kt:72-101" .planning/research/PITFALLS.md && grep -q "BluetoothClient.kt:258-259" .planning/research/PITFALLS.md && grep -q "typed data classes" .planning/research/PITFALLS.md && ! grep -q "activity_summary" .planning/research/SUMMARY.md && ! grep -q "2 new message types" .planning/research/SUMMARY.md && grep -q "1 new message type" .planning/research/SUMMARY.md && grep -q "oauth/mobile/authorize" .planning/REQUIREMENTS.md && ! grep -q "com/oauth/authorize" .planning/REQUIREMENTS.md && ! grep -q "After navigation ends" .planning/REQUIREMENTS.md && grep -q "After recording stops" .planning/REQUIREMENTS.md && echo T2-PASS</automated>
  </verify>
  <done>PITFALLS.md Pitfall 3 describes the forward-only 150m step-advancement mechanism with off-route-only closest-point matching; prevention #3 preserves/extends existing behavior; Pitfall 7 acknowledges existing unknown-message logging and names the real gaps; W3 row recommends Gson with typed data classes. SUMMARY.md describes exactly 1 new message type with no activity_summary mention. AUTH-01 cites the mobile authorize endpoint; UPL-01 triggers on recording stop. T2-PASS prints.</done>
</task>

<task type="auto">
  <name>Task 3: Fix ROADMAP.md (W5/W6/W7), STATE.md (W8 + decisions + Last activity), PROJECT.md (W10), CLAUDE.md (W9)</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, CLAUDE.md</files>
  <action>
**ROADMAP.md — W7 (Phase 1 SC#5, line 28):** Replace "5. Protocol messages include a version field (`\"v\": 1`) to enable future cross-version compatibility negotiation" with "5. The new `sport_state` message carries a protocol version field (`\"v\": 1`); versioning of the existing message set is deferred (explicitly not Phase 1 scope)".

**ROADMAP.md — W6a (Phase 3 details):** Insert a new line after "**Requirements:** AUTH-01, AUTH-02, AUTH-03" (line 50, in the Phase 3 section) and before "**Success Criteria**": "**Delivers:** The OkHttp Strava API client + token authenticator, proven via an authenticated GET /athlete call."

**ROADMAP.md — W6b (Phase 5 Depends-on note, line 76):** Replace the parenthetical "(Phase 4 provides Strava API client used by upload; UPL-01 and UPL-04 can proceed after Phase 1 alone)" with "(Phase 4 builds route endpoints (list, GPX export) on the Phase 3 client; UPL-01 and UPL-04 can proceed after Phase 1 alone)".

**ROADMAP.md — W5 (Phase 5 SC#1, line 79):** Change "1. After navigation ends, user sees an activity summary screen" to "1. After recording stops, user sees an activity summary screen". Rest of line unchanged.

**STATE.md — Decisions (append two rows to the Decisions table, currently ending at line 49):**
Row 1: "| No activity_summary glasses message in v1; activity summary is phone-side UI (UPL-01). sport_state carries session state as a field | Keeps glasses protocol surface minimal; nothing consumes a summary message on glasses in v1 | 2026-07-03 |"
Row 2: "| sport_state carries protocol version field v:1; full-protocol version negotiation deferred | Bounds Phase 1 scope; existing message set unchanged for backward compatibility | 2026-07-03 |"

**STATE.md — W8 (Pending Todos, list currently ending at line 64):** Append bullet: "- Validate on the OPPO test phone whether ACCESS_BACKGROUND_LOCATION is actually required — the existing app already records screen-off via its location-type foreground service; keep the permission only if device testing shows it is needed". (Leave Risk Register row R8 untouched.)

**STATE.md — Last activity (line 26):** Replace "**Last activity:** 2026-07-02 -- Design-doc review fixes applied to planning docs (12 blockers)" with "**Last activity:** 2026-07-03 -- Iteration-2 design-doc fixes applied (3 blockers + 10 warnings from fresh-eyes re-review)".

**PROJECT.md — W10:** After the STRA-09 bullet (line 47, end of the Active requirements list) and before "### Out of Scope", insert a one-line note: "Note: STRA-01..09 are the original capture; they are superseded by the AUTH/RIMP/NAVV/REC/HUD/UPL requirement IDs in REQUIREMENTS.md (traceability lives there)." (blank line before and after the note for clean markdown rendering).

**CLAUDE.md — W9 (line 240):** Change "- 11 message types: `state`, `route`, ..." to "- 12 message types: `state`, `route`, ..." — only the count changes; the 12-item list already following it stays as-is.
  </action>
  <verify>
    <automated>! grep -q "After navigation ends" .planning/ROADMAP.md && grep -q "After recording stops" .planning/ROADMAP.md && ! grep -q "Phase 4 provides Strava API client" .planning/ROADMAP.md && grep -q "on the Phase 3 client" .planning/ROADMAP.md && grep -q "GET /athlete call" .planning/ROADMAP.md && ! grep -q "Protocol messages include a version field" .planning/ROADMAP.md && grep -q "versioning of the existing message set is deferred" .planning/ROADMAP.md && grep -q "OPPO test phone" .planning/STATE.md && grep -q "activity summary is phone-side UI" .planning/STATE.md && grep -q "full-protocol version negotiation deferred" .planning/STATE.md && grep -q "Iteration-2 design-doc fixes" .planning/STATE.md && grep -q "superseded by the AUTH/RIMP/NAVV/REC/HUD/UPL" .planning/PROJECT.md && grep -q "12 message types:" CLAUDE.md && ! grep -q "11 message types:" CLAUDE.md && echo T3-PASS</automated>
  </verify>
  <done>ROADMAP.md scopes Phase 1 SC#5 versioning to sport_state, credits Phase 3 with the API client (authenticated GET /athlete proof), has Phase 4 building route endpoints on the Phase 3 client, and triggers Phase 5 SC#1 on recording stop. STATE.md carries both new decision rows (dated 2026-07-03), the OPPO background-location todo, and the iteration-2 Last activity line. PROJECT.md notes STRA-01..09 superseded. CLAUDE.md counts 12 message types. T3-PASS prints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None crossed | Docs-only change (.planning/*.md + CLAUDE.md); no code, no dependencies, no external input processed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick-w4n-01 | Tampering | Planning docs | accept | No package installs, no source changes; git history provides integrity/rollback |
</threat_model>

<verification>
All three task gates pass (T1-PASS, T2-PASS, T3-PASS). Cross-file consistency spot-checks, each scoped to a single file (NEVER grep .planning/ recursively — this plan quotes the stale phrases):
- grep -q "oauth/mobile/authorize" .planning/REQUIREMENTS.md && grep -q "oauth/mobile/authorize" .planning/research/PITFALLS.md (AUTH-01 now matches the Integration Gotchas correct-approach cell)
- grep -c "sport_state" .planning/research/ARCHITECTURE.md returns a count >= 4 (naming consistent post-rename)
- git diff --stat shows exactly the 8 target files modified (plus this quick-task directory)
</verification>

<success_criteria>
- All 3 blockers (B1a-d, B2, B3a-c) and all 10 warnings (W1-W10) applied exactly as specified; the two VERIFY-only items confirmed unchanged
- STATE.md gains the two specified decision rows and the updated Last activity line
- No source code files touched; only the 8 listed docs modified
- All automated grep gates pass
</success_criteria>

<output>
Create `.planning/quick/260702-w4n-iteration-2-design-doc-fixes-from-re-rev/260702-w4n-SUMMARY.md` when done.
Commit all 8 modified docs + this quick-task directory with message: "docs(quick): iteration-2 design-doc fixes from fresh-eyes re-review (3 blockers + 10 warnings)"
</output>
