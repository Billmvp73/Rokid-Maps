---
phase: 05-activity-summary-strava-upload
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - phone/src/main/java/com/rokid/hud/phone/SportFormat.kt
  - phone/src/main/java/com/rokid/hud/phone/ActivitySummaryActivity.kt
  - phone/src/main/java/com/rokid/hud/phone/HistoryActivity.kt
  - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt
  - phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt
  - phone/src/main/res/layout/activity_activity_summary.xml
  - phone/src/main/res/layout/activity_history.xml
  - phone/src/main/res/layout/item_activity.xml
  - phone/src/main/AndroidManifest.xml
autonomous: true
requirements: [UPL-01, UPL-02, UPL-03, UPL-04]

must_haves:
  truths:
    - "After tapping Finish, the phone opens ActivitySummaryActivity showing total time, moving time, distance, avg speed AND avg pace, sport, start time, and the recorded route on an osmdroid map (UPL-01)"
    - "The summary launches with the session id ONLY (never trackPoints via intent — a thousands-point extra throws TransactionTooLargeException); the Activity reads the finalized SessionData from disk"
    - "One tap on Upload runs GPX-generate → validity-guard → POST → poll on a Thread, cycling the button through Uploading → Processing → Uploaded ✓ (View on Strava) / Retry / Pending, driven by the Plan-01 state machine (UPL-02)"
    - "Upload is disabled with a 'Connect Strava first' hint when not connected"
    - "On success the session JSON gains stravaUploaded=true + the activity id; on any failure the on-disk JSON is untouched and Retry re-runs the upload (UPL-03)"
    - "A history surface lists past sessions (date, sport, distance, duration, uploaded ✓ / not-uploaded badge) newest-first; tapping a row reopens its summary (UPL-04)"
  artifacts:
    - path: "phone/src/main/java/com/rokid/hud/phone/ActivitySummaryActivity.kt"
      provides: "Summary screen: reads SessionData by id, renders metrics+map, drives the upload Thread via driveUpload"
      contains: "class ActivitySummaryActivity"
      min_lines: 120
    - path: "phone/src/main/java/com/rokid/hud/phone/HistoryActivity.kt"
      provides: "Past-activities list from SessionStore.listFinalSessions with upload badge; row → summary"
      contains: "class HistoryActivity"
      min_lines: 60
    - path: "phone/src/main/java/com/rokid/hud/phone/SportFormat.kt"
      provides: "Extracted imperial-aware formatDist/formatSpeed/formatPace/formatElapsed (one source of truth for summary + card)"
      contains: "object SportFormat"
    - path: "phone/src/main/AndroidManifest.xml"
      provides: "ActivitySummaryActivity + HistoryActivity registered exported=false"
      contains: "ActivitySummaryActivity"
  key_links:
    - from: "MainActivity.confirmStopRecording"
      to: "ActivitySummaryActivity"
      via: "startActivity with EXTRA_SESSION_ID captured BEFORE stopRecording clears it"
      pattern: "ActivitySummaryActivity"
    - from: "ActivitySummaryActivity upload Thread"
      to: "driveUpload / StravaUploader / SessionStore.updateUploadState"
      via: "GpxWriter.write → isValidForUpload → uploader.startUpload/poll → updateUploadState on success"
      pattern: "driveUpload|updateUploadState"
    - from: "HistoryActivity"
      to: "SessionStore.listFinalSessions"
      via: "list rows → ActivitySummaryActivity(id)"
      pattern: "listFinalSessions"
---

<objective>
Ship the user-visible slice: the activity-summary screen that appears when recording stops (metrics + route map + one-tap Strava upload with live progress states) and the past-activities history list — the milestone's capstone UI. This wires Plan-01's pure seams and Plan-02's uploader into three Android surfaces, launched with a session id only (never bulk track points via intent).

Purpose: Close UPL-01 (summary), UPL-02 (one-tap upload with progress/confirmation), UPL-03 (retry on the same untouched local JSON), and UPL-04 (history) as one coherent vertical slice a user can actually operate: Finish → see summary → tap Upload → watch it upload → open history → reopen.
Output: SportFormat (extracted format helpers), ActivitySummaryActivity + layout, HistoryActivity + list layouts, MainActivity launch on finish + a history entry point, a service `currentSessionId()` passthrough, and the two manifest registrations.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-activity-summary-strava-upload/05-RESEARCH.md

# The wiring surfaces + the Wave-1/2 seams this plan consumes:
@phone/src/main/java/com/rokid/hud/phone/SessionModels.kt
@phone/src/main/java/com/rokid/hud/phone/SessionStore.kt
@phone/src/main/java/com/rokid/hud/phone/strava/StravaAuthManager.kt
@.planning/phases/05-activity-summary-strava-upload/05-01-SUMMARY.md
@.planning/phases/05-activity-summary-strava-upload/05-02-SUMMARY.md
</context>

<build_commands>
- Build (the only real gate for UI code): `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug -q`
- Full suite (no UI regressions in the pure seams): `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest -q`
Run from repo root. NO new dependencies.
</build_commands>

<wiring_facts>
Verified in the codebase (read directly):
- osmdroid `Configuration.getInstance().load(...)` is already called in `HudApplication.onCreate()` — the new Activity's MapView needs NO re-init; just `setTileSource(TileSourceFactory.MAPNIK)`, `zoomController NEVER`, `setMultiTouchControls(true)`, and call `onResume()/onPause()` in the Activity lifecycle (mirror MainActivity's nav/preview map).
- The Polyline idiom (reuse verbatim, trackPoints instead of waypoints): `overlays.removeIf { it is Polyline }`; `Polyline().apply { outlinePaint.color = Color.parseColor("#FC5200"); outlinePaint.strokeWidth = 12f; outlinePaint.isAntiAlias = true; setPoints(geoPoints) }`; add; then `BoundingBox.fromGeoPoints(geoPoints)` fitted in `mapView.post { zoomToBoundingBox(box, false); invalidate() }` (osmdroid returns a zero-size view before layout — Pitfall 5). Map trackPoints defensively: skip any non-finite lat/lng.
- `StravaAuthManager.isConnected()` and `connectedAthleteName()` are BLOCKING ESP reads (first access is slow) → call on a Thread{}, never on the main thread; render the button enabled/disabled via runOnUiThread.
- `stopRecording()` returns Unit and CLEARS `PREF_REC_SESSION_ID` at the end — so MainActivity MUST capture the session id BEFORE calling it. The service currently has no `currentSessionId()` getter; add one (Task 3).
- Format helpers (`formatDist`/`formatSpeed`/`formatPace`/`formatElapsed`) are PRIVATE in MainActivity.kt:1519-1560 and read the imperial flag from `getPreferences(MODE_PRIVATE)[PREF_IMPERIAL="use_imperial"]` (activity-scoped). Extraction must thread the Boolean in rather than read prefs inside the helper.
- Pace is DERIVED (not persisted): `SummaryMath.avgPaceMsPerKm(data.movingMs, data.distanceM)` (Plan 01) → then `SportFormat.formatPace(...)`. Avg speed is read straight from `data.avgSpeedMps` (moving-based — do NOT recompute).
- SessionStore is constructed with `SessionStore(File(filesDir, "activities"))` (the ACTIVITIES_DIR the service uses).
- Manifest activity block shape (mirror DeviceScanActivity): `<activity android:name=".ActivitySummaryActivity" android:exported="false" android:theme="@style/Theme.RokidHud" />`. Internal navigation only — exported=false (V4/T-05 exported surface).
</wiring_facts>

<tasks>

<task type="auto">
  <name>Task 1: Extract SportFormat + build ActivitySummaryActivity (metrics + osmdroid route map)</name>
  <files>phone/src/main/java/com/rokid/hud/phone/SportFormat.kt, phone/src/main/java/com/rokid/hud/phone/ActivitySummaryActivity.kt, phone/src/main/res/layout/activity_activity_summary.xml, phone/src/main/java/com/rokid/hud/phone/MainActivity.kt</files>
  <read_first>
    - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt:1519-1560 (the four private format helpers to extract) and :454-457 + :525-548 (osmdroid setup + Polyline+BoundingBox idiom to mirror).
    - phone/src/main/java/com/rokid/hud/phone/SessionModels.kt (SessionData fields the summary renders).
    - phone/src/main/java/com/rokid/hud/phone/SummaryMath.kt (Plan 01 — avgPaceMsPerKm).
    - 05-RESEARCH.md → Pattern 5 (osmdroid Polyline) + Pitfalls 5, 6 (moving-avg speed, pace-not-persisted) + Code Examples "Format helpers".
  </read_first>
  <action>
    (a) Create `object SportFormat` in `SportFormat.kt` (package `com.rokid.hud.phone`) with `formatDist(m: Double, imperial: Boolean)`, `formatSpeed(mps: Double, imperial: Boolean)`, `formatPace(msPerKm: Long, imperial: Boolean)`, `formatElapsed(ms: Long)` — the EXACT bodies from MainActivity (imperial branches identical; formatPace returns "–:–– /km|/mi" for msPerKm ≤ 0). Thread the imperial Boolean in (do NOT read prefs inside the object — it has no Context). Then update MainActivity's four private helpers to delegate to SportFormat (pass `isImperial()`), keeping the private wrappers so existing call sites are untouched — this makes SportFormat the single source of truth without a large refactor.
    (b) Create `ActivitySummaryActivity` (package `com.rokid.hud.phone`) with a companion `EXTRA_SESSION_ID = "session_id"`. In onCreate: read the id from intent (finish + toast if missing); `setContentView(R.layout.activity_activity_summary)`; wire the MapView (tile source MAPNIK, zoom controller NEVER, multitouch). On a `Thread{}`: `val store = SessionStore(File(filesDir, "activities")); val data = store.readSession(id)`; if null → runOnUiThread { toast "Activity not found"; finish() }. Else runOnUiThread render: total time `SportFormat.formatElapsed(data.elapsedMs)`, moving time `SportFormat.formatElapsed(data.movingMs)` (the REC-02 metric finally surfaced — UPL-01), distance `SportFormat.formatDist(data.distanceM, imperial)`, avg speed `SportFormat.formatSpeed(data.avgSpeedMps, imperial)` (read from data — Pitfall 5), avg pace `SportFormat.formatPace(SummaryMath.avgPaceMsPerKm(data.movingMs, data.distanceM), imperial)` (derived — Pitfall 6), sport label, start time (data.startTime; optionally prettify). Draw the route: `data.trackPoints.filter { it.lat.isFinite() && it.lng.isFinite() }.map { GeoPoint(it.lat, it.lng) }` → the Polyline+BoundingBox-in-post idiom (empty → hide/placeholder the map). Read the imperial flag from the SAME store MainActivity uses (checker WR): `getSharedPreferences("MainActivity", Context.MODE_PRIVATE).getBoolean("use_imperial", false)` — Activity.getPreferences maps to the file named by getLocalClassName()="MainActivity", so a plain getSharedPreferences with that exact name reads the user's real toggle (an app-wide getSharedPreferences would always see the default `false` → wrong units). Document the store name in a comment. Call `mapView.onResume()/onPause()` in the Activity's onResume/onPause. Leave the Upload button + Done button in the layout; the button LOGIC lands in Task 2 (stub the click to a no-op or a "coming in Task 2" guard is NOT allowed — instead Task 2 edits this same file to add the upload driver; for THIS task, wire Done/back to finish() and leave Upload disabled with its hint set from isConnected()).
    (c) Create `activity_activity_summary.xml` — dark theme consistent with existing layouts: a scrollable column of labelled metric TextViews (give them stable ids: `summaryTotalTime`, `summaryMovingTime`, `summaryDistance`, `summaryAvgSpeed`, `summaryAvgPace`, `summarySport`, `summaryStartTime`), an osmdroid `org.osmdroid.views.MapView` (`summaryMap`), a `btnUploadStrava` Button + a `uploadStatusText` TextView for the progress states, and a `btnSummaryDone` Button. Match the app's dark colors/ids conventions (camelCase ids, `activity_*` layout name).
    Do NOT pass trackPoints through the intent (TransactionTooLargeException on a real ride — 05-RESEARCH Open Q2); the id-only launch + disk read is the locked approach.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug -q</automated>
    <grep>grep -c "SummaryMath.avgPaceMsPerKm" phone/src/main/java/com/rokid/hud/phone/ActivitySummaryActivity.kt   # pace derived, not read from SessionData (>=1)</grep>
    <grep>grep -c "getParcelableArrayListExtra\|trackPoints.*Extra\|putExtra.*trackPoints" phone/src/main/java/com/rokid/hud/phone/ActivitySummaryActivity.kt   # 0 — trackPoints never via intent</grep>
  </verify>
  <done>assembleDebug builds. SportFormat holds the four helpers (imperial threaded in); MainActivity delegates to it. ActivitySummaryActivity reads SessionData by id on a Thread, renders total/moving time + distance + avg speed (from data) + avg pace (derived via SummaryMath) + sport + start time, and draws the trackPoints route via Polyline+BoundingBox-in-post. No trackPoints intent extra.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the one-tap upload (GPX → validity guard → POST → poll) with progress states</name>
  <files>phone/src/main/java/com/rokid/hud/phone/ActivitySummaryActivity.kt</files>
  <read_first>
    - 05-RESEARCH.md → Code Examples → "Poll loop + state machine" (the exact Thread{} + Thread.sleep(2000) + 120_000 deadline + @Volatile cancel idiom) and Pitfall 4 (only mark success on Ready/Duplicate; timeout → Pending; local JSON untouched on failure).
    - phone/src/main/java/com/rokid/hud/phone/strava/UploadState.kt (Plan 01 — UploadState + driveUpload) and StravaUploader/StartResult/PollResult (Plan 02) and GpxWriter (Plan 01 — write/isValidForUpload/sportType).
    - phone/src/main/java/com/rokid/hud/phone/strava/StravaAuthManager.kt (isConnected/connectedAthleteName — blocking).
  </read_first>
  <action>
    Extend `ActivitySummaryActivity`. Add `@Volatile private var cancelled = false` (set true in onDestroy). Construct `StravaAuthManager(this, StravaTokenStore(this))` and `StravaUploader(auth)` (mirror the Wave-3 Phase-3 construction from MainActivity's onCreate). On a background Thread at render time, evaluate `auth.isConnected()`; runOnUiThread enable `btnUploadStrava` with text "Upload to Strava" if connected, else disable it and set `uploadStatusText` to "Connect Strava first".
    `btnUploadStrava.setOnClickListener` → start an upload `Thread {}` (guard against double-tap by disabling the button on click). Inside the Thread, use the Plan-01 `driveUpload` seam wired to real work:
      - build GPX: `val gpx = GpxWriter.write(data.trackPoints, data.sport, data.startTime)`; if `!GpxWriter.isValidForUpload(gpx)` → runOnUiThread render Failed("Recording has no valid track to upload") and return (NO network cost — Pitfall guard).
      - `start = { when (val r = uploader.startUpload(gpx, defaultName(data), data.id, GpxWriter.sportType(data.sport))) { Started → StartOutcome.Started(r.idStr); RateLimited → StartOutcome.RateLimited; Failed → StartOutcome.Failed(r.message) } }`.
      - `poll = { idStr -> Thread.sleep(2_000); when (val p = uploader.poll(idStr)) { Ready → PollOutcome.Ready(p.activityId); Duplicate → PollOutcome.Duplicate(p.activityId); Processing → PollOutcome.ProcessingOutcome; Error → PollOutcome.ErrorOutcome(p.message) } }` — the 2s spacing lives here (Wave 3 owns timing; the driver is pure).
      - `isDeadlineReached = { cancelled || System.currentTimeMillis() >= deadline }` where `deadline = System.currentTimeMillis() + 120_000` (2-min envelope — Pitfall 4).
      - `emit = { st -> runOnUiThread { renderUploadState(st) } }`.
      - `onSuccess = { activityId -> SessionStore(File(filesDir, "activities")).updateUploadState(data.id, activityId) }` — write-back ONLY here (Plan-01 atomic add-only; UPL-03).
      Call `driveUpload(start, poll, isDeadlineReached, emit, onSuccess)`.
    Implement `renderUploadState(st: UploadState)` mapping to `uploadStatusText` + button: Uploading → "Uploading…" (button disabled); Processing → "Strava is processing…"; Done(id) → "Uploaded ✓ — View on Strava" (make the text/a button open `https://www.strava.com/activities/$id` via an ACTION_VIEW intent; button → "View on Strava"); Failed(msg) → show msg + button "Retry" (re-enable, re-run the upload Thread on tap); RateLimited → "Strava busy — retry shortly" + Retry; Pending → "Upload pending — retry later" + Retry (stravaUploaded stays false → the same untouched JSON is re-uploadable; duplicate handling makes the re-POST idempotent — Pitfall 4/UPL-03).
    Add `private fun defaultName(data: SessionData): String` = a sport+date default (e.g. "Morning Ride" style or "$sport • $startTime") — no title editing in v1 (deferred). Never rethrow anywhere; the whole upload is best-effort and the local file is the source of truth.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug -q</automated>
    <grep>grep -c "driveUpload" phone/src/main/java/com/rokid/hud/phone/ActivitySummaryActivity.kt   # uses the tested pure state machine (>=1)</grep>
    <grep>grep -c "updateUploadState" phone/src/main/java/com/rokid/hud/phone/ActivitySummaryActivity.kt   # write-back on success (>=1)</grep>
    <grep>grep -c "isValidForUpload" phone/src/main/java/com/rokid/hud/phone/ActivitySummaryActivity.kt   # validity guard before POST (>=1)</grep>
  </verify>
  <done>assembleDebug builds. Tapping Upload (only when connected) generates GPX, guards validity, POSTs+polls via StravaUploader driven by driveUpload on a Thread with 2s spacing + a 120s deadline + a cancel flag; the button/status cycle through Uploading → Processing → Uploaded ✓ (View on Strava) / Retry / Pending; updateUploadState is called ONLY on Ready/Duplicate. Retry re-runs against the untouched local JSON.</done>
</task>

<task type="auto">
  <name>Task 3: HistoryActivity + list layouts + MainActivity launch-on-finish + history entry + service currentSessionId() + manifest</name>
  <files>phone/src/main/java/com/rokid/hud/phone/HistoryActivity.kt, phone/src/main/res/layout/activity_history.xml, phone/src/main/res/layout/item_activity.xml, phone/src/main/java/com/rokid/hud/phone/MainActivity.kt, phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt, phone/src/main/AndroidManifest.xml</files>
  <read_first>
    - phone/src/main/java/com/rokid/hud/phone/SessionStore.kt (listFinalSessions returns List&lt;File&gt; newest-first; use fromJson/readSession per file) and the SessionStoreTest for the file→SessionData shape.
    - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt:1201-1212 (confirmStopRecording — the launch site) and :40-70 of AndroidManifest.xml (the activity block shape; DeviceScanActivity is the exported=false template).
    - phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt:442-508 (stopRecording clears PREF_REC_SESSION_ID; add a currentSessionId() getter near recordingState()).
  </read_first>
  <action>
    (a) Service: add `fun currentSessionId(): String? = activitySessionManager?.snapshotSession()?.id` (or read `PREF_REC_SESSION_ID` from PREFS_HUD) near `recordingState()` in HudStreamingService — a main-thread-safe passthrough so MainActivity can grab the id BEFORE stopRecording clears it. (snapshotSession is main-thread-confined per ActivitySessionManager; the UI calls this on the main thread at the Finish tap.)
    (b) MainActivity.confirmStopRecording: in the "Finish" positive-button lambda, capture `val sessionId = service?.currentSessionId()` BEFORE `service?.stopRecording()`; after stop + the existing UI update + toast, if `sessionId != null` launch `startActivity(Intent(this, ActivitySummaryActivity::class.java).putExtra(ActivitySummaryActivity.EXTRA_SESSION_ID, sessionId))`. (The summary reads on a Thread and the finalize write is atomic; a brief read miss is acceptable but unlikely — finalizeAsync runs immediately on the main handler chain.) The id-only extra avoids the trackPoints IPC-size trap.
    (c) HistoryActivity (package `com.rokid.hud.phone`): onCreate `setContentView(R.layout.activity_history)`; on a `Thread{}` `val store = SessionStore(File(filesDir, "activities")); val rows = store.listFinalSessions().mapNotNull { f -> store.readSession(f.nameWithoutExtension) ?: store.fromJson(f.readText()) }` (prefer readSession by id; listFinalSessions is newest-first so order is preserved). runOnUiThread bind into a list (a simple ListView + a small BaseAdapter, or a vertical LinearLayout of inflated `item_activity` rows — no RecyclerView dependency needed; match the no-Fragment, findViewById convention). Each row shows: date (from startTime), sport, `SportFormat.formatDist(distanceM, imperial)`, `SportFormat.formatElapsed(elapsedMs)`, and a badge "Uploaded ✓" when `stravaUploaded` else "Not uploaded". Row tap → `startActivity(Intent(this, ActivitySummaryActivity::class.java).putExtra(EXTRA_SESSION_ID, data.id))`. Empty list → a "No recorded activities yet" placeholder. Read the imperial flag from `getSharedPreferences("MainActivity", Context.MODE_PRIVATE).getBoolean("use_imperial", false)` — the SAME activity-local store MainActivity + ActivitySummaryActivity use (checker WR: an app-wide read would always be metric).
    (d) `activity_history.xml` (dark theme; a titled list container + empty-state TextView) and `item_activity.xml` (a row: date/sport/distance/duration + the upload badge, camelCase ids like `rowDate`, `rowSport`, `rowStats`, `rowUploadBadge`).
    (e) MainActivity: add a history entry point — a button/menu item (reuse an existing button row style) that `startActivity(Intent(this, HistoryActivity::class.java))`. Wire it in onCreate near the other button setups.
    (f) AndroidManifest.xml: register both, mirroring DeviceScanActivity — `<activity android:name=".ActivitySummaryActivity" android:exported="false" android:theme="@style/Theme.RokidHud" />` and `<activity android:name=".HistoryActivity" android:exported="false" android:theme="@style/Theme.RokidHud" />`. Internal-only → exported=false (V4 / T-05 exported surface). Do NOT add intent-filters.
    If `store.fromJson` is `internal` and not reachable from HistoryActivity's package path, prefer `readSession(id)` exclusively (it is public) — map each File's `nameWithoutExtension` (the session id) through readSession.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug -q</automated>
    <grep>grep -c "ActivitySummaryActivity" phone/src/main/AndroidManifest.xml   # registered (>=1)</grep>
    <grep>grep -c "HistoryActivity" phone/src/main/AndroidManifest.xml   # registered (>=1)</grep>
    <grep>grep -c "currentSessionId" phone/src/main/java/com/rokid/hud/phone/MainActivity.kt   # id captured before stopRecording (>=1)</grep>
    <grep>grep -c "listFinalSessions" phone/src/main/java/com/rokid/hud/phone/HistoryActivity.kt   # history reads the seam (>=1)</grep>
  </verify>
  <done>assembleDebug builds and both activities are registered exported=false. Finish launches the summary with the session id captured before stopRecording. HistoryActivity lists past sessions (date/sport/distance/duration + uploaded badge) newest-first from listFinalSessions and reopens a summary on row tap; MainActivity has a history entry point; the service exposes currentSessionId().</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session id (intent extra) → ActivitySummaryActivity | Internal, non-exported activity; the id names a file in app-internal storage |
| SessionData (from disk) → summary UI | Own recorded data rendered as plain text (no WebView); status strings are plain text |
| Upload button → StravaUploader (Thread) | Crosses to strava.com over HTTPS (Plan 02 owns transport); this plan owns the UI + write-back trigger |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | Elevation of Privilege | New activities exported to other apps | mitigate | Both `android:exported="false"`, no intent-filters — internal navigation only (V4 Access Control) |
| T-05-11 | Denial of Service | TransactionTooLargeException from bulk trackPoints via intent | mitigate | Launch with the session id ONLY; the Activity reads trackPoints from disk (locked 05-RESEARCH Open Q2) |
| T-05-12 | Tampering | Optimistic success marking on a network drop mid-poll | mitigate | The upload Thread calls updateUploadState ONLY in the driveUpload success branch (Ready/Duplicate); a drop lands in Pending/Failed with the local JSON untouched → safe retry (UPL-03) |
| T-05-13 | Information Disclosure | GPX/coordinates/token leaking via UI logs | mitigate | The Activity logs status transitions only (no GPX body, no coordinates, no token); the DEBUG interceptor (Plan 02 client) stays BASIC |
| T-05-14 | Information Disclosure | View-on-Strava intent leaking data | mitigate | The ACTION_VIEW opens only `https://www.strava.com/activities/<numeric id>` (the id is a bounded Long from the poll) — no user data in the URL |
| T-05-SC | Tampering | package installs | mitigate | No packages added — osmdroid 6.1.18 already present; no new dependency in build.gradle.kts |
</threat_model>

<verification>
- `:phone:assembleDebug` builds (both new activities + layouts + manifest compile).
- `:phone:testDebugUnitTest` full phone suite still green (the pure Wave-1/2 seams unaffected by UI wiring).
- Grep gates: both activities registered exported=false in the manifest; `currentSessionId` captured before `stopRecording` in MainActivity; `driveUpload` + `updateUploadState` + `isValidForUpload` present in ActivitySummaryActivity; `listFinalSessions` in HistoryActivity; NO trackPoints intent extra.
- No new dependency lines in `phone/build.gradle.kts`.
</verification>

<success_criteria>
- Finishing a recording opens ActivitySummaryActivity (via session-id-only intent) showing total time, moving time, distance, avg speed (from SessionData), avg pace (derived), sport, start time, and the recorded route on the map. (UPL-01.)
- One tap on Upload (enabled only when Strava-connected) generates GPX, guards validity, POSTs + polls on a Thread, and cycles the button through Uploading → Processing → Uploaded ✓ (View on Strava) / Retry / Pending. (UPL-02.)
- On success the session JSON gains stravaUploaded=true + the activity id; on failure it is untouched and Retry re-uploads the same file (idempotent via duplicate recovery). (UPL-03.)
- A history list shows past sessions (date/sport/distance/duration + uploaded badge) newest-first and reopens a summary on tap. (UPL-04.)
- Both new activities are exported=false; the summary never receives trackPoints via intent.
</success_criteria>

<output>
Create `.planning/phases/05-activity-summary-strava-upload/05-03-SUMMARY.md` when done. Record: the ActivitySummaryActivity EXTRA_SESSION_ID contract + launch site, the upload-state UI mapping, the HistoryActivity list source, the new service `currentSessionId()` getter, and the two manifest registrations — so the Wave-4 device verification plan knows the exact adb-launchable entry points and the on-disk assertions to make.
</output>
