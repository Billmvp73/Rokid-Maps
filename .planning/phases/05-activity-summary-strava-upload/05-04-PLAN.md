---
phase: 05-activity-summary-strava-upload
plan: 04
type: execute
wave: 4
depends_on: [05-01, 05-02, 05-03]
files_modified:
  - .planning/phases/05-activity-summary-strava-upload/05-VERIFICATION.md
autonomous: false
requirements: [UPL-01, UPL-02, UPL-03, UPL-04]

must_haves:
  truths:
    - "On the real phone, recording → Finish opens the summary with plausible time/moving-time/distance/avg speed+pace and a route line (UPL-01 on hardware)"
    - "One tap on Upload uploads the recorded activity to the user's REAL Strava; the human confirms it appears in their feed with a route + plausible metrics (UPL-02 — the one irreducible human moment)"
    - "Re-uploading the same activity resolves via duplicate recovery to the SAME activity — no second activity in the feed, no data loss (Pitfall 4 duplicate path, live)"
    - "A simulated upload failure leaves the on-disk {id}.json with stravaUploaded:false and Retry works — local data is never deleted (UPL-03, adb-provable)"
    - "The history list shows the session with an 'uploaded ✓' badge after a successful upload; tapping it reopens the summary (UPL-04)"
    - "The full unit suite is green and assembleDebug builds with no new dependencies before any device step"
  artifacts:
    - path: ".planning/phases/05-activity-summary-strava-upload/05-VERIFICATION.md"
      provides: "Recorded milestone-finale device results: pass/fail per check + logcat/screenshot evidence + the human feed confirmation"
      contains: "UPL-02"
  key_links:
    - from: "real recorded activity"
      to: "the user's Strava feed"
      via: "GPX generate → POST /uploads → poll → activity_id → human confirms in-feed"
      pattern: "activities/"
---

<objective>
The milestone finale: prove the whole Strava chain end-to-end on real hardware with the user's real Strava account, in ONE phone session. The orchestrator drives every adb-scriptable step (full unit suite, build, grep gates, adb-launched summary render, simulated-failure local-intact proof, duplicate re-upload) and brackets the single irreducible human moment — the user confirming the uploaded activity really appears in their Strava feed (no adb can see strava.com).

Purpose: Observe all four ROADMAP Phase-5 success criteria on the OPPO test phone and close UPL-01..04 on hardware. This is the last human confirmation of the v1.0 milestone. Because Phases 3 and 4 also required a live Strava connection, this session assumes/confirms that connection is live (Phase-3 Authorize) — if Strava is not connected, HALT and report before any upload step.
Output: 05-VERIFICATION.md with pass/fail per check, logcat/screenshot evidence, and the human's feed confirmation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-activity-summary-strava-upload/05-VALIDATION.md
@.planning/phases/05-activity-summary-strava-upload/05-RESEARCH.md
@.planning/phases/05-activity-summary-strava-upload/05-01-SUMMARY.md
@.planning/phases/05-activity-summary-strava-upload/05-02-SUMMARY.md
@.planning/phases/05-activity-summary-strava-upload/05-03-SUMMARY.md
</context>

<device_facts>
- Phone: OPPO Find X9 Ultra, adb serial `3B164G01Y7L00000`. adb is on PATH. (Glasses are not required for Phase 5 — the upload/summary/history flow is phone-only; batch any leftover glasses spot only if the orchestrator carries one forward.)
- Scratchpad for logcat/screenshot captures: `/private/tmp/claude-501/-Users-bilhuang-Documents-rokid-maps/e88c0254-bb6e-4022-b9e8-a1496c44aabe/scratchpad`
- Build: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug -q`; install: `adb -s 3B164G01Y7L00000 install -r phone/build/outputs/apk/debug/phone-debug.apk`.
- Summary entry point (from 05-03): `ActivitySummaryActivity` launches with the string extra `session_id` (EXTRA_SESSION_ID). adb-launchable: `adb -s 3B164G01Y7L00000 shell am start -n com.rokid.hud.phone/.ActivitySummaryActivity -e session_id "<id>"` (exported=false → use `am start` with the explicit component from an adb shell with the app's own uid may be restricted; prefer driving the in-app Finish flow, and use a pre-seeded session file for the failure-path check).
- Session files live under the app-internal `filesDir/activities/` (`{id}.json`). Reading/writing them for assertions uses `adb shell run-as com.rokid.hud.phone cat files/activities/<id>.json` (debug build → run-as works).
</device_facts>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Strava-connected precondition HALT gate + full suite + build + grep gates (no upload yet)</name>
  <what-built>
    The complete Phase-5 upload/summary/history stack (Waves 1-3). Before any real upload, the orchestrator runs the automated floor:
    1. Full unit suite green: `:shared:testDebugUnitTest :phone:testDebugUnitTest` (all Wave-0 seams + the existing ~125).
    2. `:phone:assembleDebug` builds; `adb -s 3B164G01Y7L00000 install -r ...` succeeds.
    3. Source grep gates (mirror prior phases): `GpxWriter.kt` contains NO `android.util.Xml`; the upload request builder in `StravaUploader.kt` sets NO manual `Content-Type`/`multipart/form-data`; `sport_type` present (not only the deprecated `activity_type`); `id_str` used in the poll path; `SessionStore.updateUploadState` calls `writeAtomic` and there is NO `deleteSession`/`purge` in SessionStore.kt.
  </what-built>
  <action>
    Orchestrator: run the automated floor BEFORE presenting the checkpoint — (1) `:shared:testDebugUnitTest :phone:testDebugUnitTest` must be green; (2) `:phone:assembleDebug` builds and `adb -s 3B164G01Y7L00000 install -r phone/build/outputs/apk/debug/phone-debug.apk` succeeds; (3) the source grep gates listed in what-built all pass. Report their output. THEN present this precondition checkpoint and BLOCK on the human confirming a live Strava connection. If any automated gate fails or the human reports "halt", stop the Phase-5 device verification and report — do NOT proceed to any upload step.
  </action>
  <how-to-verify>
    HALT RULE (orchestrator directive): This is the milestone finale and it uploads to the user's REAL Strava. It requires a LIVE Strava connection (the Phase-3 Authorize). Present this precondition and BLOCK on the human:
    1. On the OPPO phone, open Rokid HUD Maps → the STRAVA card must show "Connected as {name}". If it does not: tap CONNECT STRAVA, complete the browser OAuth (leave ALL checkboxes checked), tap Authorize, land back with the connected toast. (This is the same live-auth moment Phases 3/4 needed — if it was already done, just confirm the card.)
    2. If Strava is NOT connected and cannot be connected (missing `strava.client.id`/`strava.client.secret` in local.properties, or callback domain not `rokidhud`): reply "halt: {reason}". The Phase-5 device verification cannot proceed without a live connection — the orchestrator stops and reports.
    3. Confirm the three automated gates above all passed (the orchestrator reports their output). If the suite is red or the build fails, reply "halt: build/tests".
  </how-to-verify>
  <done>The automated floor (suite green + assembleDebug + install + all grep gates) passed AND the human confirmed the STRAVA card shows Connected — OR the plan halted with a recorded reason (build/tests red, or Strava not connectable). No upload step runs until this gate is green.</done>
  <resume-signal>Type "connected" once the STRAVA card shows Connected AND the orchestrator reports suite-green + build-ok + grep-gates-pass; or "halt: {reason}" (e.g. not connected / build failed) to stop and report.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Record a short activity, open the summary, assert metrics render + route draws (UPL-01, no human judgment)</name>
  <read_first>
    @.planning/phases/05-activity-summary-strava-upload/05-VALIDATION.md (Device-only checklist)
    @.planning/phases/05-activity-summary-strava-upload/05-03-SUMMARY.md (the summary entry point + on-disk contract)
  </read_first>
  <action>
    Orchestrator, on the OPPO phone (adb-scriptable; the human may hold the phone and walk a short loop, OR replay a mock-provider track if a harness exists):
    1. Start a `adb -s 3B164G01Y7L00000 logcat -c` then begin capturing to `scratchpad/upl_summary.log`.
    2. Drive the in-app recording: tap Start recording (adb input on the record button, or the human taps), let a few minutes / a mock track accumulate GPS, then tap Stop → "Finish". The app launches ActivitySummaryActivity with the session id.
    3. Screencap the summary: `adb -s 3B164G01Y7L00000 exec-out screencap -p > scratchpad/upl_summary.png`.
    4. Capture the session id from logcat or `adb shell run-as com.rokid.hud.phone ls files/activities/` (newest `{id}.json`).
    5. Assert (automated): the summary screen shows non-zero total time, a moving time, a distance, an avg speed AND an avg pace line, sport, and start time; the map drew a Polyline (log a point count OR eyeball the screencap for a route line). Record the `{id}.json` contents via run-as (metrics match what the summary shows; trackPoints present).
    This is UPL-01 on hardware — no human judgment needed (the numbers rendering + a route line is the pass condition; the screencap is the evidence).
  </action>
  <verify>
    <automated>test -f "/private/tmp/claude-501/-Users-bilhuang-Documents-rokid-maps/e88c0254-bb6e-4022-b9e8-a1496c44aabe/scratchpad/upl_summary.png"</automated>
    <human-check>The summary screencap shows total time, moving time, distance, avg speed, avg pace, sport, start time, and a route line — recorded in 05-VERIFICATION.md.</human-check>
  </verify>
  <done>A real recorded session opened its ActivitySummaryActivity showing all UPL-01 metrics + a route map; the screencap and the on-disk {id}.json (via run-as) are captured as evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Simulated upload failure → local JSON intact + Retry (UPL-03, adb-provable, no human)</name>
  <read_first>
    @.planning/phases/05-activity-summary-strava-upload/05-VALIDATION.md (§Device verification, the simulated-failure path)
  </read_first>
  <action>
    Orchestrator, BEFORE the real upload (so a failed attempt cannot be confused with the real one), prove UPL-03 without touching real Strava:
    1. Force an upload failure path. Preferred: temporarily disable the phone's network (`adb -s 3B164G01Y7L00000 shell svc data disable && adb -s 3B164G01Y7L00000 shell svc wifi disable`), then in the summary tap "Upload to Strava". The POST throws → the state machine lands in Failed/Pending (NOT Done).
    2. Screencap the Failed/Pending state (`scratchpad/upl_fail.png`) — the button shows Retry / "pending".
    3. Assert the on-disk file is UNTOUCHED: `adb -s 3B164G01Y7L00000 shell run-as com.rokid.hud.phone cat files/activities/<id>.json | grep -o '"stravaUploaded":[a-z]*'` → must be `"stravaUploaded":false` and the trackPoints array is still present (grep the file has `"trackPoints"` and a plausible byte size). This proves the write-back happened ONLY on success — a failed/again-pending upload never mutates the source of truth (UPL-03).
    4. Re-enable network (`svc data enable`, `svc wifi enable`) for the next task. Leave the Retry affordance visible.
    Record the before/after `stravaUploaded` value and the file size in 05-VERIFICATION.md.
  </action>
  <verify>
    <automated>test -f "/private/tmp/claude-501/-Users-bilhuang-Documents-rokid-maps/e88c0254-bb6e-4022-b9e8-a1496c44aabe/scratchpad/upl_fail.png"</automated>
    <human-check>After a failed upload the on-disk {id}.json still shows "stravaUploaded":false with trackPoints intact (run-as cat), and the UI offers Retry — recorded in 05-VERIFICATION.md.</human-check>
  </verify>
  <done>A network-disabled upload attempt lands in Failed/Pending with Retry, and the on-disk session JSON still shows stravaUploaded:false with all trackPoints — UPL-03 proven on hardware without touching real Strava.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 4: Real upload to Strava + human feed confirmation + duplicate re-upload + history badge (UPL-02/03/04 — the milestone capstone)</name>
  <what-built>
    The one-tap real upload against the user's live Strava account, plus the duplicate-recovery path and the history badge. Network is back on (Task 3 re-enabled it). The orchestrator keeps a logcat capture running to `scratchpad/upl_real.log` (watch for the upload id_str, the poll status lines, the rate-usage line, and the final activity_id) and takes screencaps at each state.
  </what-built>
  <action>
    Orchestrator: network is on (Task 3 re-enabled it). Start `adb -s 3B164G01Y7L00000 logcat -c` then capture to `scratchpad/upl_real.log` (grep for the upload id_str, poll status lines, the rate-usage line, and the final activity_id — status/id lines only, never the GPX body). Screencap each upload state to `scratchpad/upl_real_*.png`. Present the how-to-verify steps and BLOCK on the human's report (the Strava-feed confirmation is the one thing no adb can observe). After the human reports, extract the activity_id from the log and record the `https://www.strava.com/activities/<id>` URL. Never rethrow; the local file remains the source of truth throughout.
  </action>
  <how-to-verify>
    Present these steps to the human and BLOCK on their confirmation (this is the irreducible human moment — no adb can see strava.com):
    1. On the summary for the just-recorded activity, tap "Upload to Strava". Watch the button/status cycle: "Uploading…" → "Strava is processing…" → "Uploaded ✓ — View on Strava". (If it lands in "pending", tap Retry — Strava can take 30s+.)
    2. THE CONFIRMATION: open the Strava app (or strava.com) on any device logged into the SAME account. Confirm the new activity appears in your feed with a route line and plausible distance/time matching the summary. Report what you saw. (This is the definitive UPL-02 proof.)
    3. DUPLICATE PATH: back in the app, tap "Upload to Strava" (or Retry) on the SAME activity again. It must resolve to "Uploaded ✓" via duplicate recovery — and CRUCIALLY, refresh your Strava feed and confirm there is NO second copy of the activity (the app recovered the existing activity id, it did not create a duplicate). Report whether a second activity appeared (it must NOT).
    4. HISTORY: go to the history list in the app. Confirm the just-uploaded session shows an "uploaded ✓" badge; tap it and confirm its summary reopens. (UPL-04.)
    The orchestrator records: the logcat activity_id, the "View on Strava" URL (`https://www.strava.com/activities/<id>`), and the human's answers to steps 2-4 in 05-VERIFICATION.md.
  </how-to-verify>
  <verify>
    <human-check>The human confirmed the activity appears in their Strava feed with a route + plausible metrics (UPL-02); a second upload created NO duplicate (duplicate recovery); and the history list shows the uploaded badge and reopens the summary (UPL-04) — all quoted in 05-VERIFICATION.md with the logcat activity_id.</human-check>
  </verify>
  <done>The real upload succeeded and the human confirmed it in their Strava feed; a re-upload resolved to the same activity with no duplicate; the history badge shows and the summary reopens. If the feed check failed or a duplicate appeared, it is recorded verbatim as a gap. Evidence (screencaps + non-sensitive logcat lines + the activity URL) is captured.</done>
  <resume-signal>Report in one message: (a) "in feed: yes/no" + the activity's rough distance/time; (b) "duplicate created: yes/no" after the second upload (must be no); (c) "history badge + reopen: yes/no". If (a) is no or (b) is yes, describe what happened so it can be recorded as a gap.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Record 05-VERIFICATION.md (pass/fail per UPL + evidence) and update STATE</name>
  <action>
    Orchestrator: write `.planning/phases/05-activity-summary-strava-upload/05-VERIFICATION.md` capturing, per requirement:
    - UPL-01: summary rendered all metrics + route (Task 2 screencap ref + on-disk metrics).
    - UPL-02: real upload succeeded; human confirmed the activity in their feed (quote their answer); the logcat activity_id + View-on-Strava URL.
    - UPL-03: simulated-failure kept {id}.json stravaUploaded:false with trackPoints intact (Task 3 before/after); Retry worked.
    - UPL-04: history list showed the uploaded badge and reopened the summary (Task 4 human answer).
    - Duplicate path: second upload recovered to the same activity, NO duplicate in feed (Task 4 human answer).
    - Automated floor: suite-green count, assembleDebug ok, all grep gates (Task 1).
    Mark each PASS/FAIL/BLOCKED with its evidence path (scratchpad screenshots + logcat). If any check FAILED, list it as a gap for `/gsd-plan-phase --gaps`. Update STATE.md Pending Todos: remove the resolved milestone-finale item; note the milestone v1.0 device verification complete (or the specific gap). Do NOT mark the phase verified here — that is `/gsd-verify-work`'s job; this plan only records evidence.
  </action>
  <verify>
    <automated>test -f ".planning/phases/05-activity-summary-strava-upload/05-VERIFICATION.md" && grep -q "UPL-02" .planning/phases/05-activity-summary-strava-upload/05-VERIFICATION.md</automated>
  </verify>
  <done>05-VERIFICATION.md records pass/fail + evidence for UPL-01..04 and the duplicate path, with the human's feed confirmation quoted; STATE.md Pending Todos updated. Any failed check is listed as a gap.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phone → user's real Strava account | The real upload crosses to strava.com over HTTPS with the live Bearer token; the GPX carries real location+time history |
| adb run-as → app-internal session files | The debug-only run-as reads/asserts {id}.json; not available on release builds (verification-only access) |
| human eyes → Strava feed | The only channel that can confirm the activity truly landed (no automated surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-15 | Information Disclosure | Real GPX/token appearing in captured logcat evidence | mitigate | The capture greps for status/id_str/activity_id/rate-usage lines only; the app never logs the GPX body/coordinates/token (verified by the Task-1 grep gates); do not paste raw logcat wholesale into 05-VERIFICATION.md — quote only the non-sensitive status/id lines |
| T-05-16 | Tampering | A failed upload silently mutating local data | mitigate | Task 3 explicitly asserts stravaUploaded:false + trackPoints intact after a forced failure BEFORE the real upload — proves the success-only write-back on hardware (UPL-03) |
| T-05-17 | (data integrity) | Duplicate activity created on re-upload | mitigate | Task 4 step 3 has the human confirm NO second activity — the live proof of the duplicate-recovery path (external_id + duplicate-error parse) |
| T-05-SC | Tampering | package installs | mitigate | No packages added; Task 1 confirms assembleDebug builds with an unchanged dependency set |
</threat_model>

<verification>
- Automated floor (Task 1): `:shared:testDebugUnitTest :phone:testDebugUnitTest` green + `:phone:assembleDebug` ok + all source grep gates pass, BEFORE any device step.
- UPL-01 (Task 2): summary renders all metrics + route on real hardware (screencap + on-disk metrics).
- UPL-03 (Task 3): forced-failure leaves the local JSON stravaUploaded:false with trackPoints intact (run-as cat), adb-provable, no real Strava needed.
- UPL-02 (Task 4): real upload + human feed confirmation + no-duplicate re-upload.
- UPL-04 (Task 4): history badge + reopen.
- 05-VERIFICATION.md written with per-requirement pass/fail + evidence (Task 5).
</verification>

<success_criteria>
- All four ROADMAP Phase-5 success criteria observed on the OPPO phone: summary after stop (SC#1), one-tap upload with progress/confirmation (SC#2), past activities listed and viewable (SC#3), upload failure keeps local data for retry (SC#4).
- The human confirmed the uploaded activity is really in their Strava feed (the true UPL-02 proof) and that a re-upload did NOT create a duplicate.
- UPL-03 is proven adb-only via the forced-failure local-intact check before the real upload.
- 05-VERIFICATION.md records evidence for every check; any failure is captured as a gap for `--gaps`. The milestone v1.0 device verification is complete.
</success_criteria>

<output>
Create `.planning/phases/05-activity-summary-strava-upload/05-VERIFICATION.md` (the recorded results) and `.planning/phases/05-activity-summary-strava-upload/05-04-SUMMARY.md` (the plan summary) when done. The SUMMARY notes this is the milestone-finale session and whether v1.0 is device-clear or has gaps.
</output>
