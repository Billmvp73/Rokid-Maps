---
phase: 01-activity-recording-engine
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - phone/src/main/java/com/rokid/hud/phone/SessionStore.kt
  - phone/src/test/java/com/rokid/hud/phone/SessionStoreTest.kt
autonomous: true
requirements: [REC-06]
must_haves:
  truths:
    - "SessionData round-trips through JSON preserving all fields, track-point order, and NaN/unknown sentinels"
    - "Checkpoint writes are atomic: temp file + fsync + rename, with the renameTo boolean checked and logged on failure"
    - "Finalize writes {id}.json then deletes {id}.checkpoint.json; orphan scan finds checkpoints without a final file"
    - "Orphan checkpoint fresher than 10 minutes is returned for resume; staler ones are finalized as interrupted sessions with endTime derived from file mtime"
    - "Corrupt checkpoint JSON never crashes: logged, preserved as .corrupt, not resumed"
  artifacts:
    - path: "phone/src/main/java/com/rokid/hud/phone/SessionStore.kt"
      provides: "Atomic JSON persistence + checkpoint lifecycle + orphan recovery"
      exports: ["writeCheckpointAsync", "writeCheckpointSync", "finalizeAsync", "finalizeSync", "recoverOrphans", "listFinalSessions", "shutdown"]
      min_lines: 120
    - path: "phone/src/test/java/com/rokid/hud/phone/SessionStoreTest.kt"
      provides: "TemporaryFolder-based persistence tests"
      min_lines: 100
  key_links:
    - from: "phone/src/main/java/com/rokid/hud/phone/SessionStore.kt"
      to: "File.renameTo"
      via: "atomic replace with checked boolean"
      pattern: "renameTo"
    - from: "phone/src/main/java/com/rokid/hud/phone/SessionStore.kt"
      to: "SessionModels.kt SessionData/TrackPoint"
      via: "toJson/fromJson"
      pattern: "SessionData"
---

<objective>
Build SessionStore: crash-safe local JSON persistence for recording sessions (REC-06). One file per session under a caller-supplied directory (the service passes filesDir/activities), checkpoint files every 60s/500 points, atomic writes, and orphan-checkpoint recovery implementing the locked <10-minute resume rule.

Purpose: Success Criterion 4 — session data survives app restart and crashes. All decision logic is JVM-testable because the store takes java.io.File, not Context (PATTERNS Integration Warning 7).
Output: SessionStore.kt + SessionStoreTest.kt.

MVP slice: after this plan, a session serialized to disk survives simulated crashes (temp-file interruption, corrupt data, stale checkpoints) — provable in tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-activity-recording-engine/01-CONTEXT.md
@.planning/phases/01-activity-recording-engine/01-RESEARCH.md
@.planning/phases/01-activity-recording-engine/01-PATTERNS.md
@phone/src/main/java/com/rokid/hud/phone/SessionModels.kt
@shared/src/main/java/com/rokid/hud/shared/cache/DiskTileCache.kt
@phone/src/main/java/com/rokid/hud/phone/SavedPlacesManager.kt

Locked decisions (CONTEXT.md "Session storage & identity" — NON-NEGOTIABLE):
- Storage dir: context.filesDir/activities (app-internal ONLY — never external storage; the service supplies the File in plan 01-04)
- Files: {id}.json final, {id}.checkpoint.json during recording; checkpoint atomically finalized on stop
- Schema fields: id, sport, startTime, endTime (ISO-8601 UTC), elapsedMs, movingMs, distanceM, avgSpeedMps, stravaUploaded, trackPoints[] of {lat, lng, alt, ts, speed, acc}
- Orphan detection on service start; resume if checkpoint <10 min stale, else finalize as interrupted
- Keep all sessions in v1 — implement NO purge/deletion logic (history UI + deletion is Phase 5)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: JSON serialization + atomic write primitive</name>
  <files>phone/src/main/java/com/rokid/hud/phone/SessionStore.kt, phone/src/test/java/com/rokid/hud/phone/SessionStoreTest.kt</files>
  <read_first>
    - phone/src/main/java/com/rokid/hud/phone/SessionModels.kt (SessionData/TrackPoint contracts from plan 01-01)
    - shared/src/main/java/com/rokid/hud/shared/cache/DiskTileCache.kt (class skeleton, single-thread executor, try/catch+Log shape, shutdown pattern)
    - phone/src/main/java/com/rokid/hud/phone/SavedPlacesManager.kt (org.json data-class-list encode/decode convention)
    - .planning/phases/01-activity-recording-engine/01-RESEARCH.md — Pattern 6 (writeAtomic: temp + fd.sync + renameTo) and Pitfall 8 (renameTo returns false silently)
  </read_first>
  <behavior>
    - toJson→fromJson round-trip preserves every SessionData field including trackPoints order and values; JSON includes "schemaVersion":1 (written as a constant at the JSON layer)
    - Top-level JSON keys are exactly: schemaVersion, id, sport, startTime, endTime (omitted when null), elapsedMs, movingMs, distanceM, avgSpeedMps, stravaUploaded, trackPoints
    - Per-point keys: lat, lng, alt, ts, speed, acc, brg — alt/speed/brg keys are OMITTED when the value is NaN (org.json throws on non-finite doubles) and read back as Double.NaN via optDouble default; acc uses the finite -1.0 sentinel and is always written
    - fromJson on corrupt input ("{not json") returns null without throwing; fromJson on JSON missing the required id field returns null
    - writeAtomic(target, json): after success target contains the full JSON and no .tmp sibling remains; writing over an existing target replaces its content atomically
    - stravaUploaded defaults to false in JSON output (Phase 5 upload contract)
  </behavior>
  <action>
    Write the failing tests first (SessionStoreTest.kt using org.junit.rules.TemporaryFolder with @Rule), then implement:

    class SessionStore(private val dir: File) — constructor takes java.io.File, NOT Context (locked JVM-testability divergence from DiskTileCache, PATTERNS Integration Warning 7). init calls dir.mkdirs(). companion object: TAG = "SessionStore", SCHEMA_VERSION = 1, CHECKPOINT_SUFFIX = ".checkpoint.json", FINAL_SUFFIX = ".json", MAX_RESUME_AGE_MS = 10 * 60_000L.

    1. internal fun toJson(data: SessionData): String — JSONObject().apply{} per repo convention; put schemaVersion first; endTime omitted when null (org.json has no nullable put — just skip the key); trackPoints as JSONArray of JSONObject with keys lat/lng/alt/ts/speed/acc/brg, omitting alt/speed/brg when isNaN (CRITICAL: JSONObject.put with NaN throws "Forbidden numeric value" — omission is the sentinel encoding). brg is additive per REC-01 bearing requirement (matches TrackPoint.bearingDeg from plan 01-01).
    2. internal fun fromJson(json: String): SessionData? — wrap entirely in try/catch returning null with Log.w (new-code rule: LOG in catch blocks, never silently swallow — CONCERNS Pitfall 6); required fields via getString("id")/getString("sport")/getString("startTime") (throw→null), everything else via optXxx with defaults (endTime via optString then takeIf notEmpty → null; numerics 0; per-point alt/speed/brg via optDouble(key, Double.NaN), acc via optDouble("acc", -1.0), brg optDouble("brg", Double.NaN)).
    3. internal fun writeAtomic(target: File, json: String) — exactly RESEARCH Pattern 6: tmp = File(dir, target.name + ".tmp"); FileOutputStream(tmp).use { write UTF-8 bytes; fos.fd.sync() }; if (!tmp.renameTo(target)) Log.e(TAG, rename-failed message) (Pitfall 8 — boolean MUST be checked); catch Exception → Log.e + best-effort tmp.delete(). Same-directory rename only (same filesystem = atomic POSIX rename; never write tmp to another dir).
    4. Single-thread executor field (Executors.newSingleThreadExecutor — DiskTileCache pattern) for async wrappers added in Task 2; fun shutdown() calls executor.shutdown() NOT shutdownNow — a queued final checkpoint must complete during service teardown (documented divergence from DiskTileCache.shutdownNow).

    File uses android.util.Log — JVM tests rely on isReturnDefaultValues=true installed by plan 01-01. No other android.* imports allowed.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "*SessionStoreTest*" -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "class SessionStore(private val dir: File)" phone/src/main/java/com/rokid/hud/phone/SessionStore.kt succeeds (File constructor, not Context)
    - grep -q "renameTo" phone/src/main/java/com/rokid/hud/phone/SessionStore.kt succeeds AND the call site checks the boolean (visible if/! usage)
    - grep -q "schemaVersion" phone/src/main/java/com/rokid/hud/phone/SessionStore.kt succeeds
    - grep -q "import android.content.Context" phone/src/main/java/com/rokid/hud/phone/SessionStore.kt fails (exit 1)
    - :phone:testDebugUnitTest --tests "*SessionStoreTest*" exits 0
  </acceptance_criteria>
  <done>SessionData persists to and restores from disk atomically with exact locked schema keys plus documented additive fields (schemaVersion, brg).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Checkpoint lifecycle, finalize, and orphan recovery (<10-min resume rule)</name>
  <files>phone/src/main/java/com/rokid/hud/phone/SessionStore.kt, phone/src/test/java/com/rokid/hud/phone/SessionStoreTest.kt</files>
  <read_first>
    - phone/src/main/java/com/rokid/hud/phone/SessionStore.kt (Task 1 result — extend)
    - .planning/phases/01-activity-recording-engine/01-RESEARCH.md — Pattern 6 "Lifecycle" paragraph and Pattern 7 L3 (orphan scan on restart)
    - .planning/phases/01-activity-recording-engine/01-CONTEXT.md — "START_STICKY restart mid-recording" locked decision
  </read_first>
  <behavior>
    - writeCheckpointSync(data) creates {id}.checkpoint.json in dir; calling again overwrites atomically (single checkpoint per session)
    - finalizeSync(data) writes {id}.json and deletes {id}.checkpoint.json; after finalize, findOrphanCheckpoints() does not report that id
    - findOrphanCheckpoints() lists only *.checkpoint.json files lacking a matching {id}.json
    - recoverOrphans(nowWallMs) with a checkpoint whose lastModified is 5 minutes ago returns RecoveryResult(resumable = that SessionData, finalizedInterrupted = 0, corrupt = 0) and leaves the checkpoint file in place (the service finalizes it later through the normal stop path)
    - recoverOrphans with a checkpoint 11 minutes old returns resumable = null, finalizedInterrupted = 1; a final {id}.json now exists with endTime == ISO-8601 of the checkpoint file's lastModified, and the checkpoint is deleted
    - recoverOrphans with two fresh orphans resumes only the newest (by lastModified) and finalizes the other as interrupted
    - recoverOrphans with a corrupt checkpoint renames it to {id}.checkpoint.corrupt (bytes preserved for post-mortem), reports corrupt = 1, does not throw
    - listFinalSessions() returns final .json files only (no checkpoints, no .tmp, no .corrupt)
  </behavior>
  <action>
    Write the failing tests first (TemporaryFolder; use File.setLastModified to construct staleness scenarios), then implement:

    1. data class RecoveryResult(val resumable: SessionData?, val finalizedInterrupted: Int, val corrupt: Int) — declare in SessionStore.kt (top-level or nested; it is a store concern, not a SessionModels contract).
    2. private fun checkpointFile(id: String) / finalFile(id: String) helpers building names from the locked convention.
    3. fun writeCheckpointSync(data: SessionData) — toJson + writeAtomic to checkpointFile(data.id). fun writeCheckpointAsync(data: SessionData) — executor.execute wrapping the sync call in try/catch+Log.w (DiskTileCache put() shape). Serialization happens ON the executor (SessionData is immutable — safe hand-off; keeps multi-thousand-point JSON off the main thread).
    4. fun finalizeSync(data: SessionData) — writeAtomic final file, then delete checkpoint (checked delete with Log.w on false). fun finalizeAsync(data: SessionData) — executor wrapper.
    5. fun findOrphanCheckpoints(): List of Files — dir.listFiles filtered by CHECKPOINT_SUFFIX where finalFile(id) does not exist; id extracted by stripping the suffix.
    6. fun recoverOrphans(nowWallMs: Long = System.currentTimeMillis()): RecoveryResult — for each orphan: read via fromJson; corrupt (null) → rename to id + ".checkpoint.corrupt" + Log.w + count; readable → age = nowWallMs - file.lastModified(); age < MAX_RESUME_AGE_MS → candidate for resume (keep newest only); else finalize-as-interrupted: copy(endTime = Instant.ofEpochMilli(file.lastModified()).toString()) then finalizeSync + count. If multiple fresh candidates, newest resumes and older ones are finalized as interrupted. Synchronous (runs once at service start before location updates begin).
    7. fun listFinalSessions(): List of Files sorted newest-first — cheap Phase-5 seam (UPL-04 will consume; no UI now).

    No purge/delete-session API — locked "keep all sessions in v1".
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "*SessionStoreTest*" -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "fun recoverOrphans" phone/src/main/java/com/rokid/hud/phone/SessionStore.kt succeeds; grep -q "MAX_RESUME_AGE_MS = 10 \* 60_000L" succeeds (or equivalent 600000 constant)
    - Test file contains a test asserting the 11-minute-old checkpoint is finalized as interrupted and a test asserting the 5-minute-old checkpoint is returned for resume
    - grep -qi "fun deleteSession\|fun purge" phone/src/main/java/com/rokid/hud/phone/SessionStore.kt fails (exit 1) — no deletion API in v1
    - :phone:testDebugUnitTest --tests "*SessionStoreTest*" exits 0; full suite (:shared: + :phone:) exits 0
  </acceptance_criteria>
  <done>Crash recovery decision logic (resume vs finalize-as-interrupted vs corrupt) is fully unit-tested; the service (plan 01-04) only wires it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → filesystem | Precise location-history JSON written to app storage |
| Disk (post-crash state) → app | Checkpoint files read back after unclean shutdown are untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | Session JSON (precise location history) at rest | mitigate | Written ONLY under the caller-supplied dir which the service binds to context.filesDir (app-private, ASVS V4/V8); SessionStore has no Context and cannot reach external storage; acceptance criteria forbid Context import. Note: android:allowBackup=true means files enter device backups — accepted for a personal app (RESEARCH V8 advisory, revisit in Phase 5) |
| T-03-02 | Tampering / Integrity | Checkpoint files across crashes | mitigate | temp + fsync + checked renameTo (atomic same-dir POSIX rename); validate-on-read (fromJson null on corrupt); corrupt checkpoints quarantined as .corrupt, never resumed, never deleted silently |
| T-03-03 | DoS (data loss) | finalize path | mitigate | Final file written BEFORE checkpoint deletion; upload (Phase 5) never deletes local data (UPL-03 contract starts here with stravaUploaded=false) |
</threat_model>

<verification>
- Full suite green (shared + phone)
- SessionStoreTest exercises: round-trip, NaN sentinel omission, corrupt input, atomic overwrite, orphan scan, 10-minute boundary both sides, multi-orphan, corrupt quarantine
</verification>

<success_criteria>
- Success Criterion 4: session JSON persists on stop with 60s/500pt checkpoint support and survives restart (store layer proven; service wiring in plan 01-04)
- Success Criterion 7 (SessionStore portion): persistence tests pass on plain JVM
</success_criteria>

<output>
Create `.planning/phases/01-activity-recording-engine/01-03-SUMMARY.md` when done. Record: final SessionStore public API (plan 01-04 consumes it) and the exact JSON schema as written.
</output>
