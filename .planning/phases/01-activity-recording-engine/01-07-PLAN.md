---
phase: 01-activity-recording-engine
plan: 07
type: execute
wave: 5
depends_on: ["01-05", "01-06"]
files_modified:
  - phone/src/debug/java/com/rokid/hud/phone/MockRouteFeeder.kt
autonomous: false
requirements: [REC-03, REC-04, REC-05, REC-06, REC-07]
must_haves:
  truths:
    - "sport_state lines appear in logcat at ~1Hz on the OPPO while recording (Success Criterion 1 runtime proof)"
    - "A mock GPS track drives distance up during moving segments and holds it flat during the stationary-drift segment (Success Criterion 2 end-to-end proof)"
    - "adb shell am kill mid-recording leads to auto-resume from checkpoint (Success Criterion 4 restart proof; settles RESEARCH A7/R8 background-location question)"
    - "A 30-minute screen-off recording on the OPPO Find X9 Ultra completes with no fix gap >30s (Success Criterion 3 — the phase gate)"
    - "Checkpoint file mtime advances during recording; stop produces a final {id}.json and removes the checkpoint"
  artifacts: []
  key_links: []
---

<objective>
Prove the recording engine on the real target device: the OPPO Find X9 Ultra (ColorOS, Android 16) — the project's aggressive-battery-management worst case. Scripted mock-GPS verification of metrics/broadcast/persistence, a kill-restart recovery check, and the human-run 30-minute screen-off phase gate.

Purpose: Success Criteria 1-4 all have on-device halves that JVM tests cannot reach (OEM kill behavior is unmockable — RESEARCH). This plan is the orchestrator's device-verification pass.
Output: Verification evidence recorded in the SUMMARY; STATE.md pending-todo updates (ACCESS_BACKGROUND_LOCATION validation result; 2-hour pre-release run stays pending).

Devices (per 01-VALIDATION.md; re-verify identity in Task 1 — RESEARCH A6 had the serials swapped): OPPO phone expected at serial 3B164G01Y7L00000, Rokid glasses at 1901092544802583. adb at /opt/homebrew/share/android-commandlinetools/platform-tools/adb (NOT on PATH).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-activity-recording-engine/01-RESEARCH.md
@.planning/phases/01-activity-recording-engine/01-VALIDATION.md
@.planning/phases/01-activity-recording-engine/01-CONTEXT.md

Key RESEARCH sections: "Phase-gate verification (30-min screen-off on the OPPO)", "Alternative: pure-adb mock provider", Pitfall 3 (Doze needs stationary + unplugged to engage — USB charging defeats the test), Pitfall 4 (ColorOS user-side checklist, assumptions A1/A2), Pitfall 9 (mock teardown).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build, install, identify devices, and prepare the mock-GPS harness</name>
  <files>phone/src/debug/java/com/rokid/hud/phone/MockRouteFeeder.kt (FALLBACK ONLY — see step 5)</files>
  <read_first>
    - .planning/phases/01-activity-recording-engine/01-RESEARCH.md — "Alternative: pure-adb mock provider" (exact cmd location syntax, AOSP-verified), "Debug mock-route feeder" (fallback design), Environment Availability table (absolute paths)
    - .planning/phases/01-activity-recording-engine/01-VALIDATION.md — On-device validation checklist and device serials
  </read_first>
  <action>
    Use ADB=/opt/homebrew/share/android-commandlinetools/platform-tools/adb and JAVA_HOME=/opt/homebrew/opt/openjdk@17 throughout (neither is on PATH).

    1. Device identity (settles RESEARCH A6): for each serial (3B164G01Y7L00000, 1901092544802583) run $ADB -s SERIAL shell getprop ro.product.model and ro.product.brand. The OPPO/Find X9 device is the phone target; record the mapping for the SUMMARY. All later commands use -s with the confirmed OPPO serial.
    2. Build + install: GradleWrapperMain assembleDebug, then $ADB -s OPPO install -r phone/build/outputs/apk/debug/phone-debug.apk. Launch check: $ADB -s OPPO shell am start -n com.rokid.hud.phone/.MainActivity then verify with pidof com.rokid.hud.phone.
    3. Mock provider setup (zero-app-code primary path, AOSP-verified): $ADB -s OPPO shell appops set com.android.shell android:mock_location allow; then cmd location providers add-test-provider gps --supportsSpeed --supportsBearing --supportsAltitude; set-test-provider-enabled gps true; push one fix via set-test-provider-location gps --location 37.7749,-122.4194 --accuracy 5. If any cmd errors (ColorOS divergence — RESEARCH A4), record the error and go to step 5.
    4. Write two helper scripts into the session scratchpad directory (they are tool inputs for Task 2, not repo files):
       a. feed_track.sh — loops set-test-provider-location at 1Hz through three segments: (i) 90 moving fixes advancing latitude by 0.00005 deg/tick with --accuracy 5 (approx 5.5 m/s ground speed; the shell provider derives speed or accepts --speed if supported — probe the syntax first with cmd location providers set-test-provider-location gps help semantics from the AOSP source cited in RESEARCH); (ii) 60 stationary fixes jittering ±0.00001 deg around a point (drift simulation — distance must NOT grow); (iii) 60 moving fixes again. Takes the OPPO serial and adb path as variables.
       b. cleanup_mock.sh — cmd location providers set-test-provider-enabled gps false; remove-test-provider gps; appops set com.android.shell android:mock_location deny (Pitfall 9 teardown — mock must never leak into real use).
    5. FALLBACK (only if step 3 fails on ColorOS): create phone/src/debug/java/com/rokid/hud/phone/MockRouteFeeder.kt per the RESEARCH "Debug mock-route feeder" example (FLP setMockMode/setMockLocation on a raw Thread, setMockMode(false) in finally) in the DEBUG SOURCE SET ONLY — phone/src/debug/ is physically absent from release builds (security requirement: mock path must not ship enabled in release). Trigger it from a temporary debug-only hook and grant the app appops android:mock_location. If the primary path works, do NOT create this file.
    6. Preflight status capture for the SUMMARY: $ADB -s OPPO shell dumpsys deviceidle whitelist | grep -i rokid (exemption state), am get-standby-bucket com.rokid.hud.phone, and getprop ro.build.version.release/ro.build.display.id (ColorOS version — updates RESEARCH A1/A2 assumptions).
  </action>
  <verify>
    <automated>/opt/homebrew/share/android-commandlinetools/platform-tools/adb -s 3B164G01Y7L00000 shell pm list packages | grep -q com.rokid.hud.phone</automated>
  </verify>
  <acceptance_criteria>
    - Both device serials identified with model strings recorded; OPPO serial confirmed (expected 3B164G01Y7L00000 — if it is the other serial, use that everywhere and flag the VALIDATION.md correction in the SUMMARY)
    - assembleDebug exit 0; package com.rokid.hud.phone present on the OPPO; app process starts
    - Mock provider registered without error OR fallback MockRouteFeeder.kt created in phone/src/debug/ (never in src/main) with setMockMode(false) teardown in a finally block
    - feed_track.sh and cleanup_mock.sh exist in the scratchpad and are executable
  </acceptance_criteria>
  <done>Device harness ready: fresh APK installed on the confirmed OPPO, mock feed + cleanup scripted, preflight state captured.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Interactive device verification + 30-minute screen-off phase gate</name>
  <files></files>
  <read_first>
    - Task 1's scratchpad scripts (feed_track.sh, cleanup_mock.sh) and preflight capture — the executor runs these during Part A
    - .planning/phases/01-activity-recording-engine/01-RESEARCH.md — Pitfall 3 (USB charging defeats Doze — Part B must run unplugged) and Pitfall 4 (ColorOS checklist wording, assumption A1)
  </read_first>
  <action>Pause for human-in-the-loop device verification. Present the Part A / Part B protocol below, run the scripted Part-A checks (feed_track.sh, logcat cadence capture, checkpoint mtime snapshots, am kill + restart observation) between the human's "recording started" and Part-A completion, then hand off Part B (the 30-minute screen-off run) to the human. This is the orchestrator's device-verification pass — the 30-minute run cannot execute inside a task loop.</action>
  <what-built>The complete Phase-1 recording engine on the OPPO: recording card UI, GPS filtering + metrics, 1Hz sport_state BT broadcast with logcat mirror, 60s/500pt checkpoints, orphan auto-resume, staleness watchdog, live notification.</what-built>
  <how-to-verify>
    PART A — interactive pipeline check (~10 min, phone attached via USB):
    1. On the OPPO: open Rokid HUD Maps, tap "Start Streaming", then in the RECORD card pick "Ride" and tap "Start Recording". Accept the battery-exemption prompt with "Allow" (grant "Don't optimize"), and complete the background-location prompt with "Allow all the time" when routed to settings. Confirm the REC badge + live metrics panel appear and the notification shows "Recording —".
    2. Reply here with "recording started" — I will then run the scripted checks myself (mock track feed, 1Hz logcat cadence, distance-flat-while-stationary assertion, checkpoint mtime advance, adb-kill + auto-resume check) and report each result. Leave the phone attached and the recording running until I say Part A is done.
    3. When I confirm Part A, tap "Stop" → "Finish" in the app.

    PART B — 30-minute screen-off gate (Success Criterion 3):
    4. ColorOS prep on the phone (RESEARCH Pitfall 4; menu names vary by ColorOS version — note what you actually see): Settings → Apps → Rokid HUD Maps → Battery → set "Allow background activity" (and "Allow auto launch" if present); ensure Power Saving Mode is OFF; optionally lock the app in the recents view.
    5. Start a NEW recording (Ride) via the app. Then either:
       Option A (preferred, realistic): unplug the phone, screen off, and take a ≥30-minute walk/ride with the phone in your pocket.
       Option B (desk): leave the phone UNPLUGGED (USB charging defeats Doze — the test must run on battery) and stationary with the screen off for 30+ minutes. Note: with real GPS indoors this tests service survival, not distance; that is acceptable — Part A already proved distance semantics.
    6. After 30+ minutes: turn the screen on, stop the recording (Stop → Finish), reconnect USB, and reply "part B done" (mention which option you ran and anything odd — prompts, killed app, missing notification).
  </how-to-verify>
  <resume-signal>Reply "recording started" after step 1-2, then "part B done" after step 6 (or describe any failures observed)</resume-signal>
  <verify>
    <automated>MISSING — inherently human/device-gated (OEM kill behavior is unmockable per RESEARCH); scripted assertions run in Tasks 1 and 3 around this checkpoint</automated>
    <human-check>User confirms Part A (recording started, prompts granted, kill survived) and Part B (30-minute screen-off session recorded and stopped)</human-check>
  </verify>
  <acceptance_criteria>
    - Part A confirmed: recording started with exemption + background location granted; REC badge and live metrics visible; recording survived the scripted kill (or the failure is documented as an R8 finding)
    - Part B confirmed: a ≥30-minute screen-off session was recorded and stopped normally
    - Any ColorOS settings-path deviations from the RESEARCH A1 wording captured for the SUMMARY
  </acceptance_criteria>
  <done>Human confirms both parts complete (or reports failures verbatim); raw evidence (logcat capture, session files) is in place for Task 3 analysis.</done>
</task>

<task type="auto">
  <name>Task 3: Evidence analysis — logs, session files, and phase-gate verdict</name>
  <files></files>
  <read_first>
    - .planning/phases/01-activity-recording-engine/01-RESEARCH.md — "Phase-gate verification" command block (run-as file inspection)
    - .planning/phases/01-activity-recording-engine/01-VALIDATION.md — on-device success definitions (SC#1-4)
  </read_first>
  <action>
    Using the captured Part-A logs and the Part-B session file, produce the phase-gate verdict:

    1. SC#1 (1Hz broadcast): from the Part-A logcat capture, count "sport_state" lines over a 60s window — PASS if ≥45 lines and the JSON contains "v":1 and monotonic et/d across samples.
    2. SC#2 (filtering): from Part-A ticker logs, extract the d (distance) values: PASS if d grows during segment (i), stays flat (delta < 2m) across the 60-fix stationary segment (ii), and resumes growing in (iii).
    3. SC#4 (restart): confirm the Part-A kill test showed a resumed session — logcat "Resumed interrupted session" line and the same session id continuing. Record whether resume worked WITH background-location granted (settles STATE todo "Validate on the OPPO whether ACCESS_BACKGROUND_LOCATION is actually required" — record observed behavior; if restart failed, file it as a known R8 limitation with the orphan-recovery-on-open fallback noted).
    4. SC#3 (30-min gate): pull the Part-B final session via $ADB -s OPPO shell "run-as com.rokid.hud.phone cat files/activities/{newest}.json" (list first with run-as ls -la). Parse trackPoints ts values (python3 one-liner in the scratchpad): PASS if total span ≥30 min AND max inter-point gap ≤30s. If gaps of 30-120s exist, report them with timestamps — borderline results go to the human with the data rather than silent pass/fail. Also verify: endTime set, stravaUploaded false, schemaVersion 1, no {id}.checkpoint.json remaining.
    5. Checkpoint cadence: from Part-A run-as ls -la snapshots, confirm the checkpoint file mtime advanced at ~60s intervals during recording.
    6. Run cleanup_mock.sh (Pitfall 9 — mock provider must not leak) and verify with a real-GPS fix reaching the app afterwards (or at minimum provider removal exit 0).
    7. Record all verdicts in a table for the SUMMARY; update targets: STATE.md pending todos (background-location validation result; keep the 2-hour pre-release run pending; note ColorOS settings-path wording from Part B for A1).
  </action>
  <verify>
    <automated>/opt/homebrew/share/android-commandlinetools/platform-tools/adb -s 3B164G01Y7L00000 shell "run-as com.rokid.hud.phone ls files/activities/" | grep -q ".json"</automated>
  </verify>
  <acceptance_criteria>
    - SC#1: ≥45 sport_state logcat lines per 60s with v:1 and monotonic et/d — verdict recorded
    - SC#2: stationary segment distance delta <2m — verdict recorded
    - SC#3: session JSON spans ≥30 min with max ts gap ≤30s (or documented borderline analysis) — verdict recorded
    - SC#4: kill→resume observed and background-location finding recorded
    - files/activities/ contains final .json session(s) with no orphan .checkpoint.json after clean stops
    - Mock provider removed (cleanup script exit 0)
  </acceptance_criteria>
  <done>Phase-gate evidence table complete; STATE todos updated with device findings; mock harness torn down.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adb/debug shell → device | Mock-location injection and run-as file access during verification |
| Verification artifacts → repo | Logs contain the user's (mock and real) location traces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Spoofing | Mock-location path | mitigate | Primary path is adb-shell-only (com.android.shell appop — nothing ships in the APK); fallback feeder lives in phone/src/debug/ only, physically absent from release builds; cleanup script tears down provider + appop after the run (Pitfall 9) |
| T-07-02 | Information Disclosure | Session JSON / logcat pulled during analysis | mitigate | Real-GPS traces from Part B stay in the scratchpad (session-scoped, outside the repo); SUMMARY records verdicts and gap statistics, never raw coordinate dumps |
| T-07-03 | Tampering | run-as file inspection | accept | Debug-build-only capability on the developer's own device; unavailable on release builds |
</threat_model>

<verification>
- All five verdicts (SC#1, SC#2, SC#3, SC#4, checkpoint cadence) recorded PASS or documented-borderline in the SUMMARY
- Mock provider torn down; no MockRouteFeeder in phone/src/main
- STATE.md pending todos updated (background-location finding; 2-hour pre-release validation remains pending)
</verification>

<success_criteria>
- Success Criterion 3: 30-minute screen-off recording on the OPPO verified (the phase gate)
- Success Criteria 1, 2, 4: runtime halves verified on-device (logcat cadence, drift-free distance, restart recovery)
- R8/A7 (background-location necessity) settled with observed evidence
</success_criteria>

<output>
Create `.planning/phases/01-activity-recording-engine/01-07-SUMMARY.md` when done. Record: device-identity mapping, verdict table, ColorOS settings-path observations (A1/A2), background-location finding, and any borderline gap analysis.
</output>
