---
phase: 01-activity-recording-engine
plan: 05
type: execute
wave: 4
depends_on: ["01-04"]
files_modified:
  - phone/src/main/res/layout/activity_main.xml
  - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt
autonomous: true
requirements: [REC-01, REC-02, REC-05]
must_haves:
  truths:
    - "User can tap Start Recording on a dedicated card and a session starts (opt-in — navigation never auto-starts recording)"
    - "While recording, the card shows elapsed time, distance, current speed, and pace updating at ~1Hz, respecting the imperial/metric toggle"
    - "Stop requires a 'Finish recording?' confirm dialog; there is NO discard option — sessions always save"
    - "A REC badge with elapsed time is visible while recording; UI state survives activity recreation (re-synced from the service on connect/resume)"
    - "Battery-optimization exemption prompt fires on first recording start when not exempt; ACCESS_BACKGROUND_LOCATION is requested during first-recording onboarding; declining either never blocks recording"
    - "Sport type toggle (Ride/Run, default Ride) persists and is passed to the session"
  artifacts:
    - path: "phone/src/main/res/layout/activity_main.xml"
      provides: "RECORD section card with sport toggle, start/stop buttons, live metric texts, REC badge"
      contains: "btnStartRecording"
    - path: "phone/src/main/java/com/rokid/hud/phone/MainActivity.kt"
      provides: "Recording card wiring, confirm dialog, metrics listener, onboarding prompts, formatters"
      contains: "Finish recording?"
  key_links:
    - from: "phone/src/main/java/com/rokid/hud/phone/MainActivity.kt"
      to: "HudStreamingService.startRecording"
      via: "binder call on button tap"
      pattern: "startRecording\\("
    - from: "phone/src/main/java/com/rokid/hud/phone/MainActivity.kt"
      to: "MetricsListener"
      via: "setMetricsListener + runOnUiThread card updates"
      pattern: "setMetricsListener"
---

<objective>
Build the phone recording UI: the locked recording card on MainActivity (Start/Stop, sport toggle, live metrics, REC badge, confirm-to-stop) plus first-recording onboarding (battery-optimization exemption prompt and ACCESS_BACKGROUND_LOCATION request).

Purpose: REC-01's explicit opt-in user action lives here; REC-02's metrics become user-visible; REC-05's user-consent layers (exemption + background location) are Activity-context flows.
Output: Modified activity_main.xml + MainActivity.kt.

MVP slice: after this plan, the user records a complete activity end-to-end from the phone screen — the phase's core user story is functional.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-activity-recording-engine/01-CONTEXT.md
@.planning/phases/01-activity-recording-engine/01-PATTERNS.md
@.planning/phases/01-activity-recording-engine/01-04-SUMMARY.md
@phone/src/main/java/com/rokid/hud/phone/MainActivity.kt
@phone/src/main/res/layout/activity_main.xml
@phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt

Locked decisions (CONTEXT.md "Recording controls" + "Live metrics" — NON-NEGOTIABLE):
- Dedicated recording card below the search area; big Start button; live metrics + Stop while recording
- Stop → confirm dialog "Finish recording?"; NO discard option (auto-save always; deletion is Phase 5)
- Card shows elapsed, distance, current speed + pace secondary line, honoring the existing imperial toggle
- 1Hz updates from the SAME metrics callback that feeds the BT broadcast
- Sport toggle Ride/Run (default Ride) stored as the session's sport field
- Battery-exemption prompt on FIRST recording start (not app launch)

Cross-plan note: the ACCESS_BACKGROUND_LOCATION manifest declaration ships in plan 01-06 (same wave). Manifest.permission.ACCESS_BACKGROUND_LOCATION is a framework constant, so this plan compiles and merges independently; the runtime request becomes functional when Wave 4 completes (before any device verification in plan 01-07).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recording card layout + MainActivity wiring with live metrics</name>
  <files>phone/src/main/res/layout/activity_main.xml, phone/src/main/java/com/rokid/hud/phone/MainActivity.kt</files>
  <read_first>
    - phone/src/main/res/layout/activity_main.xml (section-card wrapper ~lines 122-134, routeCard state-swapped card ~200-263, LIVE NAVIGATION comment ~267 — the recording card slots between these; navInstructionText big-number styling ~294-302)
    - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt IN FULL (companion prefs ~38-55, bindViews ~212, navCallback runOnUiThread pattern ~156-195, confirm-dialog pattern ~369-378, service-call guard ~623-632, updateStreamingUi ~485-497, formatDist/isImperial ~892-906, onResume re-attach ~452)
    - phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt (binder surface from plan 01-04: startRecording/stopRecording/recordingState/currentMetrics/setMetricsListener — use exact signatures from 01-04-SUMMARY.md)
    - .planning/phases/01-activity-recording-engine/01-PATTERNS.md — MainActivity + layout sections
  </read_first>
  <action>
    Layout (activity_main.xml) — insert a RECORD section after the SEARCH & NAVIGATE section closes (~line 264) and before the LIVE NAVIGATION comment (~line 267), using the existing section-card wrapper shape (vertical LinearLayout, bg_card, 16dp padding, 12dp bottom margin, SectionTitle header "RECORD"):
    - Sport toggle row: two weighted Buttons btnSportRide ("Ride") / btnSportRun ("Run"), 40dp height; selected = backgroundTint #00E676 with #000000 text, unselected = #2A2A2A with #AAAAAA text
    - btnStartRecording: match_parent, 48dp, text "Start Recording", backgroundTint #00E676, black bold text (btnNavigate convention)
    - recordingPanel (LinearLayout, vertical, visibility="gone", bg_card_accent, 14dp padding, 10dp top margin) containing: a horizontal row with recBadge TextView (text "● REC", bg_card_red background, #FFFFFF bold 12sp, 8dp horizontal padding) and recElapsedText (22sp #FFFFFF bold, navInstructionText style); a horizontal row with recDistanceText (22sp #FFFFFF bold) and recSpeedText (18sp #FFFFFF); recPaceText secondary line (#81C784 13sp, routeInfoText style); btnStopRecording (match_parent 44dp, text "Stop", backgroundTint #D32F2F, #FFFFFF bold text)
    All IDs camelCase per convention. Reuse existing drawables only (bg_card, bg_card_accent, bg_card_red) — create no new drawables.

    MainActivity:
    1. Companion: PREF_SPORT_TYPE = "sport_type" (lives in PREFS_HUD app-wide prefs — service-readable convention), plus RC_BG_LOCATION request code (next free RC_ number) and PREF_BG_LOC_ASKED = "bg_loc_asked" for Task 2.
    2. bindViews additions for all new views (findViewById, no ViewBinding).
    3. Sport toggle: click handlers persist getSharedPreferences(PREFS_HUD, MODE_PRIVATE) string "ride"/"run" and swap button tints; initialize from prefs (default "ride").
    4. Start flow: btnStartRecording click → guard bound/service (Toast "Start streaming first" — startNavigation guard pattern); read sport from prefs; call service!!.startRecording(sport); if true → updateRecordingUi(true) and run showFirstRecordingPrompts() (Task 2 — prompts appear over the already-running recording; recording start is NEVER gated on prompt outcomes).
    5. Stop flow: btnStopRecording click → AlertDialog.Builder(this).setTitle("Finish recording?").setMessage("Your activity will be saved.").setPositiveButton("Finish") { service?.stopRecording(); updateRecordingUi(false); Toast "Activity saved" }.setNegativeButton("Keep recording", null).show(). NO discard/delete option (locked).
    6. updateRecordingUi(recording: Boolean): toggles btnStartRecording + sport toggle row visibility vs recordingPanel visibility.
    7. Metrics listener: private val recMetricsListener = object : MetricsListener { override fun onMetrics(snapshot: MetricsSnapshot) { runOnUiThread { recElapsedText.text = formatElapsed(snapshot.elapsedMs); recDistanceText.text = formatDist(snapshot.distanceM); recSpeedText.text = formatSpeed(snapshot.currentSpeedMps); recPaceText.text = formatPace(snapshot.avgPaceMsPerKm); if (snapshot.sessionState == "finished") updateRecordingUi(false) } } } — navCallback runOnUiThread convention. Attach in onServiceConnected AND onResume (service?.setMetricsListener(recMetricsListener), mirroring the uiCallback re-attach); on both, also re-sync visible state from service.recordingState() == TRACKING so activity recreation mid-recording restores the panel.
    8. Formatters next to formatDist (reuse formatDist for distance; same isImperial() source): formatElapsed(ms) → "h:mm:ss" via String.format; formatSpeed(mps) → imperial ? String.format("%.1f mph", mps * 2.23694) : String.format("%.1f km/h", mps * 3.6); formatPace(msPerKm) → 0L yields "–:–– /km" (or /mi); else convert (imperial: msPerUnit = msPerKm * 1.609344), then "%d:%02d /km|/mi" from minutes/seconds of msPerUnit.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:compileDebugKotlin :phone:assembleDebug -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'android:id="@+id/btnStartRecording"' phone/src/main/res/layout/activity_main.xml succeeds; same for btnStopRecording, recElapsedText, recDistanceText, recSpeedText, recPaceText, recBadge, btnSportRide, btnSportRun, recordingPanel
    - grep -q '"Finish recording?"' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt succeeds
    - grep -qi "discard" phone/src/main/java/com/rokid/hud/phone/MainActivity.kt fails (exit 1) — no discard path
    - grep -q "setMetricsListener" phone/src/main/java/com/rokid/hud/phone/MainActivity.kt succeeds (attached in onServiceConnected and onResume — 2+ occurrences)
    - grep -q "fun formatPace" phone/src/main/java/com/rokid/hud/phone/MainActivity.kt succeeds
    - :phone:assembleDebug exits 0
  </acceptance_criteria>
  <done>User records an activity end-to-end from the phone with live 1Hz metrics in their chosen units; UI survives activity recreation.</done>
</task>

<task type="auto">
  <name>Task 2: First-recording onboarding — battery exemption + background location</name>
  <files>phone/src/main/java/com/rokid/hud/phone/MainActivity.kt</files>
  <read_first>
    - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt (promptBatteryOptimizationIfNeeded ~lines 803-821 — the EXACT dialog+intent template to reuse; checkPermissionsAndStart + onRequestPermissionsResult ~754-790 — the request-code switch to extend)
    - .planning/phases/01-activity-recording-engine/01-RESEARCH.md — Pitfall 1 (background-location request mechanics: separate sequential request, settings-routed on API 30+), Pitfall 3 (exemption is load-bearing in Doze), Open Question 1 (declare + request + validate decision)
  </read_first>
  <action>
    Implement showFirstRecordingPrompts(), called by Task 1's start flow AFTER recording starts (prompts never block or gate the start — declining only degrades recovery/Doze survival, logged accordingly):

    1. Battery exemption (locked: fires on first recording start): promptRecordingBatteryExemption() — clone the promptBatteryOptimizationIfNeeded structure (PowerManager.isIgnoringBatteryOptimizations gate, ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS intent with package: URI in try/catch). Recording-specific title "Keep recording when screen is off" and message explaining the OS may kill GPS recording in the pocket without the exemption (mention this phone's aggressive battery management). Track a process-scoped boolean so the prompt shows at most once per app launch, and skip entirely when already exempt. Positive "Allow", negative "Not now" (proceeds, Log.w "recording without battery exemption").
    2. Background location (research decision: declare + request during first-recording onboarding): after the battery dialog is dismissed (setOnDismissListener chain, or immediately when that dialog was skipped), if Build.VERSION.SDK_INT >= Q AND ACCESS_BACKGROUND_LOCATION not granted AND PREFS_HUD boolean PREF_BG_LOC_ASKED is false: show an explainer AlertDialog — title "Allow all-the-time location", message: background location lets the system restart a recording after the app is killed; without it a killed recording can only resume when the app is reopened. Positive "Continue" → ActivityCompat.requestPermissions(this, arrayOf(Manifest.permission.ACCESS_BACKGROUND_LOCATION), RC_BG_LOCATION) (on API 30+ the system routes to the settings screen — expected, per Pitfall 1; ACCESS_FINE_LOCATION is already granted before any recording since streaming requires it). Negative "Skip". Either way set PREF_BG_LOC_ASKED = true (ask-once).
    3. onRequestPermissionsResult: add an RC_BG_LOCATION branch — Log.i grant result; on denial Toast "Recording works, but won't auto-recover if the system kills the app". NEVER stop or block the running recording on denial (research Q1: only crash-recovery degrades).
    4. Keep both flows out of onCreate/app-launch paths — they must trigger only from the recording start flow (locked decision).
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:compileDebugKotlin :shared:testDebugUnitTest :phone:testDebugUnitTest -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "ACCESS_BACKGROUND_LOCATION" phone/src/main/java/com/rokid/hud/phone/MainActivity.kt succeeds
    - grep -q "isIgnoringBatteryOptimizations" phone/src/main/java/com/rokid/hud/phone/MainActivity.kt returns ≥2 matches (existing nav prompt + new recording prompt)
    - grep -q "RC_BG_LOCATION" phone/src/main/java/com/rokid/hud/phone/MainActivity.kt succeeds in both the request and onRequestPermissionsResult
    - showFirstRecordingPrompts is invoked ONLY from the recording start flow (grep shows a single call site, inside the start handler)
    - Verify command exits 0
  </acceptance_criteria>
  <done>First recording start walks the user through exemption + background location; declining anything still records (REC-05 consent layers in place).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User consent UI → OS permission state | Runtime permission + exemption requests elevate the app's background capabilities |
| Screen → shoulder surfer | Live metrics card shows activity data on the unlocked phone |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Elevation of Privilege | ACCESS_BACKGROUND_LOCATION request | mitigate | Requested only with in-context explainer during first-recording onboarding (not app launch); ask-once flag; app remains fully functional on denial — minimal-privilege posture (ASVS V4); declaration itself ships in plan 01-06 |
| T-05-02 | Information Disclosure | Live metrics card | accept | Metrics (speed/distance/elapsed) on the user's own unlocked screen; no coordinates displayed; equivalent exposure to any fitness app |
| T-05-03 | Repudiation / user intent | Accidental stop or accidental recording | mitigate | Stop requires explicit confirm dialog (locked); start requires explicit tap (REC-01 opt-in — navigation never auto-starts recording) |
</threat_model>

<verification>
- :phone:assembleDebug builds; full unit suite green
- git diff shows no changes to navigation, search, or settings wiring in MainActivity beyond additions
- Manual smoke deferred to plan 01-07: card starts/stops a real recording on the OPPO
</verification>

<success_criteria>
- REC-01 user surface: opt-in start, free-ride capable, sport type captured
- REC-02 user surface: 1Hz elapsed/distance/speed/pace in selected units from the single metrics callback
- REC-05 consent layers: exemption prompt on first recording start; background-location onboarding with ask-once semantics
</success_criteria>

<output>
Create `.planning/phases/01-activity-recording-engine/01-05-SUMMARY.md` when done. Record: final view IDs, prompt sequencing behavior, and any deviation from the layout sketch.
</output>
