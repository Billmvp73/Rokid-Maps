---
phase: 01-activity-recording-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shared/build.gradle.kts
  - phone/build.gradle.kts
  - shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt
  - phone/src/main/java/com/rokid/hud/phone/SessionModels.kt
  - shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolConstants.kt
  - shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt
  - shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt
autonomous: true
requirements: [REC-07]
must_haves:
  truths:
    - "Plain-JVM JUnit tests run green in both shared and phone modules (repo's first test infrastructure)"
    - "encodeSportState produces JSON with t=sport_state and v=1; decode round-trips to an equal SportStateMessage"
    - "Malformed or truncated sport_state lines decode to ParsedMessage.Unknown, never throw"
    - "Recording data contracts (SessionState, TrackPoint, MetricsSnapshot, SessionData, MetricsListener) exist so Wave-2 plans compile independently"
  artifacts:
    - path: "shared/build.gradle.kts"
      provides: "testImplementation junit:junit:4.13.2 + org.json:json:20231013"
      contains: "testImplementation"
    - path: "phone/build.gradle.kts"
      provides: "test deps + unitTests.isReturnDefaultValues = true"
      contains: "isReturnDefaultValues"
    - path: "shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt"
      provides: "smoke + sport_state round-trip tests"
      min_lines: 60
    - path: "phone/src/main/java/com/rokid/hud/phone/SessionModels.kt"
      provides: "SessionState, TrackPoint, MetricsSnapshot, MetricsListener, SessionData"
      contains: "data class TrackPoint"
    - path: "shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt"
      provides: "encodeSportState + SPORT_STATE decode case + ParsedMessage.SportState"
      contains: "encodeSportState"
  key_links:
    - from: "shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt"
      to: "ProtocolConstants.MessageType.SPORT_STATE"
      via: "decode when() branch before else"
      pattern: "MessageType\\.SPORT_STATE"
    - from: "shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt"
      to: "ProtocolCodec.encodeSportState"
      via: "round-trip assertion"
      pattern: "encodeSportState"
---

## Phase Goal

**As a** cyclist or runner, **I want to** start an activity recording on my phone and have it reliably capture my GPS track and live metrics (elapsed time, moving time, distance, speed/pace) even with the screen off, **so that** my ride or run is accurately measured, saved locally, and streamed to my glasses HUD.

(Direct transliteration of the ROADMAP Phase 1 Goal + project Core Value; MVP mode, vertical-slice plan set.)

<objective>
Install the repo's first unit-test infrastructure (Wave 0 gap closure), define the phone-side recording data contracts, and ship the `sport_state` protocol message (type + codec + tests) in the shared module.

Purpose: Every later plan's tests depend on this infrastructure; Wave-2 plans (ActivitySessionManager, SessionStore) compile against contracts defined here without cross-plan file dependencies; REC-07's message type is the protocol half of Success Criterion 1 and all of Criterion 5.
Output: Test-enabled build files, `SessionModels.kt`, sport_state codec across `ProtocolConstants.kt`/`Messages.kt`/`ProtocolCodec.kt`, and a green `ProtocolCodecTest.kt`.

MVP slice: after this plan, the sport_state message that the Phase-2 glasses HUD will consume exists and is proven by round-trip tests; the repo can run tests for the first time.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-activity-recording-engine/01-CONTEXT.md
@.planning/phases/01-activity-recording-engine/01-PATTERNS.md
@shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt
@shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolConstants.kt
@shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt

Environment (verified 2026-07-03): there is NO `gradlew` script in this repo. All Gradle invocations MUST use:
`export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain <targets>`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test infrastructure in both modules and prove it with codec smoke tests</name>
  <files>shared/build.gradle.kts, phone/build.gradle.kts, shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt</files>
  <read_first>
    - shared/build.gradle.kts (dependencies block to append to)
    - phone/build.gradle.kts (dependencies block + android block; testOptions goes inside android { } after the packaging block)
    - shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt (existing encodeState + decode STATE case — the smoke-test targets)
    - shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt (StateMessage fields)
    - .planning/phases/01-activity-recording-engine/01-RESEARCH.md — section "Gradle: first test infrastructure (Wave 0)" and Pitfall 7 (org.json stub shadowing)
  </read_first>
  <action>
    Per CONTEXT.md locked decision (repo's first unit tests) and RESEARCH Wave 0 Gaps:

    1. In shared/build.gradle.kts dependencies block, append exactly: testImplementation("junit:junit:4.13.2") and testImplementation("org.json:json:20231013"). Keep the existing implementation("org.json:json:20231013") line untouched — the testImplementation duplicate is intentional (real org.json must shadow the mockable android.jar stubs on the unit-test classpath, RESEARCH Pitfall 7).
    2. In phone/build.gradle.kts dependencies block, append the same two testImplementation lines. Inside the android { } block (after the packaging block), add testOptions { unitTests.isReturnDefaultValues = true } — required so android.util.Log calls in phone classes return defaults instead of throwing in JVM tests. Do NOT add this to shared (ProtocolCodec imports only org.json; RESEARCH Pitfall 7 cautions against enabling it where not needed).
    3. Create shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt (new directory tree — repo has zero test dirs today). JUnit4 class with two smoke tests against the EXISTING codec: (a) stateMessageRoundTrip — build a StateMessage with distinct non-default values for every constructor field, encode via ProtocolCodec.encodeState, decode via ProtocolCodec.decode, assert the result is ParsedMessage.State and each field equals the input (assertEquals with delta for Double/Float); (b) malformedLineDecodesToUnknown — ProtocolCodec.decode("not json at all") returns ParsedMessage.Unknown. These prove the JUnit runner and real-org.json classpath work before any new production code exists.
    4. Package legitimacy: both artifacts are Approved in RESEARCH.md Package Legitimacy Audit (registry-verified against repo1.maven.org; junit 4.13.2 cited verbatim in official Android docs). No [ASSUMED]/[SUS] packages — no human install gate required.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :shared:testDebugUnitTest :phone:testDebugUnitTest -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "testImplementation" shared/build.gradle.kts returns 2; grep -c "testImplementation" phone/build.gradle.kts returns 2
    - grep -q "isReturnDefaultValues = true" phone/build.gradle.kts succeeds; grep -q "isReturnDefaultValues" shared/build.gradle.kts fails (exit 1)
    - The verify command exits 0 (phone module reports NO-SOURCE for tests — acceptable; shared executes ProtocolCodecTest)
    - shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt contains @Test annotations for stateMessageRoundTrip and malformedLineDecodesToUnknown
  </acceptance_criteria>
  <done>Both modules resolve JUnit4 + real org.json on the test classpath; the repo's first two tests pass on plain JVM.</done>
</task>

<task type="auto">
  <name>Task 2: Define recording data contracts in SessionModels.kt (interface-first for Wave 2)</name>
  <files>phone/src/main/java/com/rokid/hud/phone/SessionModels.kt</files>
  <read_first>
    - .planning/phases/01-activity-recording-engine/01-CONTEXT.md — "Session storage & identity" locked schema and sport_state field list
    - .planning/phases/01-activity-recording-engine/01-RESEARCH.md — Pattern 3 (MetricsSnapshot shape, MetricsListener)
    - phone/src/main/java/com/rokid/hud/phone/SavedPlacesManager.kt (SavedPlace — the repo's plain-data-class convention)
    - shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt (data-class style: one property per field, defaults for optionals)
  </read_first>
  <action>
    Create phone/src/main/java/com/rokid/hud/phone/SessionModels.kt — a PURE-KOTLIN file (no android.* imports, no Log) so Wave-2 JVM tests use these types directly. Package com.rokid.hud.phone. Contents:

    1. enum class SessionState { IDLE, TRACKING, FINISHED } — the locked v1 state machine (PAUSED is deferred to v2 per CONTEXT.md; do NOT add it).
    2. data class TrackPoint(lat: Double, lng: Double, alt: Double, ts: Long, speedMps: Double, accuracyM: Double, bearingDeg: Double) — all val. Sentinel conventions documented in KDoc: alt/speedMps/bearingDeg use Double.NaN when the fix lacks them; accuracyM uses -1.0 when unknown; ts is epoch ms from location.time (wall clock, GPX-ready). bearingDeg is additive beyond the CONTEXT schema list because REC-01 explicitly requires bearing per point — note this in a comment.
    3. data class MetricsSnapshot(elapsedMs: Long, movingMs: Long, distanceM: Double, currentSpeedMps: Double, avgPaceMsPerKm: Long, sessionState: String, sport: String, trackPointCount: Int) — immutable snapshot handed across threads (HudState copy() philosophy). sessionState is "idle"|"tracking"|"finished"; sport is "ride"|"run".
    4. interface MetricsListener { fun onMetrics(snapshot: MetricsSnapshot) } — callback interface following NavigationCallback naming convention.
    5. data class SessionData(id: String, sport: String, startTime: String, endTime: String?, elapsedMs: Long, movingMs: Long, distanceM: Double, avgSpeedMps: Double, stravaUploaded: Boolean = false, trackPoints: List&lt;TrackPoint&gt;) — the locked session schema (CONTEXT.md). startTime/endTime are ISO-8601 UTC strings; endTime null while in progress (checkpoints). schemaVersion is intentionally NOT a field here — it is a JSON-layer constant written by SessionStore (plan 01-03).

    4-space indent, no trailing commas, KDoc on each type stating its owner (ASM produces, SessionStore persists, service/UI consume).
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:compileDebugKotlin -q</automated>
  </verify>
  <acceptance_criteria>
    - phone/src/main/java/com/rokid/hud/phone/SessionModels.kt contains: "enum class SessionState", "data class TrackPoint", "data class MetricsSnapshot", "interface MetricsListener", "data class SessionData"
    - grep -q "import android" phone/src/main/java/com/rokid/hud/phone/SessionModels.kt fails (exit 1) — file is JVM-pure
    - grep -q "PAUSED" phone/src/main/java/com/rokid/hud/phone/SessionModels.kt fails (exit 1) — deferred state not present
    - :phone:compileDebugKotlin exits 0
  </acceptance_criteria>
  <done>Wave-2 plans (ASM, SessionStore) can compile in parallel against these shared contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add sport_state message type, codec, and round-trip tests (REC-07 protocol half)</name>
  <files>shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolConstants.kt, shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt, shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt, shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt</files>
  <read_first>
    - shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolConstants.kt (field constants + MessageType object; STEPS_LIST is the last type)
    - shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt (StateMessage/SettingsMessage data-class shape)
    - shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt (ParsedMessage sealed class, encodeState apply{} style, SETTINGS decode case with optXxx defaults, catch-all returning Unknown)
    - .planning/phases/01-activity-recording-engine/01-PATTERNS.md — sections for all three protocol files (verified anchors + collision check)
  </read_first>
  <behavior>
    - Test sportStateRoundTrip: SportStateMessage(elapsedMs=945000, movingMs=823000, distanceM=8420.0, currentSpeedMps=6.2, avgPaceMsPerKm=294000, sessionState="tracking", sport="ride") encodes then decodes to ParsedMessage.SportState with an equal message (data-class equality)
    - Test versionFieldIsOne: JSONObject(encodeSportState(msg)).getInt("v") == 1 and getString("t") == "sport_state"
    - Test exactJsonKeys: encoded JSON contains exactly the keys t, v, et, mt, d, cs, ap, st, sp (assert each present via has(); assert length() == 9)
    - Test missingFieldsDecodeWithDefaults: decoding the minimal line {"t":"sport_state"} yields ParsedMessage.SportState with elapsedMs=0, movingMs=0, distanceM=0.0, currentSpeedMps=0.0, avgPaceMsPerKm=0, sessionState="idle", sport="ride"
    - Test malformedSportStateDecodesToUnknown: decode on a truncated line starting with {"t":"sport_state" but invalid JSON returns ParsedMessage.Unknown
    - Existing Task-1 smoke tests remain green (no regression to existing message types)
  </behavior>
  <action>
    Write the failing tests above first (extend ProtocolCodecTest.kt), then implement:

    1. ProtocolConstants.kt — append after FIELD_SHOW_SPEED_LIMIT: FIELD_VERSION = "v", FIELD_ELAPSED = "et", FIELD_MOVING_TIME = "mt", FIELD_SPORT_DISTANCE = "d", FIELD_CURRENT_SPEED = "cs", FIELD_AVG_PACE = "ap", FIELD_SESSION_STATE = "st", FIELD_SPORT = "sp". Inside MessageType after STEPS_LIST: SPORT_STATE = "sport_state". (PATTERNS.md verified none of these key strings collide with existing constants.)
    2. Messages.kt — append data class SportStateMessage(elapsedMs: Long, movingMs: Long, distanceM: Double, currentSpeedMps: Double, avgPaceMsPerKm: Long, sessionState: String, sport: String) following the StateMessage shape (plain data class, Message suffix, no methods).
    3. ProtocolCodec.kt — three additions in existing style: (a) ParsedMessage sealed class gains data class SportState(val msg: SportStateMessage) : ParsedMessage() placed before Unknown; (b) fun encodeSportState(msg: SportStateMessage): String using JSONObject().apply{} with FIELD_TYPE put first, then put(FIELD_VERSION, 1) hardcoded — the codec is a dumb serializer, protocol version is a codec constant, and monotonic clamping lives in ActivitySessionManager NOT here (PATTERNS Integration Warning 5); then et/mt/d/cs/ap/st/sp from the message; (c) decode when() gains a MessageType.SPORT_STATE branch before the else, constructing SportStateMessage via json.optLong(FIELD_ELAPSED, 0L), optLong(FIELD_MOVING_TIME, 0L), optDouble(FIELD_SPORT_DISTANCE, 0.0), optDouble(FIELD_CURRENT_SPEED, 0.0), optLong(FIELD_AVG_PACE, 0L), optString(FIELD_SESSION_STATE, "idle"), optString(FIELD_SPORT, "ride"). The existing try/catch wrapper already guarantees malformed input returns ParsedMessage.Unknown — do not add a second try/catch.

    Note: the glasses APK compiles the same shared module and will decode sport_state into ParsedMessage.SportState but ignore it until Phase 2 (unknown/unhandled variants are dropped — PATTERNS Integration Warning 6). No glasses-module changes in this phase.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :shared:testDebugUnitTest -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'const val SPORT_STATE = "sport_state"' shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolConstants.kt succeeds
    - grep -q "data class SportStateMessage" shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt succeeds
    - grep -q "fun encodeSportState" shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt succeeds
    - grep -q "data class SportState" shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt succeeds
    - :shared:testDebugUnitTest exits 0 with all ProtocolCodecTest tests passing (≥7 tests total)
    - grep -qi "clamp\|max(" shared/src/main/java/com/rokid/hud/shared/protocol/ProtocolCodec.kt fails (exit 1) — no clamping logic in the codec
  </acceptance_criteria>
  <done>sport_state encodes/decodes round-trip with v:1, defensive defaults, and Unknown on malformed input; SC#5 satisfied at the protocol layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Paired BT device → phone SPP | Untrusted JSON lines cross into ProtocolCodec.decode (glasses-side decode of sport_state ships via shared module) |
| Maven Central / Google Maven → build | Third-party artifacts enter the test classpath |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering/DoS | ProtocolCodec.decode (sport_state branch) | mitigate | Defensive optXxx decode with safe defaults inside existing try/catch → ParsedMessage.Unknown; existing 1024-byte line cap upstream; malformed-input test enforced in Task 3 |
| T-01-02 | Information Disclosure | sport_state over unencrypted BT SPP | accept | Existing accepted risk for the whole SPP protocol (paired-device link, no PII beyond activity metrics — no coordinates in sport_state); consistent with StateMessage which already streams coordinates; documenting disposition per phase security requirement |
| T-01-SC | Tampering (supply chain) | junit:junit:4.13.2 + org.json:json:20231013 (test-only) | mitigate | Both Approved in RESEARCH Package Legitimacy Audit — exact-version registry verification against repo1.maven.org; junit coordinates appear verbatim in official Android docs; pinned versions, no version ranges; no [ASSUMED]/[SUS] → no blocking human checkpoint required |
</threat_model>

<verification>
- Full suite green: :shared:testDebugUnitTest and :phone:testDebugUnitTest exit 0
- assembleDebug still succeeds for both APKs (shared-module change compiles into glasses too): run GradleWrapperMain assembleDebug -q
- No behavioral change to existing message types (smoke tests cover STATE round-trip)
</verification>

<success_criteria>
- Repo's first unit tests exist and pass on plain JVM (Success Criterion 7, codec portion)
- sport_state message type defined in shared module with v:1 (Success Criteria 1-protocol-half and 5)
- SessionModels.kt contracts unblock Wave-2 parallel plans
</success_criteria>

<output>
Create `.planning/phases/01-activity-recording-engine/01-01-SUMMARY.md` when done. Record: exact test count, any Gradle sync surprises, and confirmation that glasses assembleDebug still passes.
</output>
