---
phase: 03-strava-authentication
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - CLAUDE.md
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: strava
    why: "E2E verification requires real OAuth credentials — the only hard-blocking human prerequisite of the phase"
    env_vars:
      - name: "strava.client.id (local.properties)"
        source: "strava.com/settings/api — 'Client ID'"
      - name: "strava.client.secret (local.properties)"
        source: "strava.com/settings/api — 'Client Secret'"
    dashboard_config:
      - task: "Strava API application with 'Authorization Callback Domain' set to exactly: rokidhud (bare word — no scheme, no slashes)"
        location: "strava.com/settings/api"
must_haves:
  truths:
    - "adb intent-filter proof: rokidhud://callback resolves to StravaCallbackActivity on the test phone"
    - "A forged callback (state=WRONG) is REJECTED on device: logcat shows the state-mismatch rejection and NO token-exchange POST is attempted (Pitfall 5 negative test)"
    - "The user completes real OAuth: tap Connect Strava → browser/Strava-app consent → Authorize → back in app → toast + card 'Connected as {athlete}' (ROADMAP SC#1 + SC#2)"
    - "Captured logcat contains NO access_token/refresh_token/client_secret/authorization-code values (redaction scan, T-03-04)"
    - "GET /athlete returns 200 with rate-limit headers logged (X-RateLimit-Usage / X-ReadRateLimit-Usage) — the authenticated client is proven (ROADMAP Delivers)"
    - "After force-stop + relaunch the card still shows 'Connected as {athlete}' with no re-login (ROADMAP SC#3, AUTH-02)"
    - "Debug long-press forces a refresh: logcat shows a ROTATED rt# (different from the exchange's rt#) and a subsequent successful GET /athlete (ROADMAP SC#4, AUTH-03)"
    - "Phase-2 fold-ins pass on hardware: Mini-toggle spot, WR-01 SPORT-survives-settings-resend, imperial + Run variants (02-VERIFICATION human items 1-3)"
    - "CLAUDE.md's stale rate-limit figure is corrected to the live-verified values"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Corrected Strava rate-limit constraint (200/15min + 2,000/day overall; 100 reads/15min + 1,000 reads/day)"
      contains: "200"
  key_links:
    - from: "adb shell am start VIEW rokidhud://callback?state=WRONG"
      to: "StravaAuthManager.handleCallback rejection"
      via: "logcat -s StravaAuth state-mismatch line"
      pattern: "state mismatch"
    - from: "user's Authorize tap"
      to: "StravaTokenStore save + card update"
      via: "logcat exchange-ok line + on-screen card state"
      pattern: "exchange ok"
---

## Phase Goal

**As a** cyclist or runner, **I want to** connect my Strava account once from the phone app and stay securely authenticated across restarts and token expiries, **so that** route import (Phase 4) and activity upload (Phase 5) can call the Strava API on my behalf without me ever re-logging in.

<objective>
Prove the phase on real hardware. Scripted adb verification brackets the phase's ONE human moment (the user's Authorize tap on Strava's consent screen): intent-filter resolution, forged-callback rejection, live OAuth + token exchange, logcat redaction scan, restart persistence, and forced-refresh rotation. The session also closes the three Phase-2 device spots that 02-VERIFICATION folded into this phase, and corrects CLAUDE.md's stale rate-limit figure once the live headers confirm the current values.

Purpose: all four ROADMAP Phase-3 success criteria observed on the OPPO test phone; Phase-2 verification debt cleared in the same device session.
Output: evidence-backed 03-04-SUMMARY.md, corrected CLAUDE.md, phase ready for /gsd-verify-work.

HALT RULE (orchestrator directive): Task 1 begins with a credentials gate. If local.properties still lacks the strava keys, STOP the plan immediately and return a blocked report — do not attempt any device step without them.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-strava-authentication/03-VALIDATION.md
@.planning/phases/03-strava-authentication/03-03-SUMMARY.md
@.planning/phases/02-glasses-sport-hud/02-VERIFICATION.md

Devices (per 03-VALIDATION + Phase 1/2 precedent): phone = OPPO, adb serial 3B164G01Y7L00000; glasses = the second device in `adb devices` output (needed only for Task 3). adb is on PATH.

Environment: NO `gradlew` script. Build command:
`export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug -q`

Capture all logcat output and screencaps into the session scratchpad directory; cite file names as evidence in the SUMMARY.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Credentials HALT gate, deploy, intent-filter proof, forged-callback rejection (no human needed)</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/03-strava-authentication/03-VALIDATION.md — "Device-only" list and "Sampling Rate" adb command shapes
    - .planning/phases/03-strava-authentication/03-03-SUMMARY.md — logcat marker strings recorded by Wave 3
  </read_first>
  <action>
    1. CREDENTIALS GATE (HALT-and-report): run `grep -c "strava.client.id" local.properties` and `grep -c "strava.client.secret" local.properties` (do NOT print the values). If either is 0 or the file is absent: STOP THIS PLAN. Return a blocked report to the orchestrator stating exactly what the user must do: (a) create a Strava API application at strava.com/settings/api under their account; (b) set "Authorization Callback Domain" to exactly `rokidhud` — bare word, no scheme, no slashes, no ://callback (the #1 Strava OAuth failure mode, PITFALLS Pitfall 1); (c) add `strava.client.id=<Client ID>` and `strava.client.secret=<Client Secret>` lines to local.properties; (d) re-run this plan. Do not execute any later step.
    2. Confirm the phone is attached: `adb devices` shows 3B164G01Y7L00000. Build and install: assembleDebug (command in context), then `adb -s 3B164G01Y7L00000 install -r phone/build/outputs/apk/debug/phone-debug.apk`.
    3. Intent-filter resolution proof (03-VALIDATION pre-flight): `adb -s 3B164G01Y7L00000 shell pm resolve-activity -a android.intent.action.VIEW -d "rokidhud://callback"` — output must name com.rokid.hud.phone/.strava.StravaCallbackActivity. Save output to scratchpad.
    4. Forged-callback negative test (Pitfall 5 / T-03-01): start capture `adb -s 3B164G01Y7L00000 logcat -c` then `adb -s 3B164G01Y7L00000 logcat -s StravaAuth:V StravaApi:V StravaTokenStore:V StravaCallback:V MainActivity:V` to a scratchpad file (background it); fire `adb -s 3B164G01Y7L00000 shell am start -a android.intent.action.VIEW -d "rokidhud://callback?state=WRONG\&code=fake"`. Expected within ~5s of log: the callback-received line, then the state-mismatch rejection line (marker per 03-03-SUMMARY, e.g. "state mismatch — rejecting callback"). MUST NOT appear: any exchange attempt ("exchange" marker), any token-stored line, any crash/ANR. The app may cold-start to MainActivity showing the disconnected card — that is correct behavior.
    5. Static backup-exclusion re-check on the installed build: `grep -c "strava_auth.xml" phone/src/main/res/xml/backup_rules.xml` returns >= 1 and `grep -c "strava_auth.xml" phone/src/main/res/xml/data_extraction_rules.xml` returns >= 2 (belt-and-braces before the E2E writes real tokens).
  </action>
  <verify>
    <automated>adb -s 3B164G01Y7L00000 shell pm resolve-activity -a android.intent.action.VIEW -d "rokidhud://callback" | grep -q StravaCallbackActivity && grep -q "state mismatch" "/private/tmp/claude-501/-Users-bilhuang-Documents-rokid-maps/e88c0254-bb6e-4022-b9e8-a1496c44aabe/scratchpad"/forged_callback.log && ! grep -qi "exchange ok" "/private/tmp/claude-501/-Users-bilhuang-Documents-rokid-maps/e88c0254-bb6e-4022-b9e8-a1496c44aabe/scratchpad"/forged_callback.log</automated>
  </verify>
  <acceptance_criteria>
    - Credentials gate passed (both local.properties keys present) OR the plan halted with the exact user instructions — never a silent continue
    - resolve-activity names StravaCallbackActivity (deep link registered on device, not just in source)
    - Forged callback: rejection logged, zero exchange/token-store activity, no crash — evidence file saved
    - Backup rule greps pass (1 + 2 occurrences)
  </acceptance_criteria>
  <done>The device provably routes rokidhud://callback into the app and provably rejects CSRF-shaped input before any network call — the security floor is confirmed before real credentials flow.</done>
</task>

<task type="auto">
  <name>Task 2: Live OAuth E2E — the ONE human moment (Authorize tap) — plus redaction scan, restart persistence, forced-refresh rotation</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/03-strava-authentication/03-VALIDATION.md — "The one human moment" numbered protocol (steps 1-4)
  </read_first>
  <action>
    1. Start a fresh filtered logcat capture to scratchpad: `adb -s 3B164G01Y7L00000 logcat -c` then background `adb -s 3B164G01Y7L00000 logcat -s StravaAuth:V StravaApi:V StravaTokenStore:V StravaCallback:V MainActivity:V` → oauth_e2e.log.
    2. HUMAN MOMENT — prompt the user (via orchestrator output) with these exact steps, then wait for their confirmation: "On the OPPO phone: (1) open Rokid HUD Maps; (2) in the STRAVA card tap CONNECT STRAVA; (3) a browser tab or the Strava app opens — log in to Strava if asked; (4) on the permission screen LEAVE ALL CHECKBOXES CHECKED (unchecking any breaks route import/upload later — the app will reject a partial grant); (5) tap Authorize; (6) you should land back in the app and see the toast 'Connected to Strava as {your name}'. Tell me what you saw." Either consent surface (browser Custom Tab OR native Strava app) is expected and valid — both redirect to rokidhud://callback (RESEARCH A7).
    3. Verify the captured sequence in oauth_e2e.log (markers per 03-03-SUMMARY): state generated → callback received → exchange ok + expires_at → tokens stored → GET /athlete success → "Rate usage overall=... read=..." line present (both header pairs — Integration Gotchas row 9). Record the exchange's rt# value.
    4. REDACTION SCAN (T-03-04): against the FULL capture run: (a) `grep -c "$(grep strava.client.secret local.properties | cut -d= -f2)" oauth_e2e.log` returns 0 (the literal secret never logged — run this without echoing the secret itself); (b) `grep -ciE "access_token|refresh_token=|Bearer [A-Za-z0-9]" oauth_e2e.log` returns 0 (no token material; the rt# hashCode hex is allowed); (c) no full callback URI line (grep -c "rokidhud://callback?" returns 0).
    5. Persistence (AUTH-02 / SC#3): `adb -s 3B164G01Y7L00000 shell am force-stop com.rokid.hud.phone`, relaunch via `adb -s 3B164G01Y7L00000 shell monkey -p com.rokid.hud.phone -c android.intent.category.LAUNCHER 1`, screencap the STRAVA card (`adb -s 3B164G01Y7L00000 exec-out screencap -p` → strava_persist.png) — card must read "Connected as {athlete}" with no re-login. Also inventory the prefs files: `adb -s 3B164G01Y7L00000 shell run-as com.rokid.hud.phone ls shared_prefs` → strava_auth.xml present (contents are ciphertext — state verified via UI; also checks RESEARCH assumption A3 on Tink keyset file locations — record the full file list).
    6. Forced-refresh rotation (AUTH-03 / SC#4): prompt the user to LONG-PRESS the STRAVA card (debug build) — or drive it via `adb shell input swipe <x> <y> <x> <y> 1200` on the card coordinates from the screencap if the user prefers. Verify logcat: refresh ok line with a rt# DIFFERENT from step 3's rt# (rotation persisted), followed by a successful GET /athlete and a fresh rate-usage line. Screencap the result toast if visible.
  </action>
  <verify>
    <automated>grep -q "exchange ok" "/private/tmp/claude-501/-Users-bilhuang-Documents-rokid-maps/e88c0254-bb6e-4022-b9e8-a1496c44aabe/scratchpad"/oauth_e2e.log && grep -q "Rate usage" "/private/tmp/claude-501/-Users-bilhuang-Documents-rokid-maps/e88c0254-bb6e-4022-b9e8-a1496c44aabe/scratchpad"/oauth_e2e.log && [ "$(grep -oE 'rt#=[0-9a-f]+' "/private/tmp/claude-501/-Users-bilhuang-Documents-rokid-maps/e88c0254-bb6e-4022-b9e8-a1496c44aabe/scratchpad"/oauth_e2e.log | sort -u | wc -l)" -ge 2 ] && ! grep -qiE "access_token|Bearer [A-Za-z0-9]" "/private/tmp/claude-501/-Users-bilhuang-Documents-rokid-maps/e88c0254-bb6e-4022-b9e8-a1496c44aabe/scratchpad"/oauth_e2e.log</automated>
  </verify>
  <acceptance_criteria>
    - User confirmed the Authorize flow and the success toast; logcat shows the full state→callback→exchange→store→GET /athlete chain (SC#1, SC#2)
    - Rate-limit line captured with BOTH header pairs populated
    - Redaction scan clean: no secret, no token material, no raw callback URI in the entire capture
    - Post-force-stop relaunch shows Connected card without re-auth (SC#3); strava_auth.xml present in shared_prefs
    - Two distinct rt# values in the log — rotation proven live; post-refresh GET /athlete succeeded (SC#4)
  </acceptance_criteria>
  <done>All four ROADMAP Phase-3 success criteria are observed on hardware with evidence files, and the phase's security posture (CSRF rejection, log redaction, encrypted persistence) is demonstrated, not assumed.</done>
</task>

<task type="auto">
  <name>Task 3: Phase-2 fold-in device spots (glasses) + CLAUDE.md rate-limit correction</name>
  <files>CLAUDE.md</files>
  <read_first>
    - .planning/phases/02-glasses-sport-hud/02-VERIFICATION.md:146-165 (the three human items: exact test steps + expected results)
    - CLAUDE.md "Constraints" section — the line "Subscriber rate limits: 300 reads/15min, 1,000 writes/15min" (stale figure to correct)
  </read_first>
  <action>
    1. Identify the glasses device: `adb devices` — the serial that is NOT 3B164G01Y7L00000. Confirm the Phase-2 fixed builds are still installed (02-VERIFICATION routing note: fixed builds deployed on both devices); reinstall glasses-debug.apk only if the glasses app version mismatches.
    2. Fold-in spot 1 — Mini-toggle (02-VERIFICATION human item 1): with a recording streaming from the phone, enable the phone's mini-map toggle; glasses screencap (`adb -s <glasses> exec-out screencap -p` → mini_toggle.png) must show [ STRIP ] (or [ SPLIT ] per radio selection); then one spaced tap `adb -s <glasses> shell input tap 240 320` → screencap shows [ FULL ]. May be interleaved with Task 2 device time (start a short recording from the RECORD card).
    3. Fold-in spot 2 — WR-01 SPORT survives settings re-send (human item 2): during an active recording, tap glasses to reach [ SPORT ]; toggle any phone setting (e.g., TTS switch) to force a Settings re-send; glasses screencap → still [ SPORT ]. Then verify the Mini toggle STILL overrides to [ STRIP ]/[ SPLIT ] (precedence preserved).
    4. Fold-in spot 3 — Imperial + Run variants (human item 3): toggle imperial units on the phone while glasses are in SPORT → screencap shows mph primary with "mph" label; stop, switch sport to Run, record briefly → screencap shows M:SS pace primary with speed secondary. (User assistance for physical movement is optional — mock/short indoor motion acceptable; the assertion is layout/units, not metric accuracy.)
    5. CLAUDE.md correction (03-RESEARCH Summary item 2, confirmed live by Task 2's rate-usage headers): in the Constraints section, replace the stale "Subscriber rate limits: 300 reads/15min, 1,000 writes/15min." fragment inside the Strava API bullet with: "Rate limits (new-app defaults, verified live 2026-07-03): 200 requests/15 min and 2,000/day overall; 100 reads/15 min and 1,000 reads/day (reads = non-upload endpoints)." Keep the rest of the bullet (OAuth grant, BuildConfig, EncryptedSharedPreferences) untouched. Use the Edit tool, never heredoc.
    6. If any fold-in spot FAILS: do not fix glasses code in this plan — record the failure with screencap evidence in the SUMMARY as a Phase-2 regression for the orchestrator to route (gap-closure or quick-fix decision belongs upstream).
  
    ALSO (doc-sync folded per checker): (a) update REQUIREMENTS.md AUTH-01's provisional scope text to the locked `read,read_all,activity:write`; (b) correct CLAUDE.md's Key Dependencies rows for OkHttp and Gson from "(transitive, not used directly ...)" to "explicitly declared; used by the Strava client (com.rokid.hud.phone.strava)". Acceptance: `grep -q 'read,read_all,activity:write' .planning/REQUIREMENTS.md` and `! grep -q 'transitive, not used directly' CLAUDE.md`.
</action>
  <verify>
    <automated>grep -q "200 requests/15 min" CLAUDE.md && ! grep -q "300 reads/15min" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - Three screencap-backed verdicts (pass/fail each) for Mini-toggle, WR-01-on-hardware, imperial+Run — 02-VERIFICATION's human items get explicit closure either way
    - CLAUDE.md carries the corrected, live-verified rate-limit figures; no other CLAUDE.md content changed
    - Any failure is documented as evidence + routing note, not silently patched
  </acceptance_criteria>
  <done>Phase-2's verification debt is closed with hardware evidence in the same device session, and the project constraints document matches live-verified Strava reality.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adb-injected intents → exported StravaCallbackActivity | Task 1 deliberately attacks the deep link with forged state (verifying, not creating, exposure) |
| Captured logcat files → scratchpad/SUMMARY | Evidence files must not become the token leak themselves |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing/Tampering | Deep-link CSRF | mitigate (verify) | Task 1 step 4 proves the forged-state rejection on hardware with zero network activity |
| T-03-02 | Information Disclosure | Token persistence | mitigate (verify) | Task 2 step 5 proves encrypted-file presence + UI-level persistence; backup exclusions re-grepped in Task 1 |
| T-03-04 | Information Disclosure | Logcat + evidence files | mitigate (verify) | Task 2 step 4 redaction scan over the full capture; evidence files contain only redacted logs, screencaps, and rt# hashes — never token material; the secret-grep runs without echoing the secret |
| T-03-03 | Information Disclosure | client_secret in APK | accept | Locked decision; this plan handles the secret only as a silent grep needle against local.properties/logcat |
</threat_model>

<verification>
- All four ROADMAP Phase-3 success criteria have on-device evidence (logcat excerpts + screencaps referenced in SUMMARY)
- Negative path (forged state) and positive path (real OAuth) both captured
- Phase-2 human items 1-3 have explicit verdicts
- CLAUDE.md rate-limit correction applied
</verification>

<success_criteria>
- Phase 3 is ready for /gsd-verify-work with nothing left that requires the user except what only the user can do (their own Strava login)
- Any discovered regression is documented and routed, not hidden
</success_criteria>

<output>
Create `.planning/phases/03-strava-authentication/03-04-SUMMARY.md` when done. Record: evidence file inventory, the two rt# values (rotation proof), rate-usage header values observed, shared_prefs file inventory (A3 check), the three fold-in verdicts, and the CLAUDE.md diff summary.
</output>
