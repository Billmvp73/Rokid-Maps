---
phase: 03-strava-authentication
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - phone/build.gradle.kts
  - phone/src/main/res/xml/backup_rules.xml
  - phone/src/main/res/xml/data_extraction_rules.xml
  - phone/src/main/AndroidManifest.xml
  - phone/src/main/java/com/rokid/hud/phone/strava/StravaTokenStore.kt
  - phone/src/main/java/com/rokid/hud/phone/strava/StravaAuthManager.kt
  - phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt
  - phone/src/main/java/com/rokid/hud/phone/strava/StravaCallbackActivity.kt
  - phone/src/test/java/com/rokid/hud/phone/strava/StravaTokenStoreTest.kt
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
user_setup:
  - service: strava
    why: "OAuth 2.0 client credentials for the Authorization Code Grant (Strava has no PKCE; client_id + client_secret are required for every exchange/refresh)"
    env_vars:
      - name: "strava.client.id (local.properties)"
        source: "strava.com/settings/api — 'Client ID'"
      - name: "strava.client.secret (local.properties)"
        source: "strava.com/settings/api — 'Client Secret'"
    dashboard_config:
      - task: "Create a Strava API application; set 'Authorization Callback Domain' to exactly: rokidhud (bare word — no scheme, no slashes, no ://callback)"
        location: "strava.com/settings/api"
must_haves:
  truths:
    - "phone APK builds with androidx.browser:browser:1.8.0 (EXACTLY — 1.9.0+ needs compileSdk 36/AGP 8.9.1 and breaks this build) and androidx.security:security-crypto:1.1.0"
    - "BuildConfig.STRAVA_CLIENT_ID / STRAVA_CLIENT_SECRET read local.properties keys strava.client.id / strava.client.secret with empty-string defaults (graceful dev-mode degradation, CONTEXT locked 'Credentials')"
    - "strava_auth.xml is excluded from Auto Backup in backup_rules.xml AND under both <cloud-backup> and <device-transfer> in data_extraction_rules.xml (Pitfall 2 — restored ESP ciphertext is undecryptable)"
    - "A corrupt/undecryptable token store deletes itself and recreates instead of crashing (AEADBadTagException catch-and-reset), degrading to the disconnected state"
    - "Token exchange POSTs exactly client_id, client_secret, code, grant_type=authorization_code to https://www.strava.com/oauth/token — NO redirect_uri field (Strava deviation from RFC 6749)"
    - "Refresh POSTs client_id, client_secret, grant_type=refresh_token, refresh_token on a PLAIN OkHttpClient (no authenticator — prevents refresh→401→refresh recursion)"
    - "A callback with mismatched/absent state is rejected before any network call; the pending state is cleared after ONE use regardless of outcome"
    - "A callback whose granted scope lacks read/read_all/activity:write is rejected without persisting tokens (Pitfall 4)"
    - "rokidhud://callback resolves to StravaCallbackActivity (exported, BROWSABLE+DEFAULT, Theme.NoDisplay) which forwards to MainActivity with CLEAR_TOP|SINGLE_TOP and finishes synchronously"
    - "GET https://www.strava.com/api/v3/athlete sends Bearer auth, logs X-RateLimit-Usage and X-ReadRateLimit-Usage, and retries at most once on 401 via the Authenticator responseCount guard"
  artifacts:
    - path: "phone/build.gradle.kts"
      provides: "browser 1.8.0 pin + security-crypto 1.1.0 + STRAVA_* BuildConfig fields"
      contains: "androidx.browser:browser:1.8.0"
    - path: "phone/src/main/res/xml/data_extraction_rules.xml"
      provides: "strava_auth.xml sharedpref exclusion in both sections"
      contains: "strava_auth.xml"
    - path: "phone/src/main/java/com/rokid/hud/phone/strava/StravaTokenStore.kt"
      provides: "EncryptedSharedPreferences wrapper implementing TokenPersistence with injected factory + catch-and-reset"
      contains: "class StravaTokenStore"
    - path: "phone/src/main/java/com/rokid/hud/phone/strava/StravaAuthManager.kt"
      provides: "launchAuthorize (Custom Tab), handleCallback (state/scope validation + exchange), refresh transport, ensureFreshToken/forceRefresh/retryTokenAfter401 delegation, disconnect"
      contains: "class StravaAuthManager"
    - path: "phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt"
      provides: "Authenticated OkHttp client: getAthlete(), StravaAuthenticator with responseCount guard, rate-limit header logging"
      contains: "class StravaApiClient"
    - path: "phone/src/main/java/com/rokid/hud/phone/strava/StravaCallbackActivity.kt"
      provides: "No-UI deep-link forwarder with ACTION_STRAVA_CALLBACK/EXTRA_CALLBACK_URI contract"
      contains: "class StravaCallbackActivity"
    - path: "phone/src/main/AndroidManifest.xml"
      provides: "Exported StravaCallbackActivity with its own rokidhud/callback intent-filter block"
      contains: "rokidhud"
  key_links:
    - from: "phone/src/main/java/com/rokid/hud/phone/strava/StravaTokenStore.kt"
      to: "TokenPersistence"
      via: "interface implementation consumed by TokenRefreshCoordinator"
      pattern: ": TokenPersistence"
    - from: "phone/src/main/java/com/rokid/hud/phone/strava/StravaAuthManager.kt"
      to: "TokenRefreshCoordinator"
      via: "coordinator instance wired with the OkHttp RefreshTransport"
      pattern: "TokenRefreshCoordinator\\("
    - from: "phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt"
      to: "StravaAuthManager.retryTokenAfter401"
      via: "StravaAuthenticator authenticate() single retry"
      pattern: "retryTokenAfter401"
    - from: "phone/src/main/AndroidManifest.xml"
      to: "StravaCallbackActivity"
      via: "intent-filter data scheme=rokidhud host=callback"
      pattern: "android:scheme=\"rokidhud\""
---

## Phase Goal

**As a** cyclist or runner, **I want to** connect my Strava account once from the phone app and stay securely authenticated across restarts and token expiries, **so that** route import (Phase 4) and activity upload (Phase 5) can call the Strava API on my behalf without me ever re-logging in.

<objective>
Wire the tested Wave-1 core into Android: pin the two new dependencies (browser 1.8.0 EXACTLY, security-crypto 1.1.0), inject STRAVA_CLIENT_ID/SECRET BuildConfig fields, backup-exclude the encrypted token file, and ship the four integration classes — StravaTokenStore (ESP + catch-and-reset), StravaAuthManager (Custom Tab launch, state lifecycle, code exchange, OkHttp refresh transport), StravaApiClient + StravaAuthenticator (authenticated GET /athlete with reactive 401 fallback), and StravaCallbackActivity (exported no-UI deep-link forwarder) with its manifest intent-filter.

Purpose: after this plan the ENTIRE AUTH-01/02/03 machine exists — everything except the MainActivity card that triggers it (Wave 3). ROADMAP "Delivers": the OkHttp Strava API client + token authenticator.
Output: 2 pinned deps, 2 BuildConfig fields, 2 backup-rule edits, 4 new classes, 1 manifest activity, 1 new JVM test class (StravaTokenStoreTest), full build green.

MVP slice: with API keys in local.properties, an adb-fired rokidhud://callback already routes through the full validation pipeline; the phase's security machinery is live even before the CONNECT button exists.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-strava-authentication/03-CONTEXT.md
@.planning/phases/03-strava-authentication/03-RESEARCH.md
@.planning/phases/03-strava-authentication/03-01-SUMMARY.md
@phone/build.gradle.kts
@phone/src/main/AndroidManifest.xml
@phone/src/main/res/xml/backup_rules.xml
@phone/src/main/res/xml/data_extraction_rules.xml

Environment: NO `gradlew` script. All Gradle invocations:
`export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain <targets> -q`

LOGGING DISCIPLINE (applies to every task in this plan — Security Domain V7, verified by the Wave-4 logcat scan): NEVER log access_token, refresh_token, client_secret, the authorization code, or the full callback URI. Log events and non-secrets only: HTTP status codes, expires_at, state-match booleans, rate-limit headers, refresh_token hashCode hex.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Dependencies + BuildConfig + backup exclusions + StravaTokenStore with tested catch-and-reset</name>
  <files>phone/build.gradle.kts, phone/src/main/res/xml/backup_rules.xml, phone/src/main/res/xml/data_extraction_rules.xml, phone/src/main/java/com/rokid/hud/phone/strava/StravaTokenStore.kt, phone/src/test/java/com/rokid/hud/phone/strava/StravaTokenStoreTest.kt</files>
  <read_first>
    - .planning/phases/03-strava-authentication/03-RESEARCH.md — "Standard Stack (Installation snippet + version verification)", "Pitfall 1 (browser pin)", "Pitfall 2 (backup exclusion XML)", "Pattern 4 (ESP catch-and-reset)", "Pitfall 6 (main-thread ESP)"
    - phone/build.gradle.kts:8-33 (localProps loader + ROKID_* buildConfigField precedent, buildConfig=true already enabled)
    - phone/src/main/res/xml/backup_rules.xml + data_extraction_rules.xml (current: only activities/ excluded; the existing "T-03-01" comment refers to the PHASE-1 GPS-track threat register — leave it untouched, do not renumber)
  </read_first>
  <behavior>
    - StravaTokenStoreTest (JVM, using a fake in-memory SharedPreferences implementation — SharedPreferences is an interface, trivially fakeable):
    - save(StoredTokens("at","rt",123L,42L,"Bill H")) then load() round-trips all five fields; load() after clear() returns null
    - load() returns null when any of access_token/refresh_token/expires_at is absent from the prefs (partial write = disconnected, never a half-token)
    - Catch-and-reset: a factory whose FIRST invocation throws (simulating AEADBadTagException after backup restore) and SECOND succeeds → store works, onCorrupt callback (file delete) was invoked exactly once
    - Factory that ALWAYS throws → store degrades: load() returns null, save()/clear() no-op without crashing (both-fail path logs and returns)
  </behavior>
  <action>
    1. phone/build.gradle.kts — in defaultConfig, directly after the ROKID_ACCESS_KEY field, add (matching the rokid.* precedent exactly, per CONTEXT locked "Credentials"):
       buildConfigField("String", "STRAVA_CLIENT_ID", "\"${localProps.getProperty("strava.client.id", "")}\"")
       buildConfigField("String", "STRAVA_CLIENT_SECRET", "\"${localProps.getProperty("strava.client.secret", "")}\"")
       In dependencies, after the gson line, add with comments verbatim:
       // Strava auth (Phase 3)
       implementation("androidx.security:security-crypto:1.1.0")  // minCompileSdk 34 — verified compatible
       implementation("androidx.browser:browser:1.8.0")           // PINNED: 1.9.0+ needs compileSdk 36 + AGP 8.9.1 — do NOT bump
    2. backup_rules.xml — inside <full-backup-content>, after the existing activities/ exclude, add: <exclude domain="sharedpref" path="strava_auth.xml" /> with comment "Strava OAuth tokens: Keystore master key never leaves the device — a restored copy is undecryptable ciphertext (AEADBadTagException). 03-RESEARCH Pitfall 2."
    3. data_extraction_rules.xml — add the SAME <exclude domain="sharedpref" path="strava_auth.xml" /> line under BOTH <cloud-backup> and <device-transfer> (two insertions), same comment once above the first.
    4. Create phone/src/main/java/com/rokid/hud/phone/strava/StravaTokenStore.kt implementing TokenPersistence (Wave-1 interface). Design per RESEARCH Pattern 4 with an injected factory so the reset logic is JVM-testable:
       - companion object { private const val TAG = "StravaTokenStore"; const val PREFS_FILE = "strava_auth"; private const val KEY_ACCESS = "access_token"; KEY_REFRESH = "refresh_token"; KEY_EXPIRES_AT = "expires_at"; KEY_ATHLETE_ID = "athlete_id"; KEY_ATHLETE_NAME = "athlete_name" }
       - Primary constructor (visible for tests): StravaTokenStore(private val espFactory: () -> SharedPreferences, private val onCorrupt: () -> Unit, private val log: (String, Exception?) -> Unit = { m, e -> Log.e(TAG, m, e) })
       - Secondary constructor(context: Context) : this(espFactory = { createEsp(context.applicationContext) }, onCorrupt = { context.applicationContext.deleteSharedPreferences(PREFS_FILE) }) — deleteSharedPreferences is API 24+, minSdk 28 OK
       - private companion fun createEsp(context: Context): SharedPreferences — security-crypto 1.1.0 API (MasterKey.Builder, NOT the deprecated MasterKeys): MasterKey.Builder(context).setKeyScheme(MasterKey.KeyScheme.AES256_GCM).build() then EncryptedSharedPreferences.create(context, PREFS_FILE, masterKey, PrefKeyEncryptionScheme.AES256_SIV, PrefValueEncryptionScheme.AES256_GCM)
       - @Volatile private var prefs: SharedPreferences? = null; private fun prefs(): SharedPreferences? — double-checked inside synchronized(this) (ESP first-creation is not race-safe — tink #535): try espFactory() catch (e: Exception) { log("Token store corrupt, resetting", e); onCorrupt(); try espFactory() catch (e2) { log("Token store unavailable", e2); null } }
       - override fun load(): StoredTokens? — read all 5 keys; return null unless access/refresh non-blank AND expires_at present (getLong with -1 sentinel); athleteId stored as long with -1 = null
       - override fun save(tokens: StoredTokens) / override fun clear() — edit().putXxx().apply() / edit().clear().apply(); both no-op safely when prefs() is null
       - KDoc header: "First access performs Keystore + Tink keyset creation (can take hundreds of ms) — CALL ONLY FROM BACKGROUND THREADS (Pitfall 6). Read/write after creation is ordinary SharedPreferences."
    5. Create StravaTokenStoreTest.kt: a ~40-line FakeSharedPreferences (backing HashMap, minimal SharedPreferences + Editor implementation — only the methods the store uses) + the behavior cases above. No Context, no Android runtime.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "com.rokid.hud.phone.strava.*" :phone:assembleDebug -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'androidx.browser:browser:1.8.0' phone/build.gradle.kts succeeds AND grep -cE 'androidx.browser:browser:1\.(9|10)' phone/build.gradle.kts returns 0 (pin respected)
    - grep -q 'androidx.security:security-crypto:1.1.0' phone/build.gradle.kts succeeds
    - grep -q 'STRAVA_CLIENT_ID' phone/build.gradle.kts and grep -q 'strava.client.secret' both succeed (empty-string defaults via localProps.getProperty(..., ""))
    - grep -c 'strava_auth.xml' phone/src/main/res/xml/backup_rules.xml returns >= 1; grep -c 'strava_auth.xml' phone/src/main/res/xml/data_extraction_rules.xml returns >= 2 (both sections)
    - grep -q 'MasterKey.Builder' StravaTokenStore.kt succeeds AND grep -c 'MasterKeys' StravaTokenStore.kt returns 0 (1.1.0 API, not the doubly-deprecated 1.0 API)
    - grep -q 'PREFS_FILE = "strava_auth"' StravaTokenStore.kt succeeds (name matches the backup exclusions)
    - :phone:assembleDebug exits 0 (both new AARs resolve against compileSdk 34 / AGP 8.7.3); strava unit tests green including StravaTokenStoreTest
  </acceptance_criteria>
  <done>Tokens have an encrypted, backup-excluded home whose corruption path degrades to "disconnected" instead of a crash loop, and the build carries the exact pinned dependency set plus graceful-degradation credentials.</done>
</task>

<task type="auto">
  <name>Task 2: StravaAuthManager (Custom Tab launch, state lifecycle, exchange, refresh transport) + StravaCallbackActivity + manifest intent-filter</name>
  <files>phone/src/main/java/com/rokid/hud/phone/strava/StravaAuthManager.kt, phone/src/main/java/com/rokid/hud/phone/strava/StravaCallbackActivity.kt, phone/src/main/AndroidManifest.xml</files>
  <read_first>
    - .planning/phases/03-strava-authentication/03-RESEARCH.md — "Pattern 2 (Custom Tab one-shot)", "Pattern 3 (dedicated callback activity + manifest XML)", "Pattern 5 (single-flight wiring)", "Code Examples §Token exchange", "Pitfall 3 (duplicate MainActivity)", "Pitfall 5 (state single-use)", "Pitfall 4 (scope deselection)"
    - phone/src/main/java/com/rokid/hud/phone/strava/TokenRefreshCoordinator.kt + StravaOAuth.kt + StravaModels.kt (Wave-1 signatures — build against them, do not modify them)
    - phone/src/main/AndroidManifest.xml:40-52 (existing activity declarations — the new activity block goes after DeviceScanActivity)
  </read_first>
  <action>
    1. Create phone/src/main/java/com/rokid/hud/phone/strava/StravaCallbackActivity.kt — the AppAuth RedirectUriReceiverActivity pattern (RESEARCH Pattern 3, Claude's-discretion choice resolved: dedicated activity, NOT an intent-filter on MainActivity — avoids the duplicate-MainActivity trap since MainActivity has standard launchMode):
       - class StravaCallbackActivity : android.app.Activity() (plain Activity — Theme.NoDisplay requires finish() before onResume completes; it happens synchronously in onCreate)
       - companion object { private const val TAG = "StravaCallback"; const val ACTION_STRAVA_CALLBACK = "com.rokid.hud.phone.strava.CALLBACK"; const val EXTRA_CALLBACK_URI = "strava_callback_uri" }
       - onCreate: super, then build Intent(this, MainActivity::class.java) with action = ACTION_STRAVA_CALLBACK, putExtra(EXTRA_CALLBACK_URI, intent?.data?.toString()), addFlags(Intent.FLAG_ACTIVITY_CLEAR_TOP or Intent.FLAG_ACTIVITY_SINGLE_TOP); startActivity(forward); finish(). CLEAR_TOP|SINGLE_TOP delivers to the existing MainActivity via onNewIntent (warm), recreates it via onCreate (cold/process death), and pops the Custom Tab off the back stack.
       - MINIMAL EXPORTED SURFACE (threat T-03-07): no other logic, reads only intent.data, no UI, no state. Log.i(TAG, "Strava callback received") ONLY — never the URI (it contains code + state; a logged code inside the 10-min validity window plus the extractable APK secret would be a token-theft path).
    2. phone/src/main/AndroidManifest.xml — after the DeviceScanActivity block, add ITS OWN activity element with ITS OWN single intent-filter (PITFALLS Integration Gotchas: separate intent-filter blocks; do NOT add autoVerify — App Links are https-only, meaningless for custom schemes):
       <activity android:name=".strava.StravaCallbackActivity" android:exported="true" android:theme="@android:style/Theme.NoDisplay">
           <intent-filter>
               <action android:name="android.intent.action.VIEW" />
               <category android:name="android.intent.category.DEFAULT" />
               <category android:name="android.intent.category.BROWSABLE" />
               <data android:scheme="rokidhud" android:host="callback" />
           </intent-filter>
       </activity>
       android:exported="true" is MANDATORY (Android 12+ install-time failure otherwise — CONTEXT locked "OAuth redirect mechanics").
    3. Create phone/src/main/java/com/rokid/hud/phone/strava/StravaAuthManager.kt:
       - class StravaAuthManager(private val context: Context, private val tokenStore: StravaTokenStore) — manual wiring, no DI (codebase convention); companion { private const val TAG = "StravaAuth"; private const val STATE_PREFS = "strava_oauth"; private const val KEY_PENDING_STATE = "pending_state" }
       - private val tokenClient = OkHttpClient() — PLAIN client for BOTH token-endpoint calls (exchange + refresh): no authenticator, so a token-endpoint 401 can never re-enter the Authenticator (RESEARCH Anti-Patterns)
       - private val gson = Gson() (reused)
       - val coordinator = TokenRefreshCoordinator(store = tokenStore, transport = refresh-transport-below, log = { Log.i(TAG, it) })
       - Refresh transport (object : RefreshTransport inline): POST StravaOAuth.TOKEN_URL with FormBody client_id=BuildConfig.STRAVA_CLIENT_ID, client_secret=BuildConfig.STRAVA_CLIENT_SECRET, grant_type=refresh_token, refresh_token=<param>. Execute on tokenClient; response.use { }: 2xx → parseTokenResponse(body) ?.let { RefreshOutcome.Success(it) } ?: RefreshOutcome.TransientError("malformed body"); non-2xx → RefreshOutcome.Rejected(resp.code); catch IOException/Exception → RefreshOutcome.TransientError(e.message). Never log the form body.
       - Pending-state lifecycle (Pitfall 5) in PLAIN SharedPreferences file "strava_oauth" (the nonce is not a secret — it rides in the browser URL; keeping it out of the ESP avoids main-thread Keystore work at CONNECT tap, and a backup-restored stale nonce is harmless: validation just fails): persistState(state) = context.getSharedPreferences(STATE_PREFS, MODE_PRIVATE).edit().putString(KEY_PENDING_STATE, state).apply(); consumePendingState(): String? = read then IMMEDIATELY remove().apply() — single-use regardless of outcome.
       - fun launchAuthorize(activity: Activity): Boolean — if (BuildConfig.STRAVA_CLIENT_ID.isEmpty() || BuildConfig.STRAVA_CLIENT_SECRET.isEmpty()) { Log.w(TAG, "Strava keys not configured"); return false } (Pitfall 7 guard — never launch with empty client_id); state = StravaOAuth.newState(); persistState(state); Log.i(TAG, "state generated, launching authorize"); try CustomTabsIntent.Builder().build().launchUrl(activity, Uri.parse(StravaOAuth.buildAuthorizeUrl(BuildConfig.STRAVA_CLIENT_ID, state))) catch (e: ActivityNotFoundException) { Log.w(TAG, "No browser available: ${e.message}"); return false }; true. (android.net.Uri is fine HERE — this class is Android-integration, not a JVM-tested seam; launchUrl degrades to the default browser automatically, and the Strava app may natively handle the URL — both surfaces end at rokidhud://callback, RESEARCH A7.)
       - sealed class CallbackResult { data class Connected(val athleteName: String) : CallbackResult(); object StateMismatch : CallbackResult(); object Denied : CallbackResult(); object ScopesIncomplete : CallbackResult(); object ExchangeFailed : CallbackResult() }
       - fun handleCallback(uriString: String?): CallbackResult — BLOCKING, call from Thread{} only. Order matters: (a) expected = consumePendingState() — consumed FIRST so the nonce dies after one attempt; (b) params = StravaOAuth.parseCallback(uriString); (c) if (!StravaOAuth.validateState(expected, params["state"])) { Log.w(TAG, "state mismatch — rejecting callback"); return StateMismatch } — NO network call happens on mismatch; (d) code = params["code"] ?: return Denied.also { Log.w(TAG, "callback without code (user denied or error)") } (missing code = failure signal, robust to Strava's exact error param — RESEARCH A2); (e) if (!StravaOAuth.grantedScopesComplete(params["scope"])) { Log.w(TAG, "granted scopes incomplete — not persisting tokens"); return ScopesIncomplete } (Pitfall 4: checked BEFORE spending the single-use code); (f) exchangeCode(code)?.let { return Connected(it) } ?: return ExchangeFailed.
       - private fun exchangeCode(code: String): String? — POST StravaOAuth.TOKEN_URL, FormBody EXACTLY client_id, client_secret, code, grant_type=authorization_code — NO redirect_uri field (verified Strava quirk, RESEARCH Strava Facts). 2xx → r = parseTokenResponse(body) ?: return null (logs "exchange returned malformed body"); athlete = r.athlete; name = athlete?.displayName() ?: "Strava athlete"; tokenStore.save(StoredTokens(r.accessToken!!, r.refreshToken!!, r.expiresAt!!, athlete?.id, name)) (non-null asserted safe: parseTokenResponse validated them); Log.i(TAG, "exchange ok, token stored, expires_at=${r.expiresAt}"); return name. Non-2xx → Log.w(TAG, "exchange failed http=${resp.code}"); null. IOException → Log.e + null. Never log body or code value.
       - fun ensureFreshToken() = coordinator.ensureFreshToken(); fun forceRefresh() = coordinator.forceRefresh(); fun retryTokenAfter401(failed: String?) = coordinator.retryTokenAfter401(failed) — thin delegation
       - fun isConnected(): Boolean = tokenStore.load() != null; fun connectedAthleteName(): String? = tokenStore.load()?.athleteName — BLOCKING (ESP), background threads only
       - fun disconnect() { tokenStore.clear(); Log.i(TAG, "disconnected — local tokens wiped") } (CONTEXT locked: local wipe only, no remote deauthorize in v1)
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug :phone:testDebugUnitTest -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'android:scheme="rokidhud"' phone/src/main/AndroidManifest.xml AND grep -q 'android:host="callback"' both succeed; the StravaCallbackActivity element has android:exported="true"
    - grep -c '<intent-filter>' inside the StravaCallbackActivity block is exactly 1 (its own separate block); grep -c 'autoVerify' phone/src/main/AndroidManifest.xml returns 0
    - grep -q 'FLAG_ACTIVITY_CLEAR_TOP' StravaCallbackActivity.kt AND grep -q 'FLAG_ACTIVITY_SINGLE_TOP' both succeed; grep -q 'finish()' succeeds (synchronous forward+finish)
    - grep -c 'redirect_uri' StravaAuthManager.kt returns 0 (exchange body has NO redirect_uri — Strava quirk)
    - grep -q 'grant_type", "authorization_code' StravaAuthManager.kt (or equivalent add("grant_type", "authorization_code")) succeeds; grep -q '"refresh_token"' succeeds (both grant types present)
    - grep -q 'consumePendingState' StravaAuthManager.kt succeeds and the read+remove happen before validateState is evaluated (single-use nonce)
    - grep -q 'ActivityNotFoundException' StravaAuthManager.kt succeeds (no-browser fallback)
    - grep -cE 'Log\.[diwe]\(TAG, ".*(accessToken|refreshToken|client_secret|\$code)' StravaAuthManager.kt returns 0 (no secret material in log statements)
    - :phone:assembleDebug exits 0 (manifest merges, activity resolves .strava relative name)
  </acceptance_criteria>
  <done>The complete OAuth round-trip machinery exists: CONNECT can launch a Custom Tab at the mobile authorize URL, the OS routes rokidhud://callback to a minimal exported forwarder, and handleCallback enforces state single-use, fail-closed scopes, and the exact Strava exchange contract before persisting encrypted tokens.</done>
</task>

<task type="auto">
  <name>Task 3: StravaApiClient + StravaAuthenticator — authenticated GET /athlete with proactive refresh, bounded 401 retry, and rate-limit logging</name>
  <files>phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt</files>
  <read_first>
    - .planning/phases/03-strava-authentication/03-RESEARCH.md — "Code Examples §GET /athlete", "Pattern 5 (Authenticator + responseCount)", "Standard Stack Supporting (logging-interceptor rules)"
    - phone/src/main/java/com/rokid/hud/phone/strava/StravaAuthManager.kt (Task 2 — ensureFreshToken/retryTokenAfter401 signatures)
  </read_first>
  <action>
    Create phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt containing both classes:

    1. class StravaAuthenticator(private val auth: StravaAuthManager) : okhttp3.Authenticator — the REACTIVE fallback layer (CONTEXT locked: proactive is primary, this is the demoted 401 net):
       - override fun authenticate(route: Route?, response: Response): Request? — if (response.responseCount >= 2) return null (official OkHttp give-up pattern — at most ONE retry, preventing refresh-loop DoS); failed = response.request.header("Authorization")?.removePrefix("Bearer "); fresh = auth.retryTokenAfter401(failed) ?: return null (coordinator handles the already-refreshed-by-another-thread double-check inside its lock); return response.request.newBuilder().header("Authorization", "Bearer $fresh").build()
       - private extension: val Response.responseCount: Int get() = generateSequence(this) { it.priorResponse }.count()
       - KDoc: "Runs synchronously on the request's calling thread — always an app background Thread{} in this codebase (all StravaApiClient methods are blocking, OsrmClient convention). Blocking refresh here is the intended OkHttp usage."
    2. class StravaApiClient(private val auth: StravaAuthManager):
       - companion object { private const val TAG = "StravaApi"; private const val BASE = "https://www.strava.com/api/v3" }
       - private val client = OkHttpClient.Builder().authenticator(StravaAuthenticator(auth)).apply { if (BuildConfig.DEBUG) addInterceptor(HttpLoggingInterceptor().setLevel(HttpLoggingInterceptor.Level.BASIC)) }.build() — DEBUG-only and Level.BASIC ONLY (HEADERS/BODY would leak Authorization headers and token JSON — RESEARCH Anti-Patterns)
       - private val gson = Gson()
       - fun getAthlete(): StravaAthlete? — BLOCKING, Thread{} only (KDoc: NetworkOnMainThreadException otherwise). token = auth.ensureFreshToken() ?: return null (proactive 30-min refresh happens inside — the locked PRIMARY mechanism; null means no/invalid tokens → caller shows Reconnect). Request GET "$BASE/athlete" with .header("Authorization", "Bearer $token"). client.newCall(req).execute().use { resp → logRateLimits(resp); if (!resp.isSuccessful) { Log.w(TAG, "GET /athlete ${resp.code}"); return null }; gson.fromJson(resp.body?.string(), StravaAthlete::class.java) } wrapped in try/catch(Exception) → Log.e(TAG, "GET /athlete failed: ${e.message}", e); null (codebase convention: never rethrow)
       - private fun logRateLimits(resp: Response) { val overall = resp.header("X-RateLimit-Usage"); val read = resp.header("X-ReadRateLimit-Usage"); if (overall != null || read != null) Log.i(TAG, "Rate usage overall=$overall read=$read") } — BOTH header pairs (Integration Gotchas row 9; current verified limits: 200/15min + 2,000/day overall, 100 reads/15min + 1,000 reads/day; awareness now, enforcement Phase 4)
    Note the /api/v3 asymmetry is CORRECT and load-bearing: API resource calls use https://www.strava.com/api/v3/..., token endpoints use https://www.strava.com/oauth/token WITHOUT the prefix.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug :phone:testDebugUnitTest :shared:testDebugUnitTest -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'responseCount >= 2' StravaApiClient.kt succeeds (bounded retry)
    - grep -q 'BASE = "https://www.strava.com/api/v3"' StravaApiClient.kt succeeds
    - grep -q 'X-RateLimit-Usage' StravaApiClient.kt AND grep -q 'X-ReadRateLimit-Usage' both succeed
    - grep -q 'BuildConfig.DEBUG' StravaApiClient.kt succeeds AND grep -q 'Level.BASIC' succeeds AND grep -cE 'Level\.(HEADERS|BODY)' StravaApiClient.kt returns 0
    - grep -q 'ensureFreshToken' StravaApiClient.kt succeeds (proactive primary before the request)
    - Full build + all unit suites exit 0
  </acceptance_criteria>
  <done>ROADMAP's "Delivers" artifact exists: an OkHttp Strava API client whose every call proactively refreshes inside the 30-minute window, falls back to exactly one Authenticator-driven retry on 401, and logs both rate-limit header pairs — ready for Wave 3's UI and Phase 4's route endpoints.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Any app/browser → exported StravaCallbackActivity | Untrusted VIEW intents with attacker-chosen code/state (custom schemes have no OS ownership) |
| App ↔ strava.com (HTTPS) | client_secret + tokens in transit; response JSON untrusted |
| APK file → reverse engineer | BuildConfig secret extractable |
| Device backup/transfer → cloud/new device | ESP ciphertext would be orphaned from its Keystore key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing/Tampering | handleCallback (CSRF/session fixation via forged rokidhud://callback) | mitigate | state validated constant-time BEFORE any network call; nonce consumed single-use regardless of outcome; mismatch → log + reject (Wave-4 adb negative test) |
| T-03-02 | Information Disclosure | Token storage at rest | mitigate | EncryptedSharedPreferences (AES256-SIV keys / AES256-GCM values, Keystore master key); strava_auth.xml excluded from backup_rules.xml AND both data_extraction_rules.xml sections |
| T-03-03 | Information Disclosure | client_secret in BuildConfig/APK | accept | LOCKED project decision: Strava has no PKCE; sideloaded single-user app; secret rotatable at strava.com/settings/api before any public release. Residual risk documented, not mitigated further |
| T-03-04 | Information Disclosure | Tokens/secret/code in logcat | mitigate | Logging discipline block in <context>: events + expires_at + hashCode hex only; logging-interceptor DEBUG-only at Level.BASIC; grep acceptance gates this plan + Wave-4 logcat redaction scan |
| T-03-05 | Spoofing | Authorization UI | mitigate | Chrome Custom Tab (browser 1.8.0) against the mobile authorize endpoint — never WebView (Strava blocks it; RFC 8252); credentials never touch app-owned UI |
| T-03-06 | Denial of Service | 401 refresh loop | mitigate | responseCount >= 2 give-up; single-flight coordinator lock; token endpoints on a PLAIN client (no authenticator recursion); wipe only on 400/401 |
| T-03-07 | Tampering | Exported StravaCallbackActivity surface | mitigate | No UI, no state, reads only intent.data, forwards + finish() synchronously; the only new exported component; the forwarded code is useless without the state check downstream |
| T-03-08 | Elevation/Access | Consent-screen scope deselection | mitigate | grantedScopesComplete fail-closed BEFORE code exchange; tokens never persisted on partial grant |
| T-03-SC | Tampering (supply chain) | androidx.browser:1.8.0 + androidx.security:security-crypto:1.1.0 | mitigate | Both Approved in 03-RESEARCH Package Legitimacy Audit (first-party androidx on Google Maven, AARs inspected); no [ASSUMED]/[SUS] → no blocking human checkpoint; wrong coordinates fail resolution loudly |
</threat_model>

<verification>
- `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest :shared:testDebugUnitTest assembleDebug -q` exits 0 (both APKs, all suites)
- grep -c 'strava_auth.xml' across the two backup rule files totals 3 (1 + 2)
- No existing manifest entries modified (MainActivity block untouched — launchMode stays standard)
</verification>

<success_criteria>
- Every AUTH-01 mechanism (Custom Tab, deep link, state CSRF, scope validation, exact exchange contract) and AUTH-02 mechanism (ESP, backup exclusion, catch-and-reset) and AUTH-03 mechanism (proactive window via coordinator, Authenticator fallback, rotation) is implemented and compiles
- The only human-remaining gap is UI reachability (Wave 3) and live proof (Wave 4)
- Integration Gotchas rows 1-6 + 9 each map to a grep/test acceptance criterion in this plan or 03-01
</success_criteria>

<output>
Create `.planning/phases/03-strava-authentication/03-02-SUMMARY.md` when done. Record: dependency resolution confirmation (browser 1.8.0 / security-crypto 1.1.0), the CallbackResult contract (Wave 3 consumes it), and any deviation from RESEARCH Patterns 2-5.
</output>
