---
phase: 03-strava-authentication
plan: 03
type: execute
wave: 3
depends_on: [03-02]
files_modified:
  - phone/src/main/res/layout/activity_main.xml
  - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03]
must_haves:
  truths:
    - "A STRAVA card renders on MainActivity between the RECORD card and GLASSES SETTINGS (CONTEXT locked 'UI placement')"
    - "With empty BuildConfig Strava keys the card shows the 'Set up Strava API keys' hint and NO Connect button (Pitfall 7 — never launch a Custom Tab with an empty client_id)"
    - "With keys present and no stored tokens the card shows a CONNECT STRAVA button that launches the Custom Tab authorize flow"
    - "After a successful callback the user sees a toast 'Connected to Strava as {name}' and the card flips to 'Connected as {name}' + DISCONNECT (ROADMAP SC#2)"
    - "The callback is handled via onNewIntent when MainActivity is alive AND via onCreate's intent when the process was killed under the Custom Tab (Pitfall 3 both paths)"
    - "On Connected, a background GET /athlete runs and its result is logged — the authenticated client is exercised end-to-end (ROADMAP Delivers)"
    - "Card connection state is read from the token store on a background thread and applied via runOnUiThread (Pitfall 6 — no main-thread ESP)"
    - "DISCONNECT wipes local tokens only and returns the card to disconnected (CONTEXT locked: no remote deauthorize in v1)"
    - "In DEBUG builds, long-pressing the connected card forces a token refresh + GET /athlete, logging rotation proof (AUTH-03 verification hook)"
  artifacts:
    - path: "phone/src/main/res/layout/activity_main.xml"
      provides: "STRAVA card: stravaStatusText, stravaSetupHint, btnConnectStrava, btnDisconnectStrava"
      contains: "btnConnectStrava"
    - path: "phone/src/main/java/com/rokid/hud/phone/MainActivity.kt"
      provides: "Strava manager wiring, onNewIntent override, handleStravaCallbackIntent, refreshStravaCard state machine, connect/disconnect/long-press listeners"
      contains: "onNewIntent"
  key_links:
    - from: "phone/src/main/java/com/rokid/hud/phone/MainActivity.kt"
      to: "StravaAuthManager.launchAuthorize"
      via: "btnConnectStrava click listener"
      pattern: "launchAuthorize"
    - from: "phone/src/main/java/com/rokid/hud/phone/MainActivity.kt"
      to: "StravaCallbackActivity.ACTION_STRAVA_CALLBACK"
      via: "intent action match in onCreate + onNewIntent"
      pattern: "ACTION_STRAVA_CALLBACK"
    - from: "phone/src/main/java/com/rokid/hud/phone/MainActivity.kt"
      to: "StravaApiClient.getAthlete"
      via: "background thread after Connected result + debug long-press hook"
      pattern: "getAthlete"
---

## Phase Goal

**As a** cyclist or runner, **I want to** connect my Strava account once from the phone app and stay securely authenticated across restarts and token expiries, **so that** route import (Phase 4) and activity upload (Phase 5) can call the Strava API on my behalf without me ever re-logging in.

<objective>
Make the Wave-2 auth machine user-reachable: add the STRAVA card to MainActivity (setup-hint / disconnected / connected-as states), route the deep-link callback through onNewIntent + onCreate into a background token exchange with success/failure toasts, prove the authenticated client with a GET /athlete call on connect, and add the DEBUG-only long-press forced-refresh hook that Wave 4 uses to demonstrate token rotation without waiting 5.5 hours.

Purpose: completes the user-visible slice of AUTH-01 (tap → browser → back → confirmation), the visible surface of AUTH-02 (card still connected after restart), and the verification hook for AUTH-03.
Output: STRAVA card in activity_main.xml, ~150 lines of MainActivity wiring, full build + suites green.

MVP slice: after this plan the ENTIRE phase story works on a device with API keys configured — Wave 4 only proves it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-strava-authentication/03-CONTEXT.md
@.planning/phases/03-strava-authentication/03-02-SUMMARY.md
@phone/src/main/java/com/rokid/hud/phone/strava/StravaAuthManager.kt
@phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt
@phone/src/main/java/com/rokid/hud/phone/strava/StravaCallbackActivity.kt

Environment: NO `gradlew` script. All Gradle invocations:
`export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain <targets> -q`

MainActivity is ~1160 lines — read ONLY the anchors named in read_first, do not read the whole file twice.
</context>

<tasks>

<task type="auto">
  <name>Task 1: STRAVA card layout in activity_main.xml</name>
  <files>phone/src/main/res/layout/activity_main.xml</files>
  <read_first>
    - phone/src/main/res/layout/activity_main.xml:266-330 (RECORD card shape: bg_card LinearLayout, SectionTitle style, Button with backgroundTint/insetTop/insetBottom conventions) and :528-545 (GLASSES SETTINGS banner — the insertion anchor)
  </read_first>
  <action>
    Insert a new card block immediately BEFORE the three-line `<!-- ═══ ... GLASSES SETTINGS ... ═══ -->` comment banner (i.e., after the closing </LinearLayout> of the navStatus LIVE NAVIGATION card). This places STRAVA between RECORD and GLASSES SETTINGS in the visible column (navStatus is gone by default), satisfying CONTEXT locked "UI placement". Match the RECORD card's conventions exactly (bg_card, padding 16dp, marginBottom 12dp, SectionTitle):

    <!-- ═══════════════════════════════════════════════════════════════ -->
    <!-- STRAVA (account connection — Phase 3)                          -->
    <!-- ═══════════════════════════════════════════════════════════════ -->
    LinearLayout vertical, android:id="@+id/stravaCard", background @drawable/bg_card, padding 16dp, layout_marginBottom 12dp, containing:
    1. TextView style @style/SectionTitle, text "STRAVA"
    2. TextView android:id="@+id/stravaStatusText" — match_parent width, textColor #FFFFFF, textSize 14sp, layout_marginBottom 10dp. Initial text "Not connected" (overwritten at runtime).
    3. TextView android:id="@+id/stravaSetupHint" — match_parent, textColor #AAAAAA, textSize 12sp, visibility gone, layout_marginBottom 10dp, text "Set up Strava API keys: create an API application at strava.com/settings/api (Authorization Callback Domain: rokidhud), then add strava.client.id and strava.client.secret to local.properties and rebuild."
    4. Button android:id="@+id/btnConnectStrava" — match_parent, height 48dp, text "Connect Strava", backgroundTint #FC5200 (Strava brand orange — Claude's-discretion styling within bg_card conventions), textColor #FFFFFF, textStyle bold, textSize 14sp, insetTop/insetBottom 0dp, visibility gone (runtime decides)
    5. Button android:id="@+id/btnDisconnectStrava" — match_parent, height 40dp, text "Disconnect", backgroundTint #2A2A2A, textColor #AAAAAA, textSize 13sp, insetTop/insetBottom 0dp, visibility gone, layout_marginTop 8dp
    All four dynamic views default hidden/neutral; refreshStravaCard() (Task 2) is the single writer of their visibility.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q 'android:id="@+id/btnConnectStrava"' phone/src/main/res/layout/activity_main.xml succeeds; same for stravaStatusText, stravaSetupHint, btnDisconnectStrava, stravaCard
    - The STRAVA block appears BEFORE the GLASSES SETTINGS banner and AFTER the navStatus card's closing tag (verify with grep -n ordering: line(stravaCard) > line(navStatus) && line(stravaCard) < line("GLASSES SETTINGS"))
    - :phone:assembleDebug exits 0 (layout inflates at compile via aapt)
  </acceptance_criteria>
  <done>The STRAVA card exists in the locked position with all four state views addressable by id, visually consistent with the RECORD card.</done>
</task>

<task type="auto">
  <name>Task 2: MainActivity wiring — managers, card state machine, callback routing (warm + cold), connect/disconnect flows with GET /athlete proof</name>
  <files>phone/src/main/java/com/rokid/hud/phone/MainActivity.kt</files>
  <read_first>
    - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt:36-155 (companion object, lateinit view fields, manager fields — where new declarations go), :231-244 (onCreate: manager init order — savedPlacesManager precedent at :235), :246-327 (bindViews end — where findViewById calls append), :339+ (setupListeners head — where click listeners append). MainActivity has NO onNewIntent override today — you are adding the first one.
    - .planning/phases/03-strava-authentication/03-RESEARCH.md — "Pattern 3 (route in BOTH onNewIntent and onCreate)", "Pitfall 6 (background ESP reads for the card)"
  </read_first>
  <action>
    All Strava work follows the codebase's Thread{} + runOnUiThread convention (no coroutines). Reference the Wave-2 CallbackResult contract from 03-02-SUMMARY.

    1. Fields (after the "// Managers" block ~:134): private lateinit var stravaTokenStore: StravaTokenStore; stravaAuthManager: StravaAuthManager; stravaApiClient: StravaApiClient — plus view fields stravaStatusText/stravaSetupHint (TextView), btnConnectStrava/btnDisconnectStrava (Button), stravaCard (LinearLayout) after the "// Recording card" view block. Imports: com.rokid.hud.phone.strava.* names explicitly (StravaTokenStore, StravaAuthManager, StravaApiClient, StravaCallbackActivity).
    2. onCreate (:231-244): after btAudioRouter.init() add manual wiring (no DI, CONTEXT convention): stravaTokenStore = StravaTokenStore(applicationContext); stravaAuthManager = StravaAuthManager(applicationContext, stravaTokenStore); stravaApiClient = StravaApiClient(stravaAuthManager). At the END of onCreate (after updateNotifStatus()) add: refreshStravaCard(); handleStravaCallbackIntent(intent) — the cold-start path: if the process died under the Custom Tab, the forwarded callback intent arrives via onCreate (Pitfall 3).
    3. Add the override (first in this class): override fun onNewIntent(intent: Intent?) { super.onNewIntent(intent); if (intent != null) { setIntent(intent); handleStravaCallbackIntent(intent) } } — warm path: StravaCallbackActivity's CLEAR_TOP|SINGLE_TOP forward lands here when MainActivity is alive.
    4. private fun handleStravaCallbackIntent(intent: Intent?) — if (intent?.action != StravaCallbackActivity.ACTION_STRAVA_CALLBACK) return; val uri = intent.getStringExtra(StravaCallbackActivity.EXTRA_CALLBACK_URI); Log.i(TAG, "Strava callback intent received"); Thread { val result = stravaAuthManager.handleCallback(uri); when (result) is Connected → { val athlete = stravaApiClient.getAthlete(); Log.i(TAG, if (athlete != null) "GET /athlete ok: connected athlete verified" else "GET /athlete failed post-connect (tokens stored; will retry on next use)") } (the authenticated-client proof — ROADMAP Delivers; do NOT treat a null athlete as auth failure: tokens are already persisted and the Authenticator/proactive refresh will recover); runOnUiThread { toast per result + refreshStravaCard() } }.start(). Toast texts: Connected → "Connected to Strava as ${result.athleteName}" (PITFALLS UX row 2); StateMismatch → "Strava connection rejected (security check failed) — try Connect again"; Denied → "Strava authorization was declined"; ScopesIncomplete → "Strava permissions incomplete — reconnect and keep all permission boxes checked"; ExchangeFailed → "Strava connection failed — check your network and try again". Never log the uri value.
    5. private fun refreshStravaCard() — the SINGLE writer of card state: if (BuildConfig.STRAVA_CLIENT_ID.isEmpty() || BuildConfig.STRAVA_CLIENT_SECRET.isEmpty()) { stravaStatusText.text = "Not configured"; stravaSetupHint.visibility = VISIBLE; btnConnectStrava.visibility = GONE; btnDisconnectStrava.visibility = GONE; return } (Pitfall 7 — synchronous, no store read needed). Otherwise Thread { val name = stravaAuthManager.connectedAthleteName(); runOnUiThread { if (name != null) { stravaStatusText.text = "Connected as $name"; btnConnectStrava.visibility = GONE; btnDisconnectStrava.visibility = VISIBLE } else { stravaStatusText.text = "Not connected"; btnConnectStrava.visibility = VISIBLE; btnDisconnectStrava.visibility = GONE }; stravaSetupHint.visibility = GONE } }.start() — first ESP access happens on this background thread (Pitfall 6: Keystore+Tink init can take hundreds of ms).
    6. bindViews(): append the five findViewById calls before the closing brace. setupListeners(): append btnConnectStrava.setOnClickListener { if (!stravaAuthManager.launchAuthorize(this)) Toast.makeText(this, "Cannot start Strava connect — check API keys and browser", Toast.LENGTH_LONG).show() } and btnDisconnectStrava.setOnClickListener { Thread { stravaAuthManager.disconnect(); runOnUiThread { Toast.makeText(this, "Disconnected from Strava", Toast.LENGTH_SHORT).show(); refreshStravaCard() } }.start() } (CONTEXT locked: disconnect = local wipe only).
    7. Also call refreshStravaCard() from onResume() (after the existing body) — cheap idempotent re-read so the card self-corrects if state changed while backgrounded (e.g., a Reconnect wipe from a failed refresh on another thread).
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug :phone:testDebugUnitTest -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "override fun onNewIntent" phone/src/main/java/com/rokid/hud/phone/MainActivity.kt succeeds (first override in this class)
    - grep -q "handleStravaCallbackIntent(intent)" MainActivity.kt appears at least twice (onCreate cold path + onNewIntent warm path)
    - grep -q "ACTION_STRAVA_CALLBACK" MainActivity.kt succeeds
    - grep -q "Connected to Strava as" MainActivity.kt succeeds (success toast — ROADMAP SC#2)
    - grep -q "getAthlete" MainActivity.kt succeeds (authenticated-client proof on connect)
    - grep -c "runOnUiThread" MainActivity.kt is at least 3 higher than before this task (all card mutations main-thread)
    - refreshStravaCard reads the token store inside a Thread{} block (manual inspection: no stravaAuthManager.connectedAthleteName()/isConnected() call outside Thread{})
    - :phone:assembleDebug + :phone:testDebugUnitTest exit 0
  </acceptance_criteria>
  <done>Tap CONNECT STRAVA → Custom Tab → authorize → return → background exchange → toast + "Connected as {name}" works end-to-end in code, with both warm and cold callback delivery paths and no main-thread ESP or network access.</done>
</task>

<task type="auto">
  <name>Task 3: DEBUG-only long-press forced-refresh hook + full-suite gate</name>
  <files>phone/src/main/java/com/rokid/hud/phone/MainActivity.kt</files>
  <read_first>
    - .planning/phases/03-strava-authentication/03-RESEARCH.md — "Open Questions §2 (forced-refresh verification hook)" and 03-VALIDATION "Device-only" refresh row (this hook is how AUTH-03 rotation gets proven on device without waiting 5.5 hours)
  </read_first>
  <action>
    1. In setupListeners(), add: stravaCard.setOnLongClickListener { if (!BuildConfig.DEBUG) return@setOnLongClickListener false; Thread { val token = stravaAuthManager.forceRefresh(); val athlete = if (token != null) stravaApiClient.getAthlete() else null; runOnUiThread { Toast.makeText(this, when { token == null -> "Force refresh: no tokens / refresh failed"; athlete != null -> "Force refresh OK — athlete verified"; else -> "Refreshed, but GET /athlete failed" }, Toast.LENGTH_LONG).show(); refreshStravaCard() } }.start(); true }
       The coordinator's forceRefresh() already logs "refresh ok expires_at=... rt#=<hashCode hex>" — a DIFFERENT rt# after the forced refresh than after the initial exchange is the on-device rotation proof (Wave 4 greps logcat for two distinct rt# values). No release-build surface: the guard short-circuits to false when !BuildConfig.DEBUG, leaving long-press unconsumed.
    2. Run the full gate: all module test suites + both APK assemblies. Fix anything red before done.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest :shared:testDebugUnitTest :glasses:testDebugUnitTest assembleDebug -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "setOnLongClickListener" MainActivity.kt succeeds AND the handler body contains BuildConfig.DEBUG (debug-gated)
    - grep -q "forceRefresh" MainActivity.kt succeeds
    - Full suite (phone + shared + glasses) and assembleDebug exit 0 — no regression anywhere in the repo
  </acceptance_criteria>
  <done>Wave 4 has a 2-second on-device path to prove AUTH-03 rotation (long-press → logcat shows a rotated rt# + GET /athlete 200), invisible in release builds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Forwarded callback intent → MainActivity.handleStravaCallbackIntent | Untrusted URI string (originates from any app able to fire rokidhud://) enters the UI layer before delegation to the validated pipeline |
| UI thread ↔ token store | Sensitive reads must stay off the main thread and off the UI (only athlete display name surfaces) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Spoofing/Tampering | handleStravaCallbackIntent | mitigate | MainActivity does zero validation itself — the raw URI goes straight into StravaAuthManager.handleCallback where state/scope/code checks run (single validated pipeline, no bypass path) |
| T-03-04 | Information Disclosure | Toasts, card text, MainActivity logs | mitigate | UI shows athlete display name ONLY (never tokens/ids); Log lines carry event descriptions, never the callback URI or token material |
| T-03-07 | Tampering | Debug forced-refresh hook | mitigate | Gated on BuildConfig.DEBUG; performs only an operation (refresh) the app can already do; exposes no data beyond existing toasts |
| T-03-03 | Information Disclosure | client_secret in APK | accept | Locked project decision (documented in 03-02 register); this plan adds no new secret surface |
</threat_model>

<verification>
- Full repo gate: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest :shared:testDebugUnitTest :glasses:testDebugUnitTest assembleDebug -q` exits 0
- Card state truth table implemented: (keys empty → hint), (keys + no tokens → CONNECT), (keys + tokens → Connected-as + DISCONNECT) — single writer refreshStravaCard()
- No existing MainActivity behavior changed: launcher intent-filter untouched, no launchMode added, existing listeners unmodified
</verification>

<success_criteria>
- ROADMAP SC#1 + SC#2 are code-complete: tap Connect → browser login → return → visible confirmation
- ROADMAP SC#3's UI surface exists: the card reads persisted state on every onCreate/onResume
- ROADMAP SC#4's proof hook exists: debug long-press exercises forced refresh + authenticated GET /athlete
</success_criteria>

<output>
Create `.planning/phases/03-strava-authentication/03-03-SUMMARY.md` when done. Record: the card state truth table as implemented, the exact toast strings, and the logcat markers Wave 4 should grep (state generated / callback received / exchange ok / rt# values / GET /athlete).
</output>
