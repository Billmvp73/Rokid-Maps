---
phase: 04-strava-route-import-navigation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt
  - phone/src/test/java/com/rokid/hud/phone/OsrmViaUrlTest.kt
autonomous: true
requirements: [NAVV-02]

must_haves:
  truths:
    - "The multi-coordinate OSRM URL is built from all downsampled points as lng,lat pairs with waypoints=0;{lastIndex} so intermediate points are silent (single leg, no spurious mid-route arrivals)"
    - "The step-parse drops any non-final zero-distance arrive step, keeping the final arrival"
    - "getRouteVia returns a RouteResult with real turn-by-turn steps on success; the existing 2-point getRoute is untouched"
    - "A follow-route fallback builds a RouteResult with exactly one synthetic 'Follow route' step (never empty) from the downsampled waypoints"
  artifacts:
    - path: "phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt"
      provides: "getRouteVia(points) + pure buildViaUrl + pure filterArriveSteps + buildFollowRouteResult"
      contains: "fun getRouteVia"
    - path: "phone/src/test/java/com/rokid/hud/phone/OsrmViaUrlTest.kt"
      provides: "URL-builder + arrive-filter + synthetic-step unit tests (zero network)"
  key_links:
    - from: "OsrmClient.buildViaUrl"
      to: "router.project-osrm.org via-point request"
      via: "waypoints=0;{last} single-leg URL"
      pattern: "waypoints=0;"
    - from: "OsrmClient.getRouteVia"
      to: "NavigationManager.startNavigationWithRoute (Plan 03)"
      via: "RouteResult(waypoints, steps)"
      pattern: "RouteResult"
---

<objective>
Extend `OsrmClient` with the verified multi-waypoint via-point routing path — the load-bearing `waypoints=0;{last}` URL and the non-final-arrive filter — plus the follow-route fallback that builds a non-empty synthetic-step RouteResult. The URL builder and step filter are extracted as PURE functions so they unit-test with zero network (same discipline as Phase-3's pure buildAuthorizeUrl); the only untestable line is the `execute()`/HTTP read.

This is Wave 0 for NAVV-02a (via-URL shape), NAVV-02b (arrive filter), and NAVV-02c (synthetic follow-route step guards the empty-steps trap).

Purpose: These behaviors were verified live against router.project-osrm.org this session (04-RESEARCH Pattern 1). Getting `waypoints=0;{last}` wrong produces ~199 spurious mid-route "Arrived!" banners on a 200-point route; getting the synthetic step wrong makes follow-route render no guidance at all. Both are silent on-device failures with no test to catch them — so the tests are the point.
Output: extended OsrmClient.kt, OsrmViaUrlTest.kt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The verified OSRM mechanics + patterns this plan implements exactly:
@.planning/phases/04-strava-route-import-navigation/04-CONTEXT.md
@.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md

# The class this plan extends — getRoute is 2-point ONLY; add getRouteVia beside it, reuse NavigationStep/RouteResult + the GeoJSON parse:
@phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt

# Waypoint type + StepInfo shape the RouteResult carries:
@shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pure buildViaUrl + filterArriveSteps + buildFollowRouteResult with tests</name>
  <files>phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt, phone/src/test/java/com/rokid/hud/phone/OsrmViaUrlTest.kt</files>
  <read_first>
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Pattern 1 buildViaUrl verified shape; Pattern 2 non-final-arrive filter; Pattern 3 synthetic "Follow route" step + WHY it is mandatory — HudStreamingService.sendStepsList:577 early-returns on empty)
    @phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt (BASE_URL const = ".../route/v1/driving"; NavigationStep + RouteResult data classes; existing lng,lat coordinate order)
    @shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt (Waypoint)
  </read_first>
  <behavior>
    - buildViaUrl(points): coordinates joined as "{lng},{lat};{lng},{lat};..." (lng first — matches existing getRoute), suffixed with "?overview=full&geometries=geojson&steps=true&waypoints=0;{lastIndex}" where lastIndex = points.size-1
    - buildViaUrl with 4 points → the waypoints param is exactly "waypoints=0;3"
    - buildViaUrl with 2 points → "waypoints=0;1"
    - The profile segment stays "driving" (public host ignores profile — VERIFIED; do NOT switch on sport)
    - filterArriveSteps: drops any step where maneuver=="arrive" && distance==0.0 && it is NOT the last index; keeps a final zero-distance arrive; keeps all non-arrive steps
    - filterArriveSteps on a list with one mid-route zero-distance arrive + a final arrive → mid dropped, final kept, count decreases by exactly 1
    - buildFollowRouteResult(downsampled): RouteResult whose steps has size exactly 1, that step's instruction == "Follow route", maneuver == "straight", located at the first waypoint; waypoints == the downsampled input; never empty steps
  </behavior>
  <action>
    Extend `object OsrmClient` (additive — leave `getRoute` and `parseRouteResponse` untouched). Add three PURE functions (no network, so OsrmViaUrlTest needs no Android/HTTP):

    `fun buildViaUrl(points: List<Waypoint>): String` — per 04-RESEARCH Pattern 1: `val coords = points.joinToString(";") { "${it.longitude},${it.latitude}" }`; `val last = points.size - 1`; return `"$BASE_URL/$coords?overview=full&geometries=geojson&steps=true&waypoints=0;$last"`. BASE_URL stays the existing "https://router.project-osrm.org/route/v1/driving" (profile ignored by the public host — VERIFIED; the follow-route fallback is the only real bike-path mitigation, not a profile switch).

    `fun filterArriveSteps(steps: List<NavigationStep>): List<NavigationStep>` — per Pattern 2: `steps.filterIndexed { i, s -> !(s.maneuver == "arrive" && s.distance == 0.0 && i != steps.lastIndex) }`. This is a no-op on the happy path (waypoints=0;last yields exactly one final arrive) but is correct even if a host ignores the param.

    `fun buildFollowRouteResult(downsampled: List<Waypoint>): RouteResult` — per Pattern 3: a single synthetic `NavigationStep(instruction="Follow route", maneuver="straight", distance=0.0, duration=0.0, locationLat=downsampled.first().latitude, locationLng=downsampled.first().longitude)`; `RouteResult(waypoints=downsampled, steps=listOf(followStep), totalDistance=<sum of haversine legs or 0.0>, totalDuration=0.0)`. The synthetic step is MANDATORY — an empty steps list makes HudStreamingService.sendStepsList early-return (line 577) and the glasses render the route line with no guidance (Pitfall 1). Guard downsampled.isEmpty() by returning an empty RouteResult only when there are literally no points.

    Write OsrmViaUrlTest asserting every <behavior> bullet with hand-built Waypoint/NavigationStep lists (pure, zero network).
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "com.rokid.hud.phone.OsrmViaUrlTest" -q</automated>
  </verify>
  <done>OsrmViaUrlTest passes; buildViaUrl emits waypoints=0;{last} with lng,lat order; filterArriveSteps drops non-final zero-distance arrives; buildFollowRouteResult yields exactly one non-empty "Follow route" step.</done>
  <acceptance_criteria>
    - `grep -c 'waypoints=0;' phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt` returns >= 1
    - `grep -c 'Follow route' phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt` returns >= 1
    - OsrmViaUrlTest asserts the 4-point URL contains exactly "waypoints=0;3"
    - OsrmViaUrlTest asserts filterArriveSteps keeps a final zero-distance arrive and drops a mid one
    - OsrmViaUrlTest asserts buildFollowRouteResult(...).steps.size == 1 and instruction == "Follow route"
    - `grep -c 'fun getRoute(' phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt` still returns 1 (existing 2-point path untouched)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: getRouteVia network method wiring the pure seams + single-leg step parse</name>
  <files>phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt</files>
  <read_first>
    @phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt (parseRouteResponse — the GeoJSON + legs→steps parse to reuse; HttpURLConnection GET convention; buildInstruction/toManeuverKey helpers)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Environment Availability — OSRM online by design, follow-route is the degrade; A5 the single-leg steps render identically)
  </read_first>
  <action>
    Add `fun getRouteVia(points: List<Waypoint>): RouteResult` to OsrmClient. BLOCKING (Thread{} only — OsrmClient convention, documented). Build the URL via `buildViaUrl(points)`; open an HttpURLConnection GET exactly like `getRoute` (same User-Agent, 10s connect/read timeouts, disconnect in finally). Parse the body: reuse the existing GeoJSON geometry→waypoints and legs→steps logic from parseRouteResponse (extract a shared private `parseRouteBody(body): RouteResult` that BOTH getRoute and getRouteVia call, to avoid a second copy of the parse — refactor, do not duplicate). After building the flat step list, apply `filterArriveSteps(...)` before constructing the returned RouteResult (belt-and-braces against any host that ignores waypoints=0;last).

    Do NOT swallow the failure here — let getRouteVia throw on OSRM error/non-200/parse failure (matching getRoute, which throws "OSRM error"). The follow-route fallback is chosen by the CALLER (Plan 05 importer / Plan 03 NavigationManager) via try/catch → buildFollowRouteResult — keeping getRouteVia a pure "route or throw" like getRoute. Add a KDoc line stating: URL is ~4KB at 200 coords (VERIFIED HTTP 200 <1s); if a future >~500-coord request is rejected, the caller must reduce the via-point count.

    Because this task adds a network method whose only new logic beyond the pure Task-1 seams is HTTP + the shared parse refactor, its automated coverage is: OsrmViaUrlTest (Task 1, unchanged) + the full suite staying green after the parseRouteBody refactor (proving getRoute still parses). The live network path is device-verified in Plan 06.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "com.rokid.hud.phone.*" -q</automated>
  </verify>
  <done>getRouteVia exists, builds the via-URL, reuses the shared parse, applies the arrive filter, and throws on OSRM failure (caller does fallback). Existing OsrmClient tests + getRoute path unchanged (suite green).</done>
  <acceptance_criteria>
    - `grep -c 'fun getRouteVia' phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt` returns 1
    - `grep -c 'buildViaUrl' phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt` returns >= 2 (defined + called in getRouteVia)
    - `grep -c 'filterArriveSteps' phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt` returns >= 2 (defined + called)
    - getRouteVia calls the shared parse (parseRouteBody), not a second copy — `grep -c 'getJSONObject("geometry")' phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt` returns 1 (parse logic exists once)
    - Full phone test suite green (no regression in getRoute).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OsrmClient → router.project-osrm.org | Coordinates (from Strava-derived, validated waypoints) sent over HTTPS; response parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Info-disclosure | Plaintext HTTP to OSRM | mitigate | BASE_URL is https:// (existing getRoute convention preserved); no http:// literal introduced |
| T-04-06 | DoS / Tampering | Malformed OSRM response crashes the routing thread | mitigate | Reuse existing parseRouteBody which throws on code != "Ok"; caller (Plan 03/05) wraps in try/catch → follow-route fallback, so a bad response degrades gracefully, never crashes |
| T-04-07 | Tampering | Over-long URL rejected by host (>500 coords) | mitigate | Upstream <=200 cap (Plan 01 downsampleForRoute) keeps the URL ~4KB (VERIFIED 2.5x margin); KDoc instructs caller to reduce count if a host rejects |
| T-04-08 | Spoofing (UX) | Follow-route renders no guidance (empty steps trap) | mitigate | buildFollowRouteResult injects exactly one synthetic "Follow route" step (never empty) so sendStepsList does not early-return (Pitfall 1); unit-asserted |
</threat_model>

<verification>
- OsrmViaUrlTest green (pure URL/filter/synthetic-step assertions).
- Full phone suite green after the parseRouteBody refactor (getRoute unchanged).
- The live via-point request is a Plan 06 device check (single leg, no spurious mid-route arrive in logcat).
</verification>

<success_criteria>
- NAVV-02 core mechanics exist and are unit-tested: waypoints=0;{last} URL, non-final-arrive filter, non-empty synthetic follow-route step.
- getRouteVia added without touching the existing 2-point getRoute behavior.
- All new tests pass; no existing test regresses.
</success_criteria>

<output>
Create `.planning/phases/04-strava-route-import-navigation/04-02-SUMMARY.md` when done.
</output>
