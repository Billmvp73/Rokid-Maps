---
phase: 04-strava-route-import-navigation
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt
autonomous: true
requirements: [RIMP-01, RIMP-02]

must_haves:
  truths:
    - "getRoutes(page, perPage) fetches the athlete's routes over the authenticated Phase-3 client from the singular /athlete/routes endpoint and deserializes to List<StravaRoute>"
    - "exportGpx(idStr) fetches the raw GPX body for a route using its id_str"
    - "Both calls reuse the Phase-3 proactive-refresh token (ensureFreshToken) and log both rate-limit header pairs"
    - "A 429 is distinguishable by the caller so the UI can show a rate-limit message; other failures return empty/null gracefully (never throw)"
  artifacts:
    - path: "phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt"
      provides: "getRoutes() + exportGpx() authenticated route endpoints with 429 signalling"
      contains: "fun getRoutes"
  key_links:
    - from: "StravaApiClient.getRoutes"
      to: "buildRoutesUrl (Plan 01 pure builder)"
      via: "singular /athlete/routes URL"
      pattern: "buildRoutesUrl"
    - from: "StravaApiClient.exportGpx"
      to: "buildExportGpxUrl (Plan 01 pure builder)"
      via: "id_str export path"
      pattern: "buildExportGpxUrl"
    - from: "StravaApiClient"
      to: "StravaAuthManager.ensureFreshToken"
      via: "Phase-3 proactive refresh"
      pattern: "ensureFreshToken"
---

<objective>
Extend the Phase-3 `StravaApiClient` with the two route endpoints Phase 4 needs: `getRoutes()` (browse the athlete's saved/starred routes — RIMP-01) and `exportGpx()` (import a route as GPX — RIMP-02). Both reuse the existing authenticated OkHttp client, the proactive-refresh token, the rate-limit logging, and the `StravaAuthenticator` 401 fallback — this is a pure extension of the delivered Phase-3 transport, calling the pure URL builders from Plan 01.

Purpose: RIMP-01/02 need authenticated Strava reads. The Phase-3 SUMMARY explicitly names StravaApiClient as "the authenticated transport for routes + uploads" — this plan spends that investment. The two sharp edges: the singular `/athlete/routes` endpoint (the by-id form 403s — Pitfall 6) and surfacing 429 distinctly so the UI can show "rate limit — try again shortly" (locked decision).
Output: extended StravaApiClient.kt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked decisions (rate-limit logging + 429 toast; singular /athlete/routes; id_str export) + the exact extension shape:
@.planning/phases/04-strava-route-import-navigation/04-CONTEXT.md
@.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md

# The Phase-3 client this plan extends — getAthlete() is the exact pattern to mirror (ensureFreshToken PRIMARY, logRateLimits, never rethrow), private client/gson/BASE, StravaAuthenticator already wired:
@phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt

# Phase-3 auth API surface (ensureFreshToken, connectedAthleteName) + the Phase-3 SUMMARY handoff note ("StravaApiClient is the authenticated transport for routes"):
@.planning/phases/03-strava-authentication/03-02-SUMMARY.md

# The StravaRoute models + buildRoutesUrl/buildExportGpxUrl pure builders this plan calls (Plan 01 output):
@phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: getRoutes() + exportGpx() on the authenticated client with 429 signalling</name>
  <files>phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt</files>
  <read_first>
    @phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt (getAthlete() — mirror it exactly: ensureFreshToken() PRIMARY → Bearer GET → logRateLimits → Gson parse → never rethrow; the private client, gson, BASE, logRateLimits members are all in scope for the new methods)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Code Examples §StravaApiClient extension getRoutes/exportGpx; Pitfall 6 singular /athlete/routes; A2 device-verify the endpoint, A3 read_all covers private; locked 429 → toast decision)
    @phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt (StravaRoute, buildRoutesUrl(page, perPage), buildExportGpxUrl(idStr) — call these, do not re-inline the URL)
  </read_first>
  <action>
    Add two BLOCKING methods to `StravaApiClient` (Thread{} only — documented class convention; NetworkOnMainThreadException otherwise), mirroring getAthlete() precisely.

    `fun getRoutes(page: Int = 1, perPage: Int = 30): RoutesResult` — proactive `auth.ensureFreshToken()` PRIMARY (null → return the not-connected result); build the URL via `buildRoutesUrl(page, perPage)` (Plan 01 — the SINGULAR /athlete/routes form; the by-id /athletes/{id}/routes form 403s even for your own id, Pitfall 6); Bearer GET on the existing `client`; `logRateLimits(resp)`; on `resp.code == 429` return a distinct rate-limited result (so Wave 3 shows the locked "Strava rate limit — try again shortly" toast); on other non-2xx `Log.w` + return an empty/failed result; on 2xx `gson.fromJson(body, Array<StravaRoute>::class.java)?.toList()`. Wrap in try/catch → Log.e + failed result (never rethrow — CLAUDE.md convention).

    `fun exportGpx(routeIdStr: String): GpxResult` — same auth/log discipline; URL via `buildExportGpxUrl(routeIdStr)` (read_all scope, granted Phase 3, covers private routes — A3); return the raw GPX String on 2xx (application/gpx+xml), a distinct rate-limited result on 429, null/failed otherwise. Never rethrow.

    Model the return types as small sealed results (or a data class carrying `rateLimited: Boolean`) so 429 is distinguishable from an empty list and from a generic failure — the locked decision requires the UI to tell "rate limit" apart from "no routes"/"error". Keep them in this file (co-located with the client). Reuse the private `logRateLimits` (do NOT duplicate it).

    Automated coverage: these are network methods whose only new pure logic (the URL) is already unit-tested in Plan 01's StravaRouteModelTest (buildRoutesUrl/buildExportGpxUrl). This task's verify is the full suite compiling + staying green (signatures + Gson model wiring) — the live authenticated fetch is a Plan 06 device check (real route list, real GPX import). This matches the Phase-3 discipline: pure builders tested, the `execute()` line device-verified.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest assembleDebug -q</automated>
  </verify>
  <done>getRoutes()/exportGpx() exist, call the Plan-01 URL builders, reuse ensureFreshToken + logRateLimits, signal 429 distinctly, never rethrow; :phone assembleDebug + full test suite green (StravaRouteModelTest still covers the URL builders).</done>
  <acceptance_criteria>
    - `grep -c 'fun getRoutes' phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt` returns 1
    - `grep -c 'fun exportGpx' phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt` returns 1
    - `grep -c 'buildRoutesUrl' phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt` returns 1 and `grep -c 'buildExportGpxUrl' ...` returns 1 (calls the Plan-01 pure builders, no inlined URL)
    - `grep -c 'ensureFreshToken' phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt` returns >= 3 (getAthlete + getRoutes + exportGpx all proactive-refresh)
    - `grep -c '429' phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt` returns >= 1 (429 signalled distinctly)
    - `grep -cE 'Log\.[iwe]\(TAG, ".*(accessToken|refreshToken|client_secret|Bearer )' phone/src/main/java/com/rokid/hud/phone/strava/StravaApiClient.kt` returns 0 (no token material logged — the Phase-3 no-secrets gate stays green)
    - `:phone:assembleDebug` exits 0
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phone → Strava API | Authenticated HTTPS GET carrying the Bearer token; route id_str placed in the URL path |
| Strava API → app | Untrusted JSON route list + GPX body returned (parsed by Plan 01 GpxParser / Gson models) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-13 | Info-disclosure | Bearer token / route data logged | mitigate | Reuse Phase-3 logRateLimits (headers only); no token/URI in logs (acceptance grep asserts 0); wire logging DEBUG-only at BASIC inherited from the existing client |
| T-04-14 | Tampering | Route id injected into export_gpx path | mitigate | id_str originates from Strava's own JSON (trusted origin) and is a numeric string — no user free-text in the path (04-RESEARCH Security) |
| T-04-15 | Info-disclosure | Plaintext HTTP | mitigate | BASE is https://www.strava.com/api/v3 (inherited); OkHttp rejects cleartext on modern targetSdk; no http:// literal introduced |
| T-04-16 | DoS | 429 rate-limit hammering | mitigate | 429 surfaced distinctly → UI shows the locked "try again shortly" toast (no auto-retry loop); single-user well under 100 reads/15min (04-RESEARCH) |
</threat_model>

<verification>
- `:phone:assembleDebug` + full `:phone:testDebugUnitTest` green (Gson model wiring + signatures; Plan-01 URL-builder tests still cover the URLs).
- No-secrets log gate green (no token material in any new log line).
- Live authenticated route list + GPX import are Plan 06 device checks (A2 confirms the singular endpoint on a real token).
</verification>

<success_criteria>
- RIMP-01: getRoutes() returns the athlete's routes from the singular /athlete/routes endpoint over the authenticated client.
- RIMP-02: exportGpx() returns the raw GPX body using id_str.
- Both reuse Phase-3 proactive refresh + rate-limit logging; 429 is distinguishable for the UI.
- No new dependency, no new secret, no token logged; suite green.
</success_criteria>

<output>
Create `.planning/phases/04-strava-route-import-navigation/04-04-SUMMARY.md` when done.
</output>
