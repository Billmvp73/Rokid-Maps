---
phase: 04-strava-route-import-navigation
plan: 06
type: execute
wave: 4
depends_on: ["04-05"]
files_modified:
  - .planning/phases/04-strava-route-import-navigation/04-06-DEVICE-VERIFICATION.md
autonomous: false
requirements: [RIMP-01, RIMP-02, RIMP-03, RIMP-04, NAVV-01, NAVV-02, NAVV-03]

must_haves:
  truths:
    - "On a real Strava route: the list shows name/distance/elevation, tap imports GPX, preview draws the route line fitted to bounds"
    - "START NAVIGATION shows the route line + turn-by-turn steps on the glasses; logcat shows a SINGLE leg with no spurious mid-route 'arrive'"
    - "A winding/switchback route navigates without the direction arrow flipping 180 degrees (butterfly avoided)"
    - "Off-route detection fires when deviating, and the reroute preserves the route shape (line stays curvy, not a straight 2-point collapse)"
    - "Forcing an OSRM failure degrades to follow-route: 'Follow route' label + distance on glasses, route line still drawn"
    - "The pending Phase-3 live Authorize and Phase-2 fold-in device spots are exercised in the same phone session"
  artifacts:
    - path: ".planning/phases/04-strava-route-import-navigation/04-06-DEVICE-VERIFICATION.md"
      provides: "Recorded device-verification results (pass/fail per check, logcat evidence, epsilon/cap sanity note)"
      contains: "Verification Results"
  key_links:
    - from: "device verification"
      to: "the full user story (RIMP-01..04, NAVV-01..03)"
      via: "real Strava route end-to-end on both devices"
      pattern: "START NAVIGATION"
---

<objective>
Verify Phase 4 end-to-end on the real devices with the user's own Strava route — the one part of this phase that cannot be unit-tested (live Strava fetch, OSRM road-snapping, glasses rendering, butterfly-free switchbacks, off-route reroute shape, follow-route fallback). This session BATCHES the pending Phase-3 live Authorize tap and the Phase-2 fold-in device spots into ONE phone session (per STATE + VALIDATION), so the whole Strava chain is proven at once. The orchestrator drives the adb-scriptable steps; the human performs the two things only a human can (tap Authorize in the browser; confirm the on-glasses route shape looks right) and picks a real route.

Purpose: 04-VALIDATION §Device-only + §Human moment. Every seam has a unit test (Waves 1-3); this plan is the device gate that must pass before /gsd-verify-work. It also settles the STATE device-tuning knobs (DP epsilon 15m, <=200 cap) and the open assumptions A2 (singular /athlete/routes on a live token) and A4 (epsilon on a real route).
Output: 04-06-DEVICE-VERIFICATION.md with pass/fail + evidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# The device-verification script + human moment this plan executes:
@.planning/phases/04-strava-route-import-navigation/04-VALIDATION.md
@.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md
@.planning/phases/04-strava-route-import-navigation/04-CONTEXT.md

# The Phase-3 device gate (03-04) items to fold into this batched session (live Authorize, persistence/rotation, forged-callback):
@.planning/ROADMAP.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Strava-connected precondition — HALT gate</name>
  <read_first>
    @.planning/phases/04-strava-route-import-navigation/04-VALIDATION.md (Human moment — batches the pending Phase-3 Authorize)
    @.planning/phases/03-strava-authentication/03-02-SUMMARY.md (User Setup Still Required: local.properties keys + Strava app callback domain rokidhud)
  </read_first>
  <what-built>
    The entire Phase-4 route import + navigation flow is built and unit-tested (Waves 1-3). It requires a live Strava connection to verify. Phase 3's live Authorize was pending and is batched here.
  </what-built>
  <action>
    Orchestrator: build + install both debug APKs (`:phone:assembleDebug :glasses:assembleDebug`, then adb install to the real devices), then present this precondition checkpoint and BLOCK on the human confirming a live Strava connection + a real route. If the human reports "halt", stop the Phase-4 device verification and report — do NOT proceed to the automated checks.
  </action>
  <how-to-verify>
    Preconditions the human must confirm BEFORE the automated device checks run:
    1. `strava.client.id` and `strava.client.secret` are present in the main checkout's local.properties, and the Strava API app's Authorization Callback Domain is exactly `rokidhud` (Phase-3 setup). If not configured, HALT and report — Phase 4 cannot be device-verified without it.
    2. Install the phone + glasses debug APKs on the real devices (`:phone:assembleDebug :glasses:assembleDebug`, adb install).
    3. In the phone app, tap "Connect Strava" and complete the OAuth login in the browser (the human-only Authorize tap — also satisfies the pending Phase-3 live-auth check). Confirm the card shows "Connected as {name}".
    4. Confirm the user HAS at least one saved/starred route on their Strava account (needed for RIMP-01). A route with a winding/switchback section is ideal for the butterfly check.
  </how-to-verify>
  <done>Human confirmed "connected" (Strava card shows Connected + a real route exists) OR reported "halt" with a reason (device verification stopped and reported).</done>
  <resume-signal>Type "connected" once the card shows Connected and a real route exists, or "halt: {reason}" if Strava is not configured/connected (Phase 4 device verification cannot proceed — report and stop).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Route browse → import → preview → glasses navigation (the core user story)</name>
  <read_first>
    @.planning/phases/04-strava-route-import-navigation/04-VALIDATION.md (Device-only checklist)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (A2 singular endpoint; A4 epsilon; Pattern 1 single-leg logcat evidence)
  </read_first>
  <what-built>
    The MY STRAVA ROUTES list, GPX import + Douglas-Peucker downsample, OSRM via-point routing with waypoints=0;{last}, phone-map preview, and glasses route-line + turn-by-turn — all wired through the existing broadcast pipeline (Plans 01-05).
  </what-built>
  <action>
    Orchestrator: start `adb logcat` capture (filter the NavManager / StravaApi / OsrmClient / HudStreamingService tags), then present this checkpoint and BLOCK while the human drives the UI. Record each item's pass/fail + the captured logcat evidence (200 vs 403 on the list, single-leg arrive count, downsampled point count) into 04-06-DEVICE-VERIFICATION.md.
  </action>
  <how-to-verify>
    The orchestrator runs the adb-scriptable capture (logcat) while the human drives the UI. Record each in 04-06-DEVICE-VERIFICATION.md (pass/fail + evidence):
    1. RIMP-01: The MY STRAVA ROUTES section is visible (Strava connected) and lists routes with name, distance (imperial-aware if that toggle is on), and elevation. (Settles A2: the singular /athlete/routes returned the list — capture the "Rate usage" log line and a 200, not a 403.)
    2. RIMP-02/03/04: Tap a route → the GPX imports, downsamples, and the route line previews on the phone map, correctly fitted to bounds (Pitfall 5 — verify it fits on the FIRST import, not just the second). Note the downsampled point count from logcat if surfaced; sanity-check the preview line visually matches the route (A4 epsilon 15m — if the line is visibly wrong/too coarse, note it for an epsilon adjustment).
    3. NAVV-01/02: Ensure streaming is active (glasses connected), tap START NAVIGATION → the glasses show the route line AND turn-by-turn step text. In logcat confirm a SINGLE leg with exactly one final "arrive" — NO spurious mid-route "Arrived!" banners/TTS (the waypoints=0;{last} verification).
    4. Confirm recording did NOT auto-start when selecting/navigating the route (REC-01 opt-in — the recording panel is not recording unless the user started it separately).
  </how-to-verify>
  <done>All four items recorded pass/fail with logcat evidence in 04-06-DEVICE-VERIFICATION.md; single-leg / no-spurious-arrive confirmed; A2 (endpoint) + A4 (epsilon) outcomes noted.</done>
  <resume-signal>Type "verified" with pass/fail per item (and any epsilon/preview note), or describe issues (e.g., 403 on the list, spurious mid-route arrivals, preview off-screen).</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Butterfly avoidance, off-route reroute shape, follow-route fallback + batched Phase-2/3 spots</name>
  <read_first>
    @.planning/phases/04-strava-route-import-navigation/04-VALIDATION.md (winding/switchback + follow-route fallback + off-route)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Pitfall 2 reroute shape; Pitfall 3 butterfly; Pattern 3 follow-route)
  </read_first>
  <what-built>
    The forward-only index + DP spacing (butterfly avoidance), the shape-preserving off-route reroute via remaining waypoints (Plan 03), and the follow-route synthetic-step fallback (Plans 02/03/05).
  </what-built>
  <action>
    Orchestrator: keep the logcat capture running (watch for onRerouting, the single-leg step count, and the follow-route "Follow route" broadcast), present this checkpoint and BLOCK while the human exercises the switchback / off-route / forced-OSRM-failure scenarios and the batched Phase-2/3 spots. Record results + update STATE Pending Todos (epsilon/cap settled; A2/A4 resolved).
  </action>
  <how-to-verify>
    Record each in 04-06-DEVICE-VERIFICATION.md:
    1. NAVV (butterfly, SC#5): Navigate a winding/switchback route. Confirm the glasses direction arrow does NOT flip 180 degrees at a switchback, and the next-waypoint distance does not jump erratically (200m→5m→150m). The route line matches the road (not a zigzag).
    2. NAVV-03 (off-route reroute shape): While navigating, deviate >80m off the route. Confirm off-route detection fires (onRerouting) AND the reroute keeps the route shape — the glasses route line stays curvy/route-following, it does NOT snap to a straight 2-point line to the final destination (Pitfall 2). Forward-only index does not rewind.
    3. NAVV-02 (follow-route fallback): Force an OSRM failure (e.g., airplane-mode the phone's routing reachability at import time, or import a route OSRM cannot snap). Confirm the flow degrades to follow-route: the glasses show a "Follow route" label + distance-to-next-waypoint, and the route line is still drawn (the empty-steps trap is closed — steps_list still broadcasts).
    4. Batched Phase-3 fold-ins (from 03-04, same session): persistence across app restart (no re-login), token still valid / a forced refresh rotates it. Batched Phase-2 fold-in spots (any leftover from 02-04) confirmed on the glasses.
    5. Update STATE Pending Todos: mark the DP-epsilon (15m) and <=200-cap knobs as settled (or note the adjusted epsilon), and resolve the A2/A4 assumptions with the observed result.
  </how-to-verify>
  <done>All five items recorded pass/fail in 04-06-DEVICE-VERIFICATION.md; butterfly-free + shape-preserving reroute + follow-route fallback confirmed; Phase-2/3 fold-ins cleared; STATE Pending Todos updated (epsilon/cap settled, A2/A4 resolved).</done>
  <resume-signal>Type "verified" with pass/fail per item + the epsilon outcome, or describe issues (butterfly flip, reroute collapse, follow-route blank instruction).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Strava/OSRM → device | Real network responses (route list, GPX, OSRM route) exercised end-to-end on hardware |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-21 | DoS / Info-disclosure | Live device run exposes the real Strava/OSRM behavior not covered by units | mitigate | This IS the mitigation — the device gate confirms the unit-tested seams behave on real data (single leg, butterfly-free, shape-preserving reroute, graceful follow-route); HALT gate ensures the run only proceeds with a real connection |
| T-04-22 | Tampering | An unexpected 403 on /athlete/routes (A2) would mean the wrong endpoint form | accept | Documented fallback: if singular 403s, private routes need read_all (already granted) — captured and reported in the verification doc, not a code change gate |
</threat_model>

<verification>
- 04-06-DEVICE-VERIFICATION.md records pass/fail + evidence for every check (RIMP-01..04, NAVV-01..03, butterfly, follow-route, batched Phase-2/3 spots).
- Full `:phone:testDebugUnitTest :shared:testDebugUnitTest` + both assembleDebug green before the device run (regression gate).
- STATE Pending Todos updated: DP-epsilon + <=200-cap settled; A2/A4 resolved.
</verification>

<success_criteria>
- The full user story works on real devices with a real Strava route: browse → import → preview → START NAVIGATION → glasses show route line + guidance.
- Single leg / no spurious mid-route arrivals confirmed in logcat (waypoints=0;{last}).
- Butterfly avoided on a switchback; off-route reroute preserves shape; follow-route fallback shows a label + distance with the line still drawn.
- Pending Phase-3 live Authorize + Phase-2 fold-in spots cleared in the same session.
- If Strava is not connected at device time: HALT and report (no silent pass).
</success_criteria>

<output>
Create `.planning/phases/04-strava-route-import-navigation/04-06-SUMMARY.md` when done, and write 04-06-DEVICE-VERIFICATION.md with the recorded results.
</output>
