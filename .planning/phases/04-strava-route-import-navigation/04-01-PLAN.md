---
phase: 04-strava-route-import-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - phone/build.gradle.kts
  - phone/src/main/java/com/rokid/hud/phone/strava/GpxParser.kt
  - phone/src/main/java/com/rokid/hud/phone/strava/RouteDownsampler.kt
  - phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt
  - phone/src/test/java/com/rokid/hud/phone/strava/GpxParserTest.kt
  - phone/src/test/java/com/rokid/hud/phone/strava/RouteDownsamplerTest.kt
  - phone/src/test/java/com/rokid/hud/phone/strava/StravaRouteModelTest.kt
autonomous: true
requirements: [RIMP-01, RIMP-02, RIMP-03]

must_haves:
  truths:
    - "A GPX string with <trkpt lat lon> elements parses to an ordered list of coordinates; malformed GPX yields an empty list, never a crash"
    - "A dense track downsamples via Douglas-Peucker (epsilon 15m) preserving switchback shape, and never exceeds 200 output points (raise-epsilon loop caps it)"
    - "Strava route JSON deserializes with integer type/sub_type and id_str preserved (no 64-bit truncation), tolerating missing fields"
    - "The Strava routes-list URL is singular /athlete/routes with page/per_page; the export URL uses id_str"
  artifacts:
    - path: "phone/src/main/java/com/rokid/hud/phone/strava/GpxParser.kt"
      provides: "Pure GPX <trkpt> extraction via XmlPullParserFactory (JVM-testable), coordinate validation"
      contains: "XmlPullParserFactory"
    - path: "phone/src/main/java/com/rokid/hud/phone/strava/RouteDownsampler.kt"
      provides: "Equirectangular iterative Douglas-Peucker with <=200 raise-epsilon cap"
      contains: "fun simplify"
    - path: "phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt"
      provides: "StravaRoute/StravaRouteMap Gson models + pure routes/export URL builders"
      contains: "data class StravaRoute"
    - path: "phone/src/test/java/com/rokid/hud/phone/strava/RouteDownsamplerTest.kt"
      provides: "DP correctness + <=200 cap tests"
  key_links:
    - from: "GpxParser.parse"
      to: "RouteDownsampler.simplify"
      via: "List<Waypoint> handoff"
      pattern: "Waypoint"
    - from: "StravaModels URL builders"
      to: "StravaApiClient (Plan 04)"
      via: "pure buildRoutesUrl / buildExportGpxUrl"
      pattern: "athlete/routes"
---

<objective>
Build the three pure, JVM-testable seams that turn a Strava route into navigable coordinates: GPX parsing, Douglas-Peucker downsampling, and the Strava route Gson models (plus the pure URL builders that Plan 04's network methods will call). Add the one new test dependency (`net.sf.kxml:kxml2:2.3.0`, test classpath only) that lets `GpxParser` run on plain JVM.

This is Wave 0 for RIMP-03 (GpxParser/RouteDownsampler tests) and RIMP-01/02 (route-model + URL-builder tests) — every seam here ships with its failing-first test.

Purpose: These are the "getting a verified contract subtly wrong silently breaks navigation" risks (04-RESEARCH). Isolating them as pure functions with unit tests is the whole reason Phase 4 is testable at all.
Output: GpxParser.kt, RouteDownsampler.kt, extended StravaModels.kt, three test files, kxml2 test dep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked decisions + verified facts this plan implements:
@.planning/phases/04-strava-route-import-navigation/04-CONTEXT.md
@.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md
@.planning/phases/04-strava-route-import-navigation/04-VALIDATION.md

# Existing Gson-model + test discipline to mirror (all fields nullable, pure functions tested, network untested):
@phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt
@phone/src/test/java/com/rokid/hud/phone/strava/StravaModelsTest.kt

# Waypoint type this plan consumes (shared module):
@shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add kxml2 test dep + GpxParser (pure, XmlPullParserFactory) with tests</name>
  <files>phone/build.gradle.kts, phone/src/main/java/com/rokid/hud/phone/strava/GpxParser.kt, phone/src/test/java/com/rokid/hud/phone/strava/GpxParserTest.kt</files>
  <read_first>
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Code Examples §GPX parse; Anti-Pattern "android.util.Xml.newPullParser() throws Stub!"; Security §XXE)
    @phone/build.gradle.kts (testImplementation block at lines 81-82 — add kxml2 beside org.json)
    @shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt (Waypoint(latitude, longitude))
  </read_first>
  <behavior>
    - Single-trkseg GPX: all <trkpt lat lon> extracted in document order → List<Waypoint> with correct lat/lng (attribute order lat then lon)
    - Multi-trkseg GPX: every <trkpt> across all segments flattened into one ordered list (v1 continuous-track assumption — 04-RESEARCH A6)
    - <ele> child present or absent: parse succeeds either way (ele is ignored for Waypoint)
    - Malformed/unclosed/garbage XML: returns emptyList(), never throws (logged via Log.w)
    - Empty string / no trkpt: returns emptyList()
    - Coordinate validation: skip any <trkpt> whose lat is not in -90..90, lng not in -180..180, or non-finite (Security V5 — untrusted XML from network)
  </behavior>
  <action>
    Add `testImplementation("net.sf.kxml:kxml2:2.3.0")` to phone/build.gradle.kts immediately after the existing `org.json:json` testImplementation line (mirrors the org.json JVM test-dep trick — production uses Android's built-in kxml2, this satisfies XmlPullParserFactory on the JVM test classpath). No production dependency change.

    Create `object GpxParser` in package `com.rokid.hud.phone.strava` with `fun parse(gpx: String): List<Waypoint>`. Obtain the parser via `XmlPullParserFactory.newInstance().apply { isNamespaceAware = false }.newPullParser()` — NOT `android.util.Xml.newPullParser()` (that throws `RuntimeException: Stub!` on the JVM test classpath, per 04-RESEARCH Anti-Pattern). Do NOT enable DOCDECL/entity processing (defends against XXE — XmlPullParser resolves no external entities by default; keep it that way, never call setFeature(FEATURE_PROCESS_DOCDECL, true)). Walk events; on START_TAG named "trkpt" read `lat`/`lon` attributes via `getAttributeValue(null, ...)`, `toDoubleOrNull()`, validate finite + in range, add `Waypoint(lat, lon)`. Wrap the whole parse in try/catch → `Log.w("GpxParser", ...)` + return whatever was collected (graceful, never throws — CLAUDE.md convention). Import `com.rokid.hud.shared.protocol.Waypoint`.

    Write GpxParserTest covering every bullet in <behavior> using inline GPX string literals (no fixture files needed). Assert list size, first/last coordinates, and emptyList() on the malformed/empty cases.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "com.rokid.hud.phone.strava.GpxParserTest" -q</automated>
  </verify>
  <done>GpxParserTest passes on plain JVM (kxml2 resolves); malformed/multi-seg/out-of-range cases all green; no android.util.Xml reference in GpxParser.kt.</done>
  <acceptance_criteria>
    - `grep -c 'XmlPullParserFactory' phone/src/main/java/com/rokid/hud/phone/strava/GpxParser.kt` returns >= 1
    - `grep -c 'android.util.Xml' phone/src/main/java/com/rokid/hud/phone/strava/GpxParser.kt` returns 0
    - `grep -c 'FEATURE_PROCESS_DOCDECL' phone/src/main/java/com/rokid/hud/phone/strava/GpxParser.kt` returns 0 (no external-entity processing enabled)
    - `grep -c 'net.sf.kxml:kxml2:2.3.0' phone/build.gradle.kts` returns 1
    - GpxParserTest asserts an out-of-range/NaN trkpt is skipped and a malformed GPX returns an empty list
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RouteDownsampler — equirectangular Douglas-Peucker + <=200 raise-epsilon cap with tests</name>
  <files>phone/src/main/java/com/rokid/hud/phone/strava/RouteDownsampler.kt, phone/src/test/java/com/rokid/hud/phone/strava/RouteDownsamplerTest.kt</files>
  <read_first>
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Code Examples §Douglas-Peucker iterative equirectangular; Open Question 1 — the <=200 raise-epsilon loop; Pattern 4)
    @shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt (Waypoint)
  </read_first>
  <behavior>
    - < 3 points in → returned unchanged (endpoints always kept)
    - Colinear points at epsilon=15m → only the two endpoints survive
    - A synthetic switchback (sharp direction reversal) → the reversal vertex is preserved (not collapsed) at epsilon=15m
    - First and last input points are always present in the output
    - Downsample-with-cap: given a dense input that at 15m still yields > 200 points, the raise-epsilon loop returns <= 200 points and preserves proportional shape (endpoints intact)
    - Equirectangular perpendicular distance matches a hand-computed value within tolerance for one known triangle (documents the 0.0009% accuracy claim)
  </behavior>
  <action>
    Create `object RouteDownsampler` in `com.rokid.hud.phone.strava`. Implement per 04-RESEARCH Code Examples §Douglas-Peucker verbatim in spirit: private `perpM(pLat,pLng, aLat,aLng, bLat,bLng): Double` using a local equirectangular projection (one `cos(lat0)` from segment start a; x = toRadians(lng-aLng)*cos0*R, y = toRadians(lat-aLat)*R; project p and b, clamp t to 0..1, return hypot of the rejection). Constant R = 6_371_000.0. `fun simplify(points: List<Waypoint>, epsilonM: Double): List<Waypoint>` = iterative RDP with an explicit `ArrayDeque<Pair<Int,Int>>` index-range stack (NOT recursion — Strava tracks can be 10k+ points, deep recursion risks stack overflow), a BooleanArray keep-mask, endpoints pre-kept, filterIndexed to emit the subset.

    Add `fun downsampleForRoute(points: List<Waypoint>, startEpsilonM: Double = 15.0, maxPoints: Int = 200): List<Waypoint>`: call simplify at startEpsilon; while result.size > maxPoints, multiply epsilon by 1.5 and re-run (Open Q A4 — raise-epsilon loop, NOT naive every-Nth stride which destroys switchback fidelity, 04-RESEARCH Anti-Pattern). Guard against an infinite loop (cap iterations, e.g. 40) and return the last result even if still marginally over. This method is the one MainActivity/importer calls; `simplify` stays public for direct-epsilon testing.

    Write RouteDownsamplerTest covering every <behavior> bullet. For the cap test, synthesize a dense zig-zag of ~1000 points spaced ~5m apart so 15m leaves >200, and assert `downsampleForRoute(...).size <= 200` plus first==input.first and last==input.last.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "com.rokid.hud.phone.strava.RouteDownsamplerTest" -q</automated>
  </verify>
  <done>RouteDownsamplerTest passes; switchback vertex preserved; <=200 cap enforced via raise-epsilon; endpoints always retained.</done>
  <acceptance_criteria>
    - `grep -c 'ArrayDeque' phone/src/main/java/com/rokid/hud/phone/strava/RouteDownsampler.kt` returns >= 1 (iterative, not recursive)
    - RouteDownsamplerTest contains a test asserting `downsampleForRoute` output size is <= 200 on a >200-at-15m input
    - RouteDownsamplerTest contains a switchback-preservation assertion
    - `grep -vE '^\s*(//|\*|/\*)' phone/src/main/java/com/rokid/hud/phone/strava/RouteDownsampler.kt | grep -c 'fun downsampleForRoute'` returns 1
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: StravaRoute Gson models + pure routes/export URL builders with tests</name>
  <files>phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt, phone/src/test/java/com/rokid/hud/phone/strava/StravaRouteModelTest.kt</files>
  <read_first>
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Code Examples §Strava route Gson models; Pitfall 4 int type/sub_type + id_str; Pitfall 6 singular /athlete/routes)
    @phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt (existing all-fields-nullable Gson pattern + parseTokenResponse discipline to mirror)
    @phone/src/test/java/com/rokid/hud/phone/strava/StravaModelsTest.kt (existing Gson parse-test style)
  </read_first>
  <behavior>
    - StravaRoute JSON with id/id_str/name/distance/elevation_gain/type(int)/sub_type(int)/private/starred/map parses with all fields populated
    - type and sub_type deserialize as Int? (1=ride 2=run; 1=road..5=mixed) — NOT String
    - id_str preserved verbatim as String (no 64-bit truncation vs the numeric id)
    - Missing fields (partial JSON) parse to null without throwing (Gson-via-Unsafe caveat, mirrors existing StravaModels)
    - A JSON array of routes parses to List<StravaRoute>
    - buildRoutesUrl(page, perPage) → "https://www.strava.com/api/v3/athlete/routes?per_page={perPage}&page={page}" (singular /athlete, per Pitfall 6)
    - buildExportGpxUrl(idStr) → "https://www.strava.com/api/v3/routes/{idStr}/export_gpx" (uses id_str, per Pitfall 4)
  </behavior>
  <action>
    Extend StravaModels.kt (do NOT create a new file — additive, matches package cohesion). Add `data class StravaRoute` and `data class StravaRouteMap` exactly per 04-RESEARCH Code Examples §Strava route Gson models: ALL fields nullable with @SerializedName, `id: Long?`, `idStr: String?` (@SerializedName("id_str")), `name`, `distance: Double?` (meters), `elevationGain: Double?` (@SerializedName("elevation_gain"), meters), `type: Int?`, `subType: Int?` (@SerializedName("sub_type")), `isPrivate: Boolean?` (@SerializedName("private")), `starred: Boolean?`, `map: StravaRouteMap?`; StravaRouteMap { summaryPolyline: String? @SerializedName("summary_polyline") }. Add an optional `fun typeLabel(): String` mapping type 1→"Ride", 2→"Run", else "Route" for the UI (Plan 05 consumes it).

    Add the two pure URL builders as top-level funcs or a small `object StravaRouteUrls` in the same file so Plan 04's network methods AND this test both call them (single source of truth — mirrors Phase-3 pure buildAuthorizeUrl): `buildRoutesUrl(page: Int, perPage: Int): String` and `buildExportGpxUrl(idStr: String): String`, both prefixed with the `https://www.strava.com/api/v3` base. Keep the base as a shared const.

    Write StravaRouteModelTest: Gson `fromJson` a representative single-route JSON (assert int type/sub_type, id_str string, distance/elevation doubles), a partial JSON (assert nulls, no throw), an array JSON → List, and the two URL-builder assertions (exact strings, singular /athlete/routes, id_str in export path).
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "com.rokid.hud.phone.strava.StravaRouteModelTest" -q</automated>
  </verify>
  <done>StravaRouteModelTest passes; type/sub_type are Int; id_str preserved; both URL builders assert singular /athlete/routes and id_str export path.</done>
  <acceptance_criteria>
    - `grep -c 'val type: Int?' phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt` returns 1 (int, not String)
    - `grep -c 'id_str' phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt` returns >= 1
    - `grep -c '/athlete/routes' phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt` returns >= 1 (singular)
    - StravaRouteModelTest asserts buildExportGpxUrl uses the id_str argument in the path
    - Full quick suite still green: no regression in existing strava tests
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strava network → GpxParser | Untrusted GPX (XML) body from the network crosses into the parser |
| Strava network → Gson models | Untrusted JSON route list deserialized into typed models |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering / Info-disclosure | GpxParser XML entity resolution (XXE) | mitigate | XmlPullParserFactory with isNamespaceAware=false and NO DOCDECL/entity feature enabled — XmlPullParser resolves no external entities by default; acceptance grep asserts FEATURE_PROCESS_DOCDECL count = 0 |
| T-04-02 | DoS | Malformed/huge GPX (billion-laughs, giant file) | mitigate | Parse never throws (try/catch → graceful empty); the <=200 downsample cap bounds all downstream memory; parsing runs off the main thread when called by Plan 04/05 (Thread{}) |
| T-04-03 | Tampering | Out-of-range/NaN lat,lng reaching downstream OSRM URL / map | mitigate | GpxParser validates lat in -90..90, lng in -180..180, finite doubles; invalid <trkpt> skipped (acceptance-tested) |
| T-04-04 | Tampering | 64-bit route id truncation → wrong export_gpx target | mitigate | Model uses id_str (String) for the export URL path (Pitfall 4); numeric id kept only for display |
| T-04-SC | Tampering | kxml2 test-classpath dependency | mitigate | test-only (never in either APK); Maven-Central-verified, AOSP-bundled reference impl (04-RESEARCH Package Legitimacy Audit — Approved, not [ASSUMED]/[SUS], so no blocking human checkpoint required) |
</threat_model>

<verification>
- Quick suite (this plan's three test classes) green on plain JVM.
- Full phone suite unaffected: `:phone:testDebugUnitTest` still passes all pre-existing tests (Phase 1/2/3 = 110 tests).
- `kxml2` resolves on the test classpath (GpxParserTest running proves it).
</verification>

<success_criteria>
- RIMP-03 pure seams (GPX parse + Douglas-Peucker with <=200 cap) exist and are unit-tested on plain JVM.
- RIMP-01/02 route models + URL builders exist and are unit-tested (int type/sub_type, id_str, singular /athlete/routes).
- One test-only dependency added; zero production dependency changes.
- All new tests pass; no existing test regresses.
</success_criteria>

<output>
Create `.planning/phases/04-strava-route-import-navigation/04-01-SUMMARY.md` when done.
</output>
