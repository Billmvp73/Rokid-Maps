---
phase: 04-strava-route-import-navigation
plan: 05
type: execute
wave: 3
depends_on: ["04-01", "04-02", "04-03", "04-04"]
files_modified:
  - phone/src/main/res/layout/activity_main.xml
  - phone/src/main/res/layout/item_strava_route.xml
  - phone/src/main/java/com/rokid/hud/phone/MainActivity.kt
autonomous: true
requirements: [RIMP-01, RIMP-02, RIMP-03, RIMP-04, NAVV-01, NAVV-02]

must_haves:
  truths:
    - "When Strava is connected, a 'MY STRAVA ROUTES' section shows the athlete's routes with name, distance (imperial-aware), and elevation gain; when not connected it is hidden"
    - "Loading shows a spinner; no routes shows 'No routes found'; an API/parse error shows a toast; a 429 shows the 'Strava rate limit — try again shortly' toast"
    - "Tapping a route fetches its GPX, parses + downsamples it, routes it via OSRM (or follow-route fallback), and previews the route line on the phone map fitted to bounds"
    - "A START NAVIGATION button on the preview starts navigation on the imported route so the route line and guidance appear on the glasses"
    - "Route import/navigation does NOT auto-start recording (REC-01 opt-in preserved)"
  artifacts:
    - path: "phone/src/main/res/layout/item_strava_route.xml"
      provides: "Route-list row layout (name/distance/elevation)"
      contains: "TextView"
    - path: "phone/src/main/java/com/rokid/hud/phone/MainActivity.kt"
      provides: "route-list load/render/states + GPX import + downsample + OSRM route + preview + START NAVIGATION wiring"
      contains: "startNavigationWithRoute"
  key_links:
    - from: "MainActivity route list"
      to: "StravaApiClient.getRoutes / exportGpx"
      via: "Thread{} fetch, runOnUiThread render"
      pattern: "getRoutes"
    - from: "MainActivity route import"
      to: "GpxParser → RouteDownsampler → OsrmClient.getRouteVia"
      via: "GPX → <=200 waypoints → via-route (or follow-route fallback)"
      pattern: "getRouteVia"
    - from: "MainActivity START NAVIGATION"
      to: "service.startNavigationWithRoute"
      via: "waypoint-accepting nav path"
      pattern: "startNavigationWithRoute"
    - from: "MainActivity preview"
      to: "navMapView fit-to-bounds"
      via: "post{} deferred zoomToBoundingBox (layout-timing fix)"
      pattern: "navMapView.post"
---

## Phase Goal

**As a** cyclist or runner, **I want to** browse my Strava routes, import one, and navigate it on my glasses, **so that** I can follow a planned route with the route line and guidance in my field of view instead of looking at my phone.

*(Derived from ROADMAP Phase 4 Goal "User imports Strava routes and navigates them on glasses" + the project core value. The ROADMAP goal line is outcome-shaped rather than in strict user-story form; this story restates it faithfully without adding scope.)*

## MVP Slice Note

This is the user-visible vertical slice of Phase 4. Waves 1-2 built and unit-tested the seams underneath (GPX parse, downsample, via-routing, the waypoint nav path); this plan assembles them into the first thing a user can actually DO end-to-end: connect → see routes → tap → preview → START NAVIGATION → glasses show the route. The phase-end device verification (Plan 06) confirms the full user story on a real Strava route.

<objective>
Add the "MY STRAVA ROUTES" UI to MainActivity: a Strava-connected-gated list (name / distance / elevation), loading/empty/error/rate-limit states, tap-to-import (GPX → downsample → OSRM via-route or follow-route fallback), route-line preview on the existing osmdroid navMapView (with the layout-timing fix), and a START NAVIGATION button that calls the Wave-2 waypoint nav path. This closes RIMP-01, RIMP-02, RIMP-04, and the phone-side of NAVV-01/02 by wiring every seam built in Waves 1-2 into one flow.

Purpose: This is the vertical slice that makes Phase 4 real for the user. It reuses the Phase-3 connection state (isConnected/connectedAthleteName), the existing updateNavMap Polyline pattern, and the existing navCallback — adding only the route-list section and the import→preview→navigate wiring. Recording stays decoupled (REC-01 opt-in, Pitfall UX "auto-start with route").
Output: extended MainActivity.kt, new item_strava_route.xml, extended activity_main.xml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked UI decisions (Strava-gated section; name/distance/elevation rows; loading/empty/error; 429 toast; preview reuses navMapView; START NAVIGATION; NO recording auto-start):
@.planning/phases/04-strava-route-import-navigation/04-CONTEXT.md
@.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md

# The Activity this plan extends — refreshStravaCard connection-state pattern (300-344), navCallback (192-224), updateNavMap Polyline+BoundingBox (893-912), startNavigation (848-857), formatDist/isImperial (1272+), bindViews (346+):
@phone/src/main/java/com/rokid/hud/phone/MainActivity.kt

# The layout to extend — STRAVA card block (528-590) is where MY STRAVA ROUTES inserts; navStatus preview panel (422+); item_*.xml conventions:
@phone/src/main/res/layout/activity_main.xml

# The seams this plan wires (all from Waves 1-2):
@phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt
@phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt

# The Phase-3 handoff describing the connection-state API to gate on:
@.planning/phases/03-strava-authentication/03-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: MY STRAVA ROUTES layout + route-list load/render with connected-gate + states</name>
  <files>phone/src/main/res/layout/item_strava_route.xml, phone/src/main/res/layout/activity_main.xml, phone/src/main/java/com/rokid/hud/phone/MainActivity.kt</files>
  <read_first>
    @phone/src/main/res/layout/activity_main.xml (STRAVA card 528-590 — insert the routes section directly after it; SectionTitle style; item list conventions; bg_card)
    @phone/src/main/java/com/rokid/hud/phone/MainActivity.kt (refreshStravaCard 300-344 — the connected-gate pattern to reuse: BuildConfig keys → hidden, connectedAthleteName() on a background Thread → visible; bindViews 346+; formatDist 1272 + isImperial; the StravaApiClient/StravaAuthManager fields at 149-254)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Architecture diagram — Strava connected? → list → Thread getRoutes → render rows; loading/empty/error/429 states; imperial-aware distance)
    @phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt (StravaRoute.name/distance/elevationGain/typeLabel)
  </read_first>
  <action>
    Add `item_strava_route.xml` (following the existing item_*.xml convention): a horizontal/vertical row with route name (bold), a second line "distance · elevation" (imperial-aware text supplied by the adapter), styled to match the dark theme (reuse existing colors/text sizes from the Strava card).

    In activity_main.xml, insert a "MY STRAVA ROUTES" section immediately AFTER the STRAVA card's closing tag (~line 590): a vertical LinearLayout (`stravaRoutesCard`, `android:visibility="gone"` by default) with a SectionTitle "MY STRAVA ROUTES", a `ProgressBar` (`stravaRoutesProgress`, gone), a `TextView` empty-state (`stravaRoutesEmpty`, "No routes found", gone), and a `ListView`/`LinearLayout` container (`stravaRoutesList`) for the rows. Match the bg_card + margins of the STRAVA card.

    In MainActivity: bind the new views in bindViews. Add a `StravaApiClient` field (construct with the existing `stravaAuthManager`) if not already present. Extend the SINGLE Strava-card writer discipline — after refreshStravaCard determines connection, gate the routes section: keys empty OR not connected → `stravaRoutesCard` GONE; connected → visible + trigger `loadStravaRoutes()`. `loadStravaRoutes()`: show the spinner, hide empty/list, then `Thread { val result = stravaApiClient.getRoutes() ; runOnUiThread { render } }`. Render mapping: rate-limited result → toast "Strava rate limit — try again shortly" (the locked message) + hide spinner; failure → toast "Couldn't load Strava routes" + hide spinner; empty list → show `stravaRoutesEmpty`, hide spinner; non-empty → populate rows with name + `"${formatDist(distance)} · ${formatElev(elevationGain)}"` (imperial-aware via the existing isImperial()/formatDist; add a small formatElev helper if none exists — feet vs meters), each row's click → Task 2's import flow. Recording is NOT started anywhere here (REC-01 opt-in — Pitfall UX).
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug -q</automated>
  </verify>
  <done>MY STRAVA ROUTES section exists, hidden unless Strava-connected; getRoutes loads on a Thread with spinner/empty/error/429 states; rows show name + imperial-aware distance/elevation; no recording auto-start; :phone assembleDebug green.</done>
  <acceptance_criteria>
    - `grep -c 'stravaRoutesCard' phone/src/main/res/layout/activity_main.xml` returns >= 1 and the section default is `visibility="gone"`
    - `grep -c 'stravaRoutesCard' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` returns >= 1 (gated visible only when connected)
    - `grep -c 'getRoutes' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` returns >= 1 (called on a Thread{})
    - `grep -c 'Strava rate limit' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` returns >= 1 (429 toast — locked message)
    - `grep -c 'startRecording\|startSportRecording' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` in the route-import path is 0 (recording NOT auto-started by route selection)
    - `:phone:assembleDebug` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Tap-to-import → GPX parse → downsample → OSRM via-route (follow-route fallback) → preview</name>
  <files>phone/src/main/java/com/rokid/hud/phone/MainActivity.kt</files>
  <read_first>
    @phone/src/main/java/com/rokid/hud/phone/MainActivity.kt (updateNavMap 893-912 — the Polyline + BoundingBox pattern to reuse for preview; navCallback.onRouteCalculated 192-202 which already sets currentRouteWaypoints + calls updateNavMap; showNavStatus 859-867; currentRouteWaypoints field 160)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Architecture diagram import→downsample→getRouteVia→preview; Pitfall 5 zoomToBoundingBox layout-timing — wrap the preview fit in navMapView.post{}; Pattern 3 follow-route fallback in the catch)
    @phone/src/main/java/com/rokid/hud/phone/strava/StravaModels.kt (StravaRoute.idStr; GpxParser.parse; RouteDownsampler.downsampleForRoute)
    @phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt (getRouteVia; buildFollowRouteResult)
  </read_first>
  <action>
    Implement the row-click import flow on a `Thread{}` (network + parse off the main thread — Security DoS mitigation, big GPX bounded by the <=200 cap): (1) `val gpx = stravaApiClient.exportGpx(route.idStr!!)` — on rate-limited → the locked 429 toast; on null → toast "Couldn't import route"; (2) `val points = GpxParser.parse(gpx)` — if empty → toast "Route had no track points"; (3) `val downsampled = RouteDownsampler.downsampleForRoute(points)` (15m default, <=200 cap from Plan 01); (4) `val result = try { OsrmClient.getRouteVia(downsampled) } catch (e: Exception) { OsrmClient.buildFollowRouteResult(downsampled) }` — the follow-route fallback keeps the flow alive with a non-empty synthetic step when OSRM fails (Pattern 3); track a `followRouteMode` boolean = whether the catch fired. Then `runOnUiThread`: store the result waypoints + steps for the pending navigation (a field, e.g. `pendingRoute: RouteResult` + `pendingFollowRoute: Boolean`), set `currentRouteWaypoints = result.waypoints`, show the nav preview panel, draw the preview via a preview updater that reuses updateNavMap's Polyline but DEFERS the fit: `navMapView.post { navMapView.zoomToBoundingBox(box, false); navMapView.invalidate() }` (Pitfall 5 — the preview map may not be laid out yet on first import; the live-nav path gets away without post{} but the preview does not), and reveal the START NAVIGATION button.

    Do NOT start navigation here — preview only (RIMP-04). START NAVIGATION is Task 3. Keep the existing search→OSRM destination navigation path untouched (this is an additive parallel entry point).
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:assembleDebug -q</automated>
  </verify>
  <done>Tapping a route imports GPX → parse → downsample → getRouteVia (follow-route fallback on failure) → previews the route line on navMapView with a post{}-deferred fit; a pending RouteResult + followRoute flag are held for navigation; no navigation started yet; :phone assembleDebug green.</done>
  <acceptance_criteria>
    - `grep -c 'exportGpx' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` returns >= 1
    - `grep -c 'GpxParser.parse\|RouteDownsampler.downsampleForRoute' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` returns >= 2 (both seams wired)
    - `grep -c 'getRouteVia' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` returns >= 1 and `grep -c 'buildFollowRouteResult' ...` returns >= 1 (fallback wired)
    - `grep -c 'navMapView.post' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` returns >= 1 (Pitfall 5 layout-timing fix on the preview fit)
    - `:phone:assembleDebug` exits 0
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: START NAVIGATION wiring → service.startNavigationWithRoute</name>
  <files>phone/src/main/res/layout/activity_main.xml, phone/src/main/java/com/rokid/hud/phone/MainActivity.kt</files>
  <read_first>
    @phone/src/main/java/com/rokid/hud/phone/MainActivity.kt (startNavigation 848-857 — the streaming-bound guard + service call pattern to mirror; btnNavigate wiring 456; navCallback already handles onRouteCalculated/onStepChanged for the glasses broadcast via the service)
    @phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt (startNavigationWithRoute passthrough from Plan 03)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Architecture diagram — START NAVIGATION → service.startNavigationWithRoute(waypoints, steps) → existing NavigationCallback → glasses)
  </read_first>
  <action>
    Add a START NAVIGATION button to the preview panel in activity_main.xml (`btnStartRouteNav`, styled like btnNavigate; place it near the preview so it appears once a route is imported). Wire its click in MainActivity: mirror the existing `startNavigation()` guard (require bound + service != null → else toast "Start streaming first"), then call `service!!.startNavigationWithRoute(pendingRoute.waypoints, pendingRoute.steps, pendingRoute.totalDistance, pendingRoute.totalDuration, pendingFollowRoute)` (the Plan-03 passthrough). The existing navCallback already receives onRouteCalculated/onStepChanged from the service and broadcasts route/step/steps_list to the glasses — so NO new broadcast wiring is needed here (04-RESEARCH boundary note: glasses unchanged). Show the nav status panel (reuse showNavStatus). Recording is still NOT auto-started (REC-01 opt-in). Keep the button hidden until a route is imported (reveal in Task 2's import success).

    Because startNavigationWithRoute fires the same onRouteCalculated the service callback handles, sendRoute + sendStepsList fire automatically; follow-route's one synthetic step means sendStepsList does not early-return — the glasses show the route line + either real turns or the "Follow route" label.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest assembleDebug -q</automated>
  </verify>
  <done>START NAVIGATION button calls service.startNavigationWithRoute with the pending route + followRoute flag; guarded on streaming bound; reuses the existing navCallback/glasses broadcast; recording not auto-started; full :phone build + test suite green.</done>
  <acceptance_criteria>
    - `grep -c 'btnStartRouteNav' phone/src/main/res/layout/activity_main.xml` returns >= 1 and `grep -c 'btnStartRouteNav' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` returns >= 1
    - `grep -c 'service!!.startNavigationWithRoute\|service!!\.startNavigationWithRoute\|startNavigationWithRoute' phone/src/main/java/com/rokid/hud/phone/MainActivity.kt` returns >= 1
    - The START NAVIGATION handler contains the "Start streaming first" bound-guard (mirrors existing startNavigation)
    - `:phone:assembleDebug` exits 0 and the full :phone test suite is green (no Phase 1/2/3 regression)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Strava network → import flow | Untrusted GPX body fetched then parsed on a Thread{} (never main) |
| UI → service navigation | Imported waypoints/steps handed to the service nav path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | DoS | Large/malformed GPX parsed on the main thread → ANR | mitigate | Import runs on a Thread{}; GpxParser never throws (Plan 01); the <=200 downsample cap bounds all downstream memory (Plan 01) |
| T-04-18 | Spoofing (UX) | Route selection silently starts recording | mitigate | Import/navigate never call startRecording (REC-01 opt-in — Pitfall UX "auto-start with route"); acceptance grep asserts 0 recording calls in the import path |
| T-04-19 | DoS (UX) | Preview map no-ops (route off-screen) on first import | mitigate | zoomToBoundingBox deferred via navMapView.post{} (Pitfall 5 layout-timing); acceptance grep asserts the post{} fit |
| T-04-20 | Spoofing (UX) | 429 shown as a generic error, user hammers | mitigate | getRoutes/exportGpx rate-limited results map to the distinct "Strava rate limit — try again shortly" toast (Plan 04 signalling) |
</threat_model>

<verification>
- `:phone:assembleDebug` + full `:phone:testDebugUnitTest` green (UI wiring compiles; no Phase 1/2/3 regression).
- The end-to-end user story (connect → browse → tap → preview → START NAVIGATION → glasses show route line + guidance; imperial-aware rows; 429 toast; follow-route fallback) is verified live in Plan 06 on a real Strava route.
</verification>

<success_criteria>
- RIMP-01: connected-gated MY STRAVA ROUTES list with name/distance(imperial)/elevation + loading/empty/error/429 states.
- RIMP-02 + RIMP-04: tap imports GPX → downsample → route (or follow-route) → previews the route line fitted to bounds (post{} fix).
- NAVV-01/02 (phone side): START NAVIGATION calls the waypoint nav path; the existing callback broadcasts route/step/steps_list to the glasses unchanged.
- Recording stays opt-in (not auto-started); full build + suite green.
</success_criteria>

<output>
Create `.planning/phases/04-strava-route-import-navigation/04-05-SUMMARY.md` when done.
</output>
