---
phase: 04-strava-route-import-navigation
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt
  - phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt
  - phone/src/test/java/com/rokid/hud/phone/NavigationRouteTest.kt
autonomous: true
requirements: [NAVV-01, NAVV-02, NAVV-03]

must_haves:
  truths:
    - "startNavigationWithRoute(waypoints, steps, followRouteMode) fires onRouteCalculated with the passed steps and skips the internal OSRM A→B call"
    - "steps and currentStepIndex are @Volatile and assigned before the callback fires (the Phase-4-owned data-race fix)"
    - "In follow-route mode a forward-only next-waypoint pointer advances and the synthetic 'Follow route' step's live distance updates to the next downsampled waypoint"
    - "Off-route in a waypoint route re-runs via-point routing through the REMAINING downsampled waypoints (preserves route shape), not a 2-point collapse to the final destination"
    - "The forward-only currentStepIndex is preserved (never rewinds) — butterfly/switchback avoidance"
    - "HudStreamingService exposes a startNavigationWithRoute passthrough that feeds the existing NavigationCallback → sendRoute + sendStepsList pipeline unchanged"
  artifacts:
    - path: "phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt"
      provides: "waypoint-accepting nav path, @Volatile race fix, follow-route mode, shape-preserving reroute"
      contains: "fun startNavigationWithRoute"
    - path: "phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt"
      provides: "startNavigationWithRoute passthrough into the existing NavigationManager"
      contains: "startNavigationWithRoute"
    - path: "phone/src/test/java/com/rokid/hud/phone/NavigationRouteTest.kt"
      provides: "fake-callback tests: waypoint path fires onRouteCalculated, follow-route synthetic step, forward-only index"
  key_links:
    - from: "NavigationManager.startNavigationWithRoute"
      to: "NavigationCallback.onRouteCalculated"
      via: "same callback the glasses pipeline consumes"
      pattern: "onRouteCalculated"
    - from: "HudStreamingService.startNavigationWithRoute"
      to: "sendRoute + sendStepsList broadcast"
      via: "existing initNavigation callback"
      pattern: "sendStepsList"
    - from: "NavigationManager off-route reroute"
      to: "OsrmClient.getRouteVia"
      via: "remaining downsampled waypoints"
      pattern: "getRouteVia"
---

<objective>
Add the waypoint-accepting navigation path to `NavigationManager` (the existing `startNavigation` is destination-only), fix the Phase-4-owned `steps`/`currentStepIndex` data race, implement follow-route mode (forward-only next-waypoint pointer + live synthetic-step distance), and make the off-route reroute preserve the Strava route shape instead of collapsing to a 2-point A→B. Wire a thin `startNavigationWithRoute` passthrough on `HudStreamingService` so the existing NavigationCallback → sendRoute/sendStepsList → glasses pipeline is reused with ZERO glasses changes.

This is Wave 0 for NAVV-01 (waypoint path fires onRouteCalculated), NAVV-02c (follow-route synthetic step), and NAVV-03 (shape-preserving reroute + forward-only index) — tested via a fake NavigationCallback.

Purpose: This is the NavigationManager surgery seam (04-RESEARCH). Three sharp edges live here: the empty-steps trap (Pitfall 1), the shape-discarding reroute (Pitfall 2 — the NAVV-03 sharp edge), and the butterfly/switchback race (Pitfall 3). The data-race fix is STATE-assigned to this phase (the only phase that rebuilds NavigationManager).
Output: extended NavigationManager.kt, extended HudStreamingService.kt, NavigationRouteTest.kt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Locked decisions (waypoint path, @Volatile race fix, follow-route flag, reroute strategy) + the exact code seams:
@.planning/phases/04-strava-route-import-navigation/04-CONTEXT.md
@.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md

# The class this plan surgically extends — destination-only start, forward-only index at 72-101, the steps/currentStepIndex race, nearestRouteDistance off-route at 144:
@phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt

# The via-routing seam this plan calls for reroute + follow-route fallback (Plan 02 output):
@phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt

# The service wiring this plan adds a passthrough to (initNavigation callback + sendStepsList empty-guard):
@phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt

# Waypoint / RouteResult / NavigationStep the path consumes:
@shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: startNavigationWithRoute + @Volatile race fix + follow-route mode with tests</name>
  <files>phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt, phone/src/test/java/com/rokid/hud/phone/NavigationRouteTest.kt</files>
  <read_first>
    @phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt (whole file — the NavigationCallback interface, the destination-only startNavigation, the forward-only advance at 72-101 that must NOT be rewound, onLocationUpdate's steps.isEmpty() early-return at 63, the calculateRoute Thread{} that races steps/currentStepIndex)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (NavigationManager waypoint path + @Volatile race fix code sketch; Pattern 3 follow-route synthetic step; Pitfall 1 empty-steps trap; Pitfall 3 forward-only index)
    @shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt (Waypoint)
    @phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt (NavigationStep, RouteResult, buildFollowRouteResult from Plan 02)
  </read_first>
  <behavior>
    - startNavigationWithRoute(waypoints, steps, totalDistance, totalDuration, followRouteMode): sets isNavigating=true, stores waypoints+steps+dest(last waypoint), currentStepIndex=0, then posts onRouteCalculated(waypoints, totalDistance, totalDuration, steps) and (if steps non-empty) onStepChanged(steps[0]) — the fake callback records both
    - steps and currentStepIndex are @Volatile (the race fix); the assignment happens on the CALLER thread before the callback (no Thread{} in this method), so @Volatile publishes it to the main-thread onLocationUpdate reader
    - Routed mode (followRouteMode=false, real steps): existing forward-only proximity advancement is reused unchanged; index only increments
    - Follow-route mode (followRouteMode=true, one synthetic step): onLocationUpdate advances a forward-only nextWaypointIndex toward the next downsampled waypoint, updates the synthetic step's live distance = haversine to that waypoint, and re-emits onStepChanged("Follow route", "straight", thatDistance) — never rewinds the pointer
    - onLocationUpdate with empty steps still early-returns (unchanged) — but follow-route always has the one synthetic step, so it never hits that path
    - currentStepIndex is forward-only across an entire simulated traversal (monotonic non-decreasing) — asserted
  </behavior>
  <action>
    Extend NavigationManager. Change the two raced fields to @Volatile: `@Volatile var steps: List<NavigationStep> = emptyList(); private set` and `@Volatile var currentStepIndex = 0; private set` (STATE-assigned race fix per D — they are written on the calculateRoute Thread{} and read on the main-thread onLocationUpdate + HudStreamingService.sendStepsList). Add `@Volatile private var followRoute = false` and `@Volatile private var nextWaypointIndex = 0`. Also mark `routeWaypoints` @Volatile (read by nearestRouteDistance on the main thread, written on the routing thread).

    Add `fun startNavigationWithRoute(waypoints: List<Waypoint>, steps: List<NavigationStep>, totalDistance: Double, totalDuration: Double, followRouteMode: Boolean)` per 04-RESEARCH sketch: set isNavigating=true, followRoute=followRouteMode, routeWaypoints=waypoints, this.steps=steps, currentStepIndex=0, nextWaypointIndex=0, dest=last waypoint (if any). NO Thread{} — assign on the caller thread so the writes happen-before the mainHandler.post; @Volatile guarantees visibility. Then mainHandler.post { callback.onRouteCalculated(waypoints, totalDistance, totalDuration, steps); if steps.isNotEmpty() callback.onStepChanged(steps[0].instruction, steps[0].maneuver, steps[0].distance) }. Also set a `@Volatile private var isWaypointRoute = false` here (true) — Task 2 reads it in the off-route branch; the destination-only startNavigation sets it false.

    In onLocationUpdate: keep the existing routed-mode advancement exactly (do NOT touch the forward-only index logic at 72-101 — Pitfall 3 butterfly prevention; never add rewind). Add a follow-route branch: when `followRoute` is true, skip maneuver-point advancement; instead advance `nextWaypointIndex` FORWARD-ONLY (while nextWaypointIndex < routeWaypoints.lastIndex and within STEP_ADVANCE_RADIUS_M of routeWaypoints[nextWaypointIndex], increment — never decrement), compute `distToNext = haversineM(lat,lng, routeWaypoints[nextWaypointIndex])`, and mainHandler.post onStepChanged("Follow route", "straight", distToNext). Preserve arrival detection (last waypoint within ARRIVAL_RADIUS_M → onArrived). Reset followRoute=false, nextWaypointIndex=0, isWaypointRoute=false in stopNavigation().

    Write NavigationRouteTest with a fake NavigationCallback (records onRouteCalculated/onStepChanged/onArrived args). Because callbacks post to the main Looper, prefer asserting the SYNCHRONOUS post-conditions that startNavigationWithRoute sets on the caller thread BEFORE the post (isNavigating, steps, currentStepIndex, followRoute, nextWaypointIndex) — those prove the NAVV-01/02c/03 field invariants without needing a Looper on plain JVM (document this choice in the test header, matching the project's no-Robolectric discipline). Assert: (1) after startNavigationWithRoute, `steps` == passed steps, currentStepIndex==0, isNavigating true; (2) driving onLocationUpdate in follow-route mode advances nextWaypointIndex monotonically and holds exactly one "Follow route" step; (3) currentStepIndex never decreases across a routed-mode simulated traversal.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "com.rokid.hud.phone.NavigationRouteTest" -q</automated>
  </verify>
  <done>NavigationRouteTest passes; startNavigationWithRoute sets the field state (steps/index/isNavigating); follow-route keeps one synthetic step with forward-only nextWaypointIndex; routed-mode currentStepIndex is monotonic; steps/currentStepIndex/routeWaypoints are @Volatile.</done>
  <acceptance_criteria>
    - `grep -c '@Volatile' phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt` returns >= 3 (steps, currentStepIndex, routeWaypoints — the race fix)
    - `grep -c 'fun startNavigationWithRoute' phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt` returns 1
    - `grep -c 'nextWaypointIndex--\|nextWaypointIndex -=' phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt` returns 0 (forward-only, no rewind)
    - `grep -c 'currentStepIndex--\|currentStepIndex -=' phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt` returns 0 (forward-only index preserved — Pitfall 3)
    - NavigationRouteTest asserts currentStepIndex is monotonic non-decreasing across a simulated traversal
    - NavigationRouteTest asserts follow-route mode holds exactly one "Follow route" step
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Shape-preserving off-route reroute (NAVV-03) via remaining downsampled waypoints</name>
  <files>phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt</files>
  <read_first>
    @phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt (the off-route block at 104-113 that currently calls calculateRoute(lat,lng,destLat,destLng) — the 2-point reroute that discards route shape)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Pitfall 2 — the NAVV-03 sharp edge; Open Question 3 — pick a reroute strategy explicitly; Anti-Pattern "Rerouting to a single destination on off-route")
    @phone/src/main/java/com/rokid/hud/phone/OsrmClient.kt (getRouteVia + buildFollowRouteResult from Plan 02)
  </read_first>
  <action>
    Implement the DECIDED reroute strategy (this plan resolves 04-RESEARCH Open Q 3): on off-route in a WAYPOINT route (isWaypointRoute==true, set in Task 1's startNavigationWithRoute), re-run via-point routing from the current position through the REMAINING downsampled waypoints — this preserves the Strava route shape (Pitfall 2). Concretely, in onLocationUpdate's off-route branch (inside the existing REROUTE_COOLDOWN_MS gate + onRerouting() emission): when isWaypointRoute, compute remaining = routeWaypoints.subList(progressIndex, routeWaypoints.size) where progressIndex is the forward-only pointer (nextWaypointIndex in follow-route mode; the nearest-forward waypoint index in routed mode), build `listOf(Waypoint(lat,lng)) + remaining`, and on a Thread{} call `OsrmClient.getRouteVia(that)`; on success re-assign steps/waypoints/currentStepIndex=0/nextWaypointIndex=0 and mainHandler.post onRouteCalculated (same forward-only-reset semantics as startNavigationWithRoute — never rewind past the current position); on getRouteVia failure fall back to `OsrmClient.buildFollowRouteResult(listOf(Waypoint(lat,lng)) + remaining)` and set followRoute=true (never a 2-point collapse). Keep the destination-only path (isWaypointRoute==false) calling the existing 2-point calculateRoute unchanged — do NOT regress non-Strava navigation.

    Record the chosen strategy in a KDoc on the reroute path: full via-reroute through remaining waypoints keeps the route shape (Pitfall 2 warns the 2-point degrade "throws away the whole route shape"); the cooldown + forward-only index prevent butterfly (Pitfall 3). This is the "best fidelity" Open-Q-3 option, chosen because getRouteVia already exists this phase (no extra host, no new dependency).

    Automated coverage: the reroute execute() is network-bound, so unit coverage is Task 1's field-invariant assertions (forward-only index preserved through a reroute reset — add a test that simulates an off-route reroute reset and asserts currentStepIndex/nextWaypointIndex do not go negative) plus the full suite staying green. The live wrong-turn behavior (route line stays curvy, not straight) is a Plan 06 device check.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest --tests "com.rokid.hud.phone.NavigationRouteTest" -q</automated>
  </verify>
  <done>Off-route reroute for a waypoint route re-runs getRouteVia through remaining downsampled waypoints (shape-preserving), falls back to follow-route on failure, never a 2-point collapse; destination-only reroute unchanged; forward-only index preserved through reroute; suite green.</done>
  <acceptance_criteria>
    - `grep -c 'getRouteVia' phone/src/main/java/com/rokid/hud/phone/NavigationManager.kt` returns >= 1 (reroute uses via-routing)
    - The off-route branch for a waypoint route does NOT call the 2-point `calculateRoute(...destLat, destLng)` (verified by reading the branch: waypoint reroute goes through getRouteVia + remaining waypoints)
    - A KDoc/comment in NavigationManager records the chosen reroute strategy (full via-reroute through remaining waypoints; Pitfall 2 rationale)
    - NavigationRouteTest still green (forward-only index invariant holds through a simulated reroute reset)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: HudStreamingService.startNavigationWithRoute passthrough</name>
  <files>phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt</files>
  <read_first>
    @phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt (lines 302-325 initNavigation NavigationCallback → sendRoute+sendStepsList+uiCallback wiring; lines 372-378 the existing startNavigation/stopNavigation passthroughs; line 575-580 sendStepsList empty-guard)
    @.planning/phases/04-strava-route-import-navigation/04-RESEARCH.md (Architecture diagram — service.startNavigationWithRoute(waypoints, steps) feeds the existing NavigationCallback; the glasses side is UNCHANGED)
  </read_first>
  <action>
    Add `fun startNavigationWithRoute(waypoints: List<Waypoint>, steps: List<NavigationStep>, totalDistance: Double, totalDuration: Double, followRouteMode: Boolean)` beside the existing `startNavigation` (line 372). It delegates to `navigationManager?.startNavigationWithRoute(waypoints, steps, totalDistance, totalDuration, followRouteMode)`. Do NOT touch initNavigation, the NavigationCallback, sendRoute, sendStep, or sendStepsList — the whole point (04-RESEARCH boundary note) is that the existing callback pipeline broadcasts route/step/steps_list to the glasses UNCHANGED. Because startNavigationWithRoute fires the same onRouteCalculated the callback already handles, sendRoute + sendStepsList fire automatically; because follow-route always carries one synthetic step (Plan 02 buildFollowRouteResult), sendStepsList does NOT early-return (line 577) — the empty-steps trap is closed upstream, not here.

    This is a pure passthrough (no new logic, no new state). Automated coverage is the full suite compiling + staying green (the method signature must match NavigationManager's). The end-to-end broadcast is a Plan 06 device check.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :phone:testDebugUnitTest assembleDebug -q</automated>
  </verify>
  <done>HudStreamingService.startNavigationWithRoute exists and delegates to NavigationManager; initNavigation/sendStepsList untouched; :phone assembleDebug + testDebugUnitTest green (signature matches).</done>
  <acceptance_criteria>
    - `grep -c 'fun startNavigationWithRoute' phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt` returns 1
    - `grep -c 'navigationManager?.startNavigationWithRoute' phone/src/main/java/com/rokid/hud/phone/HudStreamingService.kt` returns 1
    - initNavigation callback block unchanged (sendRoute + sendStepsList still the only broadcasters; no new glasses message type)
    - `:phone:assembleDebug` exits 0 (both signatures compatible)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Routing thread → main-thread readers | NavigationManager.steps/currentStepIndex/routeWaypoints written on a Thread{}, read on the main-thread onLocationUpdate + service.sendStepsList |
| OSRM network → reroute | Off-route reroute sends the current position + remaining validated waypoints over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Tampering (data race) | NavigationManager.steps/currentStepIndex cross-thread visibility | mitigate | @Volatile on steps, currentStepIndex, routeWaypoints; startNavigationWithRoute assigns on the caller thread before the callback post (happens-before) — the STATE-assigned race fix; acceptance grep asserts >=3 @Volatile |
| T-04-10 | Spoofing (UX) | Off-route reroute discards the Strava route shape (2-point collapse) | mitigate | Waypoint reroute re-runs getRouteVia through remaining downsampled waypoints (Pitfall 2), falls back to follow-route, never a 2-point A→B; acceptance verifies the branch does not call the 2-point calculateRoute for waypoint routes |
| T-04-11 | Spoofing (UX) | Butterfly/switchback direction reversal | mitigate | Forward-only currentStepIndex + nextWaypointIndex preserved (no rewind — grep asserts 0 decrements); DP <=200 spacing (Plan 01) is the primary density fix |
| T-04-12 | DoS | Reroute network failure crashes the routing thread | mitigate | getRouteVia wrapped in try/catch on the reroute Thread{} → buildFollowRouteResult fallback (never rethrows; CLAUDE.md convention) |
</threat_model>

<verification>
- NavigationRouteTest green: waypoint-path field invariants, follow-route single synthetic step + forward-only pointer, monotonic currentStepIndex through traversal + reroute reset.
- `:phone:assembleDebug` + full `:phone:testDebugUnitTest` green (Task 3 signature compatibility; no Phase 1/2/3 regression).
- Live glasses broadcast (route line + steps, single leg, no spurious mid-route arrive), butterfly-free switchback, and off-route shape preservation are Plan 06 device checks.
</verification>

<success_criteria>
- NAVV-01: startNavigationWithRoute fires onRouteCalculated with the passed steps (unit-verified field state).
- NAVV-02c: follow-route mode carries exactly one synthetic "Follow route" step (never empty) with live next-waypoint distance.
- NAVV-03: off-route reroute preserves route shape via remaining-waypoint via-routing; forward-only index preserved.
- Data race fixed: steps/currentStepIndex/routeWaypoints @Volatile, assigned before callback.
- Glasses pipeline untouched (service passthrough only); all tests pass; no regression.
</success_criteria>

<output>
Create `.planning/phases/04-strava-route-import-navigation/04-03-SUMMARY.md` when done.
</output>
