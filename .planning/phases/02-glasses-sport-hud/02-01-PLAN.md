---
phase: 02-glasses-sport-hud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - glasses/build.gradle.kts
  - glasses/src/main/java/com/rokid/hud/glasses/SportFormat.kt
  - glasses/src/main/java/com/rokid/hud/glasses/HudState.kt
  - glasses/src/main/java/com/rokid/hud/glasses/HudView.kt
  - glasses/src/test/java/com/rokid/hud/glasses/SportFormatTest.kt
  - glasses/src/test/java/com/rokid/hud/glasses/HudStateTest.kt
autonomous: true
requirements: [HUD-01, HUD-02, HUD-03]
must_haves:
  truths:
    - "Glasses module runs its first plain-JVM unit tests green (junit 4.13.2 on the test classpath — Wave 0 gap closed)"
    - "HudState.toggleLayout() cycles FULL_SCREEN -> SMALL_CORNER -> SPORT -> FULL_SCREEN; MINI_BOTTOM and MINI_SPLIT still return FULL_SCREEN (Phase 2 SC#1 + SC#4 logic)"
    - "applySportState(msg, nowMs) maps all 7 SportStateMessage fields into HudState and stamps lastSportStateAtMs from the injected nowMs (HUD-02 mapping seam)"
    - "sportDisplayMode(nowMs) precedence: NOT_RECORDING (never received or st==idle) > FINISHED (immune to staleness) > STALE_NO_DATA (>10s) > STALE_DIM (>3s) > LIVE"
    - "SportFormat produces elapsed H:MM:SS with hour rollover, pace M:SS with ap=0 -> --:--, speed one-decimal, imperial variants, and the moving dot at the 0.7 m/s threshold"
    - "Both APKs still compile after the SPORT enum addition (all exhaustive when sites updated: toggleLayout, onDraw dispatch, drawModeIndicator)"
  artifacts:
    - path: "glasses/build.gradle.kts"
      provides: "testImplementation junit:junit:4.13.2 (glasses module's first test dependency)"
      contains: "testImplementation"
    - path: "glasses/src/main/java/com/rokid/hud/glasses/SportFormat.kt"
      provides: "Pure-Kotlin object: formatElapsed, formatPace, paceUnit, formatSpeed, speedUnit, movingDot"
      contains: "object SportFormat"
    - path: "glasses/src/main/java/com/rokid/hud/glasses/HudState.kt"
      provides: "SPORT enum value, SportDisplayMode enum, 8 sport fields, applySportState, sportDisplayMode, 3-way toggleLayout"
      contains: "fun sportDisplayMode"
    - path: "glasses/src/test/java/com/rokid/hud/glasses/HudStateTest.kt"
      provides: "Tap-cycle, mini-return, field-mapping, copy-preservation, staleness-ladder tests"
      min_lines: 60
    - path: "glasses/src/test/java/com/rokid/hud/glasses/SportFormatTest.kt"
      provides: "Formatter worked vectors incl. boundaries and imperial"
      min_lines: 40
  key_links:
    - from: "glasses/src/main/java/com/rokid/hud/glasses/HudView.kt"
      to: "drawSportLayout"
      via: "onDraw when(state.layoutMode) SPORT branch (integration point; full body is Plan 02-03)"
      pattern: "MapLayoutMode\\.SPORT -> drawSportLayout"
    - from: "glasses/src/test/java/com/rokid/hud/glasses/HudStateTest.kt"
      to: "HudState.sportDisplayMode"
      via: "staleness-ladder assertions with fabricated nowMs"
      pattern: "sportDisplayMode"
    - from: "glasses/src/main/java/com/rokid/hud/glasses/HudState.kt"
      to: "com.rokid.hud.shared.protocol.SportStateMessage"
      via: "applySportState parameter (shared module, Phase-1 locked)"
      pattern: "import com.rokid.hud.shared.protocol.SportStateMessage"
---

## Phase Goal

**As a** cyclist or runner mid-activity, **I want to** see my elapsed time, current speed or pace, and distance floating on my glasses, updating every second, **so that** I keep my eyes on the road and my phone in my pocket while my ride or run is measured.

(Direct transliteration of the ROADMAP Phase 2 Goal + project Core Value; MVP mode, vertical-slice plan set.)

<objective>
Close the glasses-module Wave 0 test gap (first `testImplementation` in glasses/build.gradle.kts), then ship the pure JVM-testable core of the Sport HUD: the `SportFormat` formatter object, the `SPORT` layout mode, the 8 sport fields on `HudState`, the `applySportState`/`sportDisplayMode` seams, and the 3-way tap cycle — all proven by the repo's first glasses unit tests. The two HudView exhaustive-`when` sites gain their one-line SPORT branches so both APKs keep compiling (Kotlin 2.1 makes non-exhaustive enum `when` a compile error).

Purpose: RESEARCH's testable-seam design — every HUD-02 staleness decision and HUD-03 cycle decision is a pure function with injected `nowMs`, JVM-tested before any device work. Waves 2-3 build on these exact signatures.
Output: Test-enabled glasses build file, `SportFormat.kt`, extended `HudState.kt`, two minimal HudView branch additions, green `SportFormatTest.kt` + `HudStateTest.kt`.

MVP slice: after this plan, SPORT exists in the tap cycle (mode indicator `[ SPORT ]` renders on device) and every metric/staleness rule is locked by tests; live data lands in Wave 2, full rendering in Wave 3.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-glasses-sport-hud/02-CONTEXT.md
@.planning/phases/02-glasses-sport-hud/02-RESEARCH.md
@glasses/src/main/java/com/rokid/hud/glasses/HudState.kt
@glasses/src/main/java/com/rokid/hud/glasses/HudView.kt
@glasses/build.gradle.kts
@shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt

Environment (verified 2026-07-03): there is NO `gradlew` script in this repo. All Gradle invocations MUST use:
`export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain <targets> -q`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install glasses test infrastructure and ship SportFormat with worked-vector tests</name>
  <files>glasses/build.gradle.kts, glasses/src/main/java/com/rokid/hud/glasses/SportFormat.kt, glasses/src/test/java/com/rokid/hud/glasses/SportFormatTest.kt</files>
  <read_first>
    - glasses/build.gradle.kts (dependencies block at lines 32-36 — currently shared + core-ktx + appcompat only)
    - .planning/phases/02-glasses-sport-hud/02-RESEARCH.md — "Standard Stack / Supporting (test-only, Wave 0)", "Code Examples §4 (SportFormat)", "Don't Hand-Roll" inversion note, Pitfall 9 (ap==0 sentinel)
    - shared/src/test/java/com/rokid/hud/shared/protocol/ProtocolCodecTest.kt (the Phase-1 test-class shape to mirror: JUnit4, assertEquals, KDoc noting pure-JVM constraints)
  </read_first>
  <behavior>
    - formatElapsed(945_000L) == "0:15:45"; formatElapsed(3_599_000L) == "0:59:59"; formatElapsed(3_600_000L) == "1:00:00"; formatElapsed(0L) == "0:00:00" (hours roll unbounded, no DateUtils)
    - formatPace(294_000L, imperial=false) == "4:54"; formatPace(294_000L, imperial=true) == "7:53" (294000 x 1.609344 = 473147ms -> 473s); formatPace(0L, false) == "--:--"; formatPace(-5L, true) == "--:--" (ap=0 is the phone's below-100m unset sentinel — Pitfall 9)
    - formatSpeed(6.2, imperial=false) == "22.3" (x3.6); formatSpeed(6.2, imperial=true) == "13.9" (x2.23694); formatSpeed(0.0, false) == "0.0"
    - speedUnit(false) == "km/h"; speedUnit(true) == "mph"; paceUnit(false) == "/km"; paceUnit(true) == "/mi"
    - movingDot(0.8) == "●"; movingDot(0.7) == "○" (strict > 0.7 per CONTEXT "cs above ~0.7"); movingDot(0.0) == "○"
  </behavior>
  <action>
    1. In glasses/build.gradle.kts dependencies block, append exactly: testImplementation("junit:junit:4.13.2"). This is the identical coordinate already resolved in shared/ and phone/ since Phase 1 (RESEARCH Package Legitimacy Audit: Approved, no new supply-chain surface). Do NOT add isReturnDefaultValues or org.json test artifacts — glasses tests are pure Kotlin (RESEARCH Validation Architecture; SportStateMessage in shared Messages.kt is a plain data class with no org.json dependency).
    2. Create glasses/src/main/java/com/rokid/hud/glasses/SportFormat.kt — a PURE-KOTLIN `object SportFormat` (no android.* imports, no Log, no SystemClock — android.jar stubs throw in JVM tests). Package com.rokid.hud.glasses, 4-space indent. Contents per RESEARCH Code Examples §4:
       - private const val MPS_TO_KMH = 3.6, MPS_TO_MPH = 2.23694, KM_TO_MI = 1.609344 — these MUST equal the constants already used in HudView.drawStatusBar/speakInstruction so the SPORT numeral never disagrees with the status strip (Don't Hand-Roll table).
       - const val MOVING_DOT_THRESHOLD_MPS = 0.7 — owned here (only the dot formatter consumes it; the phone owns real hysteresis; deviation from the RESEARCH skeleton which parked it on HudState — keeping it in SportFormat makes this task self-contained and avoids a dead constant on HudState).
       - fun formatElapsed(ms: Long): String — totalSec = ms/1000; String.format("%d:%02d:%02d", totalSec/3600, (totalSec%3600)/60, totalSec%60). Hand-rolled deliberately: DateUtils.formatElapsedTime is an android.jar stub that throws in JVM tests (RESEARCH Don't Hand-Roll inversion).
       - fun formatPace(msPerKm: Long, imperial: Boolean): String — if (msPerKm <= 0L) return "--:--"; msPerUnit = if (imperial) (msPerKm * KM_TO_MI).toLong() else msPerKm; totalSec = msPerUnit/1000; String.format("%d:%02d", totalSec/60, totalSec%60).
       - fun paceUnit(imperial: Boolean) = if (imperial) "/mi" else "/km"
       - fun formatSpeed(mps: Double, imperial: Boolean): String = String.format("%.1f", mps * if (imperial) MPS_TO_MPH else MPS_TO_KMH). Locale-default String.format matches the existing formatDistance convention (RESEARCH Pitfall 10 — pre-existing accepted exposure, do not add Locale.US).
       - fun speedUnit(imperial: Boolean) = if (imperial) "mph" else "km/h"
       - fun movingDot(currentSpeedMps: Double): String = if (currentSpeedMps > MOVING_DOT_THRESHOLD_MPS) "●" else "○" (CONTEXT locked: filled dot moving, hollow stopped; ASCII fallback "*"/"o" is a 1-line change if the 02-04 device screencap shows tofu — RESEARCH A4).
       KDoc on the object: "Pure Kotlin (no android.*) so plain-JVM tests cover it — SessionModels.kt convention."
    3. Create glasses/src/test/java/com/rokid/hud/glasses/SportFormatTest.kt (new directory tree — glasses has zero test dirs today). JUnit4 class covering every behavior case above, one @Test per formatter family, assertEquals on exact strings.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :glasses:testDebugUnitTest -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "testImplementation" glasses/build.gradle.kts returns 1 (exactly the junit line; no org.json, no isReturnDefaultValues)
    - grep -q "object SportFormat" glasses/src/main/java/com/rokid/hud/glasses/SportFormat.kt succeeds
    - grep -q "import android" glasses/src/main/java/com/rokid/hud/glasses/SportFormat.kt fails (exit 1) — file is JVM-pure
    - grep -q "MOVING_DOT_THRESHOLD_MPS = 0.7" glasses/src/main/java/com/rokid/hud/glasses/SportFormat.kt succeeds
    - :glasses:testDebugUnitTest exits 0 executing SportFormatTest (glasses module's first green test run)
  </acceptance_criteria>
  <done>The glasses module resolves JUnit4 on its test classpath and SportFormat's exact output strings (metric + imperial + sentinels) are regression-locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SPORT mode + sport fields + pure seams to HudState, with the two HudView exhaustiveness branches</name>
  <files>glasses/src/main/java/com/rokid/hud/glasses/HudState.kt, glasses/src/main/java/com/rokid/hud/glasses/HudView.kt, glasses/src/test/java/com/rokid/hud/glasses/HudStateTest.kt</files>
  <read_first>
    - glasses/src/main/java/com/rokid/hud/glasses/HudState.kt (whole file, 72 lines: MapLayoutMode enum :6-13, data class fields :22-53, companion :54-56, withNotification :58-61, toggleLayout when-expression :63-70)
    - glasses/src/main/java/com/rokid/hud/glasses/HudView.kt — onDraw dispatch :122-138 (when(state.layoutMode) at :128, no else) and drawModeIndicator :561-562 (when-expression, no else); do NOT read the whole file this task
    - shared/src/main/java/com/rokid/hud/shared/protocol/Messages.kt :75-83 (SportStateMessage: elapsedMs, movingMs, distanceM, currentSpeedMps, avgPaceMsPerKm, sessionState, sport)
    - .planning/phases/02-glasses-sport-hud/02-RESEARCH.md — Code Examples §1 (HudState skeleton), Pattern 1 (pure seam), Pitfall 3 (exhaustive when sites), Pitfall 4 (FINISHED precedence), Pitfall 5 (nowMs injection)
  </read_first>
  <behavior>
    - tapCycleFullCornerSportFull: FULL_SCREEN -> SMALL_CORNER -> SPORT -> FULL_SCREEN via three toggleLayout() calls (locked HUD-03 cycle)
    - miniModesTapReturnToFull: HudState(layoutMode=MINI_BOTTOM).toggleLayout() == FULL_SCREEN; same for MINI_SPLIT (Phase 2 SC#4 preserved)
    - applySportStateMapsAllFieldsAndStampsReceipt: sample = SportStateMessage(945000L, 823000L, 8420.0, 6.2, 294000L, "tracking", "ride"); HudState().applySportState(sample, 12_345L) has elapsedMs=945000, movingMs=823000, distanceM=8420.0, currentSpeedMps=6.2, avgPaceMsPerKm=294000, sessionState="tracking", sport="ride", lastSportStateAtMs=12345
    - sportFieldsSurviveUnrelatedCopies: applySportState(sample, 111L).copy(btConnected=true).withNotification(NotificationItem("t","x","p",1L)) still has lastSportStateAtMs==111L and distanceM==8420.0 (proves RESEARCH Pitfall-2 claim: no copy-site edits needed anywhere)
    - stalenessLadder: state received at nowMs=1_000 with st="tracking": sportDisplayMode(3_999)==LIVE (2999ms old), sportDisplayMode(4_001)==STALE_DIM (3001ms), sportDisplayMode(11_000)==STALE_DIM (exactly 10000ms — strict >), sportDisplayMode(11_001)==STALE_NO_DATA
    - finishedBeatsStaleness: st="finished" received at 1_000; sportDisplayMode(999_999)==FINISHED (Pitfall 4: phone stops the ticker after the final finished broadcast — staleness must never dim FINISHED)
    - neverReceivedIsNotRecording: HudState().sportDisplayMode(99_999)==NOT_RECORDING (lastSportStateAtMs default 0)
    - idleIsNotRecording: applySportState(sample copy with sessionState="idle", 1_000L).sportDisplayMode(1_500)==NOT_RECORDING (CONTEXT: st idle -> NOT RECORDING hint)
  </behavior>
  <action>
    All additions follow RESEARCH Code Examples §1 exactly (field names are CONTEXT-locked).

    1. HudState.kt — add `import com.rokid.hud.shared.protocol.SportStateMessage`. MapLayoutMode enum gains SPORT between SMALL_CORNER and MINI_BOTTOM with KDoc `/** Tap-cycle only: pure sport metrics, no map (HUD-01..HUD-04) */`. Add a top-level `enum class SportDisplayMode { NOT_RECORDING, FINISHED, LIVE, STALE_DIM, STALE_NO_DATA }` with KDoc citing the precedence ladder.
    2. HudState data class gains 8 fields with defaults (append after closingMessage, comment `// Sport HUD (Phase 2) — fed only by sport_state messages`): elapsedMs: Long = 0L; movingMs: Long = 0L (carried for protocol completeness, NOT rendered in v1 — moving time is summary-only, locked deferred decision); distanceM: Double = 0.0; currentSpeedMps: Double = 0.0; avgPaceMsPerKm: Long = 0L (0 = unset, phone's <100m floor); sessionState: String = "idle"; sport: String = "ride"; lastSportStateAtMs: Long = 0L (SystemClock.elapsedRealtime() at receipt; 0 = never received).
    3. Companion object gains: const val SPORT_STALE_DIM_MS = 3_000L and const val SPORT_STALE_NODATA_MS = 10_000L (CONTEXT-locked 3s/10s thresholds).
    4. Add `fun applySportState(msg: SportStateMessage, nowMs: Long): HudState` — a copy() mapping all 7 message fields plus lastSportStateAtMs = nowMs. nowMs is INJECTED — never call SystemClock inside HudState (Pitfall 5: kills JVM testability; the BluetoothClient call site supplies the clock in Plan 02-02).
    5. Add `fun sportDisplayMode(nowMs: Long): SportDisplayMode` — the exact precedence ladder: lastSportStateAtMs==0L || sessionState=="idle" -> NOT_RECORDING; sessionState=="finished" -> FINISHED; nowMs-lastSportStateAtMs > SPORT_STALE_NODATA_MS -> STALE_NO_DATA; > SPORT_STALE_DIM_MS -> STALE_DIM; else LIVE. Unknown future sessionState strings deliberately fall through to the staleness branches (lenient posture matching ProtocolCodec).
    6. toggleLayout() when-expression: SMALL_CORNER -> SPORT (was FULL_SCREEN); add SPORT -> FULL_SCREEN; FULL_SCREEN -> SMALL_CORNER and both MINI_* -> FULL_SCREEN stay unchanged.
    7. HudView.kt — exactly two one-line additions, REQUIRED for compilation (Kotlin 2.1: non-exhaustive when over enum = compile error; RESEARCH Pitfall 3): (a) onDraw dispatch (:128-133) gains `MapLayoutMode.SPORT -> drawSportLayout(canvas, w, h)`; (b) drawModeIndicator when (:562) gains `MapLayoutMode.SPORT -> "[ SPORT ]"`. Create `private fun drawSportLayout(canvas: Canvas, w: Float, h: Float)` with an EMPTY body and this comment: `// Dispatch target for MapLayoutMode.SPORT — Plan 02-03 implements the full CONTEXT-locked metric layout (geometry, dim hierarchy, staleness ticker). Status strip + "[ SPORT ]" indicator are drawn by onDraw after this branch, so SPORT is already visibly distinct.` Make NO other HudView changes this plan (no paints, no ticker). Do NOT touch BluetoothClient's Settings layout mapping — it has an else branch and settings never select SPORT in v1 (Pitfall 3).
    8. Do NOT edit any existing copy() call site in BluetoothClient/HudActivity — Kotlin data-class copy preserves the new fields automatically (Pitfall 2; the sportFieldsSurviveUnrelatedCopies test proves it).
    9. Create glasses/src/test/java/com/rokid/hud/glasses/HudStateTest.kt implementing every behavior case above, JUnit4, mirroring ProtocolCodecTest style. HudState/NotificationItem/SportStateMessage are all android-free, so no stubs needed.
  </action>
  <verify>
    <automated>export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :glasses:testDebugUnitTest :glasses:assembleDebug -q</automated>
  </verify>
  <acceptance_criteria>
    - grep -qE '^\s+SPORT,' glasses/src/main/java/com/rokid/hud/glasses/HudState.kt succeeds (enum value present)
    - grep -q "enum class SportDisplayMode" glasses/src/main/java/com/rokid/hud/glasses/HudState.kt succeeds
    - grep -q "fun applySportState" glasses/src/main/java/com/rokid/hud/glasses/HudState.kt and grep -q "fun sportDisplayMode" both succeed
    - grep -q "SPORT_STALE_DIM_MS = 3_000L" and grep -q "SPORT_STALE_NODATA_MS = 10_000L" glasses/src/main/java/com/rokid/hud/glasses/HudState.kt both succeed
    - grep -q "MapLayoutMode.SMALL_CORNER -> MapLayoutMode.SPORT" and grep -q "MapLayoutMode.SPORT -> MapLayoutMode.FULL_SCREEN" glasses/src/main/java/com/rokid/hud/glasses/HudState.kt both succeed (3-way cycle)
    - grep -q "import android" glasses/src/main/java/com/rokid/hud/glasses/HudState.kt fails (exit 1) — HudState stays JVM-pure
    - grep -q "MapLayoutMode.SPORT -> drawSportLayout" glasses/src/main/java/com/rokid/hud/glasses/HudView.kt succeeds
    - grep -q 'MapLayoutMode.SPORT -> "\[ SPORT \]"' glasses/src/main/java/com/rokid/hud/glasses/HudView.kt succeeds
    - grep -c "MapLayoutMode.SPORT" glasses/src/main/java/com/rokid/hud/glasses/BluetoothClient.kt returns 0 (settings mapping untouched — Pitfall 3)
    - :glasses:testDebugUnitTest exits 0 with SportFormatTest + HudStateTest all green (>= 13 tests total); :glasses:assembleDebug exits 0 (exhaustive when sites satisfied)
  </acceptance_criteria>
  <done>SPORT is a first-class layout mode with tested cycle semantics; the HUD-02 mapping and staleness ladder are pure, injected-clock functions locked by JVM tests; both HudView dispatch sites compile with the SPORT branch in place.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Bonded BT device -> glasses ProtocolCodec.decode -> HudState | Untrusted decoded field values (Longs/Doubles/Strings) enter the new pure functions |
| Maven cache -> glasses test classpath | Test-only dependency added to a third module |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | HudState.applySportState / sportDisplayMode / SportFormat | mitigate | All new functions are total: any Long/Double renders (negative pace -> "--:--" sentinel), unknown sport strings default toward "ride" downstream, unknown sessionState falls to staleness branches — no crash paths, no string ever used as path/URL/exec (ASVS V5 posture matching ProtocolCodec lenient decode) |
| T-02-02 | Information Disclosure | Sport metrics on a shared-view display | accept | sport_state carries metrics only, never coordinates (verified HudStreamingService.kt:617-619, Phase 1 T-04-01 upheld); lower sensitivity than the existing StateMessage which already streams position |
| T-02-SC | Tampering (supply chain) | junit:junit:4.13.2 (test-only) | mitigate | Approved in 02-RESEARCH Package Legitimacy Audit — identical coordinate already resolved and running green in shared/ and phone/ since Phase 1; pinned version, no new registry fetch class; no [ASSUMED]/[SUS] -> no blocking human checkpoint required |
</threat_model>

<verification>
- Full suite green: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :shared:testDebugUnitTest :phone:testDebugUnitTest :glasses:testDebugUnitTest assembleDebug -q` exits 0
- No behavioral change to existing modes: FULL_SCREEN/MINI_* toggle assertions green; Phase 1 shared/phone tests unaffected
</verification>

<success_criteria>
- Glasses module has green JVM tests (Wave 0 closed; Validation Architecture gaps 1-3 done)
- Tap-cycle logic implements Phase 2 SC#1 and preserves SC#4 at the unit level
- HUD-02's mapping + 3s/10s/FINISHED staleness semantics are regression-locked before any BT or rendering work
</success_criteria>

<output>
Create `.planning/phases/02-glasses-sport-hud/02-01-SUMMARY.md` when done. Record: exact test count, confirmation both APKs assemble, and any deviation from the RESEARCH skeletons (e.g., MOVING_DOT_THRESHOLD_MPS ownership).
</output>
