---
phase: 02-glasses-sport-hud
plan: 04
type: execute
wave: 4
depends_on: [02-01, 02-02, 02-03]
files_modified: []
autonomous: true
requirements: [HUD-01, HUD-02, HUD-03, HUD-04]
must_haves:
  truths:
    - "On the physical glasses, three single taps cycle the mode indicator [ FULL ] -> [ CORNER ] -> [ SPORT ] -> [ FULL ] while sport_state streams at 1Hz (Phase 2 SC#1)"
    - "Layout-revert regression: the glasses stay in [ SPORT ] >= 3s into an active 1Hz stream — the RESEARCH critical finding is proven fixed on device"
    - "Two SPORT screencaps ~5s apart show elapsed advanced ~5s and distance grown — real-time updates observed (Phase 2 SC#2)"
    - "Sport-aware primary + units: Ride shows km/h (then mph after imperial toggle); Run shows M:SS pace as primary with speed secondary"
    - "Staleness on device: values dim within ~5s of killing the phone app; NO DATA appears in the primary slot by ~12s; a fresh recording stopped on the phone shows FINISHED still bright >= 15s later"
    - "Mini modes preserved: phone mini toggle -> [ STRIP ] on glasses; one tap returns [ FULL ] (Phase 2 SC#4)"
    - "SPORT screencap pixel scan: every pixel has R==0 and B==0 (green-on-black only — Phase 2 SC#3 / HUD-04)"
    - "Glasses logcat shows zero Unknown-message warnings attributable to sport_state"
  artifacts:
    - path: ".planning/phases/02-glasses-sport-hud/02-04-SUMMARY.md"
      provides: "Phase-gate verdict table mapping each success criterion to device evidence (01-07 SUMMARY format)"
      contains: "verdict"
  key_links:
    - from: "phone HudStreamingService sport_state broadcast (1Hz)"
      to: "glasses HudView SPORT numerals"
      via: "BT SPP -> ProtocolCodec -> applySportState -> onStateUpdate -> drawSportLayout (the full Phase 2 pipeline, observed end-to-end)"
      pattern: "screencap evidence"
---

## Phase Goal

**As a** cyclist or runner mid-activity, **I want to** see my elapsed time, current speed or pace, and distance floating on my glasses, updating every second, **so that** I keep my eyes on the road and my phone in my pocket while my ride or run is measured.

(Direct transliteration of the ROADMAP Phase 2 Goal + project Core Value; MVP mode, vertical-slice plan set.)

<objective>
Prove all four Phase 2 success criteria on the physical devices (OPPO Find X9 Ultra + Rokid glasses) using the Phase 1 mock-GPS harness, with the layout-revert regression as an explicit gate. This plan changes no repo code — it deploys the Wave 1-3 APKs and drives both devices over adb, exactly as 01-07 did under the user's auto-mode grant.

Purpose: HUD-01..04 are display requirements; Canvas output has no JVM-assertable surface, so screencaps + logcat on hardware are the only honest phase gate (02-VALIDATION contract).
Output: Verdict table in 02-04-SUMMARY.md with screencap/logcat evidence in the session scratchpad (outside the repo, per the 01-07 T-07-02 evidence-hygiene precedent — raw coordinates never enter .planning/).

CRITICAL COMMAND SAFETY (supersedes 02-VALIDATION.md's tap-cycle line): NEVER send `input keyevent 66` / KEYCODE_ENTER to the glasses — it is wired to shutdownApp() and kills the app (verified HudActivity.kt:160-167). Tap-cycling is done with `input tap 240 320`, single taps spaced >= 2s (two taps < ~300ms apart register as a double-tap, which is ALSO shutdown — RESEARCH Pitfall 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-glasses-sport-hud/02-RESEARCH.md
@.planning/phases/02-glasses-sport-hud/02-VALIDATION.md
@.planning/phases/01-activity-recording-engine/01-07-SUMMARY.md

Device + harness facts (from 01-07, re-verified in 02-RESEARCH Environment Availability):
- ADB=/opt/homebrew/share/android-commandlinetools/platform-tools/adb (NOT on PATH — always absolute)
- PHONE=3B164G01Y7L00000 (OPPO Find X9 Ultra, Android 16 / ColorOS); GLASSES=1901092544802583 (Rokid, Android 12)
- `$ADB kill-server && $ADB start-server` re-attaches sleeping glasses
- Mock feeder (debug source set only): `$ADB -s $PHONE shell am broadcast -n com.rokid.hud.phone/.MockRouteFeeder -a com.rokid.hud.phone.debug.MOCK_ROUTE --es cmd start --ei loops 3` — profile per loop: 90s moving @5.5 m/s, 60s stationary, 60s moving, 1Hz fixes; stop with `--es cmd stop`
- Phone UI ids driven via screencap + `input tap` in 01-07: btnStart (streaming), btnSportRide/btnSportRun, btnStartRecording; stop-recording uses a confirm dialog
- ColorOS sideload shows a "sensitive permissions" confirm (继续安装) — automatable via input tap
- Expected logcat noise: pre-existing empty-line keep-alive Unknown warnings (contain no "sport_state") — not failures
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preflight both devices, deploy Wave 1-3 APKs, and start the recording + mock-feed pipeline</name>
  <files>none (device operations only; screencaps to the session scratchpad)</files>
  <read_first>
    - .planning/phases/02-glasses-sport-hud/02-RESEARCH.md — "On-Device Verification Plan" steps 1-3, "Environment Availability", Open Questions 1-2
    - .planning/phases/01-activity-recording-engine/01-07-SUMMARY.md — device identity, ColorOS observations, residual items (mock-app selection state)
  </read_first>
  <action>
    1. PRECONDITION GATE: run `$ADB devices`. Both serials (3B164G01Y7L00000, 1901092544802583) must appear as `device`. If the glasses are missing, run `$ADB kill-server && $ADB start-server`, wait ~5s, re-check (sleeping glasses re-attach this way). If either device is still absent or `unauthorized` after two retries, HALT this plan and report the blocker to the orchestrator (do not fall back to unit-only claims — the phase gate is device evidence).
    2. Build both APKs with the full suite: `export JAVA_HOME=/opt/homebrew/opt/openjdk@17 && cd /Users/bilhuang/Documents/rokid-maps && java -cp gradle/wrapper/gradle-wrapper.jar org.gradle.wrapper.GradleWrapperMain :shared:testDebugUnitTest :phone:testDebugUnitTest :glasses:testDebugUnitTest assembleDebug -q` (must exit 0). Install: `$ADB -s $PHONE install -r phone/build/outputs/apk/debug/phone-debug.apk` and `$ADB -s $GLASSES install -r glasses/build/outputs/apk/debug/glasses-debug.apk` (drive the ColorOS 继续安装 confirm via screencap + input tap if it appears; both must report Success).
    3. Phone side up (screencap-guided taps, 01-07 method): launch the app, tap btnStart (streaming), select sport Ride (btnSportRide), tap btnStartRecording. Then start the feeder broadcast (command in context above).
    4. FEED SANITY GATE (Open Question 2): confirm fixes actually flow before any glasses assertion — phone logcat shows MockRouteFeeder mock-mode ON and, within a 15s logcat window, >= 10 sport_state occurrences (grep the phone logcat dump for "sport_state"); the recording card shows a moving speed. If no fixes flow, the Developer-Options mock-app selection (选择模拟位置信息应用) likely needs re-selecting — this requires the UNLOCKED phone; attempt via screencap+taps, and if the phone is locked (face/PIN — not automatable), HALT and report.
    5. Glasses up: `$ADB -s $GLASSES shell am start -n com.rokid.hud.glasses/.HudActivity`; screencap and confirm the status strip shows BT:ON (paired connection live). Save all screencaps to the session scratchpad with descriptive names (e.g., preflight_bt_on.png).
  </action>
  <verify>
    <automated>/opt/homebrew/share/android-commandlinetools/platform-tools/adb devices | grep -c "device$" returns 2 (both serials attached); full-suite build exited 0; both installs reported Success</automated>
  </verify>
  <acceptance_criteria>
    - `$ADB devices` lists both 3B164G01Y7L00000 and 1901092544802583 as `device`
    - Full-suite build + both `install -r` commands exit 0 / report Success
    - Phone logcat window (15s) contains >= 10 "sport_state" occurrences — the 1Hz broadcast is live
    - Glasses screencap shows BT:ON in the status strip
  </acceptance_criteria>
  <done>Both devices run the Phase 2 APKs with an active recording and a verified 1Hz mock feed — the assertion tasks can trust the pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the tap cycle under 1Hz load (revert regression), live metric updates, and sport/unit variants</name>
  <files>none (device operations only)</files>
  <read_first>
    - .planning/phases/02-glasses-sport-hud/02-RESEARCH.md — "On-Device Verification Plan" steps 4-6, Pitfall 6 (tap safety), Pitfall 1 warning signs
    - .planning/phases/02-glasses-sport-hud/02-VALIDATION.md — layout-revert regression + live-update assertions (NOTE: its "input keyevent KEYCODE_ENTER" line is WRONG and superseded — KEYCODE_ENTER kills the app; use input tap 240 320 only)
  </read_first>
  <action>
    All taps: `$ADB -s $GLASSES shell input tap 240 320`, spaced >= 2s apart. NEVER keyevent 66; NEVER two taps within 300ms (double-tap = shutdown).
    1. HUD-03 tap cycle under load (SC#1): with sport_state streaming, from [ FULL ] send three spaced taps, screencap ~1s after each. The mode-indicator sequence must read [ CORNER ] -> [ SPORT ] -> [ FULL ].
    2. LAYOUT-REVERT REGRESSION (the RESEARCH critical finding — explicit gate): tap twice (spaced) to re-enter SPORT, then wait >= 3 full seconds with the stream active (>= 3 messages arrive), then screencap: the indicator MUST still read [ SPORT ]. If it reverted to a previous mode, the Pitfall-1 fix failed — mark HUD-03 FAIL and stop for diagnosis (BluetoothClient.toggleLayout / currentState ownership).
    3. HUD-01/HUD-02 live updates (SC#2): while in SPORT during a MOVING segment of the feed, take two screencaps ~5s apart. Assert by reading the images: elapsed (top row) advanced ~5s (4-6s tolerance); distance (bottom row) increased (~27m expected at 5.5 m/s); primary numeral reads ≈19.8 km/h (5.5 m/s x 3.6, ±0.5 tolerance for feed jitter); the moving dot is filled (●) — then during the stationary segment of the feed, one screencap: dot hollow (○) and speed ≈0. Also verify the layout composition matches CONTEXT: no map tiles anywhere in SPORT, elapsed top / huge primary center / secondary line / distance bottom, and glyphs render (no tofu boxes for ●/○ — if tofu appears, record it; the ASCII fallback is a 1-line 02-03 follow-up).
    4. Units + sport variants (SC#2): via phone UI (screencap-guided taps) toggle imperial units ON → glasses SPORT screencap primary reads ≈12.3 mph with "mph" unit; toggle back to metric. Then stop recording (confirm dialog), select Run (btnSportRun), start a new recording with the feeder running → SPORT screencap: primary is pace in M:SS with "/km" unit and secondary is speed (values approximate — assert format + unit text, not exact pace).
    Save every screencap to the session scratchpad with step-numbered names.
  </action>
  <verify>
    <automated>Screencap sequence in the scratchpad shows: [ CORNER ] -> [ SPORT ] -> [ FULL ]; [ SPORT ] persisting >= 3s under stream; elapsed +~5s and distance growth between two SPORT captures</automated>
  </verify>
  <acceptance_criteria>
    - Three-tap screencap sequence shows the exact indicator order [ CORNER ], [ SPORT ], [ FULL ] (SC#1)
    - Revert-regression screencap >= 3s after entering SPORT still shows [ SPORT ] while the feed runs (RESEARCH critical finding closed)
    - Two SPORT screencaps ~5s apart: elapsed delta 4-6s, distance strictly increased, Ride primary ≈19.8 km/h during the moving segment, dot ● moving / ○ stationary
    - Imperial screencap shows ≈12.3 "mph"; Run screencap shows M:SS "/km" primary with speed secondary
    - No screencap in this task shows a map tile while in SPORT (pure metrics screen)
  </acceptance_criteria>
  <done>SC#1 and SC#2 hold on hardware, and the layout-revert bug is demonstrably fixed under the exact 1Hz load that used to trigger it.</done>
</task>

<task type="auto">
  <name>Task 3: Prove staleness/FINISHED precedence, mini-mode preservation, green-only pixels, clean logcat — then teardown</name>
  <files>none (device operations only)</files>
  <read_first>
    - .planning/phases/02-glasses-sport-hud/02-RESEARCH.md — "On-Device Verification Plan" steps 7-10, Pitfall 4 (FINISHED precedence)
    - .planning/phases/02-glasses-sport-hud/02-VALIDATION.md — staleness timings, pixel-scan and zero-Unknown assertions
  </read_first>
  <action>
    1. Mini preserved (SC#4), while the Run recording streams: on the phone, enable the mini-map toggle → glasses screencap shows [ STRIP ]; one spaced tap → [ FULL ]. Disable the mini toggle afterward.
    2. Staleness (locked 3s/10s UX): tap (spaced) until the indicator reads [ SPORT ] (screencap first to know the current mode; max 3 taps). With tracking active, kill the phone side: `$ADB -s $PHONE shell am force-stop com.rokid.hud.phone`. Glasses screencaps at +5s and +12s: at +5s all values dimmed (mid-green, visibly darker than the live capture) and status strip shows BT disconnected (BT:--); at +12s "NO DATA" sits in the primary slot with dimmed values retained elsewhere. This also proves the 02-03 ticker: no messages arrive, yet the display transitions.
    3. FINISHED precedence (Pitfall 4): relaunch the phone app, restart streaming (btnStart), ensure a recording is active (the <10-min interrupted session may auto-resume — either resumed or freshly started is fine; restart the feeder if stopped). Re-enter SPORT on the glasses if a reconnect Settings message reset the layout (screencap, then spaced taps as needed). Then stop the recording on the phone (confirm dialog) → glasses screencap: FINISHED banner with final values at FULL brightness. Wait >= 15s, screencap again: STILL the bright FINISHED banner — not dimmed, not NO DATA (the phone stopped its ticker after the final finished broadcast; staleness must not apply).
    4. HUD-04 green-only scan (SC#3): on the best LIVE SPORT screencap from Task 2, assert every pixel has R==0 and B==0. Preferred: `python3 -c "from PIL import Image; im=Image.open('<scratchpad>/sport_live.png').convert('RGB'); bad=[p for p in im.getdata() if p[0]!=0 or p[2]!=0]; print(len(bad))"` must print 0 (screencap -p is lossless PNG; anti-aliased green-on-black blends keep R=B=0, and SPORT draws no tiles, so the scan holds exactly). If PIL is unavailable in the environment, fall back to reading the PNG visually and confirming no non-green hue, and note the substitution in the SUMMARY.
    5. Zero-Unknown gate (HUD-02): `$ADB -s $GLASSES logcat -d -s HudBtClient | grep "Unknown" | grep -c "sport_state"` must return 0 (pre-existing empty-line keep-alive warnings contain no "sport_state" and are expected noise per 01-07).
    6. Teardown: feeder `--es cmd stop`; stop any active recording and streaming via phone UI; leave both apps installed. Note in the SUMMARY the standing 01-07 residual (clearing the Developer-Options mock-app selection needs the unlocked phone) if it remains.
    7. Write the verdict table (one row per Phase 2 success criterion + the revert regression + green scan + zero-Unknown, each with PASS/FAIL and evidence filename) into 02-04-SUMMARY.md. Evidence files stay in the session scratchpad — never copy device coordinates or raw logs into .planning/ (T-07-02 precedent).
  </action>
  <verify>
    <automated>Pixel scan prints 0; the zero-Unknown grep returns 0; screencap set shows dim at +5s, NO DATA at +12s, FINISHED bright at +15s, and [ STRIP ] -> [ FULL ]</automated>
  </verify>
  <acceptance_criteria>
    - +5s screencap after force-stop: dimmed values + BT disconnected; +12s screencap: "NO DATA" in the primary slot, dimmed values retained
    - FINISHED screencap pair: banner + bright final values immediately after stop AND >= 15s later (staleness precedence proven)
    - Mini screencaps: [ STRIP ] while phone toggle on, [ FULL ] after one tap
    - Green scan result is 0 non-green pixels (or documented visual fallback with no non-green hue)
    - Unknown-x-sport_state grep count == 0 on the glasses logcat dump
    - 02-04-SUMMARY.md contains the verdict table with an entry for every Phase 2 success criterion
  </acceptance_criteria>
  <done>SC#3 and SC#4 hold on hardware, the staleness ladder and FINISHED precedence behave exactly as CONTEXT locked them, and the phase gate has a complete evidence trail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adb host -> both devices | Automated UI driving + log capture on user-authorized hardware |
| Device evidence -> repo docs | Screencaps/logs may contain location traces |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | Verification evidence (logcat, session JSON) | mitigate | Evidence stays in the session scratchpad; SUMMARY carries verdicts + filenames only — raw coordinates never enter .planning/ (01-07 T-07-02 precedent) |
| T-02-10 | Elevation | MockRouteFeeder (exported debug receiver) | accept | Lives in phone/src/debug/ — physically absent from release builds (verified Phase 1, T-07-01); used here on debug APKs only |
| T-02-11 | DoS (self-inflicted) | Wrong input events killing the HUD mid-test | mitigate | Hard rule in every task: input tap 240 320 spaced >= 2s only; KEYCODE_ENTER and sub-300ms tap pairs are shutdown paths (HudActivity.kt:160-167, :76) |
</threat_model>

<verification>
- All four Phase 2 success criteria have device evidence with PASS verdicts
- The layout-revert regression assertion (SPORT persists >= 3s under 1Hz stream) is explicitly recorded — this was the phase's blocking pre-existing bug
- Full suite was green at deploy time (Task 1 build gate)
</verification>

<success_criteria>
- Phase 2 SC#1-4 proven on the physical OPPO + Rokid glasses pair
- Zero sport_state Unknown warnings; green-only rendering confirmed by pixel scan
- Verdict table + evidence trail ready for /gsd-verify-work
</success_criteria>

<output>
Create `.planning/phases/02-glasses-sport-hud/02-04-SUMMARY.md` when done. Record: the verdict table (01-07 format), device serial confirmations, any deviations (e.g., glyph tofu, PIL fallback, phone-unlock blockers), and teardown state.
</output>
